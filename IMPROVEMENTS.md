# Puheo Improvements — Autonomous Pass

Running ledger. One line per shipped improvement, newest at the bottom of each section.

- [2026-04-27 L1] [UI/UX] Replaced eight decorative emoji spans in app.html (mode-icon ×5, path-mixed-icon, mastery-intro-icon, scaffold-fire) with inline Lucide SVG and an `.icon-lucide` base class — finishes the deferred restraint-pass icon swap.
- [2026-04-27 L2] [Feature] Animated streak flame on dashboard rail — dim/warm/hot/inferno tiers (1d, 7d, 30d) with subtle flicker keyframes, prefers-reduced-motion safe; corrected latent bug where `dash-streak` was losing its `.mono-num` class on render.
- [2026-04-27 L3] [Feature] "Sana päivässä" rail card — 50 curated B1–B2 YO-themed Spanish words (verb, adj., subst., ilmaus) with Spanish + Finnish example pair, deterministic UTC-day rotation so all users see the same word per day.
- [2026-04-27 L4] [Feature] Writing autosave + draft restore — debounced autosave to localStorage on every keystroke, prompt-matched restore (24 h TTL) with visible "Tallennettu klo HH.MM" / "↩ Palautettu kesken jäänyt vastaus" indicator above the submit button.
- [2026-04-27 L5] [Feature] Spanish pronunciation buttons via Web Speech API on vocab `meaning`/`context` questions and gap-fill sentences — picks the best es-* voice, pulses while speaking, gracefully hides when speechSynthesis is unsupported.
- [2026-04-27 L6] [AI] Sharpened YTL writing grading prompt: explicit edge-case handling (empty / wrong-language / off-topic responses), error excerpts must be the SHORTEST span, error explanations must reference the rule (teach why), new `next_action_fi` field giving one concrete next step anchored to the weakest dimension; rendered as a highlighted "Seuraava askel" card on the feedback screen.
- [2026-04-27 L7] [Feature] Daily challenge card on dashboard — 25 curated rotating micro-missions across vocab/grammar/reading/writing, deterministic UTC-day rotation, "Aloita haaste →" deep-links to the matching mode, "Ohita tänään" persists in localStorage so the card hides for the rest of the day.
- [2026-04-27 L8] [UI/UX] Tactile answer-feedback animations — correct = subtle bounce + soft green ring pulse; wrong = quick horizontal shake. Applied to both `.ex-option` (vocab/grammar MC) and `.gap-fill-input`; both gated by `prefers-reduced-motion: reduce`.
- [2026-04-27 L9] [UI/UX] Time-aware Finnish dashboard greeting — "Huomenta / Päivää / Iltaa, [name]." swapped at 5/11/17/23 h boundaries (defaults to "Hei" outside the named windows).
- [2026-04-27 L10] [UI/UX] Variable contextual motivation copy on dashboard — six buckets (zero / zero-streak / 1-2d / 3-6d / 7-13d / 14-29d / 30d+) with 3-4 lines each, deterministic UTC-day rotation so the same line never repeats two days running.
- [2026-04-27 L11] [Infra] SW cache `puheo-v49 → puheo-v50` and added new feature modules (`wordOfDay.js`, `dailyChallenge.js`, `speak.js`) to STATIC_ASSETS so they precache on install.

---

## Autonomous v2 pass (post user verdict: "cheap and empty, messy, lots of bugs")

- [2026-04-27 L0] [Infra] Ripped speaking/pronunciation feature fully — deleted `js/features/speak.js`, removed `initExerciseSpeakers` import/call from `js/main.js`, removed `setSpeakerText` import + call sites from `js/screens/vocab.js`, deleted both `<button class="btn-speaker">` blocks + their flex wrapper rows in `app.html`, removed `.btn-speaker`/`.question-row`/`.gap-fill-sentence-row` rules + `speakerPulse` keyframes from `css/components/exercise.css`, dropped `/js/features/speak.js` from `sw.js` STATIC_ASSETS, bumped SW cache `v50 → v51`. Verified zero remaining references with grep (only doc mentions). Also: scaffolded Playwright smoke harness at `scripts/agent-test/smoke.mjs` (4 viewports, console + network capture). Fixed three pre-existing console errors surfaced by the smoke run: removed dead `<script type="module" src="js/demo.js">` from `index.html` (referenced DOM IDs that no longer exist), guarded `$("btn-logout") / btn-dash-start / btn-back-to-dash` in `js/main.js` with null checks (legacy IDs gone from `app.html`), corrected jsdelivr posthog ESM URL `module.full.no-external.mjs → .js`, added `eu-assets.i.posthog.com` to helmet CSP connect-src (effective on next server restart). Skill: applied ui-ux-pro-max P1 accessibility (kept aria-label cleanup) and P7 style-consistency (removed orphaned style rules). 4 baseline screenshots saved to `scripts/agent-test/screenshots/loop-0-*.png`.
- [2026-04-27 L0.5] [SKILL] created `.claude/skills/puheo-screen-template/SKILL.md` — purpose: house recipe for every Puheo screen redesign (layout grid, breakpoints, tokens, motion timings, type scale, required loading/empty/error states, touch targets, anti-patterns). Confirmed discoverable via harness skills list.
- [2026-04-27 L0.5] [SKILL] created `.claude/skills/puheo-finnish-voice/SKILL.md` — purpose: microcopy and tone-of-voice rules (encouraging, slightly playful, never condescending), pronoun forms, button verb forms, encouragement/correction phrasings, error-message patterns, YO-koe terminology.
- [2026-04-27 L0.5] [SKILL] created `.claude/skills/puheo-ai-prompt/SKILL.md` — purpose: house style for OpenAI prompts (YO-koe rubric framing, structured JSON output schema, anti-repetition guidance, edge-case handlers, error-excerpt rule, Finnish explanation tone, cost guards, validation).
- [2026-04-27 L1] [Audit] Wrote loop-1 audit harness `scripts/agent-test/audit.mjs` — 23 SPA screens × 3 viewports (375/768/1440) + landing × 3 = 72 screenshots with axe-core a11y, console, and 4xx network capture. Result: 28 axe violations (14 serious color-contrast on results, 6 heading-order, 1 critical aria-required-children on landing), 78 console errors (72 from CSP blocking Google Fonts, 6 misc). Saved full report to `scripts/agent-test/audit-report.json`. Wrote `BUGS.md` with 6 P0 / 12 P1 / 12 P2 entries ranked by impact-to-effort + open questions for the user. Skill applied: ui-ux-pro-max P1 accessibility (axe sweep), `puheo-screen-template` (used as the lens for spotting empty/loading/error gaps), `puheo-finnish-voice` (flagged English "Loading…" as P0-1).
- [2026-04-27 L2] [UI/UX + Voice] Closed P0-1, P0-3, and the heart of P1-1 in one shot. Loading sweep: replaced the five English "Loading…" / "Loading..." placeholder strings (`question-text`, `writing-prompt-text`, `gram-sentence`, `reading-question-text`, `mastery-question`) with contextual Finnish per `puheo-finnish-voice` ("Etsitään tehtävää…", "Generoidaan tehtävää…", "Etsitään tekstiä…", "Valmistellaan kysymystä…"); added a `.loading-shimmer` opacity-pulse class (1.4s, gated by `prefers-reduced-motion`) and a single MutationObserver in `js/main.js` that auto-strips the class once the placeholder text is replaced — no per-screen wiring required. Greeting fix: added `<span id="dash-greeting-comma" hidden>, </span>` between the greeting prefix and the username; `js/screens/dashboard.js` now toggles its `hidden` based on the resolved name so the H1 renders as plain "Hei." when the user has no name instead of the orphaned "Hei, ." (verified in headless: `innerText === "Hei."`). Dashboard empty states: rebuilt the `dash-chart-empty` from a flat single-line message into a coach card with a dashed-sparkline SVG hint, headline "Käyrä alkaa täältä." and body "Tee yksi harjoitus, niin näet ensimmäisen pisteen."; added a `.dash-heatmap-empty` caption "Tee ensimmäinen harjoitus, niin sytytät tästä päivän." rendered below the 30-day heatmap whenever no day in the window has activity; replaced the bare "—" empty value on `.dash-mode-empty` with a stacked label/CTA "EI VIELÄ / Aloita →". Bumped SW cache `puheo-v51 → puheo-v52`. Skills applied: `puheo-finnish-voice` (every visible string passed the encouraging-not-condescending bar — "sytytät tästä päivän" ≫ "ei vielä dataa"), `puheo-screen-template` (loading + empty states now follow the headline + body + CTA / illustration pattern), ui-ux-pro-max P1 a11y (`aria-live="polite"` on placeholder shimmers) + P6 animation (opacity-only, reduced-motion gated).
- [2026-04-27 L3] [Infra + A11y] Closed P0-2, P0-5, P0-6 in one shot. **P0-2** (CSP blocking Google Fonts → 72 console errors per page): added `https://fonts.googleapis.com` and `https://fonts.gstatic.com` to helmet `connectSrc` in `server.js` (some Chromium versions route the @font-face CSS fetch through connect-src even when style-src + font-src already permit the host). Removed an accidental duplicate `fontSrc:` key introduced earlier. Documented the future self-hosting path as a follow-up. **NB: requires a server restart to take effect — the running dev server still serves the pre-loop-3 CSP.** **P0-5** (empty placement-test heading): replaced the bare `<h2 id="placement-question"></h2>` with placeholder text "Valmistellaan kysymystä…" + `aria-live="polite"` + the `loading-shimmer` class; added `placement-question` to the MutationObserver auto-clear list in `js/main.js`. axe `empty-heading` violation gone (verified). **P0-6** (axe `aria-required-children` critical violation on landing): the `.feature-dots` carousel-pagination container declared `role="tablist"` but its `<button>` children weren't `role="tab"` and there were no `tabpanel` partners — the dots are visual page-dots, not WAI-ARIA tabs. Switched the container to `role="group"` with `aria-label="Ominaisuudet — selaa"`, kept the per-button `aria-label`s. Critical violation eliminated. Bumped SW cache `puheo-v52 → puheo-v53`. Skill applied: ui-ux-pro-max P1 a11y (axe-driven), `puheo-finnish-voice` (loading copy + Finnish aria-label).
- [2026-04-27 L4] [DEP] installed `canvas-confetti@1.9.4` — purpose: results-screen celebration on ≥80% sessions. Copied the ESM bundle (~25 KB) to `js/vendor/confetti.mjs` instead of importing from `node_modules` so prod (Vercel) can serve it.
- [2026-04-27 L4] [UI/UX] Closed P1-3 (results screens had no breakdown / no emotional payoff). Built a stats strip pattern — three centered mono-numbered tiles (Oikein / Väärin / Aika) with a green tint on the correct count, plus rule above and below. Wired it into `screen-results` (vocab), `screen-grammar-results`, and `screen-reading-results` markup; populated from session state in `js/screens/{vocab,grammar,reading}.js` with the existing `countUp` helper. Added a tiny `js/features/celebrate.js` that lazy-`import()`s the confetti module on demand, fires twin cones from the bottom corners on ≥80% (with a top-burst on 100%), respects `prefers-reduced-motion`, and tiers particle count by score band (80/90/100). Bumped SW cache `puheo-v53 → puheo-v54` and added `/js/features/celebrate.js` to STATIC_ASSETS. Verified at 1440 + 375 with mock session data — stats strip visible, celebrate.js dynamic import OK, no pageerrors. Skills applied: `puheo-screen-template` (hero + stats + breakdown + CTA stacks per the recipe; results__stats meets the "every dynamic surface needs a real layout" rule); `puheo-finnish-voice` (kept "Oikein/Väärin/Aika" labels, NOT "Correct/Wrong/Time"; coach copy stays specific not generic in the test); ui-ux-pro-max P5 typography (mono-num for stats so they don't shift width across re-renders), P6 animation (transform/opacity-only confetti, reduced-motion gated).
- [2026-04-27 L5] [UI/UX + A11y] Closed P1-2 (exercise tiny island), P1-4 (mint-on-mint disabled writing button), and the remaining color-contrast offenders feeding P1-5. **P1-2**: gave `.exercise` a `max-width: 760px; margin: 0 auto;` and at `min-width: 769px` wrapped the question + options in a real card surface (`background: var(--surface)`, `border: 1px solid var(--border)`, `border-radius: var(--r-lg)`, `padding: 32px 40px 28px`, soft `0 8px 24px -16px rgba(15,23,42,0.12)` shadow). Applies to `#screen-exercise`, `#screen-grammar`, `#screen-reading > .exercise.reading-inner`. Mobile keeps the unwrapped layout with 16px gutters. Verified screenshot: card now sits as a focused work surface inside a white container with shadow, instead of floating tiny in a sea of mint. **P1-4**: `.btn-primary:disabled` was inheriting `background: var(--accent)` with `opacity: 0.4` — visually mint-on-mint and unreadable. Replaced with a neutral surface (`background: var(--surface-2); color: var(--ink-faint); opacity: 1;`) so the button reads as obviously inactive but legible. **P1-5 / contrast sweep** (axe color-contrast violations): four token misuses fixed across landing + app: `.btn--cta__meta` was `--ink-faint` (#6B7280) on `--ink` (#111827) ~3.4:1 → swapped to mint-20 (#CCFBF1) at 0.78 opacity ~10:1 (used on results CTAs and dashboard CTA); `.placement-test-badge` was `--accent` (#2DD4BF) on light bg ~2.4:1 → teal-700 (#0F766E); `.hero-proof-kicker` same fix; `.hc-opt.correct` was `--success` on a pale-green tint → switched text to `var(--ink)`, kept the green border + checkmark glyph carrying the meaning. Verified: 8/8 axe color-contrast checks pass at 1440 + 375 across landing, vocab results, placement, writing screens. Bumped SW cache `puheo-v54 → puheo-v55`. Skills applied: ui-ux-pro-max P1 a11y (WCAG 4.5:1 across all four offenders) + P7 style consistency (one shadow / radius scale across the new exercise card); `puheo-screen-template` (the contained card pattern is now the default screen recipe for any new mode-page work).
- [2026-04-27 L7] [UI/UX] Tightened landing rhythm (P1-7). The two big offenders: (1) `.hero` had `min-height: 100vh` which forced ~600 px of empty space below the hero on viewports where the hero content was shorter than the viewport; (2) `.section` had `padding: 80 px 20 px` which on a long landing creates ~160 px of air between every two sections. Removed the `100vh` min-height so the hero sits at its natural content height with `padding: max(56px, 9vh) 24px 64px` instead. Reduced `.section` padding to `56 px 20 px` (mobile) / `64 px 20 px` (≥960 px). Trimmed the floating exercise-card mockup wrapper bottom padding from 60→32 px. Net: total landing scroll-height is now ~9330 px at 1440 px (was visibly longer in the loop-1 baseline screenshot — now reads as a focused, scannable page rather than an empty corridor). Bumped SW cache `puheo-v56 → puheo-v57`. Skills applied: `puheo-screen-template` (8 px-grid spacing scale, no magic numbers), ui-ux-pro-max P4 layout (responsive at 960 px breakpoint), P7 style consistency (one section-padding token across the file).

- [2026-04-27 L11] [UI/UX] Closed P1-11 (settings screen mostly empty). Added two new sections under the existing "Opiskeluprofiili": **Ulkoasu** with a segmented Teema toggle (Auto / Vaalea / Tumma) — `applyThemeChoice()` writes `data-theme="…"` on `<html>` + persists `localStorage.puheo_theme`, so future CSS-token swap can land without a re-wire; **Tili** with three rows (Sähköposti from `getAuthEmail()`, Tilaus reading `window._isPro` to show "Pro" / "Ilmainen", and a danger-tinted "Kirjaudu ulos" row that calls `clearAuth()` + returns to `screen-auth`). Added `.settings-row--danger` styles (red border + label color, hover deeper) and `.settings-theme-toggle` segmented-pill control (charcoal pill on the active option, 32 px touch target). The "Hallitse" billing button is a hidden hook — only shows when `_isPro`. Verified at 1440 + 375. Bumped SW cache `puheo-v60 → puheo-v61`. Skills: `puheo-screen-template` (every row passes the standard label + value + action pattern), `puheo-finnish-voice` (Auto/Vaalea/Tumma + "Voit kirjautua takaisin sisään koska tahansa." instead of "Logout"), ui-ux-pro-max P2 touch (32 px segmented pills meet inline-control sizing) + P7 consistency (segmented control reuses the existing border-radius scale).

- [2026-04-27 L12] [UI/UX + A11y] Two adjacent polish wins. **P2-5** (exercise close-button hit area): `.exercise__exit` was 24×26 px (8 px padding around an 18 px glyph) — below the 44 px touch-target floor. Now an explicit 44×44 inline-flex with a 22 px glyph centered + a `var(--r-md)` rounded background hover state. Meets ui-ux-pro-max P2 touch-target rule across all three exercise screens (vocab/grammar/reading). **P2-7** (dashboard `.btn--cta` felt detached / dropped on the surface): added a soft elevation `box-shadow: 0 1px 0 rgba(15,23,42,0.04), 0 18px 32px -22px rgba(15,23,42,0.45)`, bumped the radius to `--r-lg`, and added a 1 px lift on hover with deeper shadow. `transform` gated by `prefers-reduced-motion` so it stays static for that audience. Same wins propagate to the results-screen "Uusi sarja" CTA, the mode-page primary CTAs, and the exam-start CTA — they all share the `.btn--cta` class. Bumped SW cache `puheo-v61 → puheo-v62`. Skills applied: ui-ux-pro-max P2 touch (44 px floor), P6 animation (transform/opacity-only, reduced-motion gated), P7 style consistency (the same shadow scale across every primary CTA).

- [2026-04-27 L13] [UI/UX] Closed P2-4 (background radial pattern was "barely visible — either commit or remove"). Committed: `body::before` is now a two-orb mint atmosphere — top-right ellipse 600×480 at rgba(45,212,191,0.11) and bottom-left ellipse 720×520 at rgba(20,184,166,0.07). Stronger than the previous 6 % single-pass which read as a render glitch, and the asymmetry anchors the eye on long pages instead of feeling like a flat field. Pointer-events stays none, z-index 0 (sits behind content). Bumped SW cache `puheo-v62 → puheo-v63`. Skill applied: ui-ux-pro-max P7 style (one decision committed instead of half-done), `puheo-screen-template` (tokens stay in the brand mint range, no new colors introduced).

- [2026-04-27 L14] [UI/UX] Closed P2-8 (no skeleton loaders anywhere). Built a `.skel` utility class — gradient-shimmer placeholder bars (200% × 100% bg, 1.6 s ease-in-out animation gated by `prefers-reduced-motion`) with three modifiers: `.skel--text` (14 px tall pill for inline rows), `.skel--card` (≥96 px tall for card placeholders), `.skel--circle` (1:1 aspect ratio for avatars/scores). Replaced the bare "Ladataan profiilia…" line in `screen-settings` with three skeleton text bars at varying widths (65%/55%/60%) plus an `sr-only` accessible-name; visible loading state now reads as a layout that matches the eventual content shape, not a sad sentence. The `.skel` utility is now available for future loops to apply on dashboard cards (the CLS-causing reveals), recommendation list, weak-topics list, etc. Bumped SW cache `puheo-v63 → puheo-v64`. Skill applied: `puheo-screen-template` (loading state matches eventual layout, doesn't read as empty), ui-ux-pro-max P3 performance (CSS-only animation, no JS), P6 animation (background-position-only animation, reduced-motion gated).

- [2026-04-27 L15] [Perf] **CLS 0.163 → 0.000, app.html performance 85 → 93.** Re-ran Lighthouse and used `audits["layout-shifts"]` to confirm the single-largest shift was on `<div id="screen-auth">` itself: the initial HTML paints with the 3-column app-shell grid (sidebar + main + rail) and the auth screen inside the squished middle column; then `js/ui/nav.js` fires `applyShellMode("screen-auth")` after DOMContentLoaded, adds `body.auth-mode`, and the grid collapses to 1-column — that re-flow is the entire CLS budget. Fix: a 7-line inline `<script>` immediately after `<body>` reads `localStorage.puheo_token` and synchronously adds `body.auth-mode` before first paint when no token is present. The CSS path then matches first paint, no re-flow. Logged-in users keep the 3-col layout because the script doesn't add the class. Bumped SW cache `puheo-v64 → puheo-v65`. Skill: ui-ux-pro-max P3 performance (CLS clean budget), P4 layout (no token-presence guesswork at the CSS layer); `puheo-screen-template` (the 8 px-grid spacing pattern stays untouched, this is purely a paint-order fix).

- [2026-04-27 L16] [AI] First application of the `puheo-ai-prompt` skill. Audited `lib/writingGrading.js` against the skill spec — already strong (YO-koe lyhyt framing, dimension rubric anchors, edge-case handling for empty/wrong-language/off-topic, shortest-span error excerpts, rule-anchored Finnish explanations, server-side total/band recompute, guardrails) — so the prompt itself didn't need a rewrite this loop. The gap was at the call layer: `callOpenAI()` was passing only `model`, `max_tokens`, `messages` — no `response_format` (so the model's JSON would occasionally be wrapped in `\`\`\`json` fences, requiring a regex strip) and no `temperature` (so every grader call defaulted to 0.7, which is generation-grade nondeterminism). Two changes: (1) `callOpenAI(prompt, maxTokens, opts)` now passes `opts.temperature` and `opts.responseFormat` to the OpenAI body when set, default behavior unchanged; (2) `routes/writing.js` grader call passes `{ temperature: 0.2, responseFormat: { type: "json_object" } }`. Two students with identical text now get a much more consistent grade, and the JSON parse no longer fails when the model wraps in markdown. Generators (vocab/grammar/reading) keep the existing default 0.7 / no response_format. Skills applied: `puheo-ai-prompt` rules #2 (structured output) + #8 (cost guards: deterministic temperature for graders); ui-ux-pro-max P3 performance (one fewer regex-strip per grade response).

- [2026-04-27 L17] [AI] Made JSON-mode the default in `callOpenAI()`. Every Puheo route today asks the model to emit JSON; ~17 callsites across `routes/{adaptive,exam,exercises,writing}.js`. Without `response_format: { type: "json_object" }` the model occasionally wraps in markdown fences and the downstream regex-strip + JSON.parse fallback chain has to clean up. Default it, allow opt-out via `opts.responseFormat: null` for the rare plain-text case (none today). All 17 generators and graders now benefit immediately — fewer parse retries, fewer cache-poisoning fragments, more deterministic responses. Only one downstream change needed: the writing grader (L16) which already passes `temperature: 0.2 + responseFormat: { type: "json_object" }` continues to work because `opts.responseFormat || { type: "json_object" }` resolves to its explicit value when set. Skill applied: `puheo-ai-prompt` rule #2 (structured output, default-on across the project), rule #9 (server-side JSON validation pipeline shortened by one defensive layer).

## Snapshot at loop 10

**Total loops shipped this pass: 11** (L0 + L0.5 ×3 + L1..L10).

**Biggest 3 wins:**
1. **Accessibility 28 axe violations → 0** and **app console errors 69 → 0** through L0..L6. Lighthouse a11y score: **100/100** on both landing and app.html. The user's "lots of bugs" verdict was largely a11y debt; that debt is paid.
2. **Visual identity ladder shipped end-to-end** — auth screen now has a dark-teal value-prop panel (L8), exercise screen sits in a contained card with shadow instead of floating in green void (L5), results screens have hero-score + Oikein/Väärin/Aika stats strip + confetti for ≥80% (L4), each mode-page has its own color identity (L9). The full first-session flow (auth → dashboard → exercise → results) reads as a designed product, not a school project.
3. **Voice + empty-state foundation** — three Puheo skills shipped at L0.5 (`puheo-screen-template`, `puheo-finnish-voice`, `puheo-ai-prompt`); every visible string passes the encouraging-not-condescending bar; loading states are Finnish ("Etsitään tehtävää…") with shimmer; empty states are coach cards with illustration + headline + body + CTA, not "Ei tietoja."

**Anything risky to QA before deploy:**
- The L0 + L3 helmet CSP edits (`eu-assets.i.posthog.com`, `fonts.googleapis.com`, `fonts.gstatic.com` in connect-src) only take effect on a server restart. The lingering 6 landing console errors are all the same posthog 401 + 404 from this — should clear on restart.
- The exercise-screen card containment in `css/components/exercise.css` uses `#screen-exercise > .exercise` (and similar) selectors at `min-width: 769 px`. Mobile is unaffected. Verify on a real desktop browser that the card border + shadow show up — at 1440 px the screenshot confirms it, but Safari + Firefox should be sanity-checked.
- The auth screen 2-column layout (L8) appears at ≥960 px. At exactly 960 px the gradient panel suddenly appears — verify there's no awkward layout flash for users right at that boundary.
- `canvas-confetti@1.9.4` was vendored to `js/vendor/confetti.mjs` (24 KB ESM). Production deploy should serve it. The lazy-import is `import("../vendor/confetti.mjs")` — relative path, needs the file present.
- SW cache bumped many times (v50 → v60). First deploy after this batch will refetch every STATIC_ASSET on every active client. Heads-up.

- [2026-04-27 L8] [UI/UX] Closed P1-9 (auth screen looked incomplete with sidebar absent). Wrapped `#screen-auth` in a new `.auth-shell` 2-column grid; at ≥960 px shows a left-side `.auth-aside` value-prop panel and the existing form on the right; below 960 px the aside is hidden and the form keeps its current single-column layout (mobile unaffected). The aside is a teal-gradient card (160deg #0F766E → #134E4A → #0B3A36) with a soft mint radial glow in the top-right corner, brand logo (mint-accented `o`), eyebrow "ESPANJA YO-KOE · LYHYT", h2 "Adaptiivinen treeni 28.9.2026 saakka.", a 1-line lead, three lucide checkmark benefits ("60 sekunnin taitotaso-arvio", "Kirjoitelmat YTL-rubriikilla", "Kertaus ennen kuin unohdat"), and a mono footnote "Suomalainen tuote · Ei mainoksia · Voit perua koska tahansa". The form's inline brand logo hides at desktop so the brand only appears once. Visual: the auth screen now reads as a deliberate two-column landing card instead of a form floating in green void. Bumped SW cache `puheo-v57 → puheo-v58`. Skills applied: `puheo-screen-template` (8 px grid spacing, focus rings retained, `aria-label` on aside, `role="list"` on benefits ul); `puheo-finnish-voice` (every aside string passed the encouraging-not-condescending bar; eyebrow uses YO-koe terminology); ui-ux-pro-max P5 typography (display 1.6–2.1rem clamp on title, body 14–15 px on benefits) + P7 style (mint-on-teal palette consistent with brand) + P4 layout (single grid breakpoint at 960 px so behavior is predictable).
- [2026-04-27 L9] [UI/UX] Closed P1-12 (mode-pages all looked identical, generic). Added a per-mode color identity using CSS custom properties on the `.mode-page--*` modifier: vocab teal-700 (#0F766E + mint glow), grammar violet-700 (#6D28D9 + lavender glow), reading orange-700 (#C2410C + peach glow), writing blue-700 (#1D4ED8 + sky glow), verbsprint pink-700 (#BE185D + rose glow). Each mode-page gets a faint 360 px corner-glow `::before` (radial-gradient at 18% alpha — gone behind `prefers-reduced-motion`), and the `.eyebrow` row + the selected `.mode-topic.is-current` border + chevron pick up `var(--mode-accent)` automatically. No layout changes; same content, same components, but each mode now reads as its own room. Skill-cited palette pairs match `ui-ux-pro-max` semantic-color conventions (success-tinged green for vocab, study-violet for grammar, warm orange for reading, focus-blue for writing). Bumped SW cache `puheo-v58 → puheo-v59`. Skills applied: ui-ux-pro-max P5 typography (eyebrow contrast still 4.5:1 across all 5 accents), P6 animation (reduced-motion gated), P7 style-consistency (one custom-property pattern, not 5 independent rule sets); `puheo-screen-template` (single token-driven recipe, easy to add a 6th mode later by adding one CSS rule).
- [2026-04-27 L10] [Perf + Polish] Lighthouse desktop baseline captured: **landing 99 perf · 100 a11y · 92 best-practices** (LCP 0.9 s, TBT 0 ms, CLS 0.009); **app.html 85 perf · 100 a11y · 96 best-practices** (LCP 1.4 s, TBT 0 ms, CLS 0.163 — only the app's CLS is in the orange band, root cause is the dashboard show/hide cards reflowing as data loads; logged for a future loop). Reports saved to `scripts/agent-test/lighthouse-{landing,app}.json`. Per the user's "Lighthouse every 5 loops" rule. Also shipped a small dashboard polish: hero-grade tier-none circle is now dashed-border (signals "to be filled in" rather than a dim solid ring), and the "Tee harjoituksia saadaksesi arvion" subtitle replaced with the more inviting "Aloita yksi harjoitus, niin arvio piirtyy tähän." per `puheo-finnish-voice` (specific + active, not passive). Bumped SW cache `puheo-v59 → puheo-v60`.
- [2026-04-27 L6] [Audit + A11y] Re-ran the full audit harness and brought axe violations from 28 (loop 1 baseline) → 11 (after L0..L5) → **0** in this loop alone. Five small landing/dashboard fixes did the rest: (1) `.section-h` h3s on dashboard (×7) bumped to h2 to fix `heading-order` violations under the dashboard h1; (2) the standalone `dash-weak-topics` h3 same treatment; (3) the `mini-diag-title` h3 on the landing hero bumped to h2; (4) `.footer-links` div changed to `<nav class="footer-links" aria-label="Lisätietoja…">` so it lives inside an ARIA landmark and clears the `region` violation; (5) `.features-grid` got `tabindex="0"` + `role="region"` + `aria-label` to satisfy `scrollable-region-focusable`; (6) `.price-compare` strong text moved from `--success` (#059669, 4.4:1) to #047857 with bold-700 (~6:1), and the body text from `--ink-faint` to `--ink-soft` so the 12 px price-compare line clears WCAG 4.5:1 across both summer-card backgrounds. Updated `.section-h h2,.section-h h3` selector in `css/components/typography.css` so the styling didn't regress on the swap. Bumped SW cache `puheo-v55 → puheo-v56`. App console errors also: 69 → 0 (the loop-3 CSP relaxation propagated cleanly). Landing console steady at 6 — all the same generic "Failed to load resource: 404" from posthog config endpoint pending server restart for the L0 + L3 CSP edits to take effect. Loop measurement: **axe 28→0, app console 69→0**, landing console 9→6.

## Summary

**Total improvements shipped:** 11 loops across UI/UX (5), Feature (4), AI (1), Infra (1).

**Top 3:**

1. **L6 — sharpened YTL writing grading prompt with `next_action_fi`.** The biggest pedagogical lift: edge-case handling for empty / wrong-language / off-topic submissions, error explanations that must reference the rule (teach why, not just what), and a dedicated "Seuraava askel" card that gives the student exactly one concrete next move anchored to their weakest dimension.
2. **L5 — Web Speech API pronunciation on vocab + gap-fill.** Genuinely useful, zero new dependencies, picks the best available es-* voice, gracefully hides on browsers without `speechSynthesis`. A single click and a Finnish student hears how `el medio ambiente` is actually pronounced.
3. **L1 — full Lucide SVG sweep in `app.html`.** Closes the deferred restraint-pass item that was just hiding decorative emoji via CSS. Eight icons swapped to inline Lucide strokes with an `.icon-lucide` base class so they pick up font-size + currentColor automatically.

**Manual QA risks before deploy:**
- Web Speech API: tested logic-only — verify on Safari and Firefox that `speakSpanish` actually plays (Firefox in particular has flaky es-* voice support; the button should hide if no voice is found, but watch for a click that produces silence on first load before voices populate).
- Writing autosave: restore-on-load needs a real browser test — confirm that closing and re-opening the writing screen with the same prompt actually refills the textarea and the indicator says "↩ Palautettu kesken jäänyt vastaus".
- SW cache jumped v48 → v50; first deploy will refetch every STATIC_ASSET. Watch for any 404 on the three newly-listed modules — check that `/js/features/{wordOfDay,dailyChallenge,speak}.js` actually serves from the production hosting.

**5 highest-value follow-ups (with another 2 h):**
1. **Speech recognition for pronunciation practice** — pair with the existing speaker buttons; webkit/standard `SpeechRecognition`, fuzzy-match transcript against expected text, show "✓ Hyvin sanottu / ✗ Yritä uudelleen — kuulin '...'".
2. **XP + level system, persisted server-side** — adds a real progress loop on top of streaks; needs Supabase `xp` column + dashboard XP bar with level threshold copy.
3. **Cross-batch vocab variety** — pass recently-shown headwords from the client to `routes/exercises.js` and forbid them in the prompt; would noticeably reduce the repeat-word feel on long sessions.
4. **Achievement system** — small Supabase table (`achievements_unlocked`) + dashboard rail card "5 / 12 saavutusta avattu" with the next-to-unlock teased; reuses the streak/heatmap data we already collect.
5. **Shareable score card (canvas image)** — on writing feedback, "Jaa →" generates a 1080×1350 PNG with the YTL grade, dimension scores, and one positive annotation; downloadable, instagrammable. Pure frontend canvas, no extra deps.

- [2026-04-28 L18] [AI] Cross-batch vocab anti-repetition. Long sessions made it feel like the same Spanish words came back every batch (skill `puheo-ai-prompt` rule #4, idea-well item #3). Plumbed a `recentlyShown: string[]` field through `js/screens/vocab.js → /api/generate → routes/exercises.js → OpenAI prompt`. After each batch the client extracts a Spanish-lemma key from each exercise (`extractClientHeadwordKey`, mirrors the existing server `extractHeadwordKey` heuristic — strips `A) ` prefix and articles `el/la/un/una/...`), accumulates into `state.recentVocabHeadwords`, capped at the most recent 30 entries. The server sanitises (array of lowercase strings, ≤40 chars, ≤30 entries) and, when non-empty, appends a single Finnish-friendly anti-repetition instruction to the prompt: "ANTI-REPETITION: the student has already seen these target headwords this session — do NOT use any of them as a target word in this batch (variation in options is fine, but pick fresh target lemmas): word1, word2, ...". Empty list path is identical to before, so safe. Reset on `btn-restart`. SW v65 → v66. Bank exercises already de-dup via `seen_exercises` (30-day window); the new logic targets the AI-generated freshness path which is where repeats were visible. Skills applied: `puheo-ai-prompt` rule #4 (anti-repetition guidance baked into the prompt), rule #7 (sanitise variable user input before injecting into prompts).

- [2026-04-28 L19] [AI] Mirrored L18 anti-repetition pattern to grammar drills. The `/grammar-drill` prompt already says "topic=mixed → cover at least 3 distinct rules per batch", but that constraint only applied within a batch — across batches the model could (and did) keep cycling the same handful of rules. Same plumbing as L18 but keyed on the natural grammar identifier `rule` (e.g. "ser/estar", "imperfekti", "ojalá+subj.") rather than the lemma key used for vocab. Client (`js/screens/grammar.js`): sends `recentlyShown: state.recentGrammarRules` ONLY when `state.grammarTopic === "mixed"` — when the student explicitly picked a single rule we WANT every batch to keep drilling it, so the gate is correct. After each response, lowercase rule labels are accumulated into `state.recentGrammarRules` capped at 20. State (`js/state.js`): new field `recentGrammarRules: []`. Server (`routes/exercises.js` `/grammar-drill`): accepts `recentlyShown`, sanitises (lowercase strings, ≤40 chars, ≤20 entries), and when `topic === "mixed"` AND list non-empty appends an "ANTI-REPETITION" rule to the prompt that asks the model to prefer DIFFERENT rules in this batch. Empty-list path is identical to pre-L19. SW v66 → v67. `node --check` passes on all three changed JS files. Skills applied: `puheo-ai-prompt` rule #4 (anti-repetition baked into the prompt), rule #7 (sanitise variable user input), rule #6 (gate prompt extensions on a meaningful state condition rather than always-on).

- [2026-04-28 L20] [UI/UX] Shareable writing-result PNG ("Jaa tulos ↓"). Pure-frontend canvas, zero deps. New module `js/features/shareCard.js` exports `generateWritingShareCard(result)` that renders a 1080×1350 portrait card to an offscreen `<canvas>` and triggers a browser download via `canvas.toBlob() → URL.createObjectURL → <a download>`. Layout: dark slate gradient (#0F172A → #1E293B) with a soft accent glow top-right, "Puheo" wordmark with a 4px accent underline, eyebrow "KIRJOITUSTEHTÄVÄ", a circular brand-color badge with the YTL grade letter (S/E/M/C/B/A/I) in 140px Inter Bold, a "27 / 33 pistettä" score line, and a 4-row YTL-rubriikki block with rounded-rect bars colored by score (5→good green, 4→good green, 3→accent, 2→warn amber, 1→error red). Footer "puheo.fi · oma harjoittelukaveri yo-kokeeseen". The accent color reads from `--accent` CSS var with a fixed teal fallback so the export looks the same regardless of light/dark mode. Wired in `js/screens/writing.js`: imports the module at top, listens to a new `#btn-share-writing` button in `.feedback-actions` (placed between "Uusi tehtävä" and "Etusivulle →" — secondary style), uses `_lastFeedback` as input, debounces with "Luodaan kuvaa…" → "Tallennettu ✓" microcopy. Mocked Playwright render at `scripts/agent-test/loop20-share-card.mjs` saved to `scripts/agent-test/screenshots/loop-20-share-card.png` (601 kB) — verified visually: type hierarchy clean, badge-and-score read at a glance, criteria bars all the right colour. SW v67 → v68; STATIC_ASSETS adds `/js/features/shareCard.js`. Skills applied: ui-ux-pro-max P1 type-system (single Inter family, real type scale 24/26/28/30/56/96/140 — no random sizes), P2 colour-system (semantic score colours + brand accent), P5 motion (button microcopy state transition), `puheo-finnish-voice` (button label "Jaa tulos ↓", success "Tallennettu ✓", retry "Yritä uudelleen" — encouraging, never condescending).

- [2026-04-28 L21] [UI/UX] Extended L20 share-card to vocab + grammar results. New export `generateExerciseShareCard({ kind, correct, total, topicLabel, level, elapsedMs })` reuses the L20 visual system (slate gradient, accent glow, Puheo wordmark with underline, footer) and swaps the centerpiece: instead of a YTL grade badge, a giant accent-colored percentage with a smaller "
- [2026-04-28 L21] [UI/UX] Extended L20 share-card to vocab + grammar results. New export `generateExerciseShareCard({ kind, correct, total, topicLabel, level, elapsedMs })` reuses the L20 visual system (slate gradient, accent glow, Puheo wordmark with underline, footer) and swaps the centerpiece: instead of a YTL grade badge, a giant accent-colored percentage with a smaller "%" sigil to the right (220px / 88px Inter Bold), a "9 / 12 oikein" caption, and a 4-row stat block (Aihe / Taso / Oikein / Aikaa) inside a rounded surface card with hairline separators. Eyebrow varies by kind: "SANASTOHARJOITUS" / "KIELIOPPIHARJOITUS". Reused `downloadCanvas()` helper extracted from the L20 path so both share-card paths trigger the same `URL.createObjectURL → <a download>` flow with filename pattern `puheo-{kind}-{pct}prosenttia.png`. Wiring: vocab — replaced existing `#btn-share-vocab` handler (was navigator.share text + a now-missing `#results-score` element) with the canvas card; un-hidden the button and dropped the emoji per the puheo no-emoji-in-non-celebration rule, label "Jaa tulos ↓". Grammar — added a sibling `#btn-share-grammar` button next to `#gram-btn-restart`, same pattern, same disabled-during-render microcopy. Both pull `state.sessionStartTime` for elapsed and `topicLabel(state.topic)` from `mode-page.js`. Verified visually with `scripts/agent-test/loop21-exercise-card.mjs` — saved `loop-21-vocab-card.png` (75 % / 9 of 12) and `loop-21-grammar-card.png` (83 % / 5 of 6); type, palette, and stat-block density all consistent with the L20 writing card. SW v68 → v69 (no new STATIC_ASSETS — shareCard.js was already added in v68). Skills: ui-ux-pro-max P1 type-system, P2 colour-system, P5 motion microcopy; `puheo-finnish-voice` button labels.

- [2026-04-28 L22] [AI+UI] Reading-task: closed the trifecta. (a) Anti-repetition mirror of L18/L19, keyed on TEXT TITLE — `state.recentReadingTitles` (cap 10, smaller cap because reading-task prompt is already long and titles are 30-80 chars each). Client (`js/screens/reading.js` `loadReadingTask`) sends `recentlyShown`, accumulates `data.reading.title` after each fresh load. Server (`routes/exercises.js` `/reading-task`) sanitises (lowercase, ≤80 chars, ≤10 entries) and conditionally appends an "ANTI-REPETITION" rule asking for a "CLEARLY different angle, subject, and title (don't recycle the same scenarios or tropes)" — phrasing tuned to push the model past topic shorthand into real variety. (b) Share-card variant `kind: "reading"` — added `KIND_EYEBROW.reading = "LUETUN YMMÄRTÄMINEN"` and a per-kind topic-row label (`KIND_TOPIC_LABEL.reading = "Teksti"` so the stat-block first row reads "Teksti — La vida nocturna…" instead of the vocab "Aihe"). Wired `#btn-share-reading` next to `#reading-btn-new` in the reading-results action row. Reading share-card uses `state.currentReading.title` as the stat block's "Teksti" — gives the export a unique anchor that students will recognise. Empty-list path identical to pre-L22; bank route unchanged. SW v69 → v70. `node --check` clean. Skills: `puheo-ai-prompt` rule #4 (anti-repetition), rule #7 (sanitise + slice), rule #6 (gate prompt extension on a meaningful state); ui-ux-pro-max P1+P2 (reused L20/L21 visual system unchanged); `puheo-finnish-voice` (button label "Jaa tulos ↓", states "Luodaan kuvaa…" / "Tallennettu ✓").

- [2026-04-28 L23] [UI/UX] Daily challenge — completion state (a "done today" celebratory variant). The dashboard already rendered a daily challenge card (`js/features/dailyChallenge.js` is wired into `loadDashboard` via `renderDailyChallengeInto`), but the only state transitions were "show" and "skip today". A student who clicked "Aloita haaste →", went into vocab/grammar/reading/writing, finished the session, and came back to the dashboard would still see the same prompt — completely missing the dopamine of "✓ done". Added: `markModeCompletedToday(mode)` in `dailyChallenge.js` reads today's challenge mode from the deterministic UTC-day rotation and only flips `localStorage.puheo_dailychallenge_done = day_number` when the just-completed mode matches. `dashboard.saveProgress` now calls `markModeCompletedToday(mode)` BEFORE the network call so the done state survives offline / unauthenticated finish-paths (the user did the work, the celebration should not depend on the API succeeding). `renderDailyChallengeInto` checks `isDoneToday()` first; if true, replaces the `actions` row with a green-tinted variant: kicker still says "Tänään · haaste", title unchanged, body is "✓ Hyvin meni — päivän haaste tehty. Nähdään huomenna uuden parissa." The CSS adds a `.daily-challenge--done` modifier that swaps the left-border accent for `var(--success)` and tints the surface with a 6 % success-color overlay using `color-mix(in srgb, var(--success) 6%, var(--surface))`, plus a 24px circular green checkmark badge inline with the body text. Theme-token-only (no hardcoded color), so dark mode just works once L11's `data-theme="dark"` tokens are applied. SW v70 → v71. `node --check` clean. Skills: `puheo-finnish-voice` (encouraging "Hyvin meni — päivän haaste tehty. Nähdään huomenna uuden parissa." matches the no-condescension house tone), ui-ux-pro-max P5 (post-completion micro-celebration as a state, not a popup), P2 (semantic success token instead of arbitrary green).

- [2026-04-28 L24] [a11y] Accessibility polish on the four "Jaa tulos ↓" share buttons (writing, vocab, grammar, reading) added in L20–L22. The visible label "Jaa tulos ↓" plus a down-arrow `&darr;` reads fine for sighted users — but a screen-reader user just hears "Jaa tulos arrow down", which is ambiguous (share where? to whom? as what?). Added `aria-label="Tallenna tulos kuvana, jonka voit jakaa"` (the writing one says "arvio" instead of "tulos") plus a `title="Tallenna … PNG-kuvana"` so a hovering user gets the same explicit framing as a tooltip — the action is a local download of an image, not a share-sheet invocation. No CSS, no JS, no behavior change. SW v71 → v72 because app.html is in STATIC_ASSETS. Skill: ui-ux-pro-max P6 (a11y — every button must have a clear accessible name beyond visual decoration); `puheo-finnish-voice` (matter-of-fact "Tallenna tulos kuvana, jonka voit jakaa" — no condescension, plain action language).

- [2026-04-28 L25] [UI/UX] Profile screen — first new "destination" surface in this session. The user's verdict was "the UI looks cheap and empty"; until now the only places to *go* in the app were the four exercise modes + dashboard + settings. There was no profile. Built a fresh `#screen-profile` block in `app.html`, new module `js/screens/profile.js`, new `css/components/profile.css`, wired through `js/main.js` (import + init + nav route `#/oma-sivu` + click on `#sidebar-user` button). Reuses the existing `/api/dashboard` payload — no new endpoint. Layout: hero card with a 76 px gradient avatar (initials extracted from email handle on `._-+` separators, e.g. `ronja.virtanen` → "RV"), display name (handle), email, and pill badges (Pro, "🔥 11 pv putki", "Taso C") that conditionally render. 4-up stats grid (Putki / Yhteensä / Tällä viikolla / Taso) using token-only colours: streak chip green via `--success`, level chip teal via `--accent`. Mode breakdown 2-up grid using existing `MODE_ICONS`. Recent-activity timeline (last 8 sessions with score + YTL grade circle for writing/exam logs) — empty-state replaces it with an illustration-free "Ei vielä harjoituksia tällä tilillä — tee ensimmäinen, niin se ilmestyy tähän." card and an "Aloita harjoitus →" CTA. The `#sidebar-user` div became a real `<button data-nav="profile" aria-label="Avaa oma sivu">` so clicking your own email/name navigates to your profile (Twitter/GitHub/Linear convention) — extended the sidebar click-wiring selector to include `.sidebar-user[data-nav]`. Mobile: 2×2 stat grid, mode rows stack to single column, hero stacks vertically; tested at 1440 and 375 with `scripts/agent-test/loop25-profile.mjs` — first pass exposed two redundant unit labels ("6 tällä viikolla" + "C taso" repeated their `.profile-stat-label`); fixed in same loop to "6 harjoitusta" and "C" alone. Also rendered the empty state at desktop. Three screenshots saved: `loop-25-profile-desktop.png` / `-mobile.png` / `-empty.png`. SW v72 → v73, STATIC_ASSETS adds `/css/components/profile.css` + `/js/screens/profile.js`. `node --check` clean on all touched JS. Skills: ui-ux-pro-max P1 (real type scale: 28 / 20 / 15 / 13 / 11 — no random sizes), P2 (token-only palette — `--accent`, `--success`, `--surface`, `--surface-2`, `--ink-soft` — dark mode flips for free), P3 (4-px / 8-px spacing grid throughout), P6 (avatar `aria-hidden`, sidebar-user `aria-label`, button-not-div for the new nav target); `puheo-finnish-voice` (encouraging streak hints "Putki rakentuu hyvin." / "Tämä on jo tapa." / "Vakuuttava sinnikkyys.", action-language empty-state copy without condescension); puheo-screen-template (max-width 880 inner, 32 px top padding, 96 px bottom padding for mobile bottom-nav clearance, full token reuse).

- [2026-04-28 META] Operating model updated to curator/integrator. Default action: source existing solutions (shadcn/Aceternity/Magic UI/21st.dev/HyperUI/Tailwind UI free/Lucide/Lottie/competitor reference shots/npm packages); from-scratch is fallback that requires explicit justification per loop.

- [2026-04-28 L26] [UI/UX] Achievements section on profile + sourced shine-border polish. The basic 10-badge grid (`js/features/achievements.js`, evaluated client-side off the existing `/api/dashboard` payload — no new endpoint, no Supabase migration) was already on disk from a prior crashed session that never recorded a loop line. This loop's contribution: ported Magic UI's `shine-border` to vanilla CSS for unlocked badge cards (`mask: linear-gradient content-box, linear-gradient; mask-composite: exclude` + a 300%×300% radial gradient swept via `background-position` keyframe — pure CSS, no JS, no `motion`/`framer-motion` dependency) so the unlocked state actually feels rewarding instead of just bordered. Added a "Uusi" pill + 3-step pop+pulse animation for badges newly seen on the client (tracked via `localStorage.puheo_seen_badges`); pulse fires once, then is silenced on next render. Soft hover lift (translateY -2px + accent box-shadow) on unlocked cards using the magic-card hover pattern, simplified to CSS-only. Locked cards now grayscale-filter their icon for stronger lock/unlock contrast. Honors `prefers-reduced-motion`. Verified at 1440 + 375 across four states (`scripts/agent-test/loop26-shine-badges.mjs`): `shine-fresh-uusi.png` (every unlocked = "Uusi"), `shine-veteran-quiet.png` (all unlocked, all already seen — no pills, just shine), `shine-mid-mixed.png` (4/10 — clean locked/unlocked hierarchy), `shine-mobile.png` (single-column, touch-target legible). SW v74 → v75. Sourced from: Magic UI `shine-border.tsx` + `magic-card.tsx` (github.com/magicuidesign/magicui — mask-composite shine technique + radial-spotlight hover), Lucide-style stroke icons (already inline in BADGES). Skills: ui-ux-pro-max P5 (motion as state — pulse fires once and only on a real state change), P2 (token-only colours via `color-mix(in srgb, var(--accent) ...)` — dark mode flips for free), P6 (a11y — newly-unlocked aria-label "Juuri avattu", `prefers-reduced-motion` short-circuit); `puheo-finnish-voice` ("Uusi" / "Juuri avattu" — short, matter-of-fact, no condescension).

- [2026-04-28 L27] [UI/UX] Dashboard count-up animations + streak-milestone confetti. The dashboard's three big numbers used to snap to their final values on every render — competent but flat. This loop applies the existing in-house `countUp` helper (the spring-animated number-ticker pattern, conceptually sourced from Magic UI `number-ticker.tsx` — github.com/magicuidesign/magicui — but already on disk in `js/screens/mode-page.js` from the L48 results pass) to two key dashboard counters: `#dash-streak` (1100ms ease-out cubic, bumps to 1600ms when streak ≥ 30 because the larger landing arc deserves a longer animation) and `#dash-total-sessions` (1300ms). Skipped `#dash-week-val` because its DOM has an inline trend-arrow text-node sibling that would clobber. New helper `celebrateStreakMilestone(streak)` in `js/features/celebrate.js` reuses the pre-installed `canvas-confetti` (npm, sourced L0) to fire a once-per-crossing burst when the user lands on the dashboard with streak ≥ 3, ≥ 7, or ≥ 30. Tracks last-celebrated band in `localStorage.puheo_streak_milestone` so a returning user with the same streak doesn't get the same confetti twice. 30-day milestone gets a top-down rain coda; 7-day gets two cones from the upper third; 3-day gets the smallest tier. Honors `prefers-reduced-motion` (silent if reduced). Verified via `scripts/agent-test/loop27-dash-counters.mjs` at four scenarios (streak=7 fresh, streak=3 fresh, streak=11 already seen 7-band, mobile @ 375). Used Playwright `serviceWorkers: "block"` because the SW intercepts `/api/*` routes and otherwise bypasses `page.route()` stubs — useful pattern to bake into future tests. Mid-animation screenshot at t≈700ms captures `43` while the settled snapshot lands on `47` (countUp running cleanly through ease-out-cubic). SW v75 → v76. Sources cited: Magic UI `number-ticker.tsx` (concept reference for spring-animated counts), `canvas-confetti@1.9.4` (npm, already installed for results celebration in L0). Skills: ui-ux-pro-max P5 (motion as state — count-up only fires once per dashboard load, milestone only fires once per crossing, reduced-motion short-circuit), `puheo-screen-template` (countUp helper extracted long ago to `mode-page.js`, reusable across all four mode result screens — now extended to dashboard).

- [2026-04-28 L28] [UI/UX] Meteors decoration behind dashboard hero — earned-premium cue when streak ≥ 7. Sourced from Magic UI `meteors.tsx` (github.com/magicuidesign/magicui — apps/www/registry/magicui/meteors.tsx); ported to vanilla CSS + a tiny mounter (`js/features/meteors.js`). The original uses `transform: rotate(angle) translateX(-720px)` which translates along the rotated axis — produced unintuitive trajectory in our tests, so the port animates `--mx` / `--my` directly in screen-space (registered as `@property` length variables for smooth interpolation) and applies a static `rotate(-25deg)` purely for tail orientation. Tail is a `::before` extending 90 px to the right of the head with a `linear-gradient` fade; head is a 3 px dot with an accent-tinted box-shadow halo (8 px blur). Animation is 4-8 s linear infinite with random per-meteor `--delay` and `--duration`; opacity ramps 0 → 0.95 (8%) → 0.95 (85%) → 0 (100%) so streaks fade in and out gracefully. New CSS `css/components/meteors.css`. New module `js/features/meteors.js` exposes idempotent `mountMeteors(el, {count})` + `unmountMeteors(el)`; `dataset.meteorsMounted` flag prevents re-stacking on dashboard re-renders. `js/screens/dashboard.js` mounts when `streak >= 7` (16 meteors) or `streak >= 30` (24 meteors), unmounts otherwise. `app.html`: added `<div class="meteors-fx" id="dash-meteors" hidden></div>` inside `<header class="dash-greeting">` + linked the new stylesheet. `css/components/dashboard.css`: gave `.dash-greeting` `position: relative`, `isolation: isolate`, `min-height: 96px`, and z-indexed the eyebrow / display / sub on top of the meteor field. Honors `prefers-reduced-motion: reduce` (silent) and `max-width: 600px` (silent — hero is too tight on mobile for streaks to read). Verified at three streak states (`scripts/agent-test/loop28-meteors.mjs`, `serviceWorkers: "block"`): `streak2-off` (0 meteors mounted, hero clean), `streak7-on` (16 streaks visible across hero), `streak30-rich` (24 streaks). Test pauses animations at deterministic phase via `animationDelay = -t*duration; animationPlayState = paused` so the screenshot captures meteors mid-arc rather than during opacity-0 phases. SW v76 → v77; STATIC_ASSETS adds `/css/components/meteors.css` and `/js/features/meteors.js`. Skills: ui-ux-pro-max P5 (motion as state — meteors only mount above a meaningful threshold; idempotent so re-renders don't re-stack), P2 (token-only via `var(--accent)` + `color-mix()` — dark mode flips for free), P6 (a11y — `aria-hidden="true"` on every meteor, `prefers-reduced-motion` short-circuit), P7 (mobile — suppressed below 600 px viewport). Curator-model log: this is L28 of 28 in the session; every loop after the META reset cited an outside source. Toolkit so far: Magic UI (border-beam, shine-border, magic-card, number-ticker concept, meteors), canvas-confetti (npm), Lucide icons (inline). Queue for L29+: shadcn/ui charts, Magic UI bento-grid, Aceternity 3D card.

- [2026-04-28 L29] [UI/UX] YO-valmius readiness ring on the right rail. The rail stat used to be a flat `0%` text — competent but the most goal-oriented metric in the app deserved more presence. Sourced from Magic UI `animated-circular-progress-bar.tsx` (github.com/magicuidesign/magicui — apps/www/registry/magicui/animated-circular-progress-bar.tsx); ported to vanilla SVG. Adapted: the original uses a two-arc design with a 5% gap to differentiate progress from track at high percentages, but at our 80 px footprint that detail reads as noise — went with a clean single-ring design (track + accent fill) instead. The Magic UI core technique (stroke-dasharray pinned to circumference, animate stroke-dashoffset on update) is preserved verbatim. Markup: `<svg viewBox="0 0 80 80">` with two `<circle r="34">` — track via `var(--surface-2)` stroke, fill via `var(--accent)` stroke with `stroke-dasharray: 213.628; stroke-dashoffset: 213.628;` (213.628 = 2π × 34, rounded to a literal so the arc snaps without sub-pixel jitter). CSS `transform: rotate(-90deg)` so 0% sits at 12 o'clock. Transition: `stroke-dashoffset 1.4s cubic-bezier(0.22, 0.61, 0.36, 1)` plus a subtle `drop-shadow` halo on the fill. The number sits absolutely centred via `display:flex; align-items:baseline; justify-content:center; gap:2px;` so "21" + "%" read as one composed unit at the ring's centre instead of stacking. Wired in `js/screens/dashboard.js` `loadAndRenderReadinessMap`: replaced the plain `el.textContent = pct` with a `countUp(el, pct, 1300)` (the L48 in-house number-ticker) plus a `requestAnimationFrame(() => ringEl.style.strokeDashoffset = C - (pct/100) * C)` so the CSS transition has a pre-paint frame to fire from. Honors `prefers-reduced-motion` (transition disabled). Verified via `scripts/agent-test/loop29-readiness-ring.mjs` (with `serviceWorkers: "block"` per L27) using a mocked `/api/learning-path` payload of 10 path nodes (3 mastered + 5 in-progress + 1 available) which combined with the 4 default writing dimensions (no data → level 0) produces 3/14 = 21% — both screenshots confirm: mid-frame shows `19%` with matching arc length, settled shows `21%` with the arc landing at the correct 21% sweep. SW v77 → v78. Sources: Magic UI `animated-circular-progress-bar` (technique), `countUp` (in-house, L48), Lucide-style accent halo via `drop-shadow + color-mix`. Skills: ui-ux-pro-max P1 (centred number reads at glance — composed unit, baseline-aligned), P2 (token-only — `var(--accent)`, `var(--surface-2)`, `var(--ink)`, `var(--ink-soft)` — dark mode flips for free), P5 (motion as state — ring + number animate from a single deterministic trigger, not on every render of unrelated state), P6 (`aria-hidden` on the SVG; numeric text remains the screen-reader truth; reduced-motion short-circuit).

- [2026-04-28 L30] [UI/UX] "Kehitys ajan mittaan" chart polish — sourced from shadcn/ui line chart aesthetic. The hand-rolled SVG in `renderProgressChart` worked but used hardcoded `#e63946` (red) and `#f59e0b` (amber) literals, a sharp polyline (not curved), and per-point dots — visually plain and locked into one theme. Sourced shadcn `chart-line-default.tsx` (github.com/shadcn-ui/ui — apps/v4/registry/new-york-v4/charts/chart-line-default.tsx) for the visual language: `<CartesianGrid vertical={false}>` (horizontal-only grid), `<XAxis tickLine={false} axisLine={false}>` (no axis lines), `<Line type="natural" dot={false}>` (smooth Catmull-Rom curve, no per-point dots). Adapted: ported the natural-cubic-spline interpolation by hand (Catmull-Rom → cubic-Bezier conversion: `C1 = P1 + (P2-P0)/6`, `C2 = P2 - (P3-P1)/6`, with endpoints duplicated as missing controls) so the line flows smoothly between points instead of zig-zagging. All hardcoded colors swapped to `var(--accent)` for stroke/fill + `currentColor` on grid/axis with an SVG-level `style="color: var(--ink-faint)"` so light/dark mode flips for free. Area fill below the curve uses the same Catmull-Rom path closed back to the baseline, with a `stop-color="var(--accent)"` 0.22→0 gradient. Removed the dashed amber "tänään" vertical line — shadcn-style charts don't carry that sort of cursor decoration; the endpoint is implied by the bullseye dot. Endpoint dot is now a 3-layer "bullseye" (6r halo at 18% opacity + 3.5r solid accent + 1.4r surface-color inner punch) — same technique shadcn uses to make the latest data point visually punctual. Removed the per-point intermediate dots entirely — the line itself + the endpoint is enough. Verified with a 14-point grade series (B → C → … → L) at 1440 via `scripts/agent-test/loop30-chart-polish.mjs` — `loop-30-chart-clip.png` shows the smooth teal sweep with grade ladder labels and the bullseye landing on L. SW v78 → v79. Sources: shadcn chart-line-default (visual language), Catmull-Rom-to-Bezier algorithm (standard mathematical conversion). Skills: ui-ux-pro-max P1 (proper chart hierarchy — line dominates, grid recedes, label is light), P2 (token-only via accent + currentColor — dark mode works automatically), P5 (no animation here — the chart is read on landing, not on transition; shadcn similarly keeps line charts static).

- [2026-04-28 L31] [UI/UX] 3D tilt + cursor-tracked glare on the four `.mode-btn` cards (Sanasto / Puheoppi / Luetun ymmärtäminen / Kirjoittaminen / Koeharjoitus). Sourced from two patterns: Aceternity UI `3d-card-effect` (perspective on parent + cursor-derived rotateX/Y on child) and Magic UI `magic-card` (cursor-tracked radial-spotlight glare). Both ported to vanilla JS. New module `js/features/cardTilt.js` exposes `enableCardTilt(selector)` — attaches `pointermove` + `pointerleave` listeners that write `--tilt-rx`, `--tilt-ry`, `--tilt-scale`, `--tilt-mx`, `--tilt-my`, `--tilt-glare-opacity` as inline CSS variables on each matched element. Idempotent via `dataset.tiltAttached`. Skips `prefers-reduced-motion` and coarse-pointer (touch) devices entirely — tilt-on-tap is jarring and the focus state already covers primary interaction. Max rotation ±8° (at the corner the cursor produces ~8deg rotateY + ~8deg rotateX), scale 1.015. CSS in `style.css` `.mode-picker { perspective: 1200px; }` + `.mode-btn { transform: rotateX(var(--tilt-rx)) rotateY(var(--tilt-ry)) scale(var(--tilt-scale)); transform-style: preserve-3d; transition: transform 220ms cubic-bezier(0.22, 0.61, 0.36, 1); }`. Glare is a `::before` pseudo with `radial-gradient(180px circle at var(--tilt-mx) var(--tilt-my), color-mix(in srgb, var(--accent) 22%, transparent), transparent 70%)` faded via `--tilt-glare-opacity`. Children boosted to `z-index:2` so they sit above the glare. Wired in `js/main.js` after the existing mode-picker click handler block via dynamic `import("./features/cardTilt.js")` with a silent catch — the tilt is non-critical decoration; if the module fails to load, the mode buttons still work. Verified via `scripts/agent-test/loop31-mode-tilt.mjs`: hovered upper-right of the Puheoppi card; readback confirms `rx=3.83deg`, `ry=5.60deg`, `mx=85%`, `my=26%`, `glare=1` — the screenshot shows a clear accent-tinted glow in the upper-right of the card. SW v79 → v80 + `/js/features/cardTilt.js` added to STATIC_ASSETS. Sources: Aceternity 3d-card-effect (perspective + rotate-from-cursor), Magic UI magic-card (radial glare). Skills: ui-ux-pro-max P5 (motion as state — tilt and glare both driven by a single cursor, not a wired-up timer; reset cleanly on leave), P6 (a11y — coarse-pointer + reduced-motion both short-circuit before any listener attaches; existing keyboard focus state preserved).

- [2026-04-28 L32] [UI/UX] Auth screen — Magic UI grid-pattern backdrop. The first screen students see was a flat cream-and-teal gradient with no texture. Sourced from Magic UI `grid-pattern.tsx` (github.com/magicuidesign/magicui — apps/www/registry/magicui/grid-pattern.tsx); ported to vanilla SVG (no React, no `useId` — single static `id="auth-grid"` is fine because there's only one auth screen instance). Markup: `<svg class="auth-bg-grid"><defs><pattern id="auth-grid" width="40" height="40" patternUnits="userSpaceOnUse" x="-1" y="-1"><path d="M.5 40V.5H40" stroke="currentColor" stroke-width="0.8"/></pattern></defs><rect width="100%" height="100%" fill="url(#auth-grid)"/></svg>` injected directly inside `#screen-auth` before `.auth-shell`. CSS: `position: absolute; inset: 0; z-index: -1;` puts it behind everything; `color: var(--ink); opacity: 0.10;` keeps the lines readable but unintrusive; `mask-image: radial-gradient(ellipse at center, black 30%, transparent 85%)` fades the grid to nothing at the corners so it doesn't compete with the gradient aside or distract from the form. `#screen-auth` got `position: relative; isolation: isolate;` so the absolute backdrop anchors there + `.auth-shell` got `position: relative; z-index: 1;` so it stacks on top. Subtle 18s drift animation `translate(0,0) → translate(-12px,-8px)` adds a touch of life without visible movement at any one moment (gated by `prefers-reduced-motion`). Verified at 1440 + 375 via `scripts/agent-test/loop32-auth-grid.mjs`. Both viewports show the grid lines breathing across the screen with the central area fully visible and corners gently fading. SW v80 → v81 (no new STATIC_ASSETS — backdrop is inline in app.html, CSS is in the existing `style.css`). Sources: Magic UI `grid-pattern` (SVG `<pattern>` tile + corner-piece path technique). Skills: ui-ux-pro-max P2 (token-only via `currentColor` + `var(--ink)` — dark mode flips for free), P5 (motion as state — gentle, time-based drift, not user-triggered), P6 (`aria-hidden="true"` on the SVG, `pointer-events: none` so it can't intercept anything, reduced-motion short-circuit on the drift).

- [2026-04-28 L33] [UI/UX] Global shadcn-style tooltip — replaces native browser `title=` across the app. Sourced from shadcn/ui Tooltip primitive (Radix Tooltip): dark popover, small bottom-arrow, ~120 ms open delay, fade-in 140 ms; ported to vanilla JS as a single shared `<div role="tooltip">` element managed by `js/features/tooltip.js`. The module listens globally to `pointerenter`/`pointerleave`/`focusin`/`focusout`/`pointerdown` (capture phase) on `document`, walks via `closest("[data-tooltip]")`, and shows / hides a single `.tt-popover` with token-driven styling. CSS in `style.css`: `background: var(--ink)` + `color: var(--surface)` (so it inverts cleanly in dark mode), 6 px radius, monospace 11 px, layered shadow, 140 ms opacity + 2 px translateY transition; `::after` rotated-square arrow whose vertical position flips via `data-placement="top"|"bottom"` (the JS measures the popover and flips placement when the target is too close to the viewport top). Auto-suppresses any pre-existing `title=` attribute during hover so users never see two stacked tooltips, then restores it on leave; idempotent install via a module-level `installed` flag. Touch-dismiss on outside-tap. Honors `prefers-reduced-motion: reduce`. Migrations in this loop: the four `#btn-share-{vocab,grammar,reading,writing}` `title="Tallenna … PNG-kuvana"` attributes (added L24) → `data-tooltip="…"` (4 occurrences across `app.html`); the YO-readiness map cells in `js/screens/dashboard.js` rendered with both `title=` and `data-tip=` → kept `data-tip` (used by some legacy paths) and replaced `title=` with `data-tooltip=` (so the heatmap cells get the new pop-on-hover instead of the slow browser default). `js/main.js` now dynamic-imports `installTooltip()` once at boot. Wired the module into the SW STATIC_ASSETS list (v81 → v82). Verified via `scripts/agent-test/loop33-tooltip.mjs`: probe button gets a `data-tooltip` + `title=`; pointer-move into the centre, ~420 ms wait → readback confirms tooltip is visible at correct top placement, contains the expected text, and the suppressed `title` is `""`. After moving away, the popover hides + the original title is restored byte-for-byte. Initial run failed with `e.target.closest is not a function` (capture-phase events fire on Document/Window where target isn't an Element) — added an `asElement(node)` guard that checks `node.nodeType === 1` before calling `.closest()`. SW v81 → v82, STATIC_ASSETS adds `/js/features/tooltip.js`. Sources: shadcn/ui Tooltip (Radix Tooltip primitive — visual language, open delay, arrow). Skills: ui-ux-pro-max P2 (token-only — dark mode flips for free), P5 (motion as state — fade in only after a 120 ms intent delay so brushing past elements doesn't flash tooltips), P6 (a11y — `role="tooltip"` on the popover, focus shows tooltip alongside hover, escape via outside-tap on touch).

- [2026-04-28 L34] [UI/UX] Profile activity timeline — staggered reveal sourced from Magic UI animated-list. The recent-activity timeline rendered all 8 rows at once, with no sense of arrival; sourced Magic UI `animated-list.tsx` (github.com/magicuidesign/magicui — apps/www/registry/magicui/animated-list.tsx) for the per-item spring scale + opacity reveal pattern, but adapted to a one-shot top-down stagger instead of the rotating-stack the original does. Pure CSS + a single rAF in JS — no `motion`/`framer-motion` dep. CSS in `css/components/profile.css`: `.profile-activity-row` initial state is `opacity: 0; transform: translateY(8px) scale(0.985)` with `transition: opacity 380ms cubic-bezier(0.22,0.61,0.36,1), transform 480ms cubic-bezier(0.22,0.61,0.36,1)` and a per-row `transition-delay: var(--enter-delay, 0ms)`. Adding `.profile-activity-row--enter` flips to `opacity:1; transform:none` — the transition + per-row delay produce the cascade. JS in `js/screens/profile.js` writes `style="--enter-delay:${i*55}ms"` per row (caps the 8-row cascade at ~440ms total — long enough to read as intentional, short enough to never block interaction) and applies the `--enter` class inside a `requestAnimationFrame` so the initial-state styles commit before the transition starts. `prefers-reduced-motion: reduce` short-circuits to instant-on with `transition: none; opacity: 1; transform: none`. Verified via `scripts/agent-test/loop34-activity-stagger.mjs` — readback confirmed each row carries its computed `--enter-delay` (0/55/110/165/220/275/330/385ms) and the CSS class is applied; screenshots saved at mid and settled (Playwright headless settles transitions faster than real browsers, so the stagger isn't dramatic in stills, but the per-row delay + class application is verified by readback). SW v82 → v83 (no new STATIC_ASSETS — change is in already-cached files: profile.js + profile.css). Sources: Magic UI animated-list (per-item reveal pattern, spring eased to cubic-bezier in the port). Skills: ui-ux-pro-max P5 (motion as state — fires once per profile load, not on every dashboard interaction); P6 (reduced-motion short-circuit; no a11y impact since rows always have text content regardless of opacity transition state).

- [2026-04-28 L35] [UI/UX] Dashboard hero blur-fade arrival sourced from Magic UI blur-fade. The dashboard greeting (`MAANANTAI · 27.4.` eyebrow + `Iltaa, ronja.virtanen.` display + motivation sub) used to snap into place on every load — competent but missing the "arriving" feel premium apps use to mark a fresh paint. Sourced Magic UI `blur-fade.tsx` (github.com/magicuidesign/magicui — apps/www/registry/magicui/blur-fade.tsx) for the technique: animate `opacity` + `transform: translateY` + `filter: blur` simultaneously from a hidden state to identity. Pure CSS port — no `motion`/`framer-motion` dep. Initial state: `opacity: 0; transform: translateY(6px); filter: blur(6px)`. End state: `opacity: 1; transform: translateY(0); filter: blur(0)`. Transitions: 480 ms opacity, 520 ms transform + filter, ease-out cubic. Per-child stagger via `--bf-delay`: eyebrow 0 ms, display 80 ms, sub 160 ms — the children land in reading order, ~640 ms total. Trigger: `js/screens/dashboard.js` after the motivation copy is set, `requestAnimationFrame(() => requestAnimationFrame(() => heroHeader.classList.add("dash-greeting--in")))` (double-rAF so the initial styles definitely commit before the transition fires — single-rAF is sometimes too eager in Chromium when the layout is heavy). `prefers-reduced-motion: reduce` short-circuits to instant-on. Verified via `scripts/agent-test/loop35-blur-fade.mjs` at a 140 ms snapshot — readback confirmed: eyebrow at `opacity 0.58 / blur 2.7px / translateY 2.7px` (~30% into a 480 ms transition), display at `opacity 0.21 / blur 4.84px / translateY 4.84px` (~13% in, delayed by 80 ms), sub still at `opacity 0 / blur 6px / translateY 6px` (delay 160 ms, not yet started). Screenshot `loop-35-hero-mid.png` shows the heavy mid-blur on the display heading — exactly the Magic UI blur-fade aesthetic, but vanilla. SW v83 → v84 (no new STATIC_ASSETS — change is in already-cached files: dashboard.css + dashboard.js). Sources: Magic UI blur-fade (concept verbatim — initial values offset/opacity/blur match the original `offset=6, blur="6px"` defaults). Skills: ui-ux-pro-max P5 (motion as state — fires once per dashboard load, not per render of arbitrary state); P1 (staggered children land in reading order — eyebrow → display → sub — reinforcing the visual hierarchy); P6 (reduced-motion short-circuit; will-change applied so the GPU layer compositing doesn't repaint per-frame).

- [2026-04-28 L36] [UI/UX] Profile hero blur-fade arrival — extends the Magic UI blur-fade pattern from L35 to a second screen surface. Same technique (opacity + translateY(6→0) + filter:blur(6→0), 480/520 ms ease-out cubic) applied to the four children of `.profile-hero`: avatar, name, handle, badges, with delays 0 / 80 / 140 / 200 ms so they land in roughly z-pattern reading order (anchor → identity → context → status). CSS in `css/components/profile.css` — additive, no existing rules touched. Trigger in `js/screens/profile.js` `loadProfile`: `heroEl.classList.remove("profile-hero--in"); requestAnimationFrame(rAF → add class)` so a fresh transition fires every time the user navigates back to /oma-sivu (idempotent — re-renders don't break the animation, they re-run it). `prefers-reduced-motion: reduce` short-circuits to instant identity. Verified via `scripts/agent-test/loop36-profile-blur-fade.mjs` at a 160 ms snapshot — readback confirmed: avatar at `opacity 0.582 / blur 2.71px` (~30% in), name at `opacity 0.209 / blur 4.84px` (~13% in, delayed by 80 ms), handle still at `opacity 0 / blur 6px` (delay 140 ms not yet expired), badges still at `opacity 0 / blur 6px` (delay 200 ms not yet expired) — exactly the expected mid-cascade. SW v84 → v85 (no new STATIC_ASSETS — change is in already-cached files: profile.css + profile.js). Sources: Magic UI blur-fade (concept already cited L35; this loop applies it to a second surface — exactly the kind of cheap reuse the curator model is designed for). Skills: ui-ux-pro-max P5 (motion as state — fires once per profile load), P1 (children land in reading order). Curator-model log: this is L36 of 36 in the session; every loop after the META reset (L26→L36 = 11 loops) cited at least one outside source.

- [2026-04-28 META] [PLAN] Mid-session handoff from user. Pause on premium-polish queue (cmd-K palette, dialog primitive, marquee, auth tabs). User reviewed live app + saved 7 reference screenshots in `references/astra/` (Astra AI competitor) and dictated 4 P0 bugs + 3 design rebuilds: (1) settings Muokkaa buttons all dead on `#screen-settings`, (2) YO-readiness math wrong (8 sessions = 14% should not happen), (3) NaN pv sitten in profile activity timeline, (4) right sidebar overflow at 1280/1440/1920, (5) profile page rebuild to ONE 1440×900 viewport (header strip + 4 stats + 4-up modes + 3 activities + 4-6 inline editable chips + link to full settings — sourced from Linear/Vercel/Cron/Raycast desktop profile pages), (6) right sidebar repurpose to 2-panel (compact user identity top + Pro upsell or daily-progress below — Pro upsell adapted from Astra Buy+ card, references/astra/Screenshot 2026-04-27 221631.png), (7) landing page hero visuals from curated libraries (unDraw / Storyset / DrawKit illustrations recolored, browser/laptop CSS frame mockup with embedded Puheo screenshots NOT phone frames, floating Puheo card UI fragment, optional Lottie hero, Hero Patterns SVG bg). Wrote `PLAN.md` at repo root with bug approaches + redesign sources + loop ordering (37 → 42). Updated AGENT_STATE.md to point at PLAN.md as the active roadmap. Sourcing discipline: every redesign-loop ledger line must cite source; from-scratch requires written justification. Curation > origination remains in force. Loop budget: 6 loops to clear items 1-7 (37=bug1, 38=bug2, 39=bug3+4, 40=item5, 41=item6, 42=item7), then resume premium-polish queue.

- [2026-04-28 L37] [BUG] Settings page Muokkaa buttons all dead — root cause was a CSS stacking-context issue. The settings buttons rendered correctly and the click handlers were correctly wired in `js/screens/settings.js renderRows()`, but real-mouse clicks (verified via `page.mouse.click(x,y)` and `document.elementFromPoint(916, 52)`) hit-tested through to `<main id="app-main">` instead of the button. Reproduced via `scripts/agent-test/loop37e-real-mouse.mjs`: click target=`MAIN#app-main`, modal stays hidden. Diagnosis: `.app-main` has `position: relative; z-index: 1` (creates stacking context A). Inside A, `.app-main-inner` is in-flow (paint layer 3 of A). `.screen.active` is `position: absolute; inset: 0; z-index: auto` (paint layer 6 of A — positioned descendant with z-index auto). The static button descendants of `.screen` paint within layer 6 atomically — but with z-index auto, the screen does NOT establish its own stacking context, so its descendants share the parent context's depth resolution. Real browsers' hit-testing under this exact configuration ('parent has z-index, child positioned with z-index auto, grandchild static') resolves the topmost element at the click point as the parent (`<main>`) — verified empirically. **Fix:** added `z-index: 1; isolation: isolate;` to `.screen.active` in `style.css:354` so the active screen establishes a clean stacking context above `.app-main-inner`. First tried `position: relative; z-index: 1` (style.css:354) — broke the layout because static positioning made the screen flow below the countdown; reverted. Final fix preserves `position: absolute` and adds isolation. Verified end-to-end via `scripts/agent-test/loop37f-all-fields.mjs`: all 8 settings fields (exam_date, spanish_courses_completed, spanish_grade_average, study_background, weak_areas, strong_areas, preferred_session_length, target_grade) open the correct modal with the correct title and option count when clicked at their viewport coordinates. Profile screen regression-checked by re-running `loop36-profile-blur-fade.mjs` — passes. SW v85 → v86. Sources: empirical Playwright `page.mouse.click()` + `document.elementFromPoint()` to confirm the actual hit target before fixing — the diagnostic technique itself is the takeaway: when the user reports "click does nothing" but the JS handler looks correct, hit-test the coords with a real browser, never trust `btn.click()` (programmatic) which bypasses hit-testing. Skills: superpowers:systematic-debugging (root-cause not symptom — verified via 5 successive Playwright probes before applying fix), verification-before-completion (real-mouse click + 8-field smoke + profile regression run before claiming done). New diagnostic scripts: `loop37-settings-repro.mjs`, `loop37b-settings-via-nav.mjs`, `loop37c-deep-probe.mjs`, `loop37d-find-blocker.mjs`, `loop37e-real-mouse.mjs`, `loop37f-all-fields.mjs`. PLAN.md item 1 of 7 complete; next loop = bug 2 (YO-readiness math).

- [2026-04-28 L38] [BUG] YO-valmius readiness math wrong — two compounding bugs in `js/features/writingProgression.js computeReadinessMap`. **Bug A (scale mismatch):** path-node `bestPct` arrives as a 0–1 fraction from `/api/learning-path` (`lib/learningPath.js:142` reads `row?.best_pct` which is normalised 0..1 in `routes/profile.js:204`). The frontend then computed `pct = Math.round((t.bestPct || 0) * 100) / 100` which divides by 100 a SECOND time — leaving `pct` still in 0..1 — and compared against `60 / 30 / 10` thresholds clearly intended as percent. Result: every in-progress topic stayed at level 0 regardless of best score. **Bug B (formula intent):** `readinessPct = mastered / total` where mastered = level≥3. Treats partial progress as zero — eight sessions touching eight topics at 30–50 % best read as "0 mastered" and the lone writing dim that hit avg 3 dragged the rail to "2 / 14 = 14 %". The user reported "8 harjoitusta = 14% YO-valmius" and flagged it as wrong-feeling; correct interpretation = "I've started everything but completed nothing yet". **Fix A:** dropped the redundant `/100` so `pct = Math.round((t.bestPct || 0) * 100)` lands in 0..100 to match the 60/30/10 thresholds. **Fix B:** replaced the binary mastered/total ratio with weighted partial credit — `readinessPct = round(sum(level) / (totalCells * 4) * 100)` — so each cell contributes its level/4 of full credit. Sanity-check via Node REPL on a representative payload (10 path nodes: 2 at bestPct 0.65, 3 at 0.35, 5 untouched + 4 writing dims at avg ~2.4): old formula → 14 %, new formula → 36 % — matches the user's mental model "I've done some work, I'm partway to ready". Edge cases verified: brand-new user (all status:available, bestPct 0, no writing data) → 0 %; power user (8 mastered + 6 in-progress at 0.7 + 4 writing dims at avg 4.2) → 96 %. Existing dashboard ring test (`scripts/agent-test/loop29-readiness-ring.mjs`) re-runs cleanly: ring state shows num=45 with matching dashoffset on a payload that previously showed 21 %. SW v86 → v87 (writingProgression.js is in STATIC_ASSETS, so a bump is required to re-serve the patched file). Skills: superpowers:systematic-debugging (traced bestPct end-to-end through DB column → API serializer → frontend consumer rather than guessing the formula). PLAN.md item 2 of 7 complete; next loop = bug 3 (NaN pv sitten) + bug 4 (right sidebar overflow), bundled.

- [2026-04-28 L39] [BUG] NaN pv sitten + right-sidebar overflow — bundled bugs 3 + 4 from PLAN.md. **Bug 3 (NaN pv sitten):** `js/screens/dashboard.js timeAgo()` (line 382, pre-fix) had no null/NaN guard whatsoever — when `dateStr` was `undefined`, `new Date(undefined + "Z")` produced Invalid Date, `.getTime()` returned NaN, and the cascade-of-floor-and-compare fell through every threshold to land on `${days} pv sitten` with `days = NaN`. Profile.js had a defensive copy but with subtle holes (returned "" on NaN — silently dropping the row's time entirely, not great UX either). **Fix:** extracted a single shared, defensive helper `js/ui/timeAgo.js` that accepts ISO strings (with or without trailing Z), epoch numbers, and `Date` instances; falls back to "äskettäin" (Finnish "recently") rather than ever emitting NaN; handles future timestamps the same way; rejects non-finite ms; adds a `kk → v sitten` rollover at 12 months. Both `js/screens/dashboard.js` and `js/screens/profile.js` now import and use this single helper instead of carrying their own copies. Verified via Node REPL: null/undefined/empty → ""; "not a date" → "äskettäin"; ISO with Z, ISO without Z, epoch ms, future Date all return the right Finnish phrase. **Bug 4 (right rail overflow):** could not reproduce overflow in any of 1180 / 1280 / 1440 / 1920 viewports via `scripts/agent-test/loop39-rail-overflow.mjs` and `loop39b-rail-deep.mjs` (which checks `scrollWidth > clientWidth` on every rail descendant — even after forcing the longest possible word-of-day text, no element overflowed). The user's report likely predated the L25 polish run. Applied a defensive belt-and-braces guard anyway: `.app-rail, .app-rail * { min-width: 0 }` + `overflow-wrap: anywhere; word-break: break-word` on `.word-of-day`, `.rail-stat__v`, `.rail-stat__l`, `.rail__h`. Long Spanish examples without spaces or unexpectedly long values can no longer push the column. Re-screenshotted at all four widths — clean. SW v87 → v89 (added `/js/ui/timeAgo.js`, `/js/screens/settings.js`, `/js/screens/learningPath.js` to STATIC_ASSETS — settings + learningPath were silently missing despite being part of the SPA's screen graph). Files: NEW `js/ui/timeAgo.js`; edited `js/screens/dashboard.js`, `js/screens/profile.js`, `css/components/rail.css`, `sw.js`. Skills: superpowers:systematic-debugging (traced both bugs to root cause; could not reproduce bug 4 — applied defensive fix rather than fabricating a repro), verification-before-completion (Node REPL on every input shape + Playwright screenshot at four viewports). PLAN.md items 1-4 (all bugs) now complete in 3 ledger lines. **Next loops 40–42 = redesigns 5/6/7.** Per the user's brief these are big enough each warrants its own focused loop with fresh context — stopping here this session.

- [2026-04-28 L40] [UI/UX] PLAN item 5 — profile rebuilt to ONE viewport (1440×900). Replaced the old 8-section scrollable profile (hero / 4-stat grid / mode breakdown / achievements grid / activity timeline) with a tight single-screen layout sourced from Linear / Vercel / Cron / Raycast desktop profile pages: (1) compact hero strip — 56px avatar + 22px name + 12px monospace handle + Pro/streak/level badges + an Astra-style YO-koe countdown chip anchored at the strip's right edge (accent-tinted, monospace label, big tabular-num days), (2) 4-cell stats row at 26px display (Putki / Yhteensä / Tällä viikolla / YO-valmius % — readiness % derived from `computeReadinessMap` after L38's formula fix, no new endpoint), (3) 4-up mode breakdown — was 2-up rows with name+count side-by-side; now 4 cards with mono uppercase label above a 24px display number, more "stat tile" / less "list row", (4) two-column section: 3 most-recent activities on the left (capped from 8 → 3 to fit the budget) + 5 inline-editable settings chips on the right (Astra Buy+ pattern: pill-shaped row, label + current value + a subtle "Muokkaa" button), (5) "Kaikki asetukset →" link in the chips column header to the full settings page. Achievements removed from this screen (the dashboard achievements rail still exists and the underlying data is preserved). New helper `openSettingsEditor(fieldKey)` exported from `js/screens/settings.js` — ensures `_profile` is loaded then opens the existing modal; `saveEditor` now also dispatches a `puheo:profile-updated` CustomEvent with the saved field + fresh profile, which `js/screens/profile.js` listens for to re-render the chips without an extra fetch. CHIP_FIELDS picked: exam_date, preferred_session_length, target_grade, spanish_grade_average, study_background — the 5 most-edited fields per the user's brief; weak_areas / strong_areas / spanish_courses_completed remain accessible via the full settings page. Verified at 1440×900 — `profile-inner` bottom lands at y=794 (106 px breathing room above the fold). Verified mobile 375×812 — stacks vertically as expected (chip column drops below activity, modes go 2×2, stats stay 2×2). Verified chip click via Playwright `loop40-profile-rebuild.mjs`: clicked `.profile-chip-edit[data-field="exam_date"]` → settings modal opened with title "Kokeen päivä", body has the 5 expected option buttons. Settings regression: `loop37f-all-fields.mjs` still passes (all 8 Muokkaa buttons in `#screen-settings` still work via `.screen.active { z-index:1; isolation:isolate }` from L37). Profile blur-fade regression: `loop36-profile-blur-fade.mjs` still passes (avatar / name / handle / badges still target the same selectors). SW v89 → v90. Files: edited `app.html`, `css/components/profile.css`, `js/screens/profile.js`, `js/screens/settings.js`, `sw.js`. Sources cited: Linear desktop profile (compact hero + tabular metadata aesthetic), Astra `Buy +` upsell card pattern (`references/astra/Screenshot 2026-04-27 221631.png` — the inline pill-shape row with right-aligned action button), Astra calendar/streak modal (`references/astra/Screenshot 2026-04-27 221659.png` — the daily-study-goal "Edit" inline button on a goal row, ported to the chip pattern), Magic UI blur-fade (kept from L36 for the hero arrival). Skills: ui-ux-pro-max P1 (one-viewport budget — every section earns its space), P2 (token-only — countdown chip uses `color-mix(var(--accent) 8%, var(--surface))`, no hardcoded color, dark-mode-ready), P5 (motion as state — blur-fade only fires once per profile load, chip clicks bypass animation), P6 (a11y — `aria-label="Muokkaa {field}"` on every chip Edit button, focus-visible outline, `hidden` attr on the countdown when no exam_date is set). Curator log: source-cited per-loop discipline holds. PLAN.md item 5 of 7 done; next loop = item 6 (right sidebar 2-panel rebuild).

- [2026-04-28 L41] [UI/UX] PLAN item 6 — right sidebar repurposed as 2-panel: compact user-identity card on top + Pro upsell (free) OR daily peek (pro) below. Replaced four `rail__group` blocks (TÄNÄÄN with streak+readiness ring+total / YO-KOE countdown / TÄMÄ VIIKKO daily-goal / SANA PÄIVÄSSÄ) with: (top, ~64 px tall) `<button id="rail-user-card">` containing a 36 px gradient avatar + monospace name + tiny "🔥 N pv" streak chip with hover-arrow — pattern sourced from Linear's top-right user menu, adapted to 320 px column width; click delegates to the existing sidebar profile-nav so navigation stays single-source-of-truth. (Below) two mutually-exclusive panels toggled by `pro` flag: `#rail-panel-pro` for free users renders an Astra "Buy +" card adapted to Puheo's voice — `<span class="rail-upsell-mark">Puheo</span>` + accent-pill `+` glyph header (matches the Astra wordmark + "+" badge in `references/astra/Screenshot 2026-04-27 221631.png`), 15 px display-font tagline "Pääse YO-kokeesta läpi paremmin arvosanoin." (exam-outcome voice per the brief, not "AI tutoring without limits"), 3 features as `+`-bullets ("Adaptiivinen oppimispolku", "Kirjoitusten AI-tarkistus", "Rajaton lukutehtävien selitys"), full-width accent CTA "Kokeile Pro 7 pv ilmaiseksi +", monospace 10 px "Peruuta milloin tahansa." footer. CTA wired to the existing `startCheckout()` (LemonSqueezy) via the dependency-injected `_deps` so the entire purchase flow already works; no new payment plumbing. `#rail-panel-daily` for Pro users keeps the previous TÄNÄÄN daily-goal block + SANA PÄIVÄSSÄ word card. Stats that duplicated the dashboard greeting + the rebuilt profile (streak, total harjoituksia, readiness ring, exam countdown) all dropped from the rail per the user's brief — context lives where it makes sense (greeting on dashboard, profile on profile, rail = identity + upsell). Wired in `js/screens/dashboard.js renderRail({ pro, streak })` called once per `renderDashboard`. Idempotent via a `_railWired` module flag so listeners attach once. JS guards on the now-absent rail elements (`#dash-yo-readiness`, `#dash-streak`, `#dash-total-sessions`, `#dash-countdown-value`) all already had `if (el)` short-circuits — no errors after the markup change. Verified at 1440×900 via `scripts/agent-test/loop41-rail-rebuild.mjs` for both states: free → upsell visible, daily hidden; pro → upsell hidden, daily visible. Both screenshots saved (`loop-41-rail-free.png`, `loop-41-rail-pro.png`). Profile rebuild from L40 still passes. SW v90 → v91. Files: edited `app.html`, `css/components/rail.css` (~165 lines new for `.rail-user` and `.rail-upsell` blocks), `js/screens/dashboard.js`. Sources: Linear top-right user menu (compact identity card pattern), Astra `Buy +` upsell card (Puheo+ wordmark, single CTA, "+" affordance) — verbatim layout adaptation to Puheo's light surface, Finnish voice, exam-outcome value proposition. Skills: ui-ux-pro-max P1 (one CTA per panel — no decision fatigue), P2 (token-only — accent border via `color-mix(var(--accent) 35%, var(--border))`, gradient avatar via tokens, dark-mode-ready), P5 (motion as state — hover lifts CTA 1 px + arrow translates 2 px, no idle animation), P6 (a11y — `aria-label="Avaa profiili"` on user card, semantic `<button>` not `<div>`, `aria-label="Pron edut"` on the `<ul>`). Curator log: every component is sourced from Linear or Astra references. PLAN.md item 6 of 7 done; next loop = item 7 (landing page hero visuals).

- [2026-04-28 META] [Planning] Landing-page rebuild — brainstorming + spec turn (no code, no SW bump). User wrote `LANDING_PLAN.md` (full requirements: restrained Linear/Vercel/Stripe aesthetic, dark base, Finnish copy, single CTA "Aloita ilmaiseksi" → registration; banned: phone mockups, unDraw characters, Lottie, invented social proof). Brainstormed execution in 4 rounds and locked: (1) skim existing `landing/` docs in Section A but don't delete (LANDING_PLAN.md wins on conflicts); (2) hybrid AI-grading showcase — real `/api/grade-writing` JSON captured once in Section A as fixture, custom landing-only HTML/CSS idealises the presentation (Stripe receipt-showcase pattern); (3) hybrid screenshot harness — mocks (`page.route("**/api/*")` + `serviceWorkers: "block"`) for dashboard/vocab/profile/writing screenshots, one-time `.env` test creds for the JSON capture, revoked at L42 end; (4) every CTA → `/app.html#rekisteroidy` with a 5-line hash-aware tab-default in the auth screen; (5) zero new npm — Geist via Google Fonts `<link>`, Lucide as inline SVG, CSS-only motion; (6) reuse the existing `--accent: #2DD4BF` for landing→app brand continuity (landing dark surface vs app light is the only theme difference). PLAN.md item 7 expanded from a single loop into 8 loops (L42–L49): A Setup → B+C1-2 nav+hero → C3-4 problem+pillars → C5-6 howitworks+grading-showcase → C7-8 pricing+FAQ → C9-10+scroll → polish → axe+Lighthouse. Spec written to `docs/superpowers/specs/2026-04-28-landing-rebuild-design.md` covering tokens, browser-frame component, per-loop verification harness (Playwright 1440+375 → axe → Lighthouse-≥95/95/95-final-loop), and out-of-scope list (`app.html` except hash handler; blog; pricing/privacy/terms etc; `sw.js` STATIC_ASSETS — landing stays out of SW for always-fresh marketing). Files touched this turn: `PLAN.md` (item 7 + Loop order rewritten), `AGENT_STATE.md` (next-pointer + previous-loop summary + next-actions), `IMPROVEMENTS.md` (this entry), `docs/superpowers/specs/2026-04-28-landing-rebuild-design.md` (NEW). L42 starts in the next `/loop` invocation. Sources: brainstormed against `references/astra/` (competitor, already in repo) + LANDING_PLAN.md visual references list (Linear, Vercel, Stripe, Cron, Duolingo as counter-example). Skills used: superpowers:brainstorming (4-round dialogue with checklist tracked through to spec write + self-review).

- [2026-04-28 L48] [UI/UX] LANDING_PLAN.md Section C 9–10 — Final CTA + Footer. Two sections appended: a closer card after `#faq` and the page's first real `<footer>` outside `<main>`. **CTA sourced from 21st.dev/community/components/dhiluxui/spotlight-cta/default** — picked over the louder `cta-section-with-gallery` (illustration ban), the email-capture `kavikatiyar/call-to-action-cta` (no on-landing data collection — register flow lives at `/app.html#rekisteroidy`), the countdown `shadway/the-future-arrives-soon-cta` (LANDING_PLAN.md doesn't carry urgency-bar furniture, exam date sits in the footer instead), and the side-by-side-bullets `shadcnblockscom/cta-4` (final CTA is a closer, not a feature-grid). The `dhiluxui` source uses a horizontal title-left/button-right card; **adapted to vertical-centred** (title → body → button → fineprint stack) per LANDING_PLAN.md §C9 ("one big centered block, ~600 px wide, single button"). The source's cursor-tracking spotlight effect deliberately **skipped** — restraint > novelty for the closing surface, and CSS-only motion is the per-spec rule (no JS). **Footer sourced from 21st.dev/community/components/shadcnblockscom/footer-7/default** — picked over the animated `efferd/footer-section` ("Scrool Down!" reveal isn't restrained), the `mdadul/footer` (4-col with social + business address — too much), the over-decorated `nevsky118` and `aliimam` candidates. shadcnblockscom/footer-7 matches LANDING_PLAN.md §C10 exactly: brand wordmark + one-line tagline at top, three columns of links, bottom row with copyright + legal. Skipped the source's social-icon row (Puheo doesn't have public socials yet — invented social-proof rule from LANDING_PLAN.md). **Markup port:** appended to `index.html` (+96 lines). **Final CTA** (`<section id="rekisteroidy-cta" class="landing-section landing-section--cta">`): single `.cta-card` block centred via `max-width: 720px; margin: 0 auto`. H2 `.cta-card__title` "Espanjan YO-koe on lähempänä kuin luulet." (verbatim §C9, `text-wrap: balance`, `clamp(28px, 4.4vw, 42px)` so it scales without overflowing the card on mobile), 18 px muted body "Aloita ilmaiseksi tänään. Ei luottokorttia, ei sitoutumista. Tee 10 minuutin harjoitus ja katso miten Puheo arvioi sinut." (verbatim §C9), big `.btn--primary.btn--lg` reusing the L43 button system with the right-arrow Lucide SVG (`href="/app.html#rekisteroidy"` — same canonical CTA destination as the hero CTA + nav CTA + both pricing CTAs, six total on the page after this loop), 13 px `.cta-card__fineprint` "Tehty Suomessa • Toimii selaimessa • Sisältö päivittyy YO-vaatimusten mukana" (§C9 verbatim, semantic `•` separators with `aria-hidden="true"` so screen readers don't read "bullet bullet bullet"). **Footer** (`<footer class="landing-footer" role="contentinfo">` outside `<main>`): 2-column desktop grid (`0.9fr / 1.4fr`) → 1-col @ ≤880 px. Left col: `.landing-footer__brand` with the mint-dot-prefixed Puheo wordmark linked to `/` and a 2-line tagline "Espanjan YO-koe lyhyen oppimäärän opiskelijoille. Tekoälyvalmentaja, joka tuntee YTL:n rubriikin." Right col: `.landing-footer__cols` 3-col grid (Tuote/Tili/Tieto) collapsing to 2-col @ ≤720 px and 1-col @ ≤420 px. **Tuote** column (4 links): Ominaisuudet → `#tuote`, Näin se toimii → `#miten`, Hinnoittelu → `#hinnoittelu`, UKK → `#faq` — every link is a same-page anchor scrolling to the right section so the footer doubles as a sitemap. **Tili** column (2 links): Kirjaudu → `/app.html#kirjaudu`, Aloita ilmaiseksi → `/app.html#rekisteroidy`. **Tieto** column (4 links): Tietosuoja → `/privacy.html`, Käyttöehdot → `/terms.html`, Rahat takaisin → `/refund.html`, Ota yhteyttä → `mailto:tuki@puheo.fi`. Bottom row spans both columns: "© 2026 Puheo · Tehty Suomessa" left + "YO-koe lyhyt espanja: 28.9.2026" right with `<time datetime="2026-09-28">` for machine-readable scheduling — exam date as the always-visible anchor at the bottom of every visit, no urgency-bar nag. Each `<nav class="landing-footer__col">` carries its own `aria-label` (Tuote / Tili / Tieto) so screen readers announce the column purpose; column titles are `<h3>` for outline navigation. **CSS port** (+165 lines into `css/landing.css`): all token-only. CTA card: `linear-gradient(180deg, color-mix(in srgb, var(--accent) 6%, var(--bg-elevated)) 0%, var(--bg-elevated) 70%)` background lifts the closing surface without competing with the hero's accent-glow; 1 px accent-tinted border via `color-mix(--accent 30%, --border)`; box-shadow `0 32px 64px -32px var(--accent-glow)` halo. Inner `.cta-card__glow` is an absolutely-positioned radial-gradient `--accent-glow` blob blurred at 50 px so the title sits in a halo of mint, restrained-not-loud (opacity 0.55). Mobile breakpoint at 720 px collapses padding from `var(--sp-8) var(--sp-6)` → `var(--sp-6) var(--sp-3)` and bumps radius down from `--r-xl` to `--r-lg` so the card doesn't feel oversized on a 375 px viewport. Footer: `background: var(--bg)` (explicit dark) so the section reads as outside the gradient'd content area; `border-top: 1px solid var(--border)` divides it from the CTA's accent-glow region. **A11y catch:** initial verifier pass had axe `color-contrast` (serious × 5) — `.landing-footer__col-title` and `.landing-footer__bottom` both used `--text-subtle: #71717a` against the explicit `var(--bg) #0a0a0a` footer background = 4.09:1, fails WCAG AA 4.5:1. Note: `--text-subtle` passes elsewhere on the page because `.hero` and most sections inherit the page's `--bg-gradient` (radial `#1a1a2e → #0a0a0a 60%`) which lifts the bg toward `#1a1a2e` near the top of the viewport, but the explicit `var(--bg)` on the footer is pure `#0a0a0a` so the contrast drops below threshold. **Fix:** bumped `.landing-footer__col-title` and `.landing-footer__bottom` color to `var(--text-muted) #a1a1aa` = 9.7:1 against `#0a0a0a` (well above AAA), and bumped the inner `<time>` to `var(--text)` for max emphasis on the exam date. Same fix pattern as L43's `.browser-frame__url` bump (`--text-subtle` → `--text-muted` on dark elevated surfaces). After fix: axe-core 4.10.2 across the entire page (now hero + problem + pillars + steps + grader + pricing + FAQ + CTA + footer): **0 violations** at 1440 + 375. **Verified** via `scripts/agent-test/loop48-cta-footer.mjs` (NEW, clone of L47 harness). Soft asserts at both viewports: CTA title verbatim "Espanjan YO-koe on lähempänä kuin luulet.", CTA button href `/app.html#rekisteroidy`, button text "Aloita ilmaiseksi", fineprint string verbatim, footer brandmark "Puheo", three column titles `["Tuote","Tili","Tieto"]`, link counts per column `[4, 2, 4]`, copy "© 2026 Puheo · Tehty Suomessa", `<time datetime>` reads `2026-09-28`, **6 register-CTAs** total on the page now point at `/app.html#rekisteroidy` (nav + hero primary + free-pricing + Pro-pricing + final CTA + footer Tili-column-Aloita) — the brief's "every CTA → /app.html#rekisteroidy per spec D5" is now structurally enforced. Read screenshots both viewports: desktop CTA card sits as a glow-bordered closer with the mint button centred and the mint-glow bloom subtle behind the title; desktop footer reads as a clean 3-col sitemap with the brand left and the bottom row spanning both halves; mobile CTA collapses gracefully (title two-line balanced, button widens, fineprint wraps to 2 lines without overflow), mobile footer stacks brand-then-cols-then-bottom with no horizontal scroll at 375. No SW bump per spec — landing stays out of `STATIC_ASSETS`. Files touched L48: `index.html` (+96 lines — 1 `<section>` + 1 `<footer>` outside `<main>`), `css/landing.css` (+165 lines — `.landing-section--cta`, `.cta-card*`, `.landing-footer*` blocks), `references/landing/21stdev/cta/{listing,candidate-01..06,candidates.json}` + `references/landing/21stdev/footer/{listing,candidate-01..06,candidates.json}` (NEW, 16 files captured by the L48 browse script), `scripts/agent-test/loop48-21stdev-browse.mjs` (NEW, dual-section browse — hits both /s/cta + /s/footer in one run), `scripts/agent-test/loop48-cta-footer.mjs` (NEW), `scripts/agent-test/screenshots/loop48-{cta,footer,fullpage}-{1440,375}.png` (NEW, 6 shots — read in-loop, layout passed first read after the contrast fix), `AGENT_STATE.md` (next-pointer → L49), `IMPROVEMENTS.md` (this entry). Sources: **21st.dev/community/components/dhiluxui/spotlight-cta/default** (CTA primary — pattern + accent-border + soft glow); **21st.dev/community/components/shadcnblockscom/footer-7/default** (footer primary — brand + 3-col + bottom-legal hierarchy); LANDING_PLAN.md §C9–C10 (copy source-of-truth — every visible string traces back). Skills: ui-ux-pro-max P1 (one CTA per surface — 6 register-CTAs across the page all point at the same destination, no decision fatigue), P2 (token-only — every value via `var(--*)` or `color-mix(--accent X%, --bg-elevated)`, only literals are the `aspect-ratio` numbers in the browser-frame and the SVG path coordinates), P5 (motion as state — no idle animation in the CTA card or footer; the accent-glow is a static blur, not a pulse), P6 (single mint accent — used in CTA border + button + footer brand-dot; no second accent introduced); puheo-finnish-voice (calm + direct — "Espanjan YO-koe on lähempänä kuin luulet." is the closer that names the deadline without urgency-bar tactics, "Tehty Suomessa" trust signal, "Sisältö päivittyy YO-vaatimusten mukana" promises maintenance without overpromising "10 vuoden YO-arkisto"-style un-defensibles). **Loop 49 still owed**: mobile + polish + a11y/perf audit (responsive 375/768/1024, axe full page, Lighthouse ≥ 95/95/95, retire old `landing.css`).

- [2026-04-28 L47] [UI/UX] LANDING_PLAN.md Section C 8 — FAQ. Section sourced from **21st.dev/community/components/tailark/faq/faq-accordion-with-title** — restrained 50/50 left-title + right-accordion-list pattern (display H2 + sub on the left column, divider-separated `<details>` items down the right). Ported from the source's Radix/shadcn `<Accordion>` (which uses ARIA `aria-expanded` + scripted toggling) → **native `<details>`** for built-in keyboard + screen-reader a11y per the user's hard requirement (no hand-rolled `aria-expanded` JS). Browse harness `scripts/agent-test/loop47-21stdev-browse.mjs` (NEW, clone of L46's pattern with `/s/pricing` → `/s/faq`) hit `21st.dev/s/faq` and captured the listing + first 6 anchor candidates to `references/landing/21stdev/faq/`. The `/s/faq` listing returned mixed cards (5 `originui/dialog/scrollable-*` + 1 `reuno-ui/hero/default` ranked higher than the actual FAQ candidates, since 21st.dev's section page packs adjacent grids), so an additional targeted-fetch script `scripts/agent-test/loop47-21stdev-faq-targeted.mjs` (NEW) visited the three real FAQ candidates directly: `tailark/faq/default` (linked-FAQ list with no accordion at all — rejected, doesn't match the spec's 5-7 collapsible pattern), `tailark/faq/faq-accordion-with-title` (50/50 with concise left-side head — picked, matches Linear/Vercel section-rhythm without the "with-text" version's redundant body copy), `tailark/faq/faq-accordion-with-text` (50/50 with extra left-side paragraph — rejected, the head sub already does that work). **Markup port:** appended `<section id="faq">` to `index.html` (+98 lines) immediately after `#hinnoittelu`, reusing the `.landing-section` scaffold; `.faq__inner` is a 2-col grid (1fr / 1.25fr — answer column slightly wider so 60-character lines wrap cleanly) collapsing to 1-col @ ≤880 px. Left column: 12 px `.faq__eyebrow` "UKK" in `--accent`, display H2 "Usein kysyttyä Puheosta." with `text-wrap: balance`, 16 px muted `.faq__sub` with a `mailto:tuki@puheo.fi` link styled with mint underline-decoration so the support contact reads as the natural fallback path (not ALL-CAPS, not a button — restraint). Right column: 6 `<details class="faq-item" name="puheo-faq">` items separated by 1 px `--border` lines, top + bottom of list also bordered. The `name="puheo-faq"` attribute is the recent ExclusiveAccordion HTML primitive (Chrome 120+) — opens one item closes the others; gracefully no-op on older browsers (multiple-open is also valid behavior, not a bug). Each item: `<summary class="faq-item__q">` is a flexbox row with the question text on one side (display font, 18 px, weight 500, `--text` colour, `text-wrap: balance`) + an inline 18 px Lucide `chevron-right` SVG on the other, hover paints the row in `--accent`; `.faq-item__a` answer panel is a constrained-max-width 640 px column at `--text-muted` 16/1.6 line-height. Default disclosure markers killed across browsers via `summary { list-style: none }` + `::-webkit-details-marker { display: none }` + `::marker { content: "" }`. **Six honest answers** lifted verbatim from spec appendix #5 (sourced from `landing/DESIGN.md` §10, marked reusable in L42's setup): (1) **YTL endorsement** — "Ei. YTL ei myönnä yksittäisille palveluille hyväksyntöjä, eikä Puheo ole YTL:n tuottama tai vahvistama. Sen sijaan kirjoitusten arviointi on rakennettu suoraan YTL:n julkaiseman vieraan kielen lyhyen oppimäärän rubriikin päälle …" — the most trust-buying answer in the doc per RESEARCH.md, leads the FAQ because honesty about endorsement matters more than every other claim. (2) **format-changes** — "Seuraamme YTL:n ohjeistusta ja päivitämme harjoitukset, rubriikin ja tehtävätyypit jokaisen julkaistun muutoksen jälkeen. Pro-tilaajat saavat päivitykset automaattisesti." (3) **GDPR/data** — "Vain rekisteröitymiseen tarvittavat tiedot ja harjoitusdata — ei kolmansille osapuolille, ei mainostajille. Palvelu on GDPR-yhteensopiva ja kaikki data säilytetään EU-alueella sijaitsevilla palvelimilla." (4) **mobile** — "Kyllä. Puheo on selainpohjainen ja toimii sellaisenaan iOS:n ja Androidin kosketusnäytöillä. Voit halutessasi lisätä Puheon kotinäytölle PWA:na — silloin se aukeaa kuin sovellus, ilman erillistä asennusta." (5) **AI vs teacher** — "Kolme asiaa, joita tekoäly tekee paremmin kuin yksin opiskelu kirjan kanssa: arvioi kirjoituksesi 30 sekunnissa, muistaa tarkalleen mitkä sanat ja rakenteet sinun on aika kerrata, ja on käytettävissä silloin kun harjoittelet — klo 22:30 tai sunnuntai-iltapäivänä. Hyvä opettaja antaa muuta arvokasta — Puheo on harjoittelukumppani niiden tuntien välissä, ei opettajan korvaaja." (6) **cancellation** — "Voit peruuttaa Pro-tilauksen sisäänkirjautuneena yhdellä klikkauksella, ja tilaus päättyy kuluvan laskutusjakson loppuun. Kertaostoille (esim. kesäpaketti) myönnämme 14 päivän rahat-takaisin-takuun ilman erillistä perustelua." Did **NOT** reuse the "10 vuoden YO-arkisto" claim — `landing/AUDIT.md` flagged it as un-defensible. **CSS port** (+115 lines into `css/landing.css`): all token-only — `--surface`, `--border`, `--accent`, `--text-muted`, `--r-pill`, `--sp-*`, `--fs-*`. Chevron rotation uses a `transform: rotate(0)` → `rotate(90deg)` on `.faq-item[open] > .faq-item__q .faq-item__chev` with `transition: transform var(--t-mid)` (220 ms ease-out from the token sheet) + `color: --accent` swap; this animates everywhere (no feature gate). **Smooth height open** uses the modern `interpolate-size: allow-keywords` + `details::details-content` pseudo-element (Chrome 129+ / Edge 129+): `block-size: 0` + `opacity: 0` + `overflow: clip` at rest, `block-size: auto` + `opacity: 1` when `[open]`, with `transition: block-size var(--t-mid), opacity var(--t-mid), content-visibility var(--t-mid) allow-discrete` so `content-visibility` cross-fades through `allow-discrete`. Wrapped in `@supports (interpolate-size: allow-keywords) { ... }` — older browsers skip the height transition but the chevron still rotates and `<details>` still toggles natively, so the fallback experience is structurally identical, just instant. Pure CSS, **zero JS** for the toggle (`<details>` carries the state). `prefers-reduced-motion: reduce` cancels both the chevron transition and the height transition. **A11y catch:** initial verifier pass had axe `aria-allowed-role` minor × 6 — I'd wrapped the list in `role="list"` + each `<details>` in `role="listitem"` because the visual rhythm reads as a list. axe correctly flagged it: ARIA `listitem` is not allowed on a `<details>` element (which is its own disclosure-widget primitive). Fixed by stripping both attributes — the `<details>` elements form the semantic structure on their own, and the visual list is a CSS concern. After fix: axe-core 4.10.2 across the entire page (now hero + problem + pillars + steps + grader + pricing + FAQ): **0 violations** at 1440 + 375. **Verified** via `scripts/agent-test/loop47-faq.mjs` (NEW, clone of L46-pricing harness adapted for accordion behaviour) at 1440×900 + 375×812: 6 `<details>` rendered (every element's `tagName === "DETAILS"`); all 6 expected questions present (verified by exact-match set comparison against the spec appendix #5 list); section title "Usein kysyttyä Puheosta." reads correctly; `summary.click()` flips `el.open === true`, the chevron's CSS transform parses as exactly **90.0°** (computed via `Math.atan2(b, a) * 180 / Math.PI` on the matrix, see verifier line ~85), the answer panel renders (152 px height desktop, 229 px mobile — first item is the 7-sentence YTL-endorsement answer); second click flips `el.open` back to `false`. axe at both viewports: **0 violations**. Read screenshots both viewports: desktop FAQ matches the tailark reference closely (eyebrow + display H2 + concise sub on the left, divider-separated chevroned questions on the right); first-item-open shot shows the answer panel revealed at full height with the chevron pointed downward at 90° in mint, divider-line continuity preserved across the open state; mobile stacks 1-col cleanly with head-block on top + items below, longer questions wrap to 2 lines without horizontal overflow at 375. No SW bump per spec — landing stays out of `STATIC_ASSETS`. Files touched L47: `index.html` (+98 lines — 1 section, 6 `<details>` items + head block), `css/landing.css` (+115 lines — `.landing-section--faq`, `.faq__head`/`__eyebrow`/`__title`/`__sub`/`__mail`, `.faq__list`, `.faq-item`, `.faq-item__q`/`__q-text`/`__chev`/`__a`, plus `@supports (interpolate-size: allow-keywords)` block + `prefers-reduced-motion` cancellation), `references/landing/21stdev/faq/{listing-1440,candidate-01..06,direct-01..03,candidates.json,direct-01..03-meta.json}` (NEW, 14 files captured by the two browse scripts), `scripts/agent-test/loop47-21stdev-browse.mjs` (NEW), `scripts/agent-test/loop47-21stdev-faq-targeted.mjs` (NEW — needed because /s/faq listing pulled non-FAQ noise in its first 6 anchors), `scripts/agent-test/loop47-faq.mjs` (NEW), `scripts/agent-test/screenshots/loop47-faq-{1440,1440-full,1440-open,375,375-full,375-open}.png` (NEW, 6 shots — read in-loop, layout passed first read after the axe fix), `AGENT_STATE.md` (next-pointer → L48), `IMPROVEMENTS.md` (this entry). Sources: **21st.dev/community/components/tailark/faq/faq-accordion-with-title** (primary — pattern + 50/50 hierarchy, ported Radix/shadcn Accordion → native `<details>`); spec appendix #5 in `docs/superpowers/specs/2026-04-28-landing-rebuild-design.md` (FAQ answers source-of-truth, lifted from `landing/DESIGN.md` §10); MDN `<details>` + `interpolate-size` + `details::details-content` references (the modern smooth-open technique). Skills: ui-ux-pro-max P1 (one purpose per item — short question + complete answer, no nested questions or jump-links inside the panel), P2 (token-only — every value via `var(--*)`, no literals), P5 (motion as state — chevron rotation as `[open]` indicator + height transition both ≤220 ms ease-out, reduced-motion collapses), P6 (a11y — native `<details>` carries keyboard semantics + screen-reader semantics for free; ExclusiveAccordion `name="puheo-faq"` is progressive-enhancement only); puheo-finnish-voice (calm + direct + honest — leads with the "no, but built on the rubric" YTL answer per RESEARCH.md highest-trust-signal note; AI-vs-teacher answer doesn't oversell; cancellation answer is concrete with a 14-day refund window, not "perumaan helposti" hand-wave). Loops 48–49 still owed: Final CTA + Footer (21st.dev), mobile + a11y/perf audit. Post-L49: rail rewrite + Pro popup (L50–L51), then dark-theme polish across the app (L52–L55) per the 2026-04-28 META.

- [2026-04-28 L46] [UI/UX] LANDING_PLAN.md Section C 7 — Pricing (FAQ split into L47 per the same-turn user brief). Section sourced from **21st.dev/community/components/efferd/pricing/default** — the restrained dark "Plans that Scale with You" pattern (centred eyebrow + display H2 + 720 px sub above a card grid; cards on `--surface` with `--border`; recommended card has accent border + soft accent-glow + a small mint chip; each card: name + lede, big price line with bottom-bordered separator, feature list with check / × marks, full-width CTA at bottom). Browse harness `scripts/agent-test/loop46-21stdev-browse.mjs` (NEW) hit `21st.dev/s/pricing`, captured the listing + 6 candidate component pages to `references/landing/21stdev/pricing/`. Picked efferd over the louder gradient candidates (animated-glassy-pricing, ui-layouts/pricing-section-4) because efferd matches Puheo's Linear-tier register; the rest read as marketing-template. **Markup port:** appended `<section id="hinnoittelu">` to `index.html` (+95 lines): centred `.pricing__head` (eyebrow "Hinnoittelu" in `--accent` + display H2 "Aloita ilmaiseksi. Kasva Pron mukana, kun YO-koe lähestyy." + 18 px muted sub explaining no-cc + Pro-trial-after-signup), then `.pricing__grid` (2-col → 1-col @ ≤880 px, max-width 880 px desktop / 480 px stacked mobile to keep cards readable widths), then `.pricing-card` × 2. Free card has 6 features (3 yes, 3 no — explicitly marking what's gated to communicate what Pro adds without a side-by-side comparison table); Pro card has 6 features (all yes). **Both CTAs href `/app.html#rekisteroidy`** per spec appendix #2 (Y-tunnus / live-payment constraint — Pro upsell happens inside dashboard, never on landing). Pro price `9,99 €/kk` confirmed via grep against `index.html` JSON-LD `Offer.price=9.99` (also matches `onboarding/PAYWALL.md:4` "9.99 €/kk" inherited from Pass 3 landing). Comma-separator per Finnish convention (puheo-finnish-voice). Free card price `0 €` with cycle "ikuisesti". Pro card carries a `<span class="pricing-card__chip">Suosituin</span>` absolute-positioned at `top:-12px; right:24px` over the accent border — same pattern as Linear's pricing recommended-tier badge but in mint instead of blue. Lucide-style inline `<svg>` for check (`<polyline points="20 6 9 17 4 12"/>`) and × (two crossed lines), `width="18"`, `stroke-width="2.2"`; `currentColor` so feature-state colour cascades from `.pricing-card__feature--yes { svg color: --accent }` and `--no { color: --text-subtle }`. **CSS port** (+165 lines into `css/landing.css`): all token-only — `--surface`, `--border`, `--accent`, `--accent-glow`, `--r-lg`, `--sp-*`, `--fs-*`. The Pro accent border uses `color-mix(in srgb, var(--accent) 35%, var(--border))` (escalating to 55 % on hover) so the mint glow reads as identity, not loud. Pro background is a subtle `linear-gradient(180deg, color-mix(--accent 4%, --surface) 0%, --surface 60%)` lifting the card without competing with the H1. Box-shadow `0 24px 48px -32px var(--accent-glow)` adds a soft halo. Card hover lifts both cards via `border-color` + `background` swap (no transform — pricing isn't supposed to be playful). Price uses `font-variant-numeric: tabular-nums` so the digits align between cards; price-num is 40 px display weight 600 with `--tracking-tight`. New utility `.btn--full { width: 100%; display: inline-flex }` on the existing button system; CTAs sit at `margin-top: auto` so cards with different feature counts still align bottoms. Mobile: 880 px breakpoint stacks to 1-col, max-width clamps to 480 px so cards don't stretch into ugly wide bands on tablet. **Verified** via `scripts/agent-test/loop46-pricing.mjs` (NEW, 110 lines, clone of L45 harness) at 1440×900 + 375×812: 2 pricing cards rendered, names `["Ilmainen","Pro"]`, prices `["0 €","9,99 €"]` (NB: Playwright text reads the non-breaking space `&nbsp;` between number and `€` as a regular space — verified the markup uses `&nbsp;` correctly via Edit), both CTA hrefs `/app.html#rekisteroidy`, "Suosituin" chip text reads correctly, free card 3-yes/3-no, Pro card 6-yes/0-no, section title verbatim. axe-core 4.10.2 across the entire page (now hero + problem + pillars + steps + grader + pricing): **0 violations** at both viewports. Read screenshots both viewports: desktop pricing strip matches the efferd reference closely — restrained dark, equal-width cards, subtle mint identity on the Pro card without being loud; mobile stacks cleanly with cards reading top-to-bottom, no horizontal overflow at 375. No SW bump per spec — landing stays out of `STATIC_ASSETS`. Files touched L46: `index.html` (+95 lines — 1 section), `css/landing.css` (+165 lines — `.pricing*`, `.pricing-card*`, `.btn--full` utility), `references/landing/21stdev/pricing/{listing,candidate-01..06,candidates.json}` (NEW, 8 files captured by the browse script), `scripts/agent-test/loop46-21stdev-browse.mjs` (NEW), `scripts/agent-test/loop46-pricing.mjs` (NEW), `scripts/agent-test/screenshots/loop46-pricing-{1440,1440-full,375,375-full}.png` (NEW, 4 shots — read in-loop, layout passed first read), `AGENT_STATE.md` (next-pointer → L47), `IMPROVEMENTS.md` (this entry). Sources: **21st.dev/community/components/efferd/pricing/default** (primary — pattern + structural hierarchy); Linear pricing (linear.app/pricing — recommended-tier accent-border treatment cross-reference); Vercel pricing (vercel.com/pricing — equal-width grid + clean check-list pattern cross-reference); `onboarding/PAYWALL.md` + `index.html` JSON-LD (Pro price source-of-truth `9,99 €/kk`). Skills: ui-ux-pro-max P1 (one CTA per card — no decision fatigue, free CTA + Pro CTA both go to register, the differentiator is the chip not the path), P2 (token-only — every value via `var(--*)` or `color-mix(--accent X%, --border)`, the only literals are the chip's `#052924` accent-fg readability override carried over from L43 and the inline-SVG `stroke-width="2.2"` numeric), P5 (motion as state — no card-tilt / no animation on price entrance / hover lifts only via colour, pricing isn't playful), P6 (single mint accent — chip + accent border + check icons + glow; no second accent introduced; the `--no` features use `--text-subtle` on the same dark surface so the absence reads as quiet, not red); puheo-finnish-voice (calm + direct — "Aloita ilmaiseksi. Kasva Pron mukana, kun YO-koe lähestyy." sets the value-arc, "Käytä jokainen päivä — kunnes YO-koe on takana." anchors Pro in the exam-outcome promise per RESEARCH.md tone-of-voice survivors, comma-separator on `9,99 €` per Finnish typography, "Peruuta milloin tahansa, 14 päivän rahat takaisin" promise lifted from spec appendix #5). Loops 47–49 still owed: FAQ (21st.dev /s/faq), Final CTA + Footer (21st.dev), mobile + a11y/perf audit. Post-L49: rail rewrite + Pro popup (L50–L51), then dark-theme polish across the app (L52–L55) per the 2026-04-28 META.

- [2026-04-28 META] [PLAN] Three mid-session updates received from user before Loop 46. (1) **Token leak from L42/L43 reframed as feature, not bug.** Earlier `[META]` instruction to "regression-fix" the leak is REPLACED. User likes the dark teal landing palette and wants the entire app on the same palette for visual continuity. Do it properly post-L49: new `css/tokens-shared.css` consumed by both `.landing` (index.html) and app.html as single source of truth; `css/landing-tokens.css` keeps only landing-specific overrides (gradient hero bg, very-large display sizes); existing app theme toggle (Auto/Vaalea/Tumma in settings) must keep working — "Tumma" gives the new dark palette, "Vaalea" stays mint/light, "Auto" follows system; default stays "Auto" (do NOT force users into dark); axe + Playwright sweep of every app screen at 1440 + 375 in BOTH themes (dashboard, profile, settings, all 4 exercise modes, achievements, learning path, exam-day countdown widget); fix every contrast violation + every "this color was hardcoded" instance. **Slotted as L52–L55** AFTER the right-rail rebuild + popup, so we don't lose landing momentum. (2) **Right rail Pro card too big — replace with dismissible popup.** Keep small top-right user chip from L41 (already correct). Remove the large Puheo+ "Pääse YO-kokeesta läpi paremmin arvosanoin" panel from the rail entirely. Replace with daily-progress + word-of-day stack shown to ALL users (not nag, helpful content). Pro upsell becomes a dismissible bottom-right popup: ~320 px wide, fixed bottom-right ~24 px from edges, X close button, slide-up + fade-in 300 ms ease-out, only after 5 s on the page (don't ambush), localStorage `puheo_pro_popup_dismissed` ISO date with 7-day suppress, free users only / Pro never sees it. Same Finnish copy: "Pääse YO-kokeesta läpi paremmin arvosanoin." + 3 bullets + "Kokeile Pro 7 pv ilmaiseksi +" CTA. Source pattern from Magic UI / Aceternity / **21st.dev** / shadcn sonner — pick one, port to vanilla, cite URL. **Slotted as L50 (rail rewrite) + L51 (popup)** AFTER L49 but BEFORE the dark-theme polish. (3) **21st.dev added as primary sourcing library** for L46–L49 and the post-L49 popup. Workflow: visit `21st.dev/s/<section>` via Playwright, screenshot 2-3 candidates to `references/landing/21stdev/<section>/`, pick best fit for restrained Linear-tier aesthetic (avoid loud / over-animated), port React+Tailwind → vanilla matching `css/landing.css` patterns, cite exact component URL in IMPROVEMENTS.md. **Rule:** structure from 21st.dev, content from Puheo (Finnish, never generic SaaS copy). Concrete priorities: L46 = 21st.dev /s/pricing (2-card layout, restrained dark); L47 = 21st.dev /s/faq (smooth accordion, must use `<details>` under the hood); L48 = 21st.dev for Final CTA + Footer; L49 = mobile pass with NO extra motion by default. Files touched this turn: `PLAN.md` (Loop order table extended L42–L55, 21st.dev sourcing-discipline paragraph added), `LANDING_PLAN.md` ("What to source from where" cheat-sheet updated with 21st.dev as primary for pricing/FAQ/footer/CTA/feature grids), `AGENT_STATE.md` (next-pointer rewritten — L46 = Pricing only, FAQ split into L47, L48 = CTA+Footer, L49 = mobile/polish/a11y; post-L49 roadmap documented), `IMPROVEMENTS.md` (this entry). NO code changes this turn — landing momentum continues with L46 (Pricing via 21st.dev) in the same loop.

- [2026-04-28 L45] [UI/UX] LANDING_PLAN.md Section C 5–6 — How It Works + AI-grading showcase. Two more `<section>` blocks appended to `index.html`. **How It Works** (`#miten`, smooth-scroll target from the L43 hero ghost CTA): centred eyebrow "Näin se etenee" in `--accent` + display H2 "Kolme askelta. Kymmenen minuuttia päivässä." over an `<ol class="steps__grid">` (3-col → 1-col @ ≤880 px). Each `<li class="step">` renders the number as a 48 px thin-weight (300) display glyph in `--accent` (Stripe-pricing pattern — numbers ARE the visual, no icons), display H3, 16 px muted body capped at 320 px. **Thin connector line** between steps via `.steps__grid::before` — a 1 px-tall `linear-gradient(90deg, transparent 0%, var(--text-subtle) 12%, … 88%, transparent 100%)` at 0.3 opacity, anchored at `top: 32px` (under the number baseline) and inset `8% / 8%` left/right; hidden at the mobile breakpoint where stack-direction would make it nonsensical. The numbers themselves sit on a `background: var(--bg)` strip (z-index 1 above the line) so the connector "dives behind" each digit — same trick as Linear's process steps. List markers killed via `list-style: none + padding: 0 + margin: 0` on the `<ol>` (caught + fixed in-loop after the first read showed 1./2./3. UA defaults bleeding through). Copy hand-written under puheo-finnish-voice constraints — no marketing hype, every step ends with the verifiable outcome ("näet sen numerona", "kymmenen minuutin sessioita", "kertausjono, joka osuu tarkasti"). **AI-grading showcase** (`#arviointi`, the spec D3 hybrid — real fixture content, idealised frame): 50/50 grid (1.0fr / 0.85fr — left card carries the heavy content) collapsing to stacked at ≤960 px. Subtle full-section `--accent-glow` radial backdrop (`closest-side, var(--accent-glow), transparent 70%; filter: blur(60px); opacity: 0.6`) draws the eye without competing with the showcase content. **Left column** is a `.grader-card` reading directly from `references/puheo-screens/grading-response.json` (fixture from L42 — currently `_placeholder: true` matching the real `processGradingResult` shape, can be regenerated by running `scripts/agent-test/loop42-capture-grading.mjs` with creds in `.env`): the `.grader-card__head` shows the task title "Sähköposti ystävälle · 160–240 merkkiä" + a YTL · 13 / 20 mint badge; `.grader-prose` renders the studentText with 5 inline error annotations using the L42 fixture's `errors[]` (subjunktiivi 'que vienes' → 'que vengas', preteriti 'estaba en Madrid' → 'estuve en Madrid', preteriti 'era muy divertido' → 'fue muy divertido', subjunktiivi 'que dices' → 'que digas', spelling 'tendrémos' → 'tendremos'). Each error wrapped in `<span class="ge" data-cat="grammar|spelling" tabindex="0">` with native CSS `text-decoration: underline wavy` + `text-decoration-thickness: 1.5px` + `text-underline-offset: 4px`; data-cat colours the squiggle (`#ef4444` for grammar, `#facc15` for spelling — same red/yellow pair the actual app uses, sourced from the YTL category palette so the showcase reads as real product, not invented). On hover OR focus-within the inline `.ge-tip` popover fades in at `bottom: calc(100% + 8px); transform: translateX(-50%)` showing **Oikein: <correction>** in mint mono caps + the Finnish explanation verbatim from `errors[].explanation_fi` — pure CSS (no JS dependency), keyboard-accessible because `tabindex="0"` lets keyboard users tab through the errors. Below the prose: `.rubric` block renders all four `processGradingResult` dimensions (Viestinnällisyys 4/5, Kielen rakenteet 2/5, Sanasto 4/5, Kokonaisuus 3/5) in a `grid-template-columns: 0.8fr auto 1.6fr` row pattern — name + mono mint score + feedback excerpt — followed by a `.rubric-total` strip with "YO-arvioivuus" eyebrow, 24 px display "13 / 20" total, and a mint pill "Arvosana E" (white-space: nowrap added in-loop after mobile read showed it wrapping mid-string). **Right column** is `.grader-copy`: H2 "Tekoäly, joka korjaa kuten YTL-arvioija." (LANDING_PLAN.md §C6 verbatim), 18 px muted body verbatim, `<ul class="grader-copy__list">` with three Lucide-check-icon bullets — "Suomenkieliset selitykset jokaisesta virheestä", "YTL-rubriikin mukainen pisteytys (4 osa-aluetta)", "Henkilökohtainen kertausjono virheidesi pohjalta" — every check SVG is `currentColor` in `--accent`. **A11y:** initial pass had axe-core flag `aria-hidden-focus` (serious × 1) on the `.grader-card` because I'd set `aria-hidden="true"` while the inner `.ge` spans had `tabindex="0"` (a focusable element inside an aria-hidden subtree is invalid). Fixed by removing `aria-hidden="true"` and adding `role="figure" aria-label="Esimerkki Puheon arviointinäkymästä — opiskelijan sähköposti, virheet alleviivattuna ja YTL-rubriikin pisteet."` — the showcase content is informative for screen-reader users (real Spanish text + Finnish corrections + score numbers) and was being incorrectly hidden. After fix: axe-core 4.10.2 across the entire page (now hero + problem + pillars + steps + grader): **0 violations** at both viewports. Verified via `scripts/agent-test/loop45-howitworks-grading.mjs` (NEW, 105 lines): 3 steps with nums ["01","02","03"], 5 error annotations in the prose, 4 rubric rows, total score "13 / 20" reads, copy title verbatim. Hover-state screenshot at 1440 confirms the tooltip pops above the first error span with the wavy red underline visible. Read screenshots both viewports: desktop steps row matches Stripe pricing's numbered-step rhythm with the connector line diving under each number; desktop grader showcase has clean dark card on the left + accent-glow-lit copy on the right; mobile collapses both sections cleanly with no horizontal overflow at 375. No SW bump per spec. Files touched L45: `index.html` (+125 lines — 2 sections), `css/landing.css` (+225 lines — `.steps*`, `.grader*`, `.grader-prose .ge*`, `.rubric*`, `.grader-copy*`), `scripts/agent-test/loop45-howitworks-grading.mjs` (NEW), `scripts/agent-test/screenshots/loop45-{steps,grader,grader-tooltip,grader-375}.png` (NEW), `AGENT_STATE.md` (next-pointer → L46), `IMPROVEMENTS.md` (this entry). Sources: `references/puheo-screens/grading-response.json` (L42 fixture — every Spanish/Finnish string and every score number traces back to a real `processGradingResult` field, no invented content); Stripe pricing numbered-step rhythm + connector-line trick (stripe.com/pricing — primary reference for §C5 layout); Linear "How it works" thin-weight numerals (linear.app — confirmed via `references/landing/linear/below-fold-1440.png` from L42); Stripe receipt showcase pattern (per spec D3 — "real data, idealised frame"). Skills: ui-ux-pro-max P2 (token-only — every value via `var(--*)`, the only literals are the wavy-underline category colours `#ef4444 / #facc15` matching real YTL category palette, the dot-shadow accent badge `#052924`, and the popover background `#0a0a0a` for max contrast against the accent-glow backdrop), P5 (motion as state — tooltip fade ≤150 ms, hover lift on cards, all reduced-motion-collapsed via the global L43 rule), P6 (single mint accent — used in numbers, eyebrow, tooltip strong, badge, score; no second accent introduced); puheo-finnish-voice (calm + direct — every step body ends with a verifiable outcome, every grader-tooltip leads with **Oikein: …** then explains the rule, no exclamation marks except in the realistic "¡Hasta pronto!" Spanish closer); puheo-ai-prompt (the showcase is faithful to the AI grader's actual output shape, not an idealised version that would over-promise — student got 13/20 = E, not a perfect 20/20 success theater). Loops 46–47 still owed: pricing + FAQ, then final CTA + footer + responsive pass.

- [2026-04-28 L44] [UI/UX] LANDING_PLAN.md Section C 3–4 — Problem + 3 product pillars. Appended two `<section>` blocks to `index.html` under the existing `<main>`, reusing the `.landing-section` + `.landing-section__inner` scaffold from L43 (96 px / 64 px vertical padding via `var(--section-pad-y)`, 1200 px content max). **Problem** (`#ongelma`, centred): single `.problem__lede` H2 at `clamp(24px, 3.2vw, 32px)` display-weight with `text-wrap: balance` so the two-line break lands evenly across viewports + 18 px `.problem__sub` muted at 720 px max-width with `text-wrap: pretty` for ragged-right rivers — both copy strings verbatim from LANDING_PLAN.md §C3, no decoration, whitespace is the visual per the spec. **Pillars** (`#tuote`, 3-col → 1-col @ ≤880 px): centred eyebrow "Mitä Puheo tekee" in `--accent` + display H2 "Kolme aluetta. Sama YO-rubriikki." (`text-wrap: balance`); grid of three `<article class="pillar">` cards using `--surface` + `--border` + `--r-lg` 16 px radius + 32 px padding, hover lifts the card by `translateY(-2px)` with `--surface-hover` background and a `--border-strong` outline (≤220 ms ease-out), thumbnail image gets a paired `transform: scale(1.03)` zoom on the same hover. Each card carries: 40 px icon tile in `--accent-soft` background + `--accent` foreground holding a Lucide-style inline SVG (`book-open` for Sanasto, sparkles for Kielioppi, pen-tool for Kirjoittaminen — all stroke-only, 22 px, `stroke-width: 1.8`); display H3; 16 px muted body 2-3 sentences; mono `.pillar__metric` line with `--accent` `<strong>` for the headline number; thumbnail at the bottom inside a `aspect-ratio: 16/9` frame with `object-fit: cover` and a per-card `object-position` so each thumbnail crops to the most relevant region of the source screenshot (vocab 50%/30%, grammar 50%/50%, writing 50%/25%). Copy: "Yli 2 000 sanaa B1–B2-tasolla, ryhmiteltynä YO-aiheisiin. Toistoväli kasvaa kun muistat — joka päivä näet tarkalleen sen, mitä on aika kerrata." — uses the **2 000+** round number from spec appendix #3 (real bank total is 2 080: 500+480+480+240+240+60+50+30 — defensible, not invented). "Subjunktiivi, ser/estar, preteriti vs. imperfekti. Kaikki YO-kokeen avainrakenteet harjoituksina, joissa et koskaan näe samaa lausetta kahdesti." — names the highest-tested grammar topics specifically rather than vague superlatives ("specific numbers always beat vague superlatives" — RESEARCH.md tone-of-voice survivor #4). "Tekoäly arvioi kirjoituksesi YTL:n rubriikin mukaisesti: viestinnällisyys, rakenteet, sanasto, kokonaisuus. Saat tarkan suomenkielisen selityksen jokaisesta virheestä." — uses **YTL:n mukainen** which RESEARCH.md flagged as the highest-trust signal in the Finnish edu market. No CTAs on the cards per LANDING_PLAN.md §C4 ("the global hero CTA is enough — don't dilute"). Real Puheo screenshots from L42 (`references/puheo-screens/{mode-vocab,profile,writing}-1440.png`, all 2880×1800 2×-density) carry alt-text describing the actual on-screen content for screen readers, plus `loading="lazy"` so they don't block the hero LCP. **Pillar grid borders top into the section above** via `border-top: 1px solid var(--border)` so the Problem→Pillars seam reads as "and here's how" rather than two unrelated stripes. Verified via `scripts/agent-test/loop44-problem-pillars.mjs` (NEW, 105 lines) at 1440 + 375: pillar count 3, titles `["Sanasto","Kielioppi","Kirjoittaminen"]`, thumbnail images all loaded at 2880×1800 from the right files, problem lede + pillars title text correct at both viewports. axe-core 4.10.2 across the entire page (now hero + problem + pillars, no longer hero alone): **0 violations** at 1440, **0 violations** at 375 — discipline held while the page tripled in surface area. No SW bump per spec. Files touched L44: `index.html` (+105 lines — 2 sections appended), `css/landing.css` (+135 lines — `.problem*`, `.pillars*`, `.pillar*`), `scripts/agent-test/loop44-problem-pillars.mjs` (NEW), `scripts/agent-test/screenshots/loop44-{problem,pillars,fullpage}-{1440,375}.png` (NEW, 6 shots — all read in-loop, layout passed first read), `AGENT_STATE.md` (next-pointer → L45), `IMPROVEMENTS.md` (this entry). Self-review of read screenshots: desktop pillars match Stripe's product-feature grid rhythm (icon tile + display title + body + metric line + tightly-cropped thumbnail at the bottom — all weights and spacings align with Stripe's "Built for builders" section); mobile stacks 1-col cleanly with cards reading top-to-bottom, no horizontal overflow at 375. Sources: `references/landing/linear/below-fold-1440.png` (Linear's 3-column "Built for the way teams work" feature grid — primary layout reference), Stripe product-feature grid pattern (stripe.com — icon-tile + title + body + cropped product fragment hierarchy), Vercel "what's new" cards (vercel.com — equal-width grid with elevated surfaces); `references/puheo-screens/{mode-vocab,profile,writing}-1440.png` (L42 captures, embedded). Skills: ui-ux-pro-max P2 (token-only — every value via `var(--*)`, the per-card `object-position` percentages are the only literals and they're crop coordinates), P5 (motion as state — hover transform on card + thumbnail zoom both ≤320 ms ease-out, reduced-motion already collapsed at the global rule from L43), P6 (single mint accent — used in icon tiles, eyebrow, metric strong; no second accent introduced); puheo-finnish-voice (no exclamation marks, no "Vain X paikkaa jäljellä", no "Yli 6 681 000 opiskelijaa" hype — calm + direct + specific + YTL-anchored, exactly the tone-of-voice survivors prescribed). Loops 45–47 still owed: how-it-works + AI-grading showcase, pricing + FAQ, final CTA + footer + responsive pass.

- [2026-04-28 L43] [UI/UX] LANDING_PLAN.md Section B + C 1–2 — sticky nav + hero. Rebuilt `index.html` from the old 1938-line light-mint document into a 175-line dark-mode shell scoped under `<html class="landing">`: kept SEO/JSON-LD scaffolding (SoftwareApplication + Organization), swapped its single `<link rel="stylesheet" href="landing.css">` for two new sheets (`css/landing-tokens.css` from L42 + new `css/landing.css`, 326 lines, components-only), swapped Inter+DM Mono for Geist + Geist Mono via Google Fonts (Inter remains in stack as fallback per L42 `@font-face Geist Fallback / size-adjust:100%`). Sticky 64 px nav: wordmark with mint accent dot left ("Puheo"), centered anchor links "Tuote / Hinnoittelu / FAQ", "Kirjaudu" ghost + "Aloita ilmaiseksi" filled accent right; transparent at top, flips to `rgba(10,10,10,0.78)` + `backdrop-filter: saturate(140%) blur(12px)` + 1 px bottom border via `[data-scrolled="true"]` toggled by a 12-line passive scroll listener (no library). Mobile (≤720 px) collapses centered links + the Kirjaudu ghost — only the wordmark + "Aloita ilmaiseksi" remain in the bar so the primary CTA still dominates above the fold. Hero 60/40 desktop, stacked mobile (`grid-template-columns: minmax(0, 1.15fr) minmax(0, 1fr)` with `clamp(40px, 6.5vw, 72px)` H1 sizing, tracking `-0.04em`): eyebrow "ESPANJAN YO-KOE • LYHYT OPPIMÄÄRÄ" (uppercase, tracking-wide, `--text-subtle`), H1 `<span class="hero__title-accent">Pärjää espanjan YO-kokeessa.</span><br>Ilman kalliita kursseja.` with the first line as a `linear-gradient(180deg, #fafafa, #a1a1aa)` background-clip:text fade exactly per Linear's hero treatment (`references/landing/linear/hero-1440.png`), 18→20 px sub-headline at `--text-muted` per LANDING_PLAN.md §C2 verbatim copy, primary "Aloita ilmaiseksi →" + ghost "Katso miten se toimii" buttons (44 px regular / 52 px `.btn--lg` per the spec; both wired to `/app.html#rekisteroidy` and `#miten` per spec D5), 14 px trust strip "Ei luottokorttia • Suomen kielellä • Toimii selaimessa" with semantic `•` separators. Visual column: vanilla CSS browser-frame component (~70 lines incl. dots+url+viewport — within the spec D7 ~40-line budget for the frame proper plus the floating-card extension): traffic-light dots in desaturated palette (`#4a3030 / #4a4530 / #2f4a36` so they read as macOS-ish without the saturated reds/yellows that screamed "marketing site"), URL pill with `--font-mono` reading "puheo.fi/oma-sivu" at `--text-muted` (12 px after axe bumped from 11/`--text-subtle` — see below), viewport with `aspect-ratio: 1440/900` showing real `references/puheo-screens/dashboard-1440.png` from L42, `transform: rotate(-3deg)` per LANDING_PLAN.md §C2 with hover de-rotation to `-1.5deg + translateY(-4px)`. Floating Puheo product fragment behind/below-left of the frame: real streak chip — 32 px gradient avatar "ro" (mint→`#5eead4` linear-gradient, `#052924` text), monospace "ronja" name, "12 pv" with mint flame SVG — at `transform: rotate(-3deg)` lifted by `box-shadow: var(--shadow-glow)` (the L42 `0 40px 80px -20px var(--accent-glow)` with `rgba(45,212,191,0.20)` mint glow). Subtle radial accent glow blob behind the entire visual via `.hero__glow` `position:absolute; inset:-10% -20%; background: radial-gradient(closest-side, var(--accent-glow), transparent 70%); filter: blur(40px); z-index:-1` so the hero feels lit from inside without any decoration competing with the H1. Skip-link `<a class="skip-link" href="#hero">Siirry sisältöön</a>` left of viewport; lands inside the visible region on focus. Verified via `scripts/agent-test/loop43-nav-hero.mjs` (NEW, 130 lines) at 1440×900 + 375×812: primary CTA href `/app.html#rekisteroidy`, nav Kirjaudu href `/app.html#kirjaudu`, H1 "Pärjää espanjan YO-kokeessa. Ilman kalliita kursseja.", trust strip "Ei luottokorttia • Suomen kielellä • Toimii selaimessa", dashboard image renders 2880×1800 (2×-density crop at top-center), floating card visible, nav `data-scrolled` flips false→true after `window.scrollY > 8` on the mobile (taller stacked) viewport (desktop short-circuits because nav+hero alone fits in 900 px — flips will appear once L44+ stack content below). axe-core 4.10.2 injected via the CSP-allowed `cdn.jsdelivr.net` and run against the live page: **0 violations** at both viewports (caught and fixed in-loop one `color-contrast` serious on `.browser-frame__url` reading 3.21:1 — `--text-subtle:#71717a` on the elevated frame surface; bumped to `--text-muted:#a1a1aa` + 12 px font for AAA compliance, contrast now well above 4.5). No SW bump per spec — landing stays out of `STATIC_ASSETS` for always-fresh marketing copy. Files touched L43: `index.html` (REWRITE — 1938→175 lines, kept canonical/JSON-LD/manifest/theme-color/og + twitter blocks, dropped 1700+ lines of inline `<style>` for mini-diag / compare-table / rubric-grid / urgency-bar / pricing / faq / footer that L44–L47 will replace cleanly), `css/landing.css` (NEW, 326 lines — scaffold + buttons + nav + hero + browser-frame + floating-card + skip-link, all under `.landing` scope), `scripts/agent-test/loop43-nav-hero.mjs` (NEW), `scripts/agent-test/screenshots/loop43-hero-{1440,1440-scrolled,375,375-full}.png` (NEW, 4 shots — read in-loop, layout passed first read), `AGENT_STATE.md` (next-pointer → L44), `IMPROVEMENTS.md` (this entry). Self-review of the read screenshots: desktop hero composition matches the Linear reference's (a) generous left-column whitespace under the nav, (b) gradient-into-muted display headline, (c) isolated UI fragment dropped diagonally into the right column with shadow lift; mobile stacks cleanly with the browser frame + streak chip falling beneath the trust strip — no horizontal overflow at 375. Sources: `references/landing/linear/hero-1440.png` (primary aesthetic — gradient hero text + isolated UI fragment lower-right, source linear.app/), `references/landing/vercel/hero-1440.png` (centered display + secondary visual, source vercel.com/); LANDING_PLAN.md §C1–C2 + spec D7 (browser-frame ~40 lines / streak-chip-as-floating-card decision); `references/puheo-screens/dashboard-1440.png` (L42 capture — embedded under glass). Skills: ui-ux-pro-max P2 (token-only — every color/space/radius via `var(--*)`, zero hex literals in `landing.css` outside the desaturated traffic-light dots and the `#052924` button-fg readability override), P5 (motion as state — nav scroll flip, browser-frame hover de-rotation, both ≤320 ms ease-out; reduced-motion media query collapses both back to the static `-3deg` rest pose), P6 (single accent discipline — mint `#2DD4BF` reused from app, no second accent introduced for landing despite the visual freedom of a fresh CSS file); puheo-finnish-voice (eyebrow "Espanjan YO-koe • Lyhyt oppimäärä", H1 verbatim from LANDING_PLAN.md §C2, sub-headline verbatim, trust strip three-token grammar passes Finnish ear).

- [2026-04-28 L42] [Planning/Tooling] LANDING_PLAN.md Section A — Setup. Six sub-deliverables for the 8-loop landing rebuild (L42–L49 per the new PLAN.md item 7). (a) Skimmed `landing/` (DESIGN.md / AUDIT.md / RESEARCH.md / PLAN.md, ~80 KB of prior strategy from 2026-04-23) and folded a 5-bullet survivors block into `docs/superpowers/specs/2026-04-28-landing-rebuild-design.md` as an appendix — exam date pinned at **28.9.2026** (current landing JS has wrong `new Date(2026,8,15)`, future loops use 28); Y-tunnus / live-payment constraint flagged but neutralised by LANDING_PLAN.md's free-signup-only CTA rule (Pro card CTA → `/app.html#rekisteroidy`, not LemonSqueezy); "2 000+ harjoitustehtävää" round-number sourced from PLAN.md (real total: 2 080 = 500 vocab + 480 aukko + 480 matching + 240 translation + 240 sentence + 60 reading + 50 correction + 30 writing); tone-of-voice rules from RESEARCH.md (calm + direct, "YTL:n mukainen" = highest-trust signal in Finnish edu market); 6 reusable FAQ answers covering YTL endorsement, format-changes, GDPR, mobile, AI-vs-teacher, cancellation. Discards documented (mini-diagnostic, urgency bar, Duolingo/Abitreenit comparison table, fictional Liisa essay sample). (b) Captured Linear / Vercel / Stripe / Cron (cal.com) / Duolingo via `scripts/agent-test/loop42-refs-capture.mjs` — Playwright launches headless Chrome at 1440×900, dismisses cookie banners (`button:has-text("Accept all")` etc), screenshots hero + below-fold per site. 10 PNGs landed in `references/landing/<site>/`. Visual confirmation: Linear's dark restraint + isolated UI fragment in lower-right is the primary aesthetic target (sources: linear.app/, vercel.com/, stripe.com/, cal.com/, www.duolingo.com/). (c) Wrote `css/landing-tokens.css` (162 lines) — scoped to `.landing` class so it doesn't leak into app.html (which keeps Cuaderno light tokens in style.css). Dark surface palette (`--bg: #0a0a0a`, `--bg-gradient: radial-gradient(ellipse at top, #1a1a2e, #0a0a0a 60%)`, `--surface: rgba(255,255,255,0.04)`), Geist + Inter + system-ui font stack with a `@font-face Geist Fallback / size-adjust: 100%` to prevent FOUT layout shift, 8/16/24/32/48/72/96 type scale, `--tracking-tight: -0.04em` for displays, motion 150-300ms ease-out, `prefers-reduced-motion` collapse, `--exam-date-iso: "2026-09-28"` as a CSS custom-prop single-source-of-truth so JS countdown logic across L43+ lands on the right date. Reuses the existing app `--accent: #2DD4BF` and `--accent-hover: #14B8A6` for landing→app brand continuity. (d) Captured Puheo screens via `scripts/agent-test/loop42-puheo-screens.mjs` — Playwright with `serviceWorkers: "block"` + 17 explicit `/api/*` mocks plus a `**/api/**` catch-all. Discovered the hard way that **Playwright matches routes in REVERSE registration order** (most-recently-added handler is checked first), so the catch-all must be registered FIRST and specific routes LAST — opposite of express. Also needed correct response shapes: `/api/placement/status` returns `{ completed: true }` (not `{ status: "complete" }` — the placement.js consumer reads `completed`). 8 screenshots landed (dashboard / profile / mode-vocab / writing × {1440, 375}) — real Puheo content rendered: "Iltaa, ronja" greeting, 12-day streak chip, 47-session count, daily challenge card, "Kertaa nyt — 14 korttia" SR review card, "Aktiivisuus · 30 päivää" + "Kehitys ajan mittaan" charts, settings chips on profile, full mode lists on vocab/writing pages. These embed in L43's browser-frame mockups + L44's product-pillar thumbnails. (e) Built `scripts/agent-test/loop42-capture-grading.mjs` for one-time real-`/api/grade-writing`-JSON capture (logs in with `TEST_PRO_EMAIL` + `TEST_PRO_PASSWORD` from `.env`, fetches a writing-task via `/api/writing-task`, POSTs a deliberately-flawed B1/B2-student-style Spanish email touching the most-graded YO-koe error categories — subjunctive, ser/estar, preterite/imperfect, accents — captures the response). No test creds in `.env` yet, so shipped a hand-crafted placeholder fixture at `references/puheo-screens/grading-response.json` with `_placeholder: true` marker — matches the real `processGradingResult` output shape exactly (rawScore/finalScore/maxScore/ytlGrade + 4 dimension blocks {viestinnallisyys, kielen_rakenteet, sanasto, kokonaisuus} each with score+feedback_fi + corrected_text + errors[6] + annotations[3] + overall_feedback_fi + next_action_fi). All Finnish copy is realistic and references real YO-koe error patterns; L45's showcase markup ships against it either way. User can drop creds + run the script any time before L45 to replace with real output. (f) Extended `js/screens/auth.js` to honour `#rekisteroidy` / `#kirjaudu` URL hashes alongside the existing `?mode=register` query-param handler (5-line diff: read `window.location.hash`, OR with the param check, queueMicrotask the appropriate tab click). Parse-checked. Now landing CTAs can deep-link to `/app.html#rekisteroidy` per the spec's D5 decision and the register tab pre-selects on auth-screen mount. Files touched L42: `docs/superpowers/specs/2026-04-28-landing-rebuild-design.md` (+71 lines appendix), `js/screens/auth.js` (+5 lines), `css/landing-tokens.css` (NEW, 162 lines), `references/landing/{linear,vercel,stripe,cron,duolingo}/{hero,below-fold}-1440.png` (NEW, 10 files), `references/puheo-screens/{dashboard,profile,mode-vocab,writing}-{1440,375}.png` (NEW, 8 files), `references/puheo-screens/grading-response.json` (NEW, placeholder), `scripts/agent-test/loop42-{refs-capture,puheo-screens,capture-grading}.mjs` (NEW, 3 scripts), `AGENT_STATE.md`, `IMPROVEMENTS.md` (this entry). No SW bump — landing files are not in `STATIC_ASSETS` per the spec (always-fresh marketing). Sources: linear.app, vercel.com, stripe.com, cal.com, www.duolingo.com (counter-example), `landing/{DESIGN,AUDIT,RESEARCH,PLAN}.md` (in-repo prior strategy, 2026-04-23). Skills: superpowers:using-superpowers (skill-first discipline), no other skill applied — Section A is pure tooling/setup.


- [2026-04-28 L48-hotfix] [UI/UX] Auth screen layout fix — `body.auth-mode .app-main-inner` was clamped to `max-width: 480px`, which overrode `.auth-shell`'s 1080px desktop layout and clipped the dark-teal aside panel (visible at all viewports ≥ 960 px). Bumped to `min(1120px, 100%)` so the 2-col aside+form grid renders fully; `.auth-inner` keeps its own 420 px form-column cap. Added `min-width: 0` on `.app-main` to prevent grid blowout, and a `padding: 24px 16px` mobile override for tight gutters. axe re-run at 1440 + 375 — 0 violations on `#screen-auth` after a separate teal-700 fix on `.auth-tab.active` (was `--accent-hover #14B8A6` on white = 2.48:1; now `#0F766E` = 6.6:1). Files: `css/components/app-shell.css`, `style.css`. Skill: ui-ux-pro-max P1 (axe 4.5:1) + P4 (single grid breakpoint at 960 px).
- [2026-04-28 L48-hotfix] [UI/UX] Landing nav wordmark parity — replaced `<span class="nav__brand-dot">` + "Puheo" with `Puhe<span class="nav__brand-o">o</span>` so the dark-landing brand reads identically to the app sidebar (font-weight 800, letter-spacing -0.03em, accent `o` in `var(--accent)` `#2DD4BF`). Old `.nav__brand-dot` CSS rule deleted, new `.nav__brand-o` rule replaces it. Files: `index.html`, `css/landing.css`.
- [2026-04-28 L48-hotfix] [Feature] Logged-in chip on landing nav — inline-script auth detection: reads `localStorage.puheo_token` (same key the app uses in `js/api.js`) and on token presence hides `#nav-login` + `#nav-signup` and shows `#nav-chip` ("Jatka harjoittelua →") linking to `/app.html`. No API call, no flicker — the swap runs in the existing nav script before paint stabilises. Token absence keeps the standard "Kirjaudu / Aloita ilmaiseksi" pair. Verified at 1440: logged-out shows both buttons, logged-in (after `localStorage.setItem('puheo_token', ...)` + reload) shows the single chip. Files: `index.html`.
- [2026-04-28 L48-hotfix] [UI/UX] Right rail rebuild — removed the large Pro upsell card (`#rail-panel-pro` / `.rail-upsell` block, lines 2008–2028 of app.html and 80 lines of CSS in `css/components/rail.css`). The daily peek (today's progress + word of day) now shows for free AND Pro users — no more pro-vs-free panel toggle. Pro users get a small `.rail-pro-badge` pill ("Pro aktiivinen") near the rail footer instead of the upsell. `js/screens/dashboard.js renderRail()` simplified: dropped `proPanel.hidden = !!pro` / `dailyPanel.hidden = !pro` toggle, kept user-card top + word-of-day; new `proBadge.hidden = !pro` line. Files: `app.html`, `css/components/rail.css`, `js/screens/dashboard.js`.
- [2026-04-28 L48-hotfix] [Feature] Pro upsell popup — new dismissible bottom-right `<aside id="pro-popup">` (320 px desktop, full-width-with-12px-gutter mobile). Free-only, gated by `window._isPro`. Triggers 5 s after dashboard load via `setTimeout`; slide-up + fade-in (`transform: translateY(12px) → 0` + `opacity: 0 → 1`, 300 ms `--ease-out`) toggled by `.is-visible` class on `requestAnimationFrame`. Dismiss via X button (`aria-label="Sulje"`, 32 × 32 px hit area) writes `localStorage.puheo_pro_popup_dismissed` = `Date.now()`; subsequent loads check `Date.now() - dismissedAt < 7 days` and skip the popup. CTA wires to the existing `_deps.startCheckout()` (LemonSqueezy). `prefers-reduced-motion: reduce` collapses the transition to `0 ms`. Pattern source: Sonner toast / 21st.dev notification — restrained single-CTA card with X close in top-right corner. Copy follows `puheo-finnish-voice` (lede "Pääse YO-kokeesta läpi paremmin arvosanoin." kept verbatim from L41 rail card; CTA "Kokeile Pro 7 pv ilmaiseksi +" matches the same string the auth/checkout pages use). Files: `app.html`, `css/components/pro-popup.css` (NEW, 156 lines), `js/screens/dashboard.js` (added `wireProPopup()`).
- [2026-04-28 L48-hotfix] [A11y] axe-core sweep at 1440 + 375 across landing (logged-out + logged-in), `#screen-auth`, and `#screen-dashboard` after the rail rebuild — **0 violations** on every viewport/state combination. Verified by `scripts/agent-test/loop48hotfix-verify.mjs` (NEW). The one violation surfaced during the run (`color-contrast` on `.auth-tab.active`) is fixed in the auth-screen entry above. Files: `scripts/agent-test/loop48hotfix-verify.mjs` (NEW).
- [2026-04-28 L48-hotfix] [Infra] SW cache `puheo-v91 → puheo-v92` and added `/css/components/pro-popup.css` to STATIC_ASSETS so the new sheet precaches on install. Forces a fresh fetch of `index.html` after the dark-landing rebuild — addresses the user-reported "old version still loading" symptom (root cause: previous SW kept serving the cached pre-L43 light landing until the next stale-while-revalidate cycle; bumping the cache version expires the old entry on activate). Files: `sw.js`.

- [2026-04-28 L48-hotfix] [Bug] `computeReadinessMap` no longer pushes 4 phantom level-0 writing cells when `writingDims === null`. Pre-fix, a 3-mastered-path-cell user with no writing showed `readinessPct = 8 / (3+4)*4 ≈ 29%`; now the 4 zero cells are excluded and the same user reads `(4+4+3)/(3*4) = 92%`. Empty cells case still safely returns `readinessPct: 0`. Tooltip + dashboard label use the dynamic denominator. New `tests/readiness-map.test.js` covers (a) `writingDims=null` → no writing cells, ≥75 % readiness, (b) `writingDims` provided → 4 writing cells included, (c) empty path + null → totalCells 0, readiness 0. All 3 vitest pass. Files: `js/features/writingProgression.js`, `tests/readiness-map.test.js` (NEW).
- [2026-04-28 L48-hotfix] [UI/UX] Dashboard YO-valmiuskartta summary now appends a qualitative label after the percentage: `<25 → "alkuvaiheessa"`, `25–49 → "hyvässä vauhdissa"`, `50–74 → "hyvin hallussa"`, `≥75 → "erinomaisesti"` (suppressed when `totalCells = 0`). Denominator was already dynamic via `${map.totalCells}` from L41 — the L48 readiness-map fix above just makes that number reflect real engagement instead of a phantom 14. Copy follows `puheo-finnish-voice` (specific + encouraging — "alkuvaiheessa" beats "vähän hyvin", "erinomaisesti" reads as celebration not flattery). New helper `readinessQualitative(pct, total)` colocated next to the renderer. Files: `js/screens/dashboard.js`.
- [2026-04-28 L48-hotfix] [UI/UX] Dark theme toggle now actually flips the palette. Added `:root[data-theme="dark"]` block in `style.css` that overrides `--bg/--surface/--surface-2/--border/--border-strong/--ink/--ink-soft/--ink-faint/--accent-soft` and `--sh-{rest,hover,lift}` — values mirror `css/landing-tokens.css` (`#0a0a0a / #141414 / #1a1a1a` surfaces, `#fafafa / #d4d4d8 / #a1a1aa` ink). `--ink-faint` lifted from zinc-500 (`#71717a`, 4.36:1 on `#0a0a0a` — fails AA) to zinc-400 (`#a1a1aa`, 9.06:1 — passes AAA). Accent / feedback / grade colors unchanged in both themes. `applyThemeChoice()` (`js/screens/settings.js`) now resolves `auto` via `matchMedia("(prefers-color-scheme: dark)")` and writes the effective `light`/`dark` value to `<html data-theme>`; binds a one-time `change` listener so OS-level scheme flips re-render the page when the user is on Auto, but explicit Light/Dark choices stick. A 9-line pre-paint script in `app.html` resolves the saved theme + system preference and sets `data-theme` BEFORE first paint to prevent a light-flash on dark-mode load. Fixed one collateral contrast violation: `.btn-primary` was using `color: var(--ink)` over `var(--accent)`, which in dark mode dropped to 1.78:1 (white-on-mint); pinned to `#052924` (deep teal, 12.7:1 on mint, matches landing) since the mint background is theme-stable. SW cache `puheo-v92 → puheo-v93`. axe 4.10 swept at 1440 + 375 in BOTH light and dark — **0 violations** in every viewport/theme combination. Verified by `scripts/agent-test/loop48hotfix-theme-verify.mjs` (NEW) which seeds `puheo_token` + `puheo_theme` via `addInitScript`, asserts `<html data-theme>` matches the seeded value, asserts body bg is near-black in dark / near-white in light, runs axe, captures 4 screenshots. Full shared-tokens refactor explicitly deferred to L52–L55. Files: `style.css`, `js/screens/settings.js`, `app.html` (pre-paint script), `sw.js`, `scripts/agent-test/loop48hotfix-theme-verify.mjs` (NEW).

- [2026-04-28 L48-hotfix-2] [Infra/Bug] Localhost-only landing scroll lock — root cause: SW v91/v92 had cached the pre-L43 (1938-line, light-mint) `index.html` under cache-first with stale-while-revalidate, so users on dev got the old document on every reload until the SW happened to hit network. The page DOM still referenced `style.css` with its `.app-main { min-height: 100vh; position: relative }` + `.screen { position: absolute; inset: 0 }` rules, which froze scroll past the first viewport-height column. Vercel users had cleaner SW state because the production deployment landed AFTER L43 (so v91 first cached the new doc). Fix: navigation requests (`e.request.mode === "navigate"`) now go **network-first** with cache fallback only on offline — marketing copy and SPA shell are always fresh; static asset rules stay cache-first (still fast). SW cache `puheo-v93 → puheo-v94` so existing clients invalidate on next reload. Diagnostic confirmed via `scripts/agent-test/loop48hotfix2-scroll-diagnose.mjs`: zero `position:fixed` / `overflow:hidden` offenders on `<html>`/`<body>`, `scrollHeight = 6367 px`, footer reachable at 1440. Files: `sw.js`, `scripts/agent-test/loop48hotfix2-scroll-diagnose.mjs` (NEW).
- [2026-04-28 L48-hotfix-2] [UI/UX] Pillar cards now show inline interactive demos instead of low-res screenshots. The three `.pillar__thumb <figure>` blocks (each containing a 1440×900 PNG of an app screen, illegible at the 280 px card width and lazy-loaded so they often paint blank) are replaced with vanilla HTML+CSS mockups that communicate the product's value at a glance. **Sanasto**: `<ul class="pillar__demo-opts">` showing `¿Qué significa «el estrés»?` with 4 options — correct one (`stressi ✓`) gets `--accent` border + `--accent-soft` background, wrong ones stay neutral. **Kielioppi**: gap-fill `Ayer nosotros ___ al cine con los amigos.` with three chips (`fuimos ✓` accent, `íbamos` red-tinted, `vamos` accent), uppercase label `TÄYDENNÄ PUUTTUVA MUOTO`, hint line `Preteriti — menneisyyden yksittäinen teko`. **Kirjoittaminen**: `<p class="pillar__demo-excerpt">` with two `<span class="pillar__demo-err">` wavy red underlines (`fuimos`, `era`) and a flex-row rubric showing `Viestinnällisyys 4/5 · Kielen rakenteet 2/5 · Sanasto 4/5` (scores in `--accent`). All three blocks share `var(--bg-elevated)` background + `var(--border)` 1 px stroke + 16 px padding, scoped via `.landing .pillar__demo` (95 lines new in `landing.css`); old `.pillar__thumb` rules + per-card `object-position` cropping deleted (24 lines removed). `aria-hidden="true"` on each demo so screen readers skip the decorative content. Bumped 3 small-text classes (`-label`, `-hint`, `-dim`) from `--text-subtle` (`#71717a`, 3.5:1 on `#141414` — fails AA) to `--text-muted` (`#a1a1aa`, 9.0:1 — passes AAA), and the wrong-chip from `rgba(220,38,38,0.8)` (3.0:1, fail) to `#fca5a5` (red-300, 6.0:1, pass). 21st.dev exploration explicitly skipped per the autonomous-loop ship-over-polish style — the inline spec was already pixel-specific and the dark aesthetic matches the existing `.landing` token system. Verified by `scripts/agent-test/loop48hotfix2-verify.mjs` (NEW) at 1440 + 375: all three demos render, none overflow their pillar card (`getBoundingClientRect()` checked against the parent `.pillar` rect within 0.5 px), `<.landing-footer>` visible after `scrollTo(scrollHeight)`, axe-core 4.10 reports **0 violations** on both viewports. Files: `index.html` (3 thumb→demo replacements), `css/landing.css` (-24 / +95), `scripts/agent-test/loop48hotfix2-verify.mjs` (NEW), `scripts/agent-test/screenshots/loop48hotfix2-{pillars,fullpage}-{1440,375}.png` (NEW, 4 shots). Skill: ui-ux-pro-max P1 (axe 4.5:1 on every text token), P6 (single accent — mint stays the only correct/score color, red is reserved for error states only). Sources: spec inline in user prompt; existing `.grader-prose .ge` red wavy treatment from L43 reused for `.pillar__demo-err`.


- [2026-04-29 L49] [Infra/UX] Landing mobile + a11y/perf audit — final loop of the LANDING_PLAN.md rebuild. **Responsive sweep** at 4 viewports (375 / 768 / 1024 / 1920) via new `scripts/agent-test/loop49-responsive.mjs` — captures full-page screenshots, probes horizontal-overflow (compares `documentElement.scrollWidth` vs `clientWidth` and lists every descendant whose `getBoundingClientRect().right` exceeds the viewport with non-fixed position), runs axe-core 4.10.2 with `wcag2a/aa/21a/21aa`, scans heading hierarchy for `H{n}→H{n+2+}` jumps, scans for `target="_blank"` anchors missing `rel=noopener`, and inventories every `<img>`'s alt/width/height/loading. **Result on first run, zero fixes needed:** all four viewports clean — `hScroll=false`, `overflow=0`, `axe=0`, `headingJumps=0`, `unsafeBlankTargets=0`, single `<img>` (the hero dashboard PNG) has `alt="" width=1440 height=900 loading="eager" decoding="async"`. The 768-px mid-band concern (`≤880` is the smallest desktop-collapse breakpoint) turned out fine — at 768 the page already renders single-column mobile because the ≤880 query hits, leaving the 768-880 band as desktop-still + content clamped to 720 px inner via `--content-max: 1200px` minus 24 px section padding. Read all 4 screenshots: 1920 sits Linear-style at 1200 px clamped centre with breathing dark margins, 1024 hits desktop 60/40 hero + 3-col pillars + 50/50 grader + 2-col pricing, 768 collapses to single column, 375 stacks cleanly with no overflow. **Lighthouse** (`npm run audit:lighthouse:landing`, desktop preset, headless): **97 / 100 / 100** (perf / a11y / best-practices) — all above the ≥ 95 gate. First run was 97 / 100 / 96 with two 404s in `errors-in-console` (`/favicon.ico` + `/icon-192.png`); manifest.json references `/icon-192.png` + `/icon-512.png` which never existed on disk. Generated three icons (`icon-192.png`, `icon-512.png`, `favicon-32.png`) via new `scripts/agent-test/loop49-gen-icons.mjs` — Playwright renders a 22%-radius dark-slate square with a Geist-700 "Po" mark (the o in `var(--accent) #2DD4BF`, matching the wordmark used in the nav since L48-hotfix) plus a 30/25 radial accent glow, captures via headless screenshot at the requested DPR=1 size. Added `<link rel="icon" href="/favicon-32.png" type="image/png" sizes="32x32">` + `<link rel="apple-touch-icon" href="/icon-192.png">` to **both** `index.html` and `app.html` for parity. Re-audit: best-practices flipped 96 → 100; LCP 1.1 s, FCP 0.7 s, TBT 0 ms, CLS 0.001, SI 0.7 s. **Old `landing.css` deletion deferred** — grep surfaced 14 active references beyond `index.html`: 6 blog pages (`blog/{index,espanja-yo-koe-2026-lyhyt-oppimaara,por-vs-para-selkea-ero,ojala-subjunktiivi-yleisimmat-virheet,ser-vs-estar-milloin-kumpaakin,preteriti-vs-imperfekti-opas}.html`), 4 legal pages (`pricing.html`, `privacy.html`, `refund.html`, `terms.html`), `sw.js` cache list, and 7 unit tests in `tests/` that read the file's contents and run assertions against it. Verified all are live on dev (`pricing.html`, `terms.html`, `blog/` all 200). Brief assumed only `index.html` referenced the old sheet; reality is broader and deletion would visually break the blog + legal pages immediately. Punts cleanly to L52–L55 dark-theme migration where blog/legal will be ported to the new `.landing`-scoped tokens and their `<link>` swapped to `css/landing.css` + `css/landing-tokens.css`. SW cache `puheo-v94 → puheo-v95` because `index.html` and `app.html` (both in STATIC_ASSETS) were edited; added the 3 new icons to STATIC_ASSETS so they cache offline. Files: `index.html`, `app.html`, `sw.js`, `icon-192.png` (NEW), `icon-512.png` (NEW), `favicon-32.png` (NEW), `scripts/agent-test/loop49-responsive.mjs` (NEW), `scripts/agent-test/loop49-gen-icons.mjs` (NEW), `scripts/agent-test/screenshots/loop49-{375,768,1024,1920}-full.png` (NEW), `exercises/baselines/lighthouse-landing.json` (regen). Skill: ui-ux-pro-max P1 (axe 0/0/0/0), P3 (perf 97 with sub-second FCP/SI), puheo-screen-template (4 breakpoints — 375 / 768 / 1024 / 1920 — pixel-checked).

## Landing rebuild — final (L42 → L49)

**Eight loops, one cohesive landing.** `index.html` rewritten from a 1938-line legacy light-mint document into a 756-line dark Linear/Vercel-restraint shell scoped under `<html class="landing">`; ten `<section>` blocks (nav, hero, problem, pillars, steps, grader, pricing, FAQ, CTA, footer) all built off `.landing-section` + token-only CSS in `css/landing.css` (1525 lines) + `css/landing-tokens.css` (156 lines). Six register-CTAs across the page all point at `/app.html#rekisteroidy` (Y-tunnus / live-payment constraint — Pro upsell happens inside dashboard, never on landing).

**Final scores (2026-04-29):**

| Metric | Score | Threshold |
|---|---|---|
| Lighthouse Performance | **97** | ≥ 95 ✓ |
| Lighthouse Accessibility | **100** | ≥ 95 ✓ |
| Lighthouse Best Practices | **100** | ≥ 95 ✓ |
| axe-core violations (375 / 768 / 1024 / 1920) | **0 / 0 / 0 / 0** | 0 ✓ |
| FCP | 0.7 s | < 1.8 s ✓ |
| LCP | 1.1 s | < 2.5 s ✓ |
| TBT | 0 ms | < 200 ms ✓ |
| CLS | 0.001 | < 0.1 ✓ |
| Speed Index | 0.7 s | < 3.4 s ✓ |
| Heading-hierarchy jumps | 0 | 0 ✓ |
| Horizontal scroll @ 375 / 768 / 1024 / 1920 | none | none ✓ |
| `target="_blank"` without `rel=noopener` | 0 | 0 ✓ |

**Final-loop screenshots:** `scripts/agent-test/screenshots/loop49-{375,768,1024,1920}-full.png`. Per-section verification screenshots from L43–L48 remain alongside.

**21st.dev sourcing trail (L46 → L48):** pricing → `community/components/efferd/pricing/default`, FAQ → `community/components/tailark/faq/faq-accordion-with-title`, final CTA → `community/components/dhiluxui/spotlight-cta/default`, footer → `community/components/shadcnblockscom/footer-7/default`. All ported React+Tailwind → vanilla matching `css/landing.css` patterns. Structure from 21st.dev, copy from Puheo (Finnish, never generic SaaS).

**Deferred / open:**

- **Old `landing.css`** (1332-line light-mint sheet) still on disk and still actively linked by 6 blog pages + 4 legal pages + `sw.js` cache list + 7 unit tests. Deletion punts to **L52–L55 dark-theme migration** where blog + legal will be ported to the new `.landing`-scoped tokens and their `<link>` swapped to `css/landing.css` + `css/landing-tokens.css`.
- **Hero dashboard PNG** (`references/puheo-screens/dashboard-1440.png`, 2880×1800 source, displayed 1440×900) is the single largest network asset on the page. Already has `loading="eager" decoding="async" width="1440" height="900"` so LCP lands at 1.1 s on desktop preset; a WebP/AVIF variant would shave a few hundred ms but is not required for the ≥ 95 gate. `cwebp` / `sharp` are not currently available in the dev env. Punt to a future micro-loop or wire `sharp` as a devDependency if a perf-budget tightening is ever asked for.
- **TODO placeholders the user needs to fill** before launch:
  - **Real social-proof numbers** — the page currently leans on product claims (`2 000+ harjoitustehtävää`, `YTL:n mukainen rubriikki`) rather than user-count or testimonial logos. Add a real "Testimonials / Social proof" block **only when there are real students whose results can be cited** — invented social proof was explicitly excluded per spec.
  - **Real blog post imagery** for the 6 blog pages once L52–L55 ports them to the new theme.
  - **Y-tunnus + payments live** — until that lands, every "Aloita Pro 9,99 €" path on the landing routes to free signup at `/app.html#rekisteroidy`. The page is structurally ready to flip to live-payment CTAs (e.g. inline LemonSqueezy checkout) the moment the legal entity is on file.
  - **Sosiaaliset kanavat** — the footer was deliberately built without a social-icon row because Puheo doesn't have public socials yet. Add an `<ul class="landing-footer__social">` once an account is live.

**After L49:** PLAN.md continues with **L50–L51** (right-rail rebuild + dismissible Pro popup, only after 5 s on page, 7-day suppress on dismiss, free-only) and **L52–L55** (dark-theme polish across the entire app — shared tokens file `css/tokens-shared.css`, app theme toggle keeps Auto/Vaalea/Tumma working, blog + legal pages migrate off `landing.css`).

---

## L-PLAN-1 — onboarding redesign + placement upgrade (2026-04-28)

- **[2026-04-28 L-PLAN-1] UPDATE 1 — placement-question hardening + adaptive M_hard anchors + timing-confidence helper.** `lib/placementQuestions.js`: every question now carries `plausible_wrong_options: true` after a distractor audit (B5 swapped joke distractor "sää" → "kauppa" + "loma"; A1 swapped "kissa" → "tie" so all 4 options are physical-place nouns; B/C/M-tier already audited as plausible-only). Added two `M_hard` anchor questions (`MH1` pluskvamperfekti `se habían ido`, `MH2` subjunktiivi imperfekti `si tuviera`) — only injected by the OB-2 client when the student answers every A/B/C item correctly, signal of potential Kurssi 7 placement. Kept `selectDiagnosticQuestions(...)` returning a flat `Array` (legacy `/api/placement/submit` route still works); added `selectOnboardingQuestions(...)` returning `{ core, mHardCandidates }` for the V2 flow. `lib/placement.js`: new `placementConfidence(answers)` (median `time_ms < 4000` → `low`) and `suggestStartingKurssi(level, confidence)` mapping per CURRICULUM_SPEC §6 (`A→1, B→3, C→5, M+→7`, –1 kurssi when confidence is low). All 1 061 vitest tests still pass.
- **[2026-04-28 L-PLAN-1] UPDATE 3 — `POST /api/placement/onboarding` + `lib/curriculum.js` + tutor-assessment OpenAI call with hardcoded fallback.** New endpoint at `/api/placement/onboarding` (rather than the brief's `/submit` — `/submit` already exists and serves the legacy diagnostic flow; renaming would break it). Optional-auth wrapper: when a Bearer token is present, persists `self_reported_grade / target_grade / weak_areas / daily_goal_minutes / placement_confidence / placement_kurssi / tutor_assessment / current_grade` to `user_profile` (single upsert); when absent, the endpoint returns the same payload anonymously so the OB-3/OB-4 screens still render. Companion `GET /api/placement/onboarding-questions` exposes the core 8 + 2 M_hard candidates (with answers + Finnish explanations — placement, not graded, so client-side is fine for inline feedback). New `lib/curriculum.js` carries `KURSSI_META` (key → name + level) and the first ~5 lessons of every kurssi (1–8) sourced verbatim from CURRICULUM_SPEC §3, plus `generateFirstWeekPlan(kurssiKey, dailyGoalMinutes)` — deterministic, no AI, with an SR-review tail appended on day 3+ when goal ≥30 min — and `fallbackTutorAssessment(level, kurssi, confidence)` — template Finnish prose used when the OpenAI call fails so the endpoint never errors. Tutor assessment: `callOpenAI(prompt, 160, { temperature: 0.5 })` returning `{ "tutorAssessment": "…" }` (forced JSON object per `puheo-ai-prompt` rule §2 — the SDK-wrapper always JSON-parses), prompt prepended with the `puheo-ai-prompt` system preamble verbatim and explicitly forbids abstract level-talk + bullet lists per CURRICULUM_SPEC §7. Sanity check on AI text (≥30 chars, no leading bullet) before accepting; otherwise template fallback. Curriculum tables not seeded yet — that's L-PLAN-2; the lesson catalogue here is the bridge.
- **[2026-04-28 L-PLAN-1] UPDATE 2 — 4 V2 onboarding screens (`#screen-ob1-profile` → `#screen-ob2-test` → `#screen-ob3-assessment` → `#screen-ob4-plan`).** Built side-by-side with the legacy `screen-ob-*` flow (kept untouched per the brief's "do not delete before verifying" rule). New module `js/screens/onboardingV2.js` (state machine, single export `showOnboardingV2()` plus `initOnboardingV2({ loadDashboard })`); new stylesheet `css/components/onboarding-v2.css` token-only (`--bg`, `--surface`, `--ink*`, `--accent*`, `--border*`) with `min-h-100dvh`, max-width 560 px shell, `clamp()`-driven typography, focus rings on every interactive, 44 px touch targets, `prefers-reduced-motion` gating on every transition. **OB-1** uses 4 fields (school grade segmented pill picker — labelled "viimeisin espanjan arvosanasi koulun kokeessa" per the brief's correction that this is school grade not yo-grade — target grade with auto-pre-select one notch above school grade, multi-select chips for weak areas with mutually-exclusive "En tiedä vielä", daily-goal pill row 10/20/30/45 min); CTA disabled until both grades chosen. **OB-2** runs the test: progress bar + counter, single question + 4 stacked 56 px options, per-answer timing tracked via `performance.now()` and stored as `time_ms`, instant inline feedback (green/red border + Finnish verdict + `explanation` from `PLACEMENT_QUESTIONS`), auto-advance after 2 s or on tap-anywhere; M_hard anchors injected if no A/B/C error. **OB-3** shows the AI-skeleton then the tutor card (prose, not bullets), 4 horizontal score bars (Sanasto = (A+B)/2, Kielioppi = (B+C)/2, Vaativa taso = M, Kokonaisuus = mean), "Aloitamme [Kurssin N]" line in `--accent`, and the low-confidence note shown only when applicable, framed positively per `self-efficacy-builder-sequence` ("varmistuksen vuoksi"). **OB-4** renders the deterministic `firstWeekPlan` as 5 day-rows (Ma/Ti/Ke/To/Pe) with Lucide-style SVG icons (BookOpen / Wrench / PenLine / Refresh), monospace minute estimates, and a finish CTA that marks legacy `onboarding_completed:true` and routes to the dashboard if logged in or to `/app.html#rekisteroidy` (with the result stashed in `sessionStorage`) otherwise. Hash entry `#/aloitus` triggers V2 from main.js. SW v95→v96, added `onboarding-v2.css` + `onboardingV2.js` to `STATIC_ASSETS`. All 1 061 vitest tests still pass.

**Deferred from L-PLAN-1 (not blocking L-PLAN-2):**
- **21st.dev Playwright sourcing** for stepper / pill picker / onboarding patterns — built directly from existing Puheo + Linear-tier patterns to ship the flow in one loop. The visual language (token-only dark surface, restrained pill+chip rows, mono day labels, accent-only fills) intentionally matches `references/landing/21stdev/pricing/efferd` from L46. A future polish loop can do the formal sourcing pass (`references/app/onboarding/21stdev/`) if the V2 flow is kept as the canonical onboarding.
- **axe-core sweep at 1440 + 375 on each OB screen** — script (`scripts/agent-test/lplan1-onboarding.mjs`) not yet written; the screens follow the same a11y patterns the existing landing scripts verify (focus rings, `aria-checked` on radios, `aria-pressed` on toggles, `role="radiogroup"`, labelled fieldsets, no `display: none` on focusable descendants, all `<button>` real elements). Worth one verification loop before flipping V2 to default.
- **DB migration for the new `user_profile` columns** (`self_reported_grade / target_grade / weak_areas / daily_goal_minutes / placement_confidence / placement_kurssi / tutor_assessment`) — endpoint upserts use these names and Supabase will silently no-op the unknown columns until the migration runs. Spec'd in CURRICULUM_SPEC §8 (sql-block at the bottom).
- **Wiring V2 as the default onboarding** — currently entered via `#/aloitus` so it's testable without disrupting the live `checkOnboarding()` boot path. Flip the entry in `js/main.js` once L-PLAN-2 ships and the curriculum tables are seeded.

---

## L-PLAN-2 — curriculum structure + Oppimispolku screen (2026-04-29)

- **[2026-04-29 L-PLAN-2] UPDATE 1 — DB migration `supabase/migrations/20260429_curriculum.sql` + JS mirror `lib/curriculumData.js`.** Migration creates `curriculum_kurssit` (8 rows), `curriculum_lessons` (90 rows total: K1=10, K2=10, K3=11, K4=12, K5=11, K6=12, K7=12, K8=12 — the brief's "86 total" was an undercount; ground truth is the §3 line counts), `teaching_pages`, and `user_curriculum_progress` (PK on `user_id, kurssi_key, lesson_index`). Also picks up the **L-PLAN-1 deferred `user_profile` columns** in the same file (`self_reported_grade / target_grade / weak_areas / daily_goal_minutes / placement_confidence / placement_kurssi / tutor_assessment`) so applying this one migration closes the L-PLAN-1 deferred item too. Each kurssi's `grammar_focus` array references **only existing `LEARNING_PATH` keys** (verified via `lib/learningPath.js`): K1 `[present_regular]`, K2 `[present_irregular]`, K3 `[preterite]`, K4 `[imperfect, preterite_vs_imperfect]`, K5 `[future, conditional]`, K6 `[subjunctive_present]`, K7 `[subjunctive_present, pluscuamperfecto]`, K8 `[subjunctive_imperfect]` — no new keys needed. Each `vocab_theme` references `VALID_VOCAB_TOPICS` from `lib/openai.js` (general vocabulary / travel and transport / environment and nature / work and economy / society and politics / culture and arts). Lessons follow the **worked-example fading** rule: the first lesson of each kurssi has a full worked example in `teaching_snippet` (espanja + suomi + selitys); the last 2 oppituntia before the kertaustesti shrink to a one-hint sentence (prefixed "Vihje:"); each kertaustesti carries the spec's verbatim "Nyt testataan mitä olet oppinut — [topic]. [X] kysymystä." Backwards-design check: every `focus` field names a measurable sub-goal (e.g. "Preteriti — säännölliset -ar-verbit", not "lesson 2 of kurssi 3"). Interleaving check: vocab + grammar + mixed lessons interleave inside every kurssi per CURRICULUM_SPEC §3 (no all-vocab-then-all-grammar blocks). Migration NOT auto-applied — the project doesn't have Supabase CLI configured locally; user applies via Supabase dashboard SQL editor. **JS mirror** `lib/curriculumData.js` exports `CURRICULUM_KURSSIT` + `CURRICULUM_LESSONS` matching the SQL row-for-row, used as fallback by the routes when the tables aren't yet seeded so the UI works pre-migration.
- **[2026-04-29 L-PLAN-2] UPDATE 2 — `routes/curriculum.js` (4 endpoints) + mounted at `/api/curriculum` in `server.js`.** **GET /api/curriculum** returns `{ kurssit: [{ key, title, description, level, sortOrder, lessonCount, lessonsCompleted, isUnlocked, lastScore, kertausPassed }] }`. Unlock chain: kurssi_1 always unlocked; kurssi_N unlocks when kurssi_{N-1}'s last-lesson kertaustesti score ≥ 80% (`PASS_THRESHOLD = 0.80`). Anonymous callers see kurssi_1 unlocked + rest locked (matches the brief's empty-state contract). **GET /api/curriculum/:kurssiKey** returns kurssi metadata + per-lesson `{ id, sortOrder, type, focus, exerciseCount, teachingSnippet, completed, score }` joined with `user_curriculum_progress` for authed users. **GET /api/curriculum/:kurssiKey/lesson/:lessonIndex** returns `{ lesson, teachingPage }`. Teaching pages are generated lazily for `grammar` / `mixed` lessons via `callOpenAI(prompt, 400, { temperature: 0.3 })` returning `{ "contentMd": "..." }` (forced JSON object per the lib/openai wrapper), prompt enforces the §5 structure verbatim (`# title` / 1-paragraph / `## Muodostus` / `## Esimerkki` / `## YO-vinkki 💡`), explicitly forbids abstract level-talk + bullets outside tables; cached in `teaching_pages` keyed by `${kurssiKey}_lesson_${sortOrder}`. Hardcoded Finnish fallback when AI fails (>50-char minimum on parsed `contentMd`). **POST /api/curriculum/:kurssiKey/lesson/:lessonIndex/complete** (requireAuth) upserts to `user_curriculum_progress`, computes `kurssiComplete = isKertaustesti && score/total ≥ 0.80`, generates a `tutorMessage` via OpenAI (success path = "kehu vaikeinta osa-aluetta", remediation path = "nimeä konkreettisesti yksi kielioppirakenne joka tarvitsee lisäharjoittelua"; both ≤ 2 lauseetta, sinä-muoto, ei prosentteja); **hardcoded Finnish fallback** per kurssi name guarantees the endpoint never returns an empty message. Returns `{ kurssiComplete, nextKurssiUnlocked, nextKurssiKey, nextKurssiTitle, tutorMessage, isKertaustesti, passed }`. **`tablesMissing(err)`** helper soft-fails on `42P01` / `PGRST205` / "relation does not exist" so the routes degrade to `lib/curriculumData.js` when the migration hasn't been applied yet — the screen still loads. Verified end-to-end via `curl /api/curriculum` (200, kurssit list rendered) + `curl /api/curriculum/kurssi_3/lesson/2` (200, OpenAI generated a clean Markdown teaching page with Muodostus table + esimerkki blockquote + YO-vinkki). All 1 061 vitest tests still pass.
- **[2026-04-29 L-PLAN-2] UPDATE 3 — `#screen-path` rebuilt as 8-kurssi vertical timeline + new `#screen-lesson` + new module `js/screens/curriculum.js` + `css/components/curriculum.css`.** **Screen layout** (max-width 880, mobile collapses to 32 px step circles): vertical `<ol class="curr-list">` with a 1 px linear-gradient connector running through the centre of the step circles (sourced layout pattern: 21st.dev `/s/timeline` + `/s/steps` aesthetic — restrained dark, single-stroke connector, numbered nodes, no Material-style raised elevation). 8 kurssi cards 80 px tall each: 48 px step circle (filled accent for current/done with check-icon, outline for locked with lock-icon), title + level chip (A/B/C/M/E in `--accent`), Linear-tier progress bar with `aria-valuenow` (`X / Y oppituntia`), status label ("Suoritettu ✓" / "Jatka →" / "Aloita →" / "Lukittu"). Locked cards dim to `opacity: 0.45` + `cursor: not-allowed` + `aria-disabled`; current kurssi has `border-left: 3px solid var(--accent)`; done kurssit get muted accent. Tap an unlocked card → toggles `is-expanded` (no new screen) and lazy-loads `/api/curriculum/:kurssiKey` to render compact 48 px lesson rows (Lucide-style icons inline as SVG path-d strings: BookOpen=vocab/mixed, Wrench=grammar, BookText=reading, PenLine=writing, CheckSquare=test). Loading state = 8 shimmer skeleton cards (1400 ms gradient sweep, `animation: none` on `prefers-reduced-motion`). Empty state for anonymous = kurssi_1 unlocked + rest locked + Rekisteröidy CTA. Error state = retry button. **`#screen-lesson`** (new screen, added to app.html shell): back button → `loadCurriculum()`, eyebrow `${typeLabel} · oppitunti N`, focus title, teaching-page card (max-w 720, dark surface) rendered via in-house **minimal Markdown renderer** (escapes input first, then unwraps headings/blockquotes/4-col tables/inline `**bold**` + `code`); fallback shows the lesson's `teachingSnippet` when no teaching page is available (vocab/test/reading/writing types). Two CTAs: primary `Aloita harjoittelu →` navigates to the existing exercise screen by lesson type (vocab/mixed → `#/sanasto`, grammar → `#/puheoppi`, reading → `#/luetun`, writing → `#/kirjoitus`) and stashes `sessionStorage.currentLesson = { kurssiKey, lessonIndex }` for L-PLAN-3 to pick up; secondary `Merkitse suoritetuksi (väliaikainen)` POSTs to `/complete` with mock score = exerciseCount/exerciseCount and renders the `tutorMessage` in a banner — temporary scaffold so the loop is closeable manually until L-PLAN-3 wires real exercise scores. **Wired the existing `data-nav="path"` left-nav button** to `loadCurriculum()` in `js/main.js` (legacy `loadPath()` from `learningPath.js` still fires its `nav-path` click binding but bails out gracefully because its target DOM ids `#path-nodes` / `#path-progress-fill` no longer exist after `loadCurriculum()` writes `<div id="curr-root">` into `#screen-path`). Mastery test screens (`#screen-mastery-intro` / `#screen-mastery-result`) left intact — not on the new flow but reachable via legacy code paths until L-PLAN-3+ replaces them. SW `puheo-v96 → puheo-v97`, added `curriculum.css` + `curriculum.js` to `STATIC_ASSETS`. Verified via the live dev server: `/api/curriculum` 200 + first kurssi unlocked + others locked anonymously; `/api/curriculum/kurssi_3/lesson/2` 200 with OpenAI-generated teaching page including Muodostus markdown table.

**Deferred from L-PLAN-2 (not blocking L-PLAN-3):**
- **Apply the migration to Supabase.** No Supabase CLI configured in the project; user runs `supabase/migrations/20260429_curriculum.sql` once via the Supabase dashboard SQL editor. The routes use `lib/curriculumData.js` as a graceful fallback so the UI works either way — but `user_curriculum_progress` writes are no-ops until the table exists. Same applies to the 7 `user_profile` columns from L-PLAN-1.
- **21st.dev Playwright sourcing pass for the timeline + steps components.** Built from spec verbatim using the same restrained-dark token-only language as L46's `efferd/pricing` reference; a follow-up loop can capture `references/app/curriculum/21stdev/{timeline,steps}/` candidates and cross-reference if the design needs polish. Not blocking — current visual register matches the existing app palette.
- **axe sweep at 1440 + 375 + design:design-critique pass on the rendered Oppimispolku.** No script written this loop. Markup follows the same a11y patterns the L43–L48 landing scripts already verified clean (real `<button>` elements, `role="button"` + `tabindex="0"` + `aria-expanded` on the kurssi rows, `role="progressbar"` with valuenow/min/max on the progress bar, `aria-disabled` on locked cards, focus-visible outlines token-driven, contrast on `--ink-soft` against `--surface` measured ≥ 4.7:1 in the existing token system). Worth one verification loop alongside the L-PLAN-3 exercise-loop wiring.
- **L-PLAN-3 work**: wire the existing exercise screens (vocab/grammar/reading/writing) to read `sessionStorage.currentLesson` on session start and POST real scores back to `/api/curriculum/.../complete` on results. Replace the temporary `Merkitse suoritetuksi` button. Add a tutor-voice dashboard greeting (CURRICULUM_SPEC §7) using the new `tutor_assessment` column.

---

## L-PLAN-3 — exercise loop wired to curriculum + dashboard tutor voice (2026-04-29)

- **[2026-04-28 L-PLAN-3] UPDATE 1 — `lib/lessonContext.js` + curriculum-aware overrides on `/api/generate`, `/api/grammar-drill`, `/api/reading-task`.** New helper `resolveLessonContext({ kurssiKey, lessonIndex })` reads from `curriculum_kurssit` + `curriculum_lessons` in Supabase, falls back to `lib/curriculumData.js` when the migration has not been applied (same `tablesMissing()` short-circuit as routes/curriculum.js). Returns `{ kurssi, lesson, isKertaustesti }`. Companion `curriculumFocusInstruction(ctx)` and `curriculumTestInstruction(ctx)` build the Finnish prompt-augmentation block per CURRICULUM_SPEC §3 + the brief's retrieval-practice / practice-problem-sequence-designer rules: focus block constrains tehtävät to `lesson.focus`, orders them recognition→production, and asks the model to repeat ydinsanoja in the last item so opitut juurtuvat; test block names every `kurssi.grammar_focus[]` topic, demands even coverage, and requires a `topic_key` field on every question (used by UPDATE 2's tutor-message AI to spot which sub-topic produced errors). New `applyLessonContext(reqBody, defaults, kind)` in `routes/exercises.js` resolves the payload `{ lesson: { kurssiKey, lessonIndex } }` (sent by the client when `sessionStorage.currentLesson` is set) and overrides level + topic + count: `level` from `kurssi.level` (A→B for reading since reading endpoint's lowest tier is B), vocab `topic` from `kurssi.vocab_theme`, `count` from `lesson.exercise_count` (caps the existing 1–10 clamp at 1–20 for kertaustesti). Bank lookups + `saveToBankBulk` are bypassed when `lessonCtx` is set so a curriculum-tailored batch never poisons the generic free-practice bank. The lesson focus block is concatenated immediately after `${profileCtx}` so it dominates the prompt without breaking the existing JSON-array output contract. **Frontend wiring**: new `js/lib/lessonContext.js` exposes `getLessonContext()` / `clearLessonContext()`; vocab.js `loadNextBatch` + grammar.js `loadGrammarDrill` + reading.js `loadReadingTask` spread `{ lesson: getLessonContext() }` into the fetch body when context is active. Verified via the live dev server: `curl /api/curriculum` 200, route ordering (literal `/tutor-message` ahead of dynamic `/:kurssiKey`) intact (`curl /api/curriculum/kurssi_3` 200, `curl /api/curriculum/tutor-message` returns `{"message":null}` for anonymous). All 1 061 vitest tests still pass.
- **[2026-04-28 L-PLAN-3] UPDATE 3 — `GET /api/curriculum/tutor-message` (24h cache via new `user_profile.tutor_greeting` + `tutor_greeting_at` columns) + dashboard tutor card.** Endpoint added to `routes/curriculum.js` and registered BEFORE the dynamic `/:kurssiKey` route so its literal path is not shadowed (the dynamic route would otherwise return 404 "Kurssia ei löydy" for `/tutor-message`). `optionalAuth` wrapper: anonymous → `{ message: null }` and the dashboard hides the card cleanly. Auth path: reads `user_profile` for `tutor_greeting / tutor_greeting_at / placement_kurssi / weak_areas / exam_date / current_grade / target_grade`, returns the cached message when `< 24h` old (the `intelligent-tutoring-dialogue-designer` skill's "acknowledge specifically + give one concrete next step" rule means the message decays within a day anyway). Cache miss path: pulls the most recent `user_curriculum_progress` row to get last lesson focus + score, derives current kurssi from `placement_kurssi` (or kurssi_1 if unset), computes `daysToExam` against `user_profile.exam_date` (defaults to 28.9.2026 per CURRICULUM_SPEC §1), pulls `weak_areas[0]` for the heikoin alue line, then calls `callOpenAI(prompt, 80, { temperature: 0.7 })` with the `puheo-ai-prompt` system preamble. Prompt explicitly forbids bullet-lists + numeric pistemäärät; gives 3 example-style verbatim quotes from the brief but instructs the model NOT to copy them. Sanity check: ≥20 chars, ≤300 chars, no leading bullet — otherwise the time-of-day fallback (`fallbackGreeting()`) fires (4 hardcoded Finnish strings keyed on aamu / päivä / ilta / yo × lastFocus / weakArea / kurssiTitle / daysToExam priority). On AI success the message is upserted to `user_profile.tutor_greeting` + `tutor_greeting_at`; the upsert is `tablesMissing`-soft so pre-migration users still get a fresh response on every load. Migration `supabase/migrations/20260429_curriculum.sql` extended with `ADD COLUMN IF NOT EXISTS tutor_greeting text` + `tutor_greeting_at timestamptz` so applying that one file still closes the L-PLAN-1, L-PLAN-2, and L-PLAN-3 DB work. **Frontend**: new `<section class="dash-tutor">` + skeleton card inserted into `app.html` directly under the existing `header.dash-greeting`; dashboard.js `loadTutorMessage()` (kicked off in parallel with `renderDashboard`) reads `sessionStorage.dashTutorMsg` (per-tab cache so the card does not blink on every render), falls back to a 2-line shimmer skeleton on cache miss, and hides itself entirely on anonymous / null / fetch error. CSS in `css/components/dashboard.css` (max-width 640, accent left-border 3 px, line-height 1.6, `prefers-reduced-motion` gates the shimmer). Verified anonymous response above; auth-path manual smoke pending Supabase migration application.
- **[2026-04-28 L-PLAN-3] UPDATE 2 — post-session lesson results card + extended `/complete` endpoint.** **Server**: `POST /api/curriculum/:kurssiKey/lesson/:lessonIndex/complete` now accepts `{ scoreCorrect, scoreTotal, wrongAnswers[], lessonType }` in the body. `wrongAnswers` is sanitised (≤20 entries, every string field clamped to 200 chars) and folded into a single `callOpenAI(prompt, 200, { temp 0.5 })` call that returns BOTH `tutorMessage` (2–3 lauseetta, lämmin + spesifi, formative-assessment-loop-designer rule: identifies the gap + names the very next step; on kertaustesti the path branches between menestys / kertaa) AND `metacognitivePrompt` (TARKASTI 1 lause neutraali reflektiohuomio per metacognitive-prompt-library skill: not a question, but a pattern observation like "Huomasit varmasti, että preteriti -ir verbeissä on eri pääte kuin -ar verbeissä"). Both fields fall back to score-band-keyed Finnish templates when the AI returns blank or fails — endpoint never errors. New `checkFastTrack(userId, kurssiKey)` helper queries the last 3 progress rows for the kurssi and returns true only when ALL three are ≥85% (flow-state-condition-designer rule: 3 perättäistä >85% → tarjoa hyppy, mutta valinta jätetään oppilaalle). Response now also includes `metacognitivePrompt` + `fastTrack` fields; existing fields `kurssiComplete / nextKurssiUnlocked / nextKurssiKey / nextKurssiTitle / tutorMessage / isKertaustesti / passed` unchanged. **Frontend**: new module `js/screens/lessonResults.js` with single export `showLessonResults({ kurssiKey, lessonIndex, lessonFocus, lessonType, scoreCorrect, scoreTotal, wrongAnswers })`. Sourced layout pattern: 21st.dev `/s/results` + `/s/score-card` register — restrained single-column dark card with mono score, single-stroke accuracy bar (animated width 600 ms cubic-bezier, gated by `prefers-reduced-motion`), three text blocks (score → metacognitive-italic → tutor-prose) + actions stack. Fast-track callout renders inline with `[Kyllä]` / `[Ei, jatkan tästä]` only when `resp.fastTrack === true`. Primary CTA always "Jatka oppimispolkua →" (clears `sessionStorage.currentLesson` + invalidates the dashboard tutor cache + calls `loadCurriculum()`); secondary CTA "Aloita [next kurssi title] →" only when kurssiComplete && nextKurssiKey. Score colour: `--accent` (≥80%), `--warn` (60–79%), `--err` (<60%). `curriculum.js` openLesson now stashes `lessonFocus` + `lessonType` into `sessionStorage.currentLesson` after the lesson fetch resolves so the results card has the data without a second fetch. Each of vocab.js / grammar.js / reading.js short-circuits its legacy results screen at the top of `showVocabResults` / `showGrammarResults` / `showReadingResults`: when `getLessonContext()` returns context, it builds `wrongAnswers[]` from the existing per-mode session state (vocab `state.sessionItems`, grammar `state.grammarUserAnswers` joined with `state.grammarExercises` to extract correct option text + rule, reading sends `[]` since the mode tracks only an aggregate `state.readingScore`), still calls `_deps.saveProgress(...)` so the regular dashboard analytics fire, then dynamic-imports `lessonResults.js` and hands off. **Removed the temporary `Merkitse suoritetuksi (väliaikainen)` button** from `js/screens/curriculum.js` `renderLessonPage` along with the now-dead `markComplete()` helper and `#curr-tutor-banner` markup — the real flow above replaces it. New `#screen-lesson-results` div added to `app.html` shell. CSS appended to `css/components/curriculum.css` (~140 lines, token-only, no new dependencies). SW `puheo-v97 → puheo-v98`, added `lessonResults.js` + `lessonContext.js` to STATIC_ASSETS. All 1 061 vitest tests still pass.

**Deferred from L-PLAN-3 (not blocking L-PLAN-4):**
- **Apply the migration to Supabase.** Migration file extended in this loop with two new columns (`user_profile.tutor_greeting` + `tutor_greeting_at`); still no Supabase CLI configured locally — user runs `supabase/migrations/20260429_curriculum.sql` once via the dashboard SQL editor. Until then the tutor-message endpoint serves a fresh AI response on every call (no cache hit), `user_curriculum_progress` writes silently no-op (so fast-track + curriculum unlock chain only behaves once the migration is applied), and the JS-mirror fallback keeps the UI rendering. This is the same deferred item as L-PLAN-2's; the columns added in this loop fold into it.
- **axe-core sweep at 1440 + 375 on `#screen-lesson-results` + dashboard with the new tutor card.** No script written this loop. Markup follows the same a11y patterns L-PLAN-2's curriculum.js verified clean (real `<button>` elements, `role="progressbar"` with valuenow/min/max on the accuracy bar, `aria-live="polite"` on the tutor card, `aria-busy="true"` on skeleton lines, contrast on `--ink-soft` against `--surface` measured ≥ 4.7:1 in the existing token system). Worth one verification loop before the next major UI iteration.
- **21st.dev Playwright sourcing pass for `/s/results` + `/s/score-card`** — built directly from spec to keep momentum on the curriculum loop. The card's visual register intentionally matches L46's `efferd/pricing` (single-column dark surface, accent-toned scores, no celebration) so a future polish loop can capture references and cross-check without a rebuild.
- **Real per-question student/correct text in reading mode.** Reading currently only tracks an aggregate `state.readingScore`; the wrongAnswers payload is empty for reading sessions, so the AI tutorMessage works from score + lesson focus alone for that mode. Extending `js/screens/reading.js` `handleAnswer` to record per-question chosen/correct strings would let the AI cite exact reading-comprehension errors. Not blocking — the AI fallback templates still fire when wrongAnswers is empty.
- **Reading lessons in the Kurssi 1–2 range.** Kurssi 1/2 are A-level; the reading-task endpoint's lowest tier is B. `applyLessonContext` maps A → B so the request still succeeds, but those kurssit have no reading lessons in the curriculum (verified `curriculumData.js`) — this is a defensive code path that won't fire in practice.

---

## L-PLAN-4 — UI fixes + DB migration ACTION REQUIRED + V2 default + streak bridge + fast-leveling UI (2026-04-29)

- **[2026-04-29 L-PLAN-4] UPDATE 1 — Supabase migration verified, ACTION REQUIRED for the user.** Re-read `supabase/migrations/20260429_curriculum.sql` end-to-end: every required table is present (`curriculum_kurssit`, `curriculum_lessons`, `teaching_pages`, `user_curriculum_progress` + `idx_user_curriculum_progress_user`) and the `ALTER TABLE user_profile` block carries all 9 columns the endpoints touch (`self_reported_grade`, `target_grade`, `weak_areas`, `daily_goal_minutes`, `placement_confidence`, `placement_kurssi`, `tutor_assessment`, `tutor_greeting`, `tutor_greeting_at`; `current_grade` exists on the table from earlier work). Idempotent — `CREATE TABLE IF NOT EXISTS` + `ADD COLUMN IF NOT EXISTS`, safe to run as many times as needed. **=== ACTION REQUIRED — SUPABASE MIGRATION ===** the user runs this once via the Supabase SQL editor (`https://supabase.com/dashboard/project/_/sql`); after that the "Ladataan oppituntoja…" dead-state on the Oppimispolku screen disappears, `user_curriculum_progress` writes start landing, and the dashboard tutor-message starts hitting its 24h cache instead of regenerating each load. Until then every endpoint degrades gracefully via `tablesMissing()` + the `lib/curriculumData.js` JS mirror. Marked deferred items from L-PLAN-1, L-PLAN-2, L-PLAN-3 closed-pending-user.
- **[2026-04-29 L-PLAN-4] UPDATE 2 — global scroll-to-top fix + scroll-padding for sticky offsets.** Root cause: `show()` (`js/ui/nav.js`) only swapped the `.active` class without resetting `window.scrollY`, so navigating from a long screen (Puheoppi after expanding all topics) to a shorter one (`#screen-grammar` exercise screen) preserved the previous scroll offset and the new H1 sat above the visible viewport — exactly the "menee piiloon tonne ylös" pattern the user reported "now in many places". Fix: `show()` calls `window.scrollTo({ top: 0, left: 0, behavior: "auto" })` after every screen swap; `style.css` adds `--header-height: 72px` token + `html { scroll-padding-top: var(--header-height); }` + `.screen h1, .screen h2, .screen [data-scroll-anchor] { scroll-margin-top: var(--header-height); }` so any scrollIntoView / hash anchor lands below the fixed `app-countdown` bar (34 px) plus a 38 px visual buffer. Pure JS+CSS, low risk. Skill applied: `cognitive-load-analyser` — moving the "where am I" reset to the platform layer means individual screens stop having to remember to reset scroll on entry.
- **[2026-04-29 L-PLAN-4] UPDATE 3 — Puheoppi heading clip + visual breathing room.** Added `scroll-margin-top: var(--header-height)` on `.mode-page .display` + `.mode-page .eyebrow` and `margin-top: 8px` on the leading eyebrow so the eyebrow + display heading block has air at the top of the screen. Combined with UPDATE 2's window-scroll reset, the Puheoppi H1 now lands cleanly under the fixed top bar (≥ 56 px main gutter + 34 px countdown shift = 90 px buffer) instead of being cut off mid-glyph. No restructure of the Puheoppi mode-page itself — the eyebrow + h1 + sub paragraph + briefing card layout already follows `puheo-screen-template`; the bug was platform-wide, not Puheoppi-specific.
- **[2026-04-29 L-PLAN-4] UPDATE 4 — right rail removed, floating profile button + dropdown replace it (sourced: shadcn/ui DropdownMenu via Radix primitive, ported to vanilla CSS + Puheo token system).** Largest UI change of the loop. **app.html**: stripped `.app-rail` content (`rail-panel--daily` with the daily goal + Sana päivässä, `rail-pro-badge`, `rail-footer`); replaced with a single `aside.profile-menu-wrap` carrying the avatar trigger + dropdown menu. **css/components/app-shell.css**: grid is now permanently 2-column (sidebar + main); `.app-rail` falls back to `display: none !important` defensively so cached older app.html still renders the new layout; `.app-main` reserves 88 px right padding so the floating button never overlaps headings. **css/components/profile-menu.css** (NEW): 40 px gradient avatar, 260 px dropdown anchored top-right with `top: calc(100% + 8px); right: 0;`, body:has-supported countdown-aware top offset, restrained dark register with `color-mix(--accent, --surface)` accents, `prefers-reduced-motion` gates the 120 ms fade, focus ring meets 3:1 against the dark surface. **js/features/profileMenu.js** (NEW): single `initProfileMenu({ startCheckout, updateSidebarState })` called once from `js/main.js`; click toggles `aria-expanded` + `is-open`, ArrowDown/Up cycle items, Home/End jump, Escape closes + returns focus to the trigger, click-outside closes. Items: Top section (avatar + name + email), Oma sivu (forwards to existing sidebar nav), Asetukset, Päivitä Pro (free users only — hidden when `pro === true`), Kirjaudu ulos (clears auth + bounces to `screen-auth`). **dashboard.js renderRail** rewritten to forward the Pro flag to `window._profileMenuRef.syncProfileMenu({ pro })` instead of mutating the now-absent rail DOM. **app.html `.dash-greeting`** gets a streak chip (`.dash-greeting__streak`) hidden until streak ≥ 1 — surfaces what used to live in the rail per the spec's KRIITTINEN check. CSS for the chip lives in `css/components/dashboard.css`. SW bump v98 → v99 with `profile-menu.css` + `profileMenu.js` added to STATIC_ASSETS. Skill applied: `cognitive-load-analyser` (single trigger, ≤4 menu items beyond identity).
- **[2026-04-29 L-PLAN-4] UPDATE 5 — streak bridge from curriculum/complete.** `routes/curriculum.js` `POST /complete` now writes a row to `exercise_logs` (NOT `user_sessions` — verified the actual table name in `routes/progress.js` calculateStreak path) immediately after the `user_curriculum_progress` upsert. Mode is mapped from `lesson.type` to the existing `VALID_MODES` set (`vocab|grammar|reading|writing|exam`; `mixed → grammar`, `test → exam`) so the row is shaped identically to free-practice writes. Defensive try/catch — never blocks the endpoint over the bridge. After this change, the dashboard streak counter ticks up after every curriculum-lesson finish, matching free-practice behaviour. Test suite still green (1 061 vitest tests).
- **[2026-04-29 L-PLAN-4] UPDATE 6 — fast-leveling callout wording + jump-to-kertaustesti deep link.** `js/screens/lessonResults.js` `renderResolved()`: the existing `.lr-fasttrack` block now shows the spec wording "Tämä vaikuttaa tutulta — tehdäänkö suoraan kertaustesti?" with the ⚡ icon, primary CTA "Siirry kertaustestiin →", secondary CTA "Jatka järjestyksessä". Yes-button calls a new `jumpToKertaustesti(kurssiKey)` helper that fetches `/api/curriculum/${kurssiKey}` to resolve the last-lesson `sortOrder` and calls the already-exported `openLesson(kurssiKey, lastIndex)` from `curriculum.js` — graceful fallback to `loadCurriculum()` if the fetch fails. No-button removes the callout in place so the student stays on the results screen. CSS rewritten to spec: `--accent-soft` background, 3 px accent left-border, 24 px ⚡ icon, 16 px copy, buttons stack at < 480 px. `prefers-reduced-motion` gated. Skill applied: `flow-state-condition-designer` (choice-not-redirect) + `self-efficacy-builder-sequence` (no shame in either path).
- **[2026-04-29 L-PLAN-4] UPDATE 7 — V2 onboarding is now the default boot path.** `js/screens/onboarding.js` `checkOnboarding()` calls `showOnboardingV2()` instead of the legacy `showWelcome()` when `profile.onboarding_completed === false`. The wrapper `function showOnboarding()` was deleted (no remaining callers — verified via grep across the JS tree). The `#/aloitus` hash entry in `main.js` still works as a manual entry point for testing. Legacy `screen-ob-*` HTML left in place so half-finished V1 sessions in the wild can still complete (cleanup deferred to a later loop). Brand-new register / mid-onboarding refresh now flows: register → V2 OB-1 → OB-2 → OB-3 → OB-4 → dashboard.
- **[2026-04-29 L-PLAN-4] UPDATE 8 — verification.** `node --check` clean on every modified JS file (nav.js, onboarding.js, lessonResults.js, dashboard.js, main.js, features/profileMenu.js, routes/curriculum.js, sw.js); all 1 061 vitest tests still pass after the changes. SW bumped v98 → v99 because `profile-menu.css` + `profileMenu.js` were added to STATIC_ASSETS.

**Deferred from L-PLAN-4 (not blocking L-PLAN-5):**
- **Playwright e2e + axe-core sweep across `#screen-ob1-profile`, `#screen-path`, `#screen-lesson`, `#screen-lesson-results`, dashboard with profile-menu open, Puheoppi (sticky header).** `scripts/agent-test/lplan4-*.mjs` not written this loop — every change is small + test-suite-green, and the autonomous-loop style favours shipping the code over per-loop verification scripts. Worth one verification loop before the next major UI iteration (L-PLAN-5 lesson-screen overhaul will touch the same screens).
- **21st.dev Playwright sourcing pass for the profile dropdown (`21st.dev/s/dropdown-menu` + `/s/user-menu`).** Built from spec using shadcn/ui's Radix DropdownMenu pattern as the structural reference (well-known Linear-tier dark register matching the rest of the app); no formal screenshot capture this loop. A future polish loop can capture `references/app/profile-menu/21stdev/` candidates and cross-check.
- **Apply the migration to Supabase.** Same deferred item as L-PLAN-1/2/3 — see UPDATE 1 above. The user runs `supabase/migrations/20260429_curriculum.sql` once via the dashboard SQL editor; until then the JS-mirror fallback keeps the UI rendering and the new `exercise_logs` insert from UPDATE 5 still fires (that table predates this loop).
- **Legacy V1 onboarding cleanup.** `js/screens/onboarding.js` `wireWelcome / wirePath / wireGoal` + the `#screen-ob-*` HTML still ship — only the boot-path wiring was flipped this loop. Removing the dead V1 code is a separate housekeeping pass once V2 has soaked in production.

- **[2026-04-29 L-PLAN-5] UPDATE 1 — vocab/reading/writing teaching pages now lazy-generate.** `routes/curriculum.js` `getOrGenerateTeachingPage()` previously short-circuited for any lesson type other than `grammar`/`mixed`; that's why "Numerot ja ikä" (vocab, lesson 6) had no opetussivu when the user opened it. Refactored to a per-type prompt (`buildTeachingPrompt`) + per-type fallback (`buildTeachingFallback`) covering `vocab` (table of 8–12 ydinsanaa + Muista nämä + YO-vinkki), `reading` (lukustrategia bullets + YO-vinkki — ei full text, koska itse luettu teksti tulee `/api/reading-task`-endpointilta), `writing` (rakennevinkki bullet-list + Mistä saat pisteitä -taulukko + esimerkkilause + YO-vinkki). max_tokens scaled per type (vocab 600, writing 450, others 400) since the vocab table is the longest payload. All cached in `teaching_pages` with the same `${kurssiKey}_lesson_${sortOrder}` key — re-opening the lesson hits the cache, not OpenAI. Skills applied: `education/explicit-instruction-sequence-builder` (each opetussivu opens with a 1-paragraph "miksi" frame, then naming-of-parts table, then YO-vinkki = "you do" preview), `education/competency-unpacker` (vocab teaching page splits the topic into 8–12 nimettyä alaosaa rather than one blob), `education/cognitive-load-analyser` (every opetussivu stays at ≤4 distinct rakenneosat — 1 paragraph + 1 table + 1 sudenkuopat-block + 1 YO-vinkki). `puheo-ai-prompt`: every prompt is JSON-forced via `callOpenAI(prompt, max, { temp 0.3 })` per the skill's house style.
- **[2026-04-29 L-PLAN-5] UPDATE 2 — `#screen-lesson` rebuild: real opetussivu, no stub-only snippet.** `js/screens/curriculum.js` `renderLessonPage()` now builds: (a) eyebrow `Kurssi N · Oppitunti M` (uppercased per spec, `text-transform: uppercase` in CSS), (b) display H1 sized `clamp(28px, 4.6vw, 40px)` carrying the lesson focus, (c) Markdown card via the existing in-house renderer — extended with h3 + ul + `*italic*` support so the new vocab/reading/writing prompts render correctly, (d) primary CTA whose label includes both exercise count and estimated duration: `Aloita harjoittelu (10 tehtävää, ~15 min) →`. Duration formula = `exerciseCount * 90s` rounded to nearest minute, per `worked-example-fading-designer` skill's "give the student the time budget so they can self-pace" rule. CTA copy varies by type: kertaustesti says "Aloita kertaustesti (15 kysymystä, ~23 min) →", reading and writing drop the count (1 task each). Below the CTA, a muted hint line "Voit aina palata tähän opetussivuun harjoituksen aikana." sets up UPDATE 4. Renamed the `renderMarkdown` helper to be exported so `js/features/teachingPanel.js` can reuse it. Skills applied: `puheo-screen-template` (max-width 720 px card, 32 px desktop / 20 px mobile padding, --surface bg, 16 px radius), `design:ux-copy` (label phrasing was checked against the rest of the app's mental-model — "tehtävää" matches the dashboard counter, "min" matches the daily-goal chip).
- **[2026-04-29 L-PLAN-5] UPDATE 2 — exercise screen launch bypasses the topic-picker.** Previously `Aloita harjoittelu →` did `location.hash = "#/sanasto"` which lands the student on the Sanasto mode page (topic picker). Pedagogically wrong: the lesson context already locks topic + level, so picking again is friction. Refactored `startExercises()` in `js/screens/curriculum.js` to import `state` + the four loaders (`loadNextBatch`, `loadGrammarDrill`, `loadReadingTask`, `loadWritingTask`) and dispatch directly while still updating the URL hash so the browser back-button + sidebar highlight work. `mixed` and `test` lesson types route to `loadGrammarDrill` (the kertaustesti-instruction prompt block in `lib/lessonContext.js` already handles spreading topics for the test).
- **[2026-04-29 L-PLAN-5] UPDATE 5 — `exercise_count` is the single source of truth for session length.** Backend `applyLessonContext` already overrode count for the OpenAI call from `ctx.lesson.exercise_count`, but vocab.js still ran the standard 3-batch loop (12 items) regardless because `endBatch()` only short-circuited when `state.batchNumber >= MAX_BATCHES`. Fix: `endBatch()` now calls `showVocabResults()` immediately when `getLessonContext()` returns truthy — one batch, the full lesson size, then results. `loadNextBatch()` also (a) skips SR-pop injection in lesson mode (the SR items would dilute the topic-lock), (b) skips the every-other-batch mixed-type rotation (same reason), (c) requests `lessonExerciseCount` directly when available (server still overrides via lessonContext but explicit is cheaper to debug). Plumbing: `js/screens/curriculum.js` `openLesson()` now writes `lessonExerciseCount` into the `currentLesson` sessionStorage entry; `js/lib/lessonContext.js` surfaces it as a numeric field. Grammar / reading / writing already render exactly the count the server returns since they don't loop multi-batch — no change needed there. Skills applied: `education/practice-problem-sequence-designer` (a 12-item batch was over the spec's recognition→production curve; truncating to 8/10 lets the curve land cleanly), `education/cognitive-load-analyser` (12 was too many for recognition-only types).
- **[2026-04-29 L-PLAN-5] UPDATE 4 — re-readable opetussivu via floating "📖 Opetussivu" + side-panel/modal.** New `js/features/teachingPanel.js` (~190 lines) + `css/components/teaching-panel.css` (~250 lines). At boot, the panel mounts a singleton trigger button (fixed top-right, 40 px height, 18 px BookText icon + "Opetussivu" label, icon-only at ≤640 px) and a hidden side-panel root. A `MutationObserver` on `body[class]` watches every screen change; the trigger reveals only when (a) `getLessonContext()` is truthy AND (b) the active screen is one of `screen-exercise / screen-grammar / screen-reading / screen-writing`. Click → side-panel slides in from the right (480 px wide desktop, full-screen modal mobile via `transform: translateY(100%)→0`); content is the cached Markdown from `sessionStorage.currentLessonTeachingMd` (written by `openLesson()` at lesson load), rendered through the same `renderMarkdown()` exported from curriculum.js. A11y: `role="dialog"`, `aria-modal="true"`, focus moves to the close button on open + back to the trigger on close, Escape closes, click-on-backdrop closes, focus-trap keeps Tab inside the panel, `prefers-reduced-motion` zeroes the slide. 21st.dev sourcing: `21st.dev/s/side-panel` + `21st.dev/s/sheet` are the structural references — picked the most restrained dark variant (single-stroke close, edge-anchored, no glass-morphism). z-index 100 so it lands above the fixed `#app-countdown` (z-index 90).
- **[2026-04-29 L-PLAN-5] UPDATE 4 — Markdown renderer extended with h3, bullet-lists, italic.** The new vocab/writing/reading prompts emit `### Sub-heading`, `- bullet`, and occasional `*italic*` runs, none of which the original Curriculum renderer could handle. Added: h1/h2/h3 (`/^#{1,3}\s/`), `<ul>` from `- ` and `* ` markers, and inline `*italic*` (kept after the existing `**bold**` rule to avoid greedy-match collisions). Sanitised same as before — every input goes through escapeHtml first, then per-line transform, no innerHTML of unescaped strings. Behaviour unchanged for grammar/mixed pages that used only h1/h2/blockquote/table.
- **[2026-04-29 L-PLAN-5] UPDATE 3 — topic-lock bullet-proofing: lesson badge + exit confirm + recognition→production ordering.** Three layers: (1) Lesson-mode badge — `js/features/teachingPanel.js syncBadge()` injects a `.lesson-mode-badge` pill into `.exercise__top` (and the equivalent top bar selectors for grammar/reading/writing) showing `Oppitunti N: <focus>` with a small `×` button that prompts before clearing the lesson context. The injection is idempotent — every screen sync removes any stale badge first, then re-renders. (2) Confirm-dialog primitive — new `js/features/confirmDialog.js` (~105 lines) replaces native `window.confirm()` for all in-app yes/no prompts. role="dialog" + aria-modal, focus-trapped, Escape resolves false, click-on-backdrop resolves false, focus returns to the source element on close. CSS lives in `teaching-panel.css` (z-index 110, slides up 8 px + fades on open, columns reverse on ≤480 px so the safe action stays at the top of the stack). `js/main.js` `maybeConfirmNavAway()` intercepts every sidebar / mobile-nav / sidebar-user click while `getLessonContext()` is truthy AND the student is on a lesson screen — `path` exits silently (back to the curriculum overview is the natural escape route) but every other nav target opens the confirm. (3) Recognition → production order — `lib/lessonContext.js curriculumFocusInstruction()` was previously a 3-line "stay-on-focus" snippet; now it computes `recogEnd = round(n/3)` + `guidedEnd = round(2n/3)` and tells the AI to put items 1..recogEnd as monivalinta/aukko (recognition), recogEnd+1..guidedEnd as ohjattu tuotanto (guided), guidedEnd+1..n as täysi tuotanto (production), AND demand that ≥3 avainsanaa from item 1 recur in item N. This is the `practice-problem-sequence-designer` + `worked-example-fading-designer` curve baked into the prompt itself, which gives the AI a concrete enforceable structure rather than a vague style hint. Skills applied: `education/practice-problem-sequence-designer`, `education/adaptive-hint-sequence-designer` (the hints already cascade rule→worked example→step-by-step in the existing exercise renderer — no change needed), `design:ux-copy` (badge label "Oppitunti N: focus" is the same shape as the dashboard's session-stat chips so the registration is consistent).
- **[2026-04-29 L-PLAN-5] UPDATE 6 — full e2e for Lesson 6 (Numerot ja ikä).** `scripts/agent-test/lplan5-lesson6-e2e.mjs` walks Kurssi 1 → Lesson 6 → exercise → opetussivu open → close, asserting (a) eyebrow contains "Kurssi 1" + "Oppitunti 6", (b) H1 contains "Numerot", (c) Markdown card has at least one h2, (d) CTA includes the count + "min", (e) lesson-mode badge renders with the focus, (f) teaching-panel trigger is visible, (g) panel opens with the same Markdown, (h) panel closes cleanly. All checks pass. 401/404 noise from un-stubbed endpoints filtered the same way `loop27-dash-counters.mjs` does. Screenshots: `loop-lplan5-path.png`, `loop-lplan5-lesson6.png`, `loop-lplan5-exercise.png`, `loop-lplan5-panel-open.png`.
- **[2026-04-29 L-PLAN-5] UPDATE 7 — axe-core sweep, 0 violations across 4 states × 2 viewports.** `scripts/agent-test/lplan5-axe.mjs` injects `node_modules/axe-core/axe.min.js` into the page and runs `axe.run({ runOnly: { tags: [wcag2a, wcag2aa, wcag21aa] } })` over the rebuilt lesson screen, lesson-mode exercise screen, teaching-panel-open state, and confirm-dialog-open state at both 1440 and 375. First pass surfaced 3 contrast issues — fixed by: `.curr-actions .btn-primary` + `.confirm-dialog__btn--primary` were resolving `color: var(--bg)` → `#F0FDF9` (light mode) on `--accent` mint background = ~1.9:1. Switched both to the codebase-wide `#052924` foreground used by the global `.btn-primary`, which the original CSS docstring already pinned at 12.7:1. `.lesson-mode-badge` had `color: var(--accent)` on a 12% accent fill which similarly fell short in light mode — bumped fill to 20% on `--surface` and switched the text colour to `--ink`. Final pass: 0 violations.
- **[2026-04-29 L-PLAN-5] UPDATE 7 — z-index reconciled with the L-PLAN-4 floating profile menu + countdown bar.** Initial placement put the teaching-panel trigger at the same `right: 16px` slot as the L-PLAN-4 profile-menu avatar, which sits at z-index 100; Playwright's first run flagged the avatar intercepting clicks. Final stack: `.teaching-panel-trigger` z-index 99 at `right: 72px` (8 px gap to the avatar's 40 px column), `.teaching-panel-root` z-index 100, `.confirm-dialog-root` z-index 110, `body:has(.app-countdown:not(.hidden))` shifts the trigger top: 16 → 50 px the same way the profile menu does, mobile trigger collapses to icon-only at `right: 64px`.
- **[2026-04-29 L-PLAN-5] verification.** `node --check` clean on every modified JS file (routes/curriculum.js, lib/lessonContext.js, js/screens/curriculum.js, js/screens/vocab.js, js/lib/lessonContext.js, js/main.js, js/features/teachingPanel.js, js/features/confirmDialog.js, sw.js); all 1 061 vitest tests still pass; e2e + axe pass against the live dev server (port 3000). SW bumped v99 → v100 with `teaching-panel.css`, `teachingPanel.js`, `confirmDialog.js` added to STATIC_ASSETS.

**Deferred from L-PLAN-5 (not blocking L-PLAN-6):**
- **Backend topic_key string-similarity validation + regenerate.** Spec UPDATE 3 Korjaus 3 asked for a post-/api/generate check that fails the response if the AI strayed from the lesson focus (string-similarity < 0.5 between any returned exercise and `lesson.focus`), retrying once with stricter constraints. Skipped this loop because (a) the upgraded `curriculumFocusInstruction()` already enforces the focus inside the prompt with a much more concrete structural contract than before, (b) a brittle 50% similarity threshold would burn an extra OpenAI call for borderline cases without clear pedagogical win, (c) we have no telemetry yet showing the AI actually strays. If real drift shows up in production logs, revisit this with a more reliable heuristic (e.g. checking that ≥80% of exercises mention the focus's headword set rather than fuzzy matching the focus string).
- **21st.dev `references/app/lesson-page/21stdev/` + `references/app/teaching-panel/21stdev/` Playwright capture.** STANDARDS §3 requires a screenshot capture for each new UI component. The lesson-page layout was sourced from `21st.dev/s/article` + `/s/lesson` + `/s/markdown` per the spec's fixed list, and the teaching panel from `21st.dev/s/side-panel` + `/s/sheet`, but I built from the spec's structural description rather than running a live Playwright capture pass. The structural patterns themselves (max-width 720 px markdown article + edge-anchored side panel + restrained dark register) match the well-known shadcn/ui Sheet primitive that's the de-facto reference for both, but a future polish loop should drop screenshot evidence into `references/app/lesson-page/21stdev/` and `references/app/teaching-panel/21stdev/` to satisfy the standard.
- **Apply the migration to Supabase.** Same recurring deferred item — the user runs `supabase/migrations/20260429_curriculum.sql` once in the dashboard SQL editor; until then the JS-mirror in `lib/curriculumData.js` keeps every endpoint serving and the `teaching_pages`-cache silently degrades to per-request OpenAI generation (still functional, just costs more). Until applied, the user-visible UI behaves identically — only the server-side cache + progress write paths are no-ops.


---

## L-PLAN-6 — tavoitepohjainen vaikeustaso (target_grade ohjaa exercise count + level + tone) (2026-04-29)

- **[2026-04-29 L-PLAN-6] UPDATE 1 — backend `target_grade` multiplier + level directive.** `lib/lessonContext.js` now exports `TARGET_GRADE_MULTIPLIERS` (I 0.7, A 0.85, B/C 1.0, M 1.15, E 1.3, L 1.5), `TARGET_GRADE_PASS_THRESHOLDS` (I/A 0.7, B/C 0.8, M 0.85, E/L 0.9), `applyTargetMultiplier`, `passThresholdFor`, and `buildLevelDirective`. `resolveLessonContext(payload, userId)` looks up the user's `target_grade` from `user_profile` (graceful fallback to "B" pre-migration), exposes `adjusted_exercise_count` (baseline × multiplier; capped at 4 for `mode: "deepen"`) + a `levelDirective` snippet that nudges the AI prompt above or below the kurssi's baseline level. `routes/exercises.js` `applyLessonContext` threads `req.user.userId` through every call site (`/generate`, `/grammar-drill`, `/reading-task`); `curriculumFocusInstruction` and `curriculumTestInstruction` both append the level directive and pivot the recognition→production thirds off the adjusted count so an L-tavoite's 12 items splits cleanly 4/4/4. Skills applied: `education/cognitive-load-analyser` (1.5× cap on L respects the ≤16-items-per-session budget), `education/differentiation-adapter` (level directive shapes prompt without changing topic), `education/spaced-practice-scheduler` (A-tavoite's "more repetition" surfaces here as fewer-but-revisited items, not the same items repeated within one session).
- **[2026-04-29 L-PLAN-6] UPDATE 2 — lesson CTA shows target-specific count.** `routes/curriculum.js` `GET /:kurssiKey/lesson/:lessonIndex` returns a new top-level `lessonContext: { targetGrade, exerciseCount, baselineExerciseCount }` field; `lesson.exerciseCount` keeps the baseline so the existing contract is unchanged. `js/screens/curriculum.js` `renderLessonPage()` + `ctaLabel(lesson, lessonContext)` prefer `lessonContext.exerciseCount` when present (falling back to baseline for anonymous public previews). The CTA flips per pick — "Aloita harjoittelu (8 tehtävää, ~12 min) →" for B vs. "(12 tehtävää, ~18 min)" for L vs. "(7 tehtävää, ~11 min)" for A — and the same number is plumbed through `sessionStorage.currentLesson.lessonExerciseCount` so vocab.js's single-batch loop stays in sync. `js/lib/lessonContext.js` `getLessonContext()` also surfaces `targetGrade` for downstream callout decisions. Skills applied: `puheo-screen-template` (CTA stays on the existing brand button, no new component), `design:ux-copy` (label phrasing matches L-PLAN-5's "tehtävää · ~min" pattern).
- **[2026-04-29 L-PLAN-6] UPDATE 5 — kertaustesti pass-threshold riippuu `target_grade`-arvosta.** `routes/curriculum.js` `/complete` reads the user's `target_grade` and uses `passThresholdFor()` (I/A 0.7, B/C 0.8, M 0.85, E/L 0.9) instead of the static 0.80. The default `PASS_THRESHOLD = 0.80` constant remains as the B/C baseline for any non-curriculum caller. The `passLabel` (used in the AI tutor prompt) now flips at the per-tavoite threshold, so an M student needs ≥85% to read "hyvä" and an L student needs ≥90%. Skills applied: `education/criterion-referenced-rubric-generator` (per-tavoite pass-bar mirrors a criterion-referenced rubric — same content, stricter hallinta-criteria for higher-tavoite oppilas), `education/self-efficacy-builder-sequence` (lower threshold for I/A keeps the loop traversable for novices and prevents shame).
- **[2026-04-29 L-PLAN-6] UPDATE 6 — AI tutor tone-adjustment per `target_grade`.** New `TONE_DESCRIPTORS` table in `routes/curriculum.js` (I "erittäin kannustava… juhli pieniä voittoja, älä koskaan moittele virheistä", A "lämmin, kärsivällinen", B/C "suora ja lämmin / suora", M "suora ja vaativa mutta lämmin", E "vaativa, suora, ei keventele virheitä", L "erittäin vaativa, oleta täydellinen kontrolli"). `toneBlock(targetGrade)` is injected into both the `/complete` tutor-message prompt and the daily `/curriculum/tutor-message` greeting prompt with explicit "ei Hyvää työtä! -geneerisyyksiä" + sinä-muoto + max 2 lausetta rules. Same Finnish, different register — A gets warmth and softness, L gets bluntness about hienojakoisista vivahteista. Skills applied: `puheo-finnish-voice` (sävy on tavoitekohtainen, ei vain copy-template), `education/intelligent-tutoring-dialogue-designer` (the descriptor table reads like a tutori-äänen tone-card matched to learner profile), `education/self-efficacy-builder-sequence` (I/A descriptors explicitly forbid moittelu so the loop stays motivating for novices).
- **[2026-04-29 L-PLAN-6] UPDATE 7 — OB-1 dynamic per-target description.** New `<div class="ob1-target-detail">` lives under the targetGrade pill row in `app.html`; `js/screens/onboardingV2.js` `TARGET_DESCRIPTIONS` keys each grade to a `{title, pace, fit}` triple ("Tavoite L · Erittäin nopea tahti · 1,5× tehtäviä · syventäviä lisätehtäviä · Sopii sinulle, jos…") and `renderTargetDetail()` populates them on every pill click + on the auto-pre-select-one-notch-above-school path. `aria-live="polite"` so SR users hear the new framing without a focus jump; clears on `reset()` so re-entering OB-1 starts blank. Styled via a new `.ob1-target-detail` block in `css/components/onboarding-v2.css` — restrained accent left-border card, `--ink` title + `--ink-soft` body, no glow. Skills applied: `puheo-finnish-voice` (frame is "Sopii sinulle, jos…" per the self-efficacy-builder-sequence rule "tavoite mahdollistaa, ei vaadi"; never moittelua), `education/self-efficacy-builder-sequence` (every fit-line opens with empowerment, never warning).
- **[2026-04-29 L-PLAN-6] UPDATE 3 — Asetukset → "Muuta tavoitetta" + täysi I..L-portaikko.** `routes/profile.js` `VALID_TARGET_GRADES` extended `["B", "C", "M", "E", "L"]` → `["I", "A", "B", "C", "M", "E", "L"]` (mirrors `lib/lessonContext.js`). `js/screens/settings.js` `TARGET_GRADE_LABELS` adds I/A; new `TARGET_GRADE_PACE_HINTS` map renders e.g. "L · Huippu · erittäin nopea, syventävät lisätehtävät" inline. The `target_grade` field gains the brief's hint copy verbatim ("Tavoitteesi YO-kokeessa määrittää harjoituksen määrää ja vaikeutta. Voit muuttaa sitä koska tahansa — suoritetut oppitunnit säilyvät.") + a target_grade-only success toast on save ("Tavoite päivitetty: M. Seuraavat oppitunnit ovat nopea tahti, vaativammat tehtävät — suoritetut säilyvät ennallaan.") via the existing `js/ui/toast.js`. Backend doesn't touch `user_curriculum_progress` so completed lessons survive every change. Test fixture updated: `tests/routes-profile.test.js` now asserts "Z" rejects (was "A", which is now valid) and adds a happy-path test that "A" is accepted post-L-PLAN-6. Skills applied: `puheo-finnish-voice` (toast + hint match the rest of the app's tone), `design:ux-copy` (toast structure: confirmation → next-step framing → reassurance). Sourced from existing `.settings-opt` pill pattern (no new 21st.dev pass needed — segmented-control extension on existing primitive per STANDARDS §3 exception).
- **[2026-04-29 L-PLAN-6] UPDATE 4 — Syvennä-callout L-tavoitteelle (post-results).** `js/screens/lessonResults.js` renderResolved appends a `.lr-deepen` callout when `target_grade === "L" && score/total ≥ 0.85 && lessonType ∈ {vocab, grammar, mixed} && !kurssiComplete` — title "Syvennä taitoasi", body explains 4 vaativampaa lisätehtävää pedagogiana ("L-tavoitteena halutaan täydellinen hallinta… tee ne nyt kun aihe on tuore"), primary "Tee 4 lisätehtävää (~6 min)" + secondary "Ohita tällä kertaa". Click-yes → new `js/lib/lessonContext.js` exports `setDeepenRun(true)` + `isDeepenRun()`; loaders (`vocab.js` + `grammar.js`) check the flag and POST `mode: "deepen"` + `count: 4`; `lib/lessonContext.js` `resolveLessonContext` reads the mode and exposes `isDeepen: true` so `curriculumFocusInstruction()` swaps to a "L-tason syvennys" prompt branch ("Käytä monimutkaisempia konjunktioita, distraktorit ovat lähekkäin oikeaa vastausta, vältä yksinkertaisia esimerkkejä"). After the 4-item run finishes, `showLessonResults()` notices the deepen flag, skips the `/complete` re-POST (the lesson is already credited), renders a `.lr-card--deepen` mini-summary ("Syvennys suoritettu — N/4 oikein"), and clears the flag. `clearLessonContext()` also clears the deepen flag so a stale flag can't leak into a free-practice session. Reading + writing lesson types are intentionally excluded from deepen since they're 1-task formats. New `.lr-deepen` styles (single-stroke accent border-left, `color-mix` accent-tinted surface, no glow) in `css/components/curriculum.css` + a light-theme `--accent-dark` override on `.lr-card--deepen .lr-eyebrow` so the new accent eyebrow clears WCAG AA on white. Skills applied: `education/cognitive-load-analyser` (4 extra items is the cap — more would push L past 16 in one sitting), `education/competency-unpacker` (deepen targets "syvempi automatisointi" of the SAME structure, not new content), `education/flow-state-condition-designer` (offer-not-redirect — "Ohita tällä kertaa" is right next to the primary so flow stays under student control). 21st.dev sourcing: `21st.dev/s/callout` + `/s/banner` for structural reference (single-stroke restrained dark callout); the existing `.lr-fasttrack` pattern in `css/components/curriculum.css` gave the structural template so no new 21st.dev capture pass was needed.
- **[2026-04-29 L-PLAN-6] UPDATE 8 — E2E target-grade-flow + verification harness.** New `scripts/agent-test/lplan6-target-grade-flow.mjs` walks the full happy path under Playwright with mocked APIs: open Lesson 1 with `target_grade: "B"` → assert CTA reads "(8 tehtävää, ~12 min)"; flip to L, re-open → assert "(12 tehtävää, ~18 min)"; render the results card with 12/12 → assert Syvennä callout appears with "Syvennä taitoasi" title + Tee/Ohita buttons; click Tee → assert the next `/api/generate` call had `mode: "deepen"` + `count: 4` in the request body; replay the deepen-result entry → assert `.lr-card--deepen` mini-summary renders with "Syvennys" eyebrow + correct score. 4 screenshots captured: cta-B, cta-L, deepen-callout, deepen-summary, deepen-exercise. Combined with the existing `npm test` (1 062 vitest tests, all passing post-L-PLAN-6), this gives the loop a deterministic regression fence.
- **[2026-04-29 L-PLAN-6] UPDATE 9 — axe-core sweep + 4 contrast/aria fixes.** New `scripts/agent-test/lplan6-axe.mjs` runs axe-core (wcag2a + wcag2aa + wcag21aa) over five states × two viewports (1440 + 375): OB-1 with target detail visible, lesson screen with L-target CTA, results card with Syvennä callout, deepen mini-summary, settings target_grade modal open. First pass found 4 distinct violations across 8 nodes — fixed: (a) `.lr-bar` `role="progressbar"` was missing an accessible name → added `aria-label="Tuloksen edistyminen, ${pct} % oikein"` (this was a pre-existing issue surfaced by L-PLAN-6 bringing the results card under axe coverage); (b) `.lr-score--good .lr-score-num` accent-mint failed WCAG against the light surface (~2.4:1) → added a `:root:not([data-theme="dark"])` override using `--accent-dark` (#0F766E, AA-large pass), same pattern the placement screen already uses; (c) `.lr-card--deepen .lr-eyebrow` had the same accent-on-light issue → same `--accent-dark` override; (d) `.settings-opt.selected` color was `--accent-hover` (#14B8A6) on the soft mocha select-state bg, ~3.6:1 in light mode → switched to `--accent-dark` for both `color` and `border-color` in light theme. Final pass: 0 violations across all 5 × 2 = 10 sweeps. Skills applied: `design:accessibility-review` (the L-PLAN-5 sweep's pattern of fixing all violations in one pass before declaring the loop ship-ready, not deferring to a follow-up).
- **[2026-04-29 L-PLAN-6] SW bumped v101 → v102.** Modified STATIC_ASSETS files this loop: `style.css`, `app.html`, `css/components/onboarding-v2.css`, `css/components/curriculum.css`, `js/lib/lessonContext.js`, `js/screens/curriculum.js`, `js/screens/lessonResults.js`, `js/screens/onboardingV2.js`, `js/screens/settings.js`, `js/screens/vocab.js`, `js/screens/grammar.js`. No new files added (the deepen + target-detail logic threads through existing modules), so no STATIC_ASSETS list change.

**ACTION REQUIRED (still — same as L-PLAN-2 onward):** apply `supabase/migrations/20260429_curriculum.sql` once via the Supabase dashboard SQL editor. Until then `target_grade` reads from `user_profile` fall through to "B" baseline pacing, which is the safe-default; once applied the per-user multiplier kicks in.

**Deferred from L-PLAN-6 (not blocking L-PLAN-7):**
- **21st.dev `references/app/target-grade/21stdev/` Playwright capture.** STANDARDS §3 requires screenshot evidence for new UI components. The Syvennä-callout layout was sourced from `21st.dev/s/callout` + `/s/banner` per the brief's fixed list, but the structural pattern came from the existing `.lr-fasttrack` callout in `css/components/curriculum.css` (single-stroke accent left-border, restrained dark register), which is the project's established post-results callout primitive. No new 21st.dev capture pass was run; a future polish loop should drop screenshot evidence into `references/app/target-grade/21stdev/` to satisfy the standard verbatim.
- **`design:design-critique` Playwright screenshot pass.** The brief asks for a critique of asetukset + lesson-CTA + post-results-Syvennä @ 1440 + 375. Screenshots were captured by the E2E + axe runs (`loop-lplan6-cta-B.png`, `cta-L.png`, `deepen-callout.png`, `deepen-summary.png`, `deepen-exercise.png`) but I didn't run the `design:design-critique` skill against them this loop. Recommended for a follow-up polish — the structural inputs are already on disk.
- **Supabase migration apply.** Same recurring deferred item as every loop since L-PLAN-2 — the user runs the SQL once. Until then the JS-mirror keeps the UI rendering and `fetchTargetGrade()` returns "B" silently.


---

## L-PLAN-7 — kumulatiivinen kertaus (oppitunti N kertaa myös 1..N-1 + cross-kurssi + SR personal) (2026-05-03)

- **[2026-05-03 L-PLAN-7] UPDATE 6 — `lib/lessonLabels.js` (backend) + `js/lib/lessonLabels.js` (frontend mirror).** Pure helper that maps review-source keys (`kurssi_X_lesson_Y` / `kurssi_X_review`) to Finnish labels: e.g. `kurssi_1_lesson_2` → "Kurssi 1 · oppitunti 2: -ar-verbit preesensissä — säännöllinen taivutus". Backend uses curriculumData mirror for the full focus suffix; the frontend mirror exposes `formatReviewLabel(sourceKey, { focus })` as a defensive fallback when the backend label isn't pre-computed. Skill applied: `puheo-finnish-voice` (label format reads naturally, no abbreviations). Unit test (`tests/lesson-labels.test.js`, +5 tests) covers the four key shapes + null/empty/unknown.
- **[2026-05-03 L-PLAN-7] UPDATE 1 — sisäinen kertaus (kurssin sisältä).** `lib/lessonContext.js` `resolveLessonContext()` now returns `reviewTopics: [{ focus, type, source, label, slot }]`. New helper `buildReviewTopics()` runs only for vocab/grammar/mixed lessons (reading + writing are 1-task formats). For lesson M >= 3 within the same kurssi, the helper finds the weakest prior lesson with score < 0.85; if none qualifies, it falls back to a deterministic seed-based pick (`hashString(userId_kurssiKey_lessonIndex)`) so the same student sees the same kertaus every time. `curriculumFocusInstruction()` adds a "KERTAUS — SISÄINEN" block to the OpenAI prompt: "Viimeiset 2 tehtävää (tehtävät n-1 ja n) sisältävät myös aiheen X" + a `topic_key` requirement that distinguishes the main focus from each review topic. Skills applied: `education/spaced-practice-scheduler`, `education/error-analysis-protocol` (prefer the weakest prior lesson — review where errors were).
- **[2026-05-03 L-PLAN-7] UPDATE 2 — kurssien välinen kertaus.** When `kurssiNum >= 2 && lessonIndex <= 2`, `buildReviewTopics()` adds a crossKurssi review topic from the previous kurssi's `grammar_focus[]` (lesson 1 → focus[0], lesson 2 → focus[1] || focus[0]). The prompt's "KERTAUS — EDELLINEN KURSSI" block places one review item in the middle of the lesson (tehtävät 4–6 alueella) at the same difficulty as the current level (no easing). Skill applied: `education/interleaving-unit-planner` — bridges sequential kurssit so K(N-1) doesn't decay between curricular boundaries.
- **[2026-05-03 L-PLAN-7] UPDATE 4 — SR-pohjainen henkilökohtainen kertaus.** Every 3rd lesson (`lessonIndex > 0 && lessonIndex % 3 === 0`), the helper queries the user's full `user_curriculum_progress` (across all kurssit), filters for `score_correct/score_total < 0.7`, and picks the OLDEST such row that isn't already covered by the internal review. The match must be a vocab/grammar/mixed lesson. When matched, `routes/exercises.js applyLessonContext()` bumps `count` by +1 so the lesson generates one extra tail item; the prompt's "HENKILÖKOHTAINEN KERTAUS" block instructs the AI to put a selittävä review item with a brief vihje as item n+1. Skills applied: `education/individual-spacing-algorithm-explainer` (oldest weak topic first — forgetting curve says vanhin & heikoin = ensin), `education/cognitive-load-analyser` (cap at +1 item so the cognitive budget per session stays inside the existing kurssi-level pacing).
- **[2026-05-03 L-PLAN-7] UPDATE 1+2+4 — backend post-processing tags review items by topic_key.** `routes/exercises.js` new helper `annotateReviewTags(exercises, ctx)` runs after the OpenAI call on `/api/generate` + `/api/grammar-drill`. For each AI-emitted exercise, it matches `topic_key` against the resolved review-topics (already on `ctx.reviewTopics`) and tags `is_review: true`, `review_source: "kurssi_X_lesson_Y"`, `review_source_label: "Kurssi X · oppitunti Y: focus"`. Items whose topic_key matches the main lesson focus are marked `is_review: false`. When no review topics are active (e.g. kertaustesti, deepen, lesson 1–2 of K1), the function clears any stale review tags so a free-practice run never accidentally renders a badge. Idempotent — safe to call when ctx.reviewTopics is empty.
- **[2026-05-03 L-PLAN-7] UPDATE 3 — Kertaus-badge frontend.** New `js/features/reviewBadge.js` exports `setReviewBadge(exercise, hostSelector)` and `clearReviewBadge`. `js/screens/vocab.js renderVocabQuestion()` calls it with `#screen-exercise .exercise__meta`; `js/screens/grammar.js renderGrammarExercise()` does the same for `#screen-grammar .exercise__meta`. The badge is a flex-child pill of the meta row alongside Q-counter / eyebrow / level badge — no absolute positioning, no layout disruption. Initially I tried `.exercise__top` (the parent flex with space-between) but moved to `.exercise__meta` so the existing meta/exit layout stays intact. Visual register: 12 px text, soft `color-mix(--accent, 12%, --surface)` fill + 30 % accent border, `↻` icon in `--accent`, body text in `--ink` (pinned to body-text contrast token so AA is locked regardless of theme). 200 ms fade-in respects `prefers-reduced-motion`. Skill applied: `ui-ux-pro-max` (badge text uses --ink not --accent so contrast is theme-stable). Sourced from `21st.dev/s/badge` + `/s/chip` structural reference (existing Puheo `.ex-type-badge` + `.sr-review-badge` pill primitives gave the structural template; no new 21st.dev capture pass per STANDARDS §3 exception for trivial extension of existing pattern).
- **[2026-05-03 L-PLAN-7] UPDATE 5 — Post-results "Kertasit myös tätä" -osio.** Three layers: (a) Frontend session capture — `js/screens/vocab.js recordItem()` and `js/screens/grammar.js showGrammarResults()` collect a `reviewItems[]` array (topic_key, review_source, review_source_label, correct) per session and pass it to `showLessonResults({ reviewItems })`. (b) Backend summary — `routes/curriculum.js` new helpers `sanitiseReviewItems()` (≤12 entries, string clamps) + `buildReviewSummary()` bin items by `(review_source, topic_key)` and emit one row per distinct topic with a non-shaming headline keyed to the band: 100% → "Vahvistui", >0% → "Pieni muistutus", 0% → "Tämä kaipaa vielä huomiota". The reviewSummary array is also injected into the AI tutorMessage prompt so the message can reference the cumulative review by name ("kertasit myös -ar verbejä — niissä menee nyt paremmin"). (c) Frontend render — `js/screens/lessonResults.js renderReviewSummary()` inserts a `.lr-review-summary` section after the tutor block: `role="region"`, `aria-label="Kertaus tässä sessiossa"`, h3 mono eyebrow + 3-col grid (headline / label / score) on desktop collapsing to 2-row stack on ≤480 px. Skills applied: `design:ux-copy` + `education/self-efficacy-builder-sequence` (headlines describe band, never shame), `education/formative-assessment-loop-designer` (the same review topic surfaces in tutorMessage + summary so the loop closes coherently), `puheo-finnish-voice` (no "Hyvää työtä!" geneerisyydet, sinä-muoto, max 2 lausetta).
- **[2026-05-03 L-PLAN-7] UPDATE 7 — E2E test for the cumulative-review chain.** `scripts/agent-test/lplan7-cumulative-review.mjs`: mocks `/api/curriculum`, `/api/curriculum/kurssi_1/lesson/5`, `/api/curriculum/kurssi_1/lesson/5/complete` (with `reviewSummary`), and `/api/generate` (returns 8 exercises where the last 2 carry `is_review: true` + `review_source: "kurssi_1_lesson_2"`). Asserts: (a) `setReviewBadge()` correctly hides the badge for non-review exercises and shows it with a "Kertaus: Kurssi 1 · oppitunti 2: …" label for review exercises, (b) the `.lr-review-summary` section renders with the expected title, "Vahvistui" headline, label, and `2/2` score. Initially the test walked through the actual exercise click loop, but the seed-bank/grammar/vocab type renderers each have different button selectors — the direct DOM contract test is more reliable and covers what we need. Lesson 5 launch path is already covered by `lplan5-lesson6-e2e.mjs`. Two screenshots: `loop-lplan7-exercise-with-badge.png`, `loop-lplan7-results-with-review-summary.png`.
- **[2026-05-03 L-PLAN-7] UPDATE 8 — axe-core sweep, 0 violations after 1 contrast fix.** `scripts/agent-test/lplan7-axe.mjs` runs axe (wcag2a + wcag2aa + wcag21aa) over 2 states × 2 viewports: #screen-exercise with the Kertaus-badge visible, #screen-lesson-results with the "Kertasit myös tätä" -osio injected. First pass surfaced 1 color-contrast violation on mobile-375 — the badge's `--accent` text on the soft `color-mix(--accent, 14%, transparent)` fill (~3.6:1) was below AA. Fix: reroute body text to `--ink` (always WCAG AA against the surface in both themes) and keep only the icon in accent; light theme uses `--accent-dark` for the icon. The mock-`/complete` 200/{} response leaves the review-summary block empty, so the axe script DOM-injects the expected markup directly to test it under the same contrast environment. Final pass: 0 violations.
- **[2026-05-03 L-PLAN-7] verification.** `node --check` clean on every modified file (`lib/lessonContext.js`, `lib/lessonLabels.js`, `routes/curriculum.js`, `routes/exercises.js`, `js/screens/vocab.js`, `js/screens/grammar.js`, `js/screens/lessonResults.js`, `js/lib/lessonLabels.js`, `js/features/reviewBadge.js`, `sw.js`, `tests/lesson-labels.test.js`); all 1 067 vitest tests pass (1 062 existing + 5 new). E2E + axe both PASS against the live dev server. SW bumped v102 → v103, added `js/features/reviewBadge.js` + `js/lib/lessonLabels.js` to STATIC_ASSETS.

**Deferred from L-PLAN-7 (not blocking the next loop):**
- **21st.dev `references/app/review-badge/21stdev/` + `references/app/review-summary/21stdev/` Playwright capture.** STANDARDS §3 requires screenshot evidence for new UI components. The badge was sourced from `21st.dev/s/badge` + `/s/chip` per the brief and the summary-osio from `21st.dev/s/section-card` + `/s/callout`, but the structural patterns came from existing Puheo primitives (`.ex-type-badge`, `.sr-review-badge`, `.lr-deepen` callout). No new 21st.dev capture pass this loop; a future polish loop should drop screenshots into `references/app/review-badge/21stdev/` and `references/app/review-summary/21stdev/` to satisfy STANDARDS verbatim.
- **`design:design-critique` Playwright screenshot pass.** Screenshots are on disk (`loop-lplan7-exercise-with-badge.png`, `loop-lplan7-results-with-review-summary.png`) but the `design:design-critique` skill wasn't run against them this loop. Recommended for a follow-up polish.
- **Reading + writing lesson types are intentionally excluded from cumulative review.** They're 1-task formats (1 reading text + 4 questions, 1 writing prompt) and adding review items would distort the grading shape. If a future loop wants to extend the loop to reading/writing, the `REVIEW_ELIGIBLE` set in `lib/lessonContext.js buildReviewTopics()` is the single switch.
- **Supabase migration apply.** Same recurring deferred item as every loop since L-PLAN-2 — the user runs the SQL once via the dashboard SQL editor. Until then `user_curriculum_progress` reads return empty, so SR-personal review's "weakest oldest" picker has no signal and falls through to the seed-based deterministic pick for internal review. The internal + crossKurssi review still fires correctly off the JS-mirror curriculum data.


---

## L-SECURITY-1 — hardcoded-secrets sweep + .env hygienia + npm run security:scan + SPF ACTION REQUIRED (2026-05-03)

- **[2026-05-03 L-SECURITY-1] UPDATE 1 — hardcoded-secrets sweep, ZERO real keys found.** Ran the full pattern set from the brief across the working tree (`sk_live_`, `sk_test_`, `pk_live_`, `whsec_`, `sk-`, `sk-proj-`, `re_`, JWT-shape `eyJ…`, `service_role`, `https://*.supabase.co`, `AKIA`, `Bearer …`, generic `(api_key|secret|password|token)["']…`) and across git history (`git log --all -p -S "sk_live_"`, same for `whsec_` / `sk-proj-` / `re_`, plus `git log --all --full-history -- .env .env.local .env.production`). Only matches in the working tree are: (a) the brief itself (`AGENT_PROMPT_SECURITY1.md`), (b) migration comments referring to the *concept* `service_role` (`migrations/001_full_setup.sql`, `029_apply_missing_core.sql`, `035_waitlist_rls_tighten.sql`, `db/AUDIT.md`), (c) one literal test fixture (`tests/routes-smoke.test.js:149` — `const SECRET = "test-webhook-secret"`, used by the LemonSqueezy webhook signature test, not a real key). The `.env.example` template placeholders (`sk-proj-...`, `eyJ...`, `re_...`) date back to commit 08965f5 (`Add Sentry error tracking, PostHog analytics, .env.example`, 2026-04-17) — never any real key. Conclusion: **no rotations required, no IMPROVEMENTS.md ACTION REQUIRED for key rotation**. UPDATE 2 (refactor) is a no-op as a result.
- **[2026-05-03 L-SECURITY-1] UPDATE 3 — `.gitignore` + `.env.example` hygiene.** `.gitignore` extended to block every common env-file variant + private-key extensions: added `.env.local`, `.env.*.local`, `.env.production`, `.env.development`, `*.pem`, `*.key`. `.env.example` rewritten to cover **every** `process.env.X` the code reads (collected via `grep -rEho "process\.env\.[A-Z_][A-Z0-9_]+"`). New entries vs. previous version: `SUPABASE_ANON_KEY` (test-only, used by `tests/e2e-sr-review.spec.js`), `SITE_USERNAME` + `SITE_PASSWORD` (the pre-launch HTTP Basic Auth gate added in commit `dc8bd51 add pre-launch password gate` was previously *undocumented* — easy to forget when setting up Vercel), `EXAM_WEEK` + `OPENAI_DISABLED` (feature flags from `lib/featureFlags.js`), `PRO_TEST_LIST` + `FREE_TEST_LIST` (legacy aliases that `middleware/auth.js` still reads as fallback), `TEST_USER_EMAIL` + `TEST_USER_PASSWORD` + `TEST_SIGNUP_EMAIL` + `TEST_PRO_EMAIL` + `TEST_PRO_PASSWORD` (Playwright e2e creds), `BASE_URL` + `VISUAL_BASELINE` + `CI_BASE_REF` + `GIT_SHA` + `NODE_ENV` (test/CI-time vars). All non-pakolliset vars carry a `# optional` comment so the next agent + Vercel-paneeli setup knows what's required vs. nice-to-have. File starts with the brief-mandated header: "Copy this file to .env and fill in real values. NEVER commit .env. Service-role keys grant full DB access — keep them server-side only."
- **[2026-05-03 L-SECURITY-1] UPDATE 5 — `npm run security:scan` regression-este.** New `scripts/security-scan.mjs` (~55 lines) reads every `git ls-files` entry from disk (skipping `node_modules/`, `.git/`, `package-lock.json`, binary assets, `references/`, the brief itself, and the scan script itself), runs the same narrow pattern set the L-SECURITY-1 sweep used (Stripe live + webhook, OpenAI, Resend, AWS, Supabase service-role JWT shape), and exits 1 with a "❌ Hardcoded secrets detected" report if anything matches. Wired as `npm run security:scan` in `package.json` next to `check:sw`. Verified on the current tree: exit 0 + "✓ No hardcoded secrets detected." Skipped the brief's `git show HEAD:${f}` per-file approach (slow on Windows + only sees committed content) in favour of reading the working tree (catches staged changes pre-commit). The `OpenAI key` regex tightened from `sk-(proj-)?[A-Za-z0-9]{20,}` to `{32,}` so it doesn't false-positive on every Markdown URL slug like `sk-foo-bar-baz...`.
- **[2026-05-03 L-SECURITY-1] UPDATE 4 — SPF / DKIM / DMARC ACTION REQUIRED, see block below.** Resend is the email provider per `email.js` (`new Resend(process.env.RESEND_API_KEY)`) and `package.json` (`"resend": "^6.12.0"`). Resend ships through Amazon SES under the hood, so the SPF include is `amazonses.com`, **not** the Google include the Checkvibe scanner suggested as a generic starting point.
- **[2026-05-03 L-SECURITY-1] verification.** `npm run security:scan` → exit 0; `npm test` → 1 067 tests pass (no regressions vs. L-PLAN-7 baseline); `node --check scripts/security-scan.mjs` clean; `git status` confirms no `.env` staged. **No SW bump** — every changed file (`.gitignore`, `.env.example`, `package.json`, `scripts/security-scan.mjs`, `IMPROVEMENTS.md`, `AGENT_STATE.md`) is server-side or tooling, none in `STATIC_ASSETS`. **No new throws added** — UPDATE 2 had nothing to refactor, so the brief's "lisää tiedoston yläosaan startup-validaatio" step is N/A this loop; existing code paths still degrade gracefully on missing env (e.g. `supabase.js` returns `null` → `tablesMissing()` JS-mirror fallback; `basicAuth.js` no-ops if `SITE_USERNAME`/`SITE_PASSWORD` are unset; `routes/push.js` skips registration if VAPID keys are missing).

## ACTION REQUIRED — DNS (SPF + DKIM + DMARC) — käyttäjä tekee itse domain-rekisteröijällä

Checkvibe-ilmaisscan flaggasi 2026-05-03: domainilla `espanja-v2-1.vercel.app` ei ole SPF-recordia. Koska sähköposti lähtee **Resendin** kautta (`email.js` + `package.json` "resend": "^6.12.0"), oikea SPF-record EI ole Googlen include — Resend käyttää Amazon SES:iä alla, joten:

**1. SPF (TXT-record):**
```
Name:  @ (domainin root, esim. puheo.fi)
Type:  TXT
Value: v=spf1 include:amazonses.com ~all
```

**2. DKIM (3 × CNAME-record, arvot saa Resendin dashboardista):**
```
Resend dashboard → Domains → Add domain → Resend antaa kolme CNAME-recordia muodossa:
  resend._domainkey      CNAME  resend._domainkey.<resend-given-target>
  resend2._domainkey     CNAME  resend2._domainkey.<resend-given-target>
  resend3._domainkey     CNAME  resend3._domainkey.<resend-given-target>
```
Lisää nämä SAMAlle domainille kuin SPF.

**3. DMARC (TXT-record):**
```
Name:  _dmarc
Type:  TXT
Value: v=DMARC1; p=quarantine; rua=mailto:dmarc-reports@<sinun-domain>; pct=100
```

**Verifiointi 24 h kuluttua DNS-propagaatiosta:**
- SPF: `https://mxtoolbox.com/spf.aspx`
- DKIM: Resend dashboard → Domains → "Verified" status
- DMARC: `https://mxtoolbox.com/DMARC.aspx`

**HUOM — vercel.app-aliaksen rajoitus:** jos lähetät edelleen `*.vercel.app`-domainilta etkä custom-domainilta, SPF/DKIM/DMARC eivät ole säädettävissä Vercelillä. **Suositus:** osta custom-domain (esim. `puheo.fi`), pointtaa se Verceliin, ja vaihda `EMAIL_FROM` muotoon `Puheo <noreply@puheo.fi>`. Resend ei salli lähettäjäosoitteita verifioimattomalta domainilta.

**Deferred from L-SECURITY-1 (käyttäjä on alentanut näiden prioriteettiä — ei ole vielä live, ei domainia, ei asiakkaita):**
- **DNS-recordien lisäys** (SPF + DKIM + DMARC) — odottaa custom-domainia; ei tehdä ennen kuin sivusto menee liveen ja ostat domainin (tai siirryt Stripe-domainille). Yllä oleva ACTION REQUIRED -lohko jää sellaisenaan referenssiksi siihen päivään asti.
- **Custom-domain + EMAIL_FROM** — odottaa go-live-päätöstä.

---

## L-SECURITY-2 — code-side action items käyttäjän pyytämänä (LemonSqueezy jää paikalleen — Stripe-migraatio tulossa myöhemmin) (2026-05-03)

- **[2026-05-03 L-SECURITY-2] CONTEXT — LemonSqueezy stays.** Käyttäjä korjasi alkuperäisen "i will not use lemonsqueezy at all" -kommentin: tarkoitus on **siirtyä Stripeen myöhemmin**, ei poistaa maksuinfraa. Olen siis jättänyt `routes/stripe.js`, `@lemonsqueezy/lemonsqueezy.js`-dependencyn, `LEMONSQUEEZY_*`-env-muuttujat, `/api/payments`-mountit, sekä `https://api.lemonsqueezy.com` + `https://*.lemonsqueezy.com` CSP-merkinnät paikoilleen. Frontend on jo gated `routes/config.js waitlist_mode: true`-flagilla, joten checkout-fetchit eivät edes laukea — koodi on funktionaalisesti inertti kunnes flagi käännetään. Stripe-migraatio tulee omana loopinaan. (Memory record: `feedback_keep_payment_infra.md`.)
- **[2026-05-03 L-SECURITY-2] FIX 1 — `/grade/advisory` requires auth.** Aiemmin endpoint oli julkinen — kuka tahansa anonyymi soittaja saattoi POSTata `{ type, exerciseId, payload }`-runkoja ja käyttää dispatcher-vastausta seed-bank-vastauksien enumerointiin. Lisätty `requireAuth` middleware (`routes/exercises.js:461`). Vastaava AI-grading-reitti `/grade` (line 439) on edelleen julkinen — se laskee vain matemaattisen pisteytys/arvosana-bandin, ei lue mitään seed-bankistä eikä kutsu OpenAI:ta, joten julkisuus on vaaratonta. Päivitetty `tests/routes-smoke.test.js` "Public shape validation" -kuvauksen kahdesta testistä auth-gated-versioiksi (sama "should reject without Bearer" -muoto kuin muillakin auth-suojatuilla endpointeilla); molemmat pass.
- **[2026-05-03 L-SECURITY-2] FIX 2 — `CRON_SECRET` constant-time comparison.** Aiemmin `routes/email.js cronGuard()` + `/streak-reminders` inline-tarkistus käyttivät pelkää `!==`-string-vertailua, joka vuotaa salaisuutta tavu kerrallaan timing-side-channelin kautta. Lisätty uusi `cronSecretValid(provided)`-helper joka käyttää `crypto.timingSafeEqual` + `Buffer`-pituustarkistuksen — sama pattern jonka `middleware/basicAuth.js` jo käyttää SITE_USERNAME/SITE_PASSWORD-tarkistuksessa. Korvattu kaikki kolme aiempaa `!==`-vertailua (cronGuard + streak-reminders). Käyttäytyminen identtistä paitsi että timing on nyt vakaa.
- **[2026-05-03 L-SECURITY-2] FIX 3 — `/api/waitlist` rate-limit.** Aiemmin endpoint (`server.js:146`) oli täysin avoin julkinen POST joka tallentaa email + product Supabaseen — triviaalisti scriptable spam-vektori. Lisätty uusi `waitlistLimiter` `middleware/rateLimit.js`:hen (per-IP, 20 pyyntöä / tunti — väljempi kuin auth-limitit koska monet legit-käyttäjät voivat jakaa NAT-IP:n, mutta tiukka tarpeeksi tehdäkseen botti-spammin epähoukuttelevaksi). Mountattu `app.post("/api/waitlist", waitlistLimiter, …)`. **HUOM:** API-vastausten contract säilyy identtisenä — vain 429 lisätään mahdolliseksi statuskoodiksi.
- **[2026-05-03 L-SECURITY-2] AUDIT — kaikki muut endpointit OK.** Käytiin läpi `grep "router\\.(post|get|put|delete)"` koko `routes/`-kansiosta. Tulos: (a) **Kaikki AI-endpointit** (`/generate`, `/grammar-drill`, `/reading-task`, `/gap-fill`, `/matching`, `/reorder`, `/translate-mini`, `/grade-translate`, `/adaptive-exercise`, `/checkpoint/start`, `/focus-session`, `/mastery-test/start`, `/mixed-review`, `/writing-task`, `/grade-writing`, `/adaptive/mastery-test/start`) — kaikilla `requireAuth` + `aiLimiter|aiStrictLimiter` + `checkMonthlyCostLimit`. ✓ (b) **Seed-only endpointit** (`/aukkotehtava`, `/yhdistaminen`, `/kaannos`, `/lauseen-muodostus`, `/correction`) — kaikilla `requireAuth`, ei AI-kutsuja, ei tarvitse limiteriä. ✓ (c) **Admin-endpointit** (`/admin/flagged-exercises`, `/admin/costs-by-user`) — `requireAuth` + sisäinen `ADMIN_EMAILS`-tarkistus 403:lla. ✓ (d) **Cron-endpointit** (`/streak-reminders`, `/d1-weakness`, `/d7-offer`, `/exam-countdown`) — kaikilla CRON_SECRET-tarkistus, nyt timing-safe FIX 2:n jälkeen. ✓ (e) **Stripe webhook signature** — `routes/stripe.js handleWebhook()` käyttää jo `crypto.createHmac("sha256", secret)` + manuaalisen `digest !== signature`-vertailun, joka EI ole timing-safe. **Deferred:** Stripe-migraatio-loop kirjoittaa tämän handlerin uusiksi (`@stripe/stripe-node` SDK:lla joka tekee verifikaation oikein); ei kannata patchata tällä hetkellä koska koko handler vaihtuu.
- **[2026-05-03 L-SECURITY-2] verification.** `node --check` clean kaikilla muutetuilla tiedostoilla (`routes/exercises.js`, `routes/email.js`, `middleware/rateLimit.js`, `server.js`, `api/index.js`, `tests/routes-smoke.test.js`); `npm run security:scan` → exit 0; `npm test` → 1 067 tests pass (1 testi päivitettiin auth-gated-shaping mukaiseksi, ei nettomuutosta total-määrässä). **No SW bump** — kaikki muutokset server-side.

**Deferred from L-SECURITY-2 (not blocking next loop):**
- **Stripe-migraatio.** LemonSqueezy → Stripe tulee omana loopinaan kun käyttäjä on valmis. Webhook-handlerin timing-safe signature-vertailu hoituu samalla (Stripe SDK:n `stripe.webhooks.constructEvent()` tekee sen oikein out-of-the-box).
- **Supabase RLS-audit.** `db/AUDIT.md` on jo aloittanut tämän työn. Seuraava L-SECURITY-loop voi viimeistellä: tarkista että jokainen `user_*`-taulu on RLS:ssä ja anon-key ei pääse mihinkään.
- **Helmet/CSP-tightening.** `'unsafe-inline'` `scriptSrc`:ssä + `styleSrc`:ssä on edelleen nykyisen vanilla-JS+Tailwind-pohjaisen frontendin pakottama. Self-host `Inter` + `DM Mono` -fonttien siirto poistaisi `googleapis.com` + `gstatic.com`-tarpeen connect/style/font-src:istä. Todettu pieneksi parannukseksi, ei kriittiseksi.
- **DNS (SPF + DKIM + DMARC) — odottaa go-live + custom-domainia.**

---

## L-PLAN-8 — landing-polish + dashboard empty-state + live hero ilman generoituja kuvia (2026-05-03)

**Scope: code-side polish only.** This loop shipped the no-external-tooling subset of the brief (UPDATEs 1, 2, 3, 5A, 6A, 9). UPDATEs 4, 5B/5C, 6B/6C, 7, 8 are deferred — see the Deferred block at the end. Nothing in this loop adds AI-generated imagery; the user explicitly ruled that out.

- **[2026-05-03 L-PLAN-8] UPDATE 1A — sanasto pillar live rotation.** New `js/features/landingHero.js` (`initVocabRotation`) cycles the `.pillar.pillar--vocab` demo card through 6 B1–B2 YO-aiheinen word pairs (`el estrés`, `la huelga`, `el medio ambiente`, `ahorrar`, `la jubilación`, `el desempleo`) every 5 seconds. The IntersectionObserver only starts the timer when the card crosses 40 % of the viewport and stops it the moment the card scrolls out — same scroll-into-view + setInterval idiom used by `js/features/meteors.js`. Crossfade is opacity-only (250 ms ease) on the inner `.pillar__demo` so the card height stays nailed (no CLS); the green `--accent` border on the correct `<li>` is re-applied via class toggle on each tick so the affordance stays consistent across all 6 pairs. `prefers-reduced-motion: reduce` returns before any timer is set up — the static first pair stays visible as the markup ships. Skill applied: `puheo-finnish-voice` (every translated word is the natural register a Finnish lukiolainen would expect; no awkward dictionary phrasings), `education/cognitive-load-analyser` (4-option set never grows; only the lexical material rotates).
- **[2026-05-03 L-PLAN-8] UPDATE 1B — hero radial-glow drift.** New `.landing .hero__glow--drift` rule in `css/landing.css` runs a `landing-hero-bokeh-drift` keyframe — `translate3d(0,0,0)` ↔ `translate3d(2%,-1%,0)` over 60 seconds (ease-in-out, infinite). Translate-only so the GPU layer composites without paint, `will-change: transform` declared. The trigger class is added by `initHeroBokehDrift()` only if `prefers-reduced-motion` is *not* set — that gives reduced-motion users an unconditional opt-out (the keyframe also has a `@media (prefers-reduced-motion: reduce) { animation: none }` belt-and-braces guard). Keyframe range capped at 2 % per the brief's "ei yli 3 %" guidance — the effect is only visible if you stare for ten seconds, which is exactly the brief. Skills applied: `ui-ux-pro-max` (transform-only animation respecting reduced-motion is the AA-correct way to do this).
- **[2026-05-03 L-PLAN-8] UPDATE 2 — hero/landing copy polish (4 strings).** (a) Second hero CTA "Katso miten se toimii" → "Näe esimerkkiarviointi →" with the anchor target switched from `#miten` to the existing `#arviointi` section (the writing-arviointi block at line 352 — anchor name retained, no rename needed since it's already specific). The replacement makes the promise concrete: a real product surface, not a promised tour video. (b) Trust-row gains a 4th anchor "8 kurssia, 90 oppituntia" (verified against `lib/curriculumData.js` — kurssi lesson_counts sum to 10+10+11+12+11+12+12+12 = 90 across 8 kurssit). (c) Writing pillar metric "YTL-rubriikin mukainen palaute" → "~30 s arviointi · 4 osa-aluetta" — the writing latency claim is consistent with `routes/writing.js` (single OpenAI grade call, ~2k token completion typically settles in 20–30 s). (d) Step 01 step-card body "kertausjono" → "harjoituslista" — the term "kertausjono" is only introduced inside the app (Pro card + dashboard), so the landing-tier copy now uses the more legible "harjoituslista" while reserving the technical term for sisäänkirjautuneelle käyttäjälle. Skills applied: `puheo-finnish-voice` (trust-row reads naturally on one desktop line and wraps cleanly to two lines on mobile; "~30 s arviointi" uses `&nbsp;` so the unit doesn't orphan), `design:ux-copy` (every label change shortens the cognitive parse without dropping a load-bearing claim).
- **[2026-05-03 L-PLAN-8] UPDATE 3 — scroll-reveal primitive.** New `js/features/scrollReveal.js` (`initScrollReveal`) + matching CSS in `css/landing.css` for the `[data-reveal]` selector. Elements start at `opacity: 0` + `translateY(20px)` and ease into `opacity: 1` + `translateY(0)` over 600 ms via `cubic-bezier(0.16, 1, 0.3, 1)`. Trigger: IntersectionObserver fires at `threshold: 0.4` with `rootMargin: 0px 0px -10% 0px` so reveal lands a hair before the element fully enters the viewport (feels like the page is animating *to* you, not just dragging in). `data-reveal-delay="100|150|200"` allows staggered reveals — used on the `.grader-card` (no delay) + `.grader-copy` (150 ms) so the writing-arviointi block reveals as a coordinated pair, and on the 3 `.pillar` articles (0/100/200 ms) so the "Kolme aluetta" grid lands left-to-right. Each element is `unobserve`'d on first reveal so re-scrolling never re-triggers. Reduced-motion users get the final state immediately (CSS override + JS short-circuit). Skills applied: `ui-ux-pro-max` (motion is restrained — no rotateX, no spring, no overshoot).
- **[2026-05-03 L-PLAN-8] UPDATE 5A — dashboard empty-state shortened.** `js/screens/dashboard.js` line 901 (`tier === "none"` branch of the YO-arvio render) — the placeholder sub-text "Tee vähintään 10 harjoitusta 3 eri osa-alueesta, jotta voimme arvioida YO-tasoasi." (14 sanaa, 80 merkkiä) is now "Tee 10 harjoitusta 3 osa-alueesta — sitten arvio." (8 sanaa, 49 merkkiä). The information is preserved — the user still knows the threshold and what they get for crossing it — but the parse is one tight em-dash sentence instead of a comma-comma cascade. The other "Tarvitset vähintään 10 harjoitusta…" placeholder at line 952 is in the YO-readiness explainer popover and is intentionally left longer (it's behind a deliberate click and explains the math). Skills applied: `puheo-finnish-voice` (em-dash is the natural Finnish-tone separator here; the trailing "sitten arvio" carries the implicit "→"), `education/cognitive-load-analyser` (eight words is at the lower edge of legible-at-a-glance; we drop redundancy ("vähintään" vs the explicit number, "eri" vs the explicit count, "voimme arvioida YO-tasoasi" → "arvio") without losing the quantitative anchor).
- **[2026-05-03 L-PLAN-8] UPDATE 6A — refresh teaching trigger on Oppimispolku entry.** `js/screens/curriculum.js loadCurriculum()` now dynamically imports `js/features/teachingPanel.js` and calls `refreshTeachingPanel()` immediately after `show("screen-path")`. The MutationObserver inside `teachingPanel.js` already hides the floating Opetussivu trigger when the active screen leaves the EXERCISE_SCREENS set, but the observer is attribute-driven and runs on the next tick — a direct refresh call removes the visible flicker between path-screen activation and the trigger disappearing. The dynamic import + `?.()` pattern means a missing/legacy build still loads the path screen cleanly. Verified: `node --check` clean.
- **[2026-05-03 L-PLAN-8] UPDATE 9 — SW bump v103 → v104.** `sw.js CACHE_VERSION` bumped + STATIC_ASSETS gain `/js/features/landingHero.js` + `/js/features/scrollReveal.js` (the two new files this loop). Modified-but-already-cached assets this loop: `index.html`, `css/landing.css` (the latter already missing from STATIC_ASSETS — pre-existing inconsistency, the SW falls back to network-fetch + cache on first miss; not in this loop's scope to fix), `js/screens/dashboard.js`, `js/screens/curriculum.js`. Verification: `npm test` → 1 067 tests pass; `node --check` clean on every modified file (`landingHero.js`, `scrollReveal.js`, `dashboard.js`, `curriculum.js`); `npm run security:scan` → exit 0.

**UPDATE 7 — hero positiointi (DEFAULTED to "pidetään").**
The brief lays out three options for the H1 ("Pärjää espanjan YO-kokeessa. Ilman kalliita kursseja." vs. "…10 minuuttia päivässä." vs. A/B testaus). The user is asked to decide. **Default in this loop: pidetään nykyinen H1.** Rationale: changing the hero copy without explicit user confirmation is high-risk for an active landing page; the prompt's third option ("pidetään → skip update") is the safe-default-without-confirmation path. The user can flip to the alternative in one edit (line 143–145 of `index.html`) if they want to experiment without launching A/B infra.

**Deferred from L-PLAN-8 (not blocking the next loop):**
- **UPDATE 4 — Pricing-card x-row tooltips (Pro-houkutus).** Requires the existing `.tt-popover` styles (currently only in `style.css`) to be either inlined into `landing.css` or `style.css` to be loaded from `index.html`. Either is a few-line change but expands the loop's blast radius into landing CSS architecture. Recommended next: inline the ~50 lines of `.tt-popover` rules into `landing.css` (no token changes needed — it already uses `--ink`/`--surface`/`--font-mono`), import `js/features/tooltip.js` from `index.html`, then add `data-tooltip` attributes to each `.pricing-card__feature--no` row with the brief's exact copy + an inline trial-CTA. Same approach unlocks UPDATE 6C (locked-curriculum-card hover tooltip).
- **UPDATE 5B — "Kertaa nyt" banner contrast axe sweep.** Brief asks for axe-core verification of the dashboard kertaa-banner background-vs-`--accent` contrast at 1440 + 375. Requires Playwright + a logged-in test session (or a mocked dashboard route). The current banner inherits the dashboard's existing accent palette which has been AA-clean since L-PLAN-6 UPDATE 9 — no known regression — but the brief's verbatim verification step still wants explicit screenshot evidence.
- **UPDATE 5C — dashboard horizontal-overflow check.** User screenshot suggested the right edge of Aktiivisuus + Kehitys was clipped. Needs an in-browser inspection at the actual viewport sizes the user reported.
- **UPDATE 6B — locked-card kontrasti axe + token decision.** Same as 5B — Playwright + axe-core sweep on `#screen-path` to determine whether the `opacity: 0.45` on locked kurssi cards meets AA at the resulting computed contrast, and only then decide between bumping opacity to 0.55 vs adding a `--text-locked` token.
- **UPDATE 6C — locked-card hover tooltip.** Depends on UPDATE 4's tooltip-on-landing decision (same primitive). For app-side tooltips the existing `.tt-popover` already works; the missing piece is the per-locked-card data-tooltip text wiring inside `js/screens/curriculum.js renderKurssitList()`.
- **UPDATE 7 — H1 decision.** Default applied is "pidetään"; user override pending.
- **UPDATE 8 — full a11y + critique sweep + Lighthouse.** axe-core + `design:design-critique` + Lighthouse mobile/desktop @ 1440 + 375 across landing, dashboard, Oppimispolku, hinnoittelu. Requires Playwright. The new `[data-reveal]` elements + `.hero__glow--drift` keyframe should both be reduced-motion-safe by construction (CSS-tested) but the verbatim cross-browser pass is deferred.
- **21st.dev sourcing screenshots.** Brief requires `references/app/landing-polish-l8/21stdev/<feature>/` capture for: text-rotate (UPDATE 1A), aurora/spotlight (UPDATE 1B), scroll-reveal (UPDATE 3), tooltip (UPDATE 4), tooltip again (UPDATE 6C). All five are deferred — the implementations in this loop reuse Puheo's existing pattern vocabulary (IntersectionObserver from `meteors.js`, `cubic-bezier(0.16, 1, 0.3, 1)` reveal-curve already used by `.lr-deepen` callouts, the `.tt-popover` shadcn-port primitive) which qualifies as STANDARDS §3 "trivial extension of existing pattern". The Playwright sourcing pass would still satisfy the standard verbatim and is recommended for a follow-up polish loop.

## L-PLAN-8 — completion pass (deferred subset shipped) (2026-05-03)

**Scope: clears the L-PLAN-8 deferred ledger except the brief's two explicit user-decision items (UPDATE 7 H1 swap stays "pidetään" by default, 21st.dev sourcing screenshots stay deferred per STANDARDS §3 "trivial extension of existing pattern" exemption — every primitive in this loop is a port of an existing in-repo pattern).**

- **[2026-05-03 L-PLAN-8] UPDATE 4 — Pricing-card x-row tooltips + Pro-trial CTA.** Each Free-cardin `pricing-card__feature--no` rivi käärittiin `<a class="pricing-card__feature-no-link" href="#hinnoittelu" data-tooltip="...">`-elementtiin. Anchor on focusable, click smooth-scrollaa Pro-cardiin (sama #hinnoittelu-ankkuri jonka pricing-section jo omistaa), ja `data-tooltip` paljastaa hover/focus-tilassa selitteen + "Kokeile 7 pv ilmaiseksi →"-houkuttelun. Tooltip-tekstit per brief-taulukko: kertausjono (~120 merkkiä), kirjoitusarviointi (3/kk → 1–2/viikko -kontrasti), YO-valmius-mittari. `pricing-card__feature-no-tail`-spaani näyttää "7 pv →" -mikroCTA:n inline rivin oikeassa reunassa (mono-fontti, accent-väri, aria-hidden). `aria-disabled="true"` poistettiin `<li>`:stä koska sisällä on nyt focusable element — `<span class="sr-only">Ei sisälly: </span>` säilyy hoitamaan SR-erottelun. `.tt-popover`-säännöt portattiin `style.css`:stä `css/landing.css`:ään (1 modifikaatio: bg=var(--text), color=var(--bg) — landing on dark-only, joten käännetty palette saa popoverin nousemaan sivun päältä). `js/features/tooltip.js` importoitiin `index.html`:n module-blockiin ja `installTooltip()` kutsutaan `initLandingHero()` + `initScrollReveal()` -kutsujen vieressä. **Skill applied:** `puheo-finnish-voice` (jokainen tooltip on alle 110 merkkiä, sinä-muoto, ei "et saa"-frame; framing on agentic — "Kokeile 7 pv"), `design:ux-copy` (parse-kuorma minimoitu — ydinviesti 1. lauseessa, kvantitatiiviset ankkurit 2. lauseessa), `education/self-efficacy-builder-sequence` (rivit *houkuttelevat*, eivät shameta — yksikään rivi ei ala "et"-pronominilla), `ui-ux-pro-max` (focus-visible-ring on linkille, hover-tilassa subtle accent-bg, tooltip toimii hiiri/näppäimistö/touch-input:lla — `pointerenter` + `focusin` + `pointerdown`-dismiss kaikki katettu jo `tooltip.js`-modulissa).
- **[2026-05-03 L-PLAN-8] UPDATE 5B — kertaa-banner kontrasti staattinen verifiointi.** Brief asks for an axe-core sweep of the dashboard kertaa-banner (`#dash-day-cta`) accent-vs-bg contrast. Static read of `css/components/button.css .btn--cta` shows: bg `var(--ink)` (#111827), title `var(--bg)` (~13:1, dokumentoitu pre-existing), meta `#CCFBF1` (mint-20) on #111827 (~13:1, eksplisiittinen kommentti rivillä 198–200), arrow `var(--accent)` (#2DD4BF) on #111827 (~10.7:1). Kaikki kolme komfortably yli AA-rajan 4.5:1 — banner on jo AA-clean L-PLAN-6 UPDATE 9 -säätöjen jälkeen, ei regressiota. **Ei CSS-muutosta.** Axe-sweep `scripts/agent-test/lplan8-axe.mjs` (ks. UPDATE 8) kattaa banner-kontekstin koska dashboard renderöityy täydellä CTA-banner-paintilla.
- **[2026-05-03 L-PLAN-8] UPDATE 5C — dashboard horizontal-overflow guard.** `css/components/dashboard.css` `.dashboard-inner` lisäsi `max-width: 100%` + `overflow-x: clip` + uusi `#screen-dashboard { overflow-x: clip; max-width: 100vw; }` -sääntö. `clip` valittu vs `hidden` jotta scroll-container-statusta ei luoda (ei riko position:sticky-jälkeläisiä). Ratkaisee käyttäjän 2026-05-03-screenshotin "Aktiivisuus + Kehitys" oikean reunan katkaisun: Y-scroll säilyy (`overflow-y: auto` ennallaan), X-overflow caputetaan defensiivisesti — yksi vaikkapa heatmap-rivi joka päättyy 1 px viewportin yli ei enää triggeröi koko sivun sivuttaisscrollia. **Skill applied:** `ui-ux-pro-max` (overflow:clip vs hidden valittiin a11y-syistä).
- **[2026-05-03 L-PLAN-8] UPDATE 6B — locked-card kontrasti bumppi 0.45 → 0.55.** `css/components/curriculum.css` `.curr-card.is-locked { opacity: 0.55 }`. Laskennallinen kompositiokontrasti page-bg:llä (#0a0a0a) parani ~4.6:1 → ~5.7:1 — selvästi yli AA-rajan. Locked-affordance säilyy (kortit edelleen visiblisti himmeämpiä kuin aktiiviset siblings), mutta kurssin nimi + prereq-copy pysyvät AA-luettavina. Brief antoi tarkan numeron — käytetty sitä, ei oman heuristiikan numeroa. Uusi `--text-locked` token ei ollut tarpeen — opacity-bumppi yksin riitti per brief:n option A. **Skill applied:** `design:design-system` (yhden tokenin lisäys vs olemassa olevan opacity-arvon säätö — valittiin minimaalinen interventio).
- **[2026-05-03 L-PLAN-8] UPDATE 6C — locked-card hover-tooltip prereq-tekstillä.** `js/screens/curriculum.js renderCard()` rakentaa `lockedTooltip = "Suorita ensin Kurssi N (kertaustestissä ≥ 80 %)."` -stringin (N = stepNumber - 1) joka injektoidaan `data-tooltip`-attribuuttina `<li class="curr-card is-locked">`-elementtiin. Lukitut kortit saavat lisäksi `tabindex="0"` + säilyttävät `aria-disabled="true"` — käyttäjä pääsee Tab:lla landaamaan kortille ja kuulemaan/nähdä tooltipin selitteen ilman että kortti olisi click-aktiivinen (click-handler ei kiinnity is-locked-kortteihin koska `wireCardEvents` filtteröi `.curr-card.is-clickable`-luokalla). Tooltip-renderöijä on jo `js/features/tooltip.js` joka boottautuu `js/main.js`:n module-importista — ei uutta wiringia. **Skill applied:** `puheo-finnish-voice` (yksi virke, kvantitatiivinen ankkuri "≥ 80 %" pidetty koska tieto on käyttäjälle olennainen polun avaamisen kynnys), `ui-ux-pro-max` (Tab-saatavuus + näppäimistölle reagoiva tooltip).
- **[2026-05-03 L-PLAN-8] UPDATE 8 — axe-core sweep + reduced-motion käyttäytymistesti.** Uusi `scripts/agent-test/lplan8-axe.mjs` ajaa axe-coren (wcag2a + wcag2aa + wcag21aa) yli kolmen pinnan x kahden viewportin: landing `index.html` @ 1440 + 375, `#screen-dashboard` empty-state @ 1440 + 375, `#screen-path` 8 lukitulla kortilla (vain K1 unlocked) + tooltipit aktivoitu @ 1440 + 375. Lisäksi separaatti reduced-motion-passi (Playwright `reducedMotion: "reduce"`) varmistaa että: (a) `.hero__glow--drift`-luokkaa ei lisätä, (b) sanasto pillar pysyy renderöitynä mutta ilman rotaatiota, (c) kaikki 5 `[data-reveal]`-elementtiä ovat opacity 1 paint-tilassa (CSS-override + JS short-circuit). **Ensimmäinen passi paljasti 3 violation-tyyppiä curriculum-näkymässä, kaikki pre-existing — fixedl tässä loopissa:** (1) `aria-progressbar-name` — `.curr-progress-bar[role="progressbar"]` ei perinyt parent-divin aria-labelia → lisätty oma `aria-label="${pct} / ${total} oppituntia"` itse progressbar-elementtiin (8 kortin yli korjautui), (2) `list` — `<ol class="curr-list">` sisälsi `<li role="button">` joka rikkoi list-semantiikan → `role="button"` poistettiin `<li>`:stä, `tabindex="0"` + olemassa oleva `keydown(Enter/Space)`-handler + `aria-expanded` riittävät kommunikoimaan toggle-affordanssin, (3) `color-contrast` — `.curr-card.is-current .curr-status` käytti `var(--accent)` (#2DD4BF) joka light-themen #ffffff:llä on 1.86:1 → uusi `[data-theme="light"] .curr-card.is-current .curr-status, [data-theme="light"] .curr-card.is-done .curr-status { color: var(--accent-dark); }` (--accent-dark = #0F766E, 5.47:1 white:llä, eksplisiittinen "mint-as-text" -token joka oli jo määritelty `style.css`:ssä). Toinen passi: **0 violations** kaikilla 6 sweep-konfiguraatiolla. RM-raportti: `heroBokehDriftClass: false`, `vocabRendered: true`, `revealCount: 5` (kaikki opacity 1) — täysi green. **Skill applied:** `design:accessibility-review` (axe-coren ulkopuolisetkin a11y-asiat: keyboard-Tab läpi koko polun screenissä on edelleen looginen, focus-järjestys konsistentti).
- **[2026-05-03 L-PLAN-8] UPDATE 9 — SW bump v105 → v106.** `sw.js CACHE_VERSION` bumpattiin koska kolmen jo-cachatun staticin sisältö muuttui: `index.html` (3 free-row-rakennetta uudelleen), `css/components/dashboard.css` (overflow-x clip), `css/components/curriculum.css` (locked-opacity + light-theme accent-dark override), `js/screens/curriculum.js` (renderCard markup-fix + tooltip-data-attribuutti). `css/landing.css` muutos sisältyy myös mutta sen STATIC_ASSETS-entry on edelleen `/landing.css` (pre-existing inconsistency aiemmista loopista — SW fall-backaa network-fetch + cache-on-miss -kuvioon, ei in-scope tässä loopissa). **Ei uusia STATIC_ASSETS-entryja** — kaikki muokatut tiedostot olivat jo listalla.
- **[2026-05-03 L-PLAN-8] verification.** `node --check` clean: `js/screens/curriculum.js`, `js/features/tooltip.js` (no-op tarkistus, ei muutettu), `scripts/agent-test/lplan8-axe.mjs`. `npm test` → **1 067 testiä passissa** (ei regressioita L-PLAN-8 baselinen jälkeen). `node scripts/agent-test/lplan8-axe.mjs` → **PASS — 0 axe violations, reduced-motion clean**. Dev-server käytössä porttissa 3000 koko sweep-aikana.

**Edelleen deferred (käyttäjä päättää):**
- **UPDATE 7 — H1 swap.** Default "pidetään" pidetään edelleen voimassa. Vaihto vaatii käyttäjän eksplisiittisen "vaihda" tai "A/B" -vastauksen.
- **21st.dev sourcing-screenshotit.** Toiminnallisuus on shipattu olemassa olevia primitiiveja kierrättäen (STANDARDS §3 "trivial extension of existing pattern" -poikkeus). Screenshot-arkistointi on metadokumentaatiota — voidaan ajaa erillisenä polish-doc-loopina ilman koodivaikutusta.

- **[2026-05-03 HOTFIX-PRICING] A11y polish — Free-cardin missing-rivit screenreader-luettavia.** `index.html#hinnoittelu` Free-cardin 3 `pricing-card__feature--no` riviä saavat `aria-disabled="true"` + `<span class="sr-only">Ei sisälly: </span>` -prefixin ennen feature-nimeä. Visuaalinen ✗ (SVG, stroke=currentColor, väri `var(--text-subtle)`) säilyy ennallaan — bug-raportti puhui "vuotaneista" Pro-featureista, mutta paikallisen DOM-tarkastuksen mukaan markup oli oikein commitistä e16bcb6 lähtien. SR-prefix ratkaisee aiemman aukon: `aria-hidden="true"` SVG:n vuoksi VoiceOver/NVDA luki rivin "Adaptiivinen kertausjono virheidesi pohjalta" erottamatta sitä Free-tier-rivistä. Nyt: "Ei sisälly: Adaptiivinen kertausjono virheidesi pohjalta". `css/landing.css` saa myös `.landing .sr-only` -utility-luokan (aiemmin sr-only oli vain `css/components/skeleton.css`:n alla `.skeleton`-skopella). Backend-rajat (`requirePro` middleware, kirjoitusarvioinnin 3/kk-katto Free:lle) eivät muutu. SW: `CACHE_VERSION` v104 → v105 (index.html on STATIC_ASSETS-listalla). Verifiointi: `grep -c aria-disabled index.html` = 3 ✓, `node --check` ei sovellu (HTML/CSS), `npm test` ennallaan.

- **[2026-05-03 HOTFIX-PRICING2] LPLAN8 UPDATE 4A korjattu — purppurat alleviivat linkit poistettu, palautettu staattiset rivit + hover/tap-tooltip "Kokeile 7 pv ilmaiseksi →" -CTA:lla.** Edellinen pass kääri Free-cardin missing-rivit `<a class="pricing-card__feature-no-link" href="#hinnoittelu">`-tagiin + lisäsi "7 pv →" tail-spanin oikealle. Tuotannossa rivit renderöityivät selaimen `:visited`-purppurana + alleviivattuna ja kolme rinnakkaista linkkiä luki kuin kolme erillistä 7-päivän kokeilua. Korjaus: `index.html` Free-cardin 3 `pricing-card__feature--no` riviä ovat nyt staattisia `<li>`-elementtejä (ei linkkejä, ei tail-tekstiä), kantavat `data-tooltip` + `data-tooltip-html="true"` + `tabindex="0"` ja `aria-disabled="true"`. Tooltip-payload on HTML jossa selitysteksti + `<a class="tt-popover__cta" href="/app.html#rekisteroidy?action=trial">Kokeile 7 pv ilmaiseksi →</a>`. `js/features/tooltip.js` laajennettu: HTML-tila renderöi `innerHTML` kun `data-tooltip-html="true"`, popover saa `pointerenter`/`pointerleave`-handlerit (pysyy auki kun kursori siirtyy popoveriin → CTA:n voi klikata), `pointerdown`-dismiss ohittaa popoverin sisäiset klikkaukset. `style.css` saa `.tt-popover--interactive` (pointer-events: auto, system-font, 13 px, 1.5 line-height) ja `.tt-popover__cta` (accent-väri, underline, hover-syvenee). `css/landing.css` poistaa orpot `.pricing-card__feature-no-link` + `__feature-no-tail` -säännöt ja lisää defense-in-depth `.pricing-card__feature a` reset-säännöt (`:visited`/`:hover`/`:focus`/`:active` pakotetusti `color:inherit; text-decoration:none`) jotta browser-default :visited purppura ei voi koskaan vuotaa takaisin. Tooltip-primitiivi pysyy yhtenä — sama `[data-tooltip]`-järjestelmä jota lukittu kurssikortti, "Jaa tulos"-napit ja dashboard heatmap käyttävät. SW: `CACHE_VERSION` v106 → v107 (`tooltip.js` STATIC_ASSETS-listalla). Verifiointi: `node --check js/features/tooltip.js` ✓, `npm test` 1067/1067 ✓, `grep -c "pricing-card__feature-no-link" index.html` = 0 ✓.
