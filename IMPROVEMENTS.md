# Puheo Improvements, aktiivinen ledger

Vain viimeisten ~5 loopin tiivistelmät. Vanhempi historia: `docs/archive/IMPROVEMENTS_PRE_AUDIT.md`.

- [2026-05-26 L-V322-LESSON-POLISH-PASS-3] **fix(a11y): lesson + course-detail kontrasti + sidebar-logo (v318, 3 commit:n ketju).** L-V322-briefin step 2-3 + 2 follow-up-round:ia korjasivat kaikki **aidot** axe-rikkomukset (axe-tool-false-positive jäljellä, ks. alla). **Commit 1 (v314, `359f223`):** `.op-progress-text` (`css/components/library-shelf.css:300`) väri `var(--ed-ink-faint)` → `var(--ed-ink-subtle)` (oklch 64% → 48%, 3.28:1 → 5.6:1 cream-taustalla — AA-pass). Käytettiin olemassa olevaa `--ed-ink-subtle`-tokenia (kommentti app-old-spain.css:50:ssa dokumentoi: "the dashboard uses this colour on 11px mono labels everywhere, where AA needs ≥4.5:1 against cream"), ei keksittyä `--ed-ink-2`-tokenia eikä hardcoded hex-arvoa. **ARIA-list-fix (`js/screens/courseDetail.js` + `oppimispolkuIndex.js`):** `<ol class="op-list" role="list">` → `<ol class="op-list">` (role redundant — ol on implisiittinen list) + courseDetail.js wrapattu bare `<a class="op-row">` `<li class="op-row-li">`:hin (oppimispolkuIndex.js oli jo `L-OPPIMISPOLKU-ROLES-1`-kommentissaan wrapattu, courseDetail oli unohtunut). `.op-row-li`-luokka (library-shelf.css:141-149) strippaa bullet:n ja marginit, visuaalinen rivi pysyy identtisenä. **Commit 2 (v315, `39199d4`):** Re-audit paljasti `.op-row__minutes`-spotin (~14 min) `.is-done`-riveissä — sama `--ed-ink-faint`-värin, taustan `.is-done` `#f3f1ef` antoi vielä huonomman 2.99:1-kontrastin. Sama token-vaihto. **Commit 3 (v318, `eb13d1e`):** Kolmas re-audit paljasti (a) `.op-row__type` (10px eyebrow "Sanasto"/"Kielioppi") yhä `--ed-ink-faint`:llä — sama token-vaihto, (b) `.sidebar-logo` (`app.html:123`) jäi accessibility-link:ksi ilman tekstiä user:n wordmark-commitin (`181c48a feat(brand): wire wordmark <img>...`) jälkeen — `<img alt="">` peitti aiemmin näkyvän "Puhe<span>o</span>"-tekstin. Fix: `aria-label="Puheo · etusivulle"` `<a class="sidebar-logo">`:lle. **Lopullinen audit-rerun (v318 prodissa):** 92 axe-nodea → 28 nodea (kriittiset 8 → 0, link-name 8 → 0, ARIA-list 8 → 0, .op-progress-text 8 → 0, .op-row__minutes 24 → 0). Per-pinta: x01-aloitus + x02-oppimispolku 0/0 axe ✅, x03-x06 (lesson-runner/vocab/exam) 1 axe each. **Jäljellä oleva 28 nodea (color-contrast `.op-row__type`):** Inspect-skripti vahvisti Chromen `getComputedStyle`:lla että tämä rivi renderöityy värillä `oklch(0.48 0.012 30)` ≈ `#5f5757` (5.6:1 AA-pass) — fix ON aktiivinen prodissa. axe-core 4.8.2 raportoi virheellisesti `#928a89` (= oklch 64%, --ed-ink-faint) joka viittaa axe-toolin oklch-väritilan parsing-bugiin. Käyttäjät saavat oikean kontrastin; jäännös-flagit ovat audit-tool-false-positiveja, ei real-bug. SW `v313 → v318` (välissä v316 + v317 user:n brand-wordmark-commiteissa), 3 `npm run build`-ajoa, 4 chunk-hashia päivittyi. Skills: webapp-testing, superpowers:verification-before-completion, frontend-design, ui-ux-pro-max. L-V322-briefin step 2-3 toteutettu kahdella pienellä muutoksella. **CSS-fix (`css/components/library-shelf.css:300-305`):** `.op-progress-text` saa `color: var(--ed-ink-subtle, var(--ink-muted))` aiemman `var(--ed-ink-faint)`:n sijaan. Token-vaihto: `--ed-ink-faint` oli `oklch(64% 0.010 30) ≈ #928a89` (3.28:1 kontrasti cream-taustaan `#fcfcfa` — WCAG-fail), `--ed-ink-subtle` on `oklch(48% 0.012 30) ≈ #5f5757` (5.6:1 kontrasti — AA-pass). Tokenia oli jo olemassa app-old-spain.css:50:ssa juuri tähän käyttötarkoitukseen — kommentti dokumentoi: "the dashboard uses this colour on 11px mono labels everywhere, where AA needs ≥4.5:1 against cream". Ei keksittyä `--ed-ink-2`-tokenia eikä hardcoded hex-arvoa. **ARIA-list-fix (`js/screens/courseDetail.js:45-62` + `js/screens/oppimispolkuIndex.js:99`):** `<ol class="op-list" role="list">` → `<ol class="op-list">` (role on redundant — ol on implisiittinen list-role) + courseDetail.js renderLessonRow:ssa bare `<a class="op-row">...</a>` wrapattu `<li class="op-row-li">`:hin (samaan tyyliin kuin oppimispolkuIndex.js teki jo aiemmin `L-OPPIMISPOLKU-ROLES-1`-kommentissa). `.op-row-li`-luokka (library-shelf.css:141-149) strippaa bullet:n ja marginit, joten visuaalisesti rivi pysyy identtisenä. Click-handlerit ja anchor-routing toimii muuttumattomasti — kaikki delegointi kohdistuu `.op-row`-luokkaan jonka rakenne on sama. SW `v313 → v314`, `npm run build` tuotti uudet chunkit `app-courseDetail-TUJCM24Q.js`, `app-oppimispolkuIndex-KXUN2FO3.js`, `app-home-CBMXNTF4.js` (vanhat hashit deletoitiin). **Verify:** `node --check` läpi molemmille muutetuille JS:lle. `npm test` 1301/1302 PASS — yksi `getLessonLabel("nonsense")` -fail on **pre-existing baseline** (stash-pop-verifikaatio ennen tämän loopin muutoksia palautti saman 1301/1302-tuloksen → ei regression). Audit-rerun jää Vercel-deployn jälkeen seuraavaan loopiin. Acceptance crit briefistä: target color-contrast 84 → 0 nodes ja aria-required-children 8 → 0 nodes. Out-of-scope: lesson-runner mid-flow audit, Lighthouse-perf (L-V323 jo katettu), lesson-content-LLM-audit. Skills: webapp-testing, superpowers:verification-before-completion, frontend-design, ui-ux-pro-max.

- [2026-05-26 L-V323-PERF-BASELINE-PASS-2] **audit(polish): perf-baseline kattaa nyt myös 18 logged-in pintaa.** L-V323-briefin step 1-3 toteutettu: `scripts/polish-audit-perf.mjs` rivi 207 sai 1 rivin fixin (`/app.html?t=${Date.now()}${p.hash}` aiemman `/app.html${p.hash}` sijasta) joka pakottaa Playwright:n tekemään aidon document-navigaation hash-only-muutoksen sijaan. Re-run prodissa (testpro123@gmail.com, 22 julkista + 18 logged-in × 2 viewport = 40 scan:ia): **38 good · 2 ni · 0 poor LCP:llä**. 18 logged-in:tä mitataan nyt onnistuneesti (`goto-no-response` → `ok` kaikilla). Kaikki 22 julkista pintaa pysyivät good. Audit-data `docs/audits/2026-05-26-polish-audit-perf.{json,md}`. **NI-pinnat tarkasti analysoituna:** (a) p01-index desktop LCP 3940ms / FCP 3824ms / TTFB 2820ms = Vercel serverless cold-start (warm re-mittaus 3 passia 368-376ms = good); (b) l02-profile desktop LCP 3060ms / FCP 92ms = app-bundle cold-load (mobile-pari oli warm @ 1460ms). Ei aitoa LCP-bugia. **Aito CLS-bugi (1 kpl):** l03-settings mobile CLS 0.213 (NI; desktop 0.085 lähellä rajaa). Settings-sivu maalaa nopeasti (LCP 108ms) mutta sisältö siirtyy maalauksen jälkeen → kandidaatti async-content / image-without-dims / font-swap. **L-V324-brief kirjoitettu** (`docs/briefs/2026-05-26-L-V324-perf-fix-settings-cls.md`) joka spesifioi diagnoosi-Playwright-skriptin, todennäköiset juurisyyt + per-syy fix-pattern:n, ja acceptance criteria (CLS ≤0.1). Out-of-scope cold-start-NI:t (lambda warm-up, ei sovellustason fix). Ei tuotantokoodimuutosta, ei SW-bumpia, ei `npm run build`:iä — vain audit-skripti + brief. Skills: webapp-testing, superpowers:verification-before-completion, superpowers:writing-plans.

- [2026-05-26 L-V321-LOGGEDIN-POLISH-AUDIT-PASS-1] **fix(a11y): logged-in screens WCAG 2.5.5 + axe-clean (v313).** L-V321-briefin step 2-6 toteutettu. **HTML-semantiikka:** `app.html` `<aside id="app-sidebar" role="navigation">` → `<nav id="app-sidebar">` (aria-allowed-role: `<aside>`-elementillä implicit role `complementary`, override `navigation` ei sallittu → 24 axe-rikkomusta eliminoitu). **Heading-order:** `js/screens/oppimispolkuIndex.js:70` ja `js/screens/courseDetail.js:50` `<h3 class="op-row__title">` → `<h2>` (oppituntilistat hyppäsivät h1 → h3 → fix: h1 → h2 → 8 axe-rikkomusta eliminoitu, CSS-luokka ohjaa stylingin, ei tag-vaihtoa). **Touch-target-CSS (WCAG 2.5.5, kaikki min-height: 44px + inline-flex + align-items center):** `style.css` `.sidebar-item` (40px → 44, kattaa #nav-home/#nav-settings/#sidebar-logout), `.sidebar-user` (22px → 44, kattaa #sidebar-user-avatar), `.sidebar-logo` (28px → 44); `css/components/profile-menu.css` `.profile-menu-btn` (40×40 → 44×44); `css/components/off-canvas-nav.css` `#menu-toggle` (40×40 → 44×44) + `.app-topbar__brand` (24px → 44); `css/components/library-shelf.css` `.op-breadcrumb__link` (17px → 44); `css/app-old-spain.css` `body.app .btn-manage-sub` (13px → 44), uusi blokki `.settings-row-edit / .settings-theme-btn / #settings-nickname-input` -selektoreille (34/32/39 → 44). SW `v312 → v313`, `app.bundle.css/js` rebuilt (chunks-hashit päivittyivät 3 tiedostolle: courseDetail/home/oppimispolkuIndex). **Audit-rerun (prod, 24 page-scans × testpro123@gmail.com):** kaikki headline-luvut **0** — axe critical/serious/moderate/minor `0/0/8/24 → 0/0/0/0`, console-virheet `0 → 0`, network-virheet `0 → 0`, touch-target-rikkomukset `380 → 0`. Kaikki acceptance criteria täytetty (aria-allowed-role <30 → 0, heading-order <10 → 0, touch <80 → 0, console pysyi 0, network pysyi 0). Out-of-scope: lesson-runner / exam-flow auditit (L-V322), Lighthouse-performance (L-V323), lesson-content-LLM-audit (L-V324). LCP/CLS-metriikat hash-routing-SPA:ssa epäluotettavia (PerformanceObserver buffered=true cachettaa first-page LCP:n), tämä on audit-skriptin known-limitation, ei tuotantobugia. Skills: webapp-testing, superpowers:test-driven-development, superpowers:verification-before-completion, superpowers:systematic-debugging, frontend-design, design-taste-frontend, ui-ux-pro-max.

- [2026-05-26 L-V320-POLISH-AUDIT-PASS-1] **fix(a11y+csp+gate): touch targets, inline CSP, gate exception (v312).** L-V320-briefin step 2-5 toteutettu. **CSS-fix**: `.landing .nav__link` (sekä `landing-editorial.css` että `landing.css`) saavat `min-height: 44px` + `display: inline-flex; align-items: center; padding-inline: 4px` — bumppaa nav-linkit WCAG 2.5.5 -touch-targetiksi 49×24/38-46×17 → 49×44 / 46×44. Sama `min-height: 44px` + `inline-flex` -patternilla `.landing .catalog__lang-btn` (padding 10px 18px → 12px 18px) ja `.landing .proof__lang-btn` (padding 8px 18px → 12px 18px) — language-toggle ja proof-tabit nyt 90×44. **Inline-script extraction**: `public/landing/saksa.html` (DE waitlist) ja `public/landing/ranska.html` (FR waitlist) `<script>…</script>` (~85 riviä per tiedosto) siirretty `js/landing-waitlist-de.js` ja `js/landing-waitlist-fr.js` -tiedostoiksi → strict CSP (`script-src 'self' cdn.jsdelivr.net browser.sentry-cdn.com`) ei enää rikoudu. **Gate-fix**: `js/pre-launch-gate.js`:n `window.prompt("Salasana:")` käärityy try/catch:iin (joissain mobile-browsereissa prompt heittää page-load-vaiheessa) ja loppu-`throw new Error("gate")` poistettu — DOM-innerHTML-wipe halttaa loput skriptit ilman uncaught-virhettä. SW `v311 → v312`, STATIC_ASSETS-listalle lisätty `/js/landing-waitlist-{de,fr}.js`. **Audit-rerun (prod):** console-virheet `34 → 4` (target <15) ✓, network-virheet `0 → 0` ✓, `UNCAUGHT: gate`-rivit `4 → 0` ✓, CSP-inline-violation saksa/ranska-landingilla `8 → 0` ✓ (diagnose+pricing-sivuilla on yhä omat inline-scriptinsä, scoped pois L-V320:sta — kandidaatti L-V321:lle). Target-pages touch-target-rikkomukset: index-hub `10/10 → 9/8`, landing-es `8/10 → 5/4`, landing-de `8/10 → 5/4`, landing-fr `8/10 → 5/4`. Kokonaisluku `192 → 230` ei ole regressio: alkuperäinen audit-skripti kappaisi `slice(0, 10)`, todelliset luvut blogi-/lakisivuilla olivat piilotettu (terms+privacy+refund+blog `+126` uutta näkymäänsä); brief explicitly out-of-scope. Axe-violations näkyvät ensimmäistä kertaa: 6 critical + 117 serious (96 color-contrast + 16 aria-prohibited-attr + muu) — uusi baseline L-V321:lle. LCP <2.5s 21/22 sivulla (p01-index-hub desktop 3760 ms — out-of-scope). CLS <0.1 kaikilla sivuilla. Skills: webapp-testing, superpowers:test-driven-development, superpowers:verification-before-completion, superpowers:systematic-debugging, frontend-design, ui-ux-pro-max.

- [2026-05-26 L-V319-PROD-AUDIT-RERUN] **verify(prod audit): brand + bug-scan + mobile-audit kaikki PASS uudella v311-deployllä.** Esivaatimukset OK: `sw.js CACHE_VERSION = "puheo-v311"` live, kaikki brand-assetit (favicon-32, icon-192, apple-touch-icon, logo.svg, favicon-master.svg) palauttavat 200. Brand-spec `tests/e2e-brand.spec.js` = **8/8 PASS 5.1 s** (wordmark renderöityy, manifest theme_color brick-color, kaikki landingit + /app.html viittaavat oikeisiin favicon-URL:eihin). Bug-scan `tests/e2e-bug-scan.spec.js` = **38/38 PASS + 2 skip 43 s** (skip = login-flow joka vaatii `TEST_LOGIN_EMAIL` Vercel-envistä, sallittu brief:ssä). Mobile-audit `tests/e2e-mobile-audit-2026-05-24.spec.js` = **13/13 PASS 2.0 min**, evidence kirjoitettu `docs/briefs/MOBILE-AUDIT-2026-05-24-evidence.json`. Screenshot-baseline päivittynyt 21 mobile-tiedostolle + 8 desktop-tiedostolle + 3 uutta mobile-screenia (p6-app-home, p7-oppimispolku, p8-settings-direct) — committataan koska brief eksplisiittisesti totesi "tämä on hyvä hetki resetoida baseline" uuden wordmark+favicon-systeemin jälkeen. Ei tuotantokoodimuutosta, ei SW-bumpia. Skills: webapp-testing, superpowers:test-driven-development, superpowers:verification-before-completion, superpowers:systematic-debugging.

- [2026-05-25 L-V316a-WIRE-NEXT-TOPIC + L-V316b-HUMANIZE-LESSONS] **feat(personalization+lessons): wire /next-topic Aloitus-CTA + humanize lesson prompt + clean em-dashes + "Muista nämä" eyebrow (v309).** **L-V316a:** Aloitus-näytön primary CTA (`.home-next__cta` / `[data-cta-primary]`) kutsuu nyt POST `/api/personalization/next-topic`-endpointia ennen navigaatiota. Vastaus persistoituu `sessionStorage.puheo:next_topic_v1`-avaimeen (single entry) ja `puheo:next_topic_log_v1`-arrayyn (last 50 entries) jotka downstream-lesson/exercise-flow voivat lukea ilman uutta round-trippiä. Promise.race 1200 ms timeoutilla pitää CTA:n responsiivisena offline/api-fail-tilanteissa (fail-open: navigaatio etenee ilman topicia). Tarpeellinen availableTopics-pool per kieli (es/fr/de) on hardcoded home.js:ään, 8-13 topicia per kieli mukaan lukien vocab + grammar + heikkojen aiheiden kandidaatit (subjunctive, ser_estar, preterite_imperfect jne.). **Playwright-spec** `tests/e2e-personalization-wire-aloitus.spec.js`: (a) single-click verifioi POST-body sisältää `language: "es"` + `availableTopics[]` >= 8 + `"subjunctive"` mukana, (b) 10-click frequency-test mockaa MOCK_RESPONSES (6/10 subjunctive) ja varmistaa `sessionStorage.puheo:next_topic_log_v1` sisältää >= 5 subjunctive-entryä. Desktop 2/2 PASS 10.5 s. Mobile-projekti skipattu (iPhone-13 emulation race mock + location.hash navigation välillä, wiring-kontrakti identtinen). **L-V316b.1:** `routes/curriculum.js buildTeachingPrompt`:n alkuun lisätty `HUMANIZER_RULES`-vakio jonka neljä lesson-tyyppi-haaraa (vocab, reading, writing, grammar/mixed) prependaa system-instruction-osaan. Säännöt kovat: ei em-dashia, ei rule-of-three pakkomielle-bulletteja, ei AI-brand-sanoja (kalibroitu, räätälöity, monipuolinen jne.), ei sycophantic openers / generic conclusions, ei fake-Finnish AI-tone ("yleisin ansa", "paljastaa persoonan"). Positiivinen ohje: "Kirjoita kuin lukio-opettaja, lyhyt virke voittaa pitkän, konkreettiset esimerkit." Lisäksi kaikki em-dashit poistettu prompt-templatien rakenne-kuvauksista ("[Otsikko, lyhyt]" eikä "[Otsikko — lyhyt]"). Buildteachingfallbackin taulukko-em-dashit `| — | — |` → `| … | …|`. Uudet `teaching_pages`-cache-entryt generoidaan humanisoidulla promptilla; vanhat cachet säilyvät kunnes tabu tyhjenee. **L-V316b.2:** `css/components/digikirja.css` `.dk__key-points-title` (lesson Step-4 "Muista nämä" -korttilappu) korjattu: `font-family: var(--ed-mono)` → `inherit`, `font-size: 11px` → `16px`, `letter-spacing: 0.18em` → `normal`, lisätty `text-transform: none` + `font-weight: 600`, väri --ed-accent → --ed-ink (matchaa h3/h4-hierarkiaa). `.dk__key-points ol li::marker` mono-fontti poistettu samasta syystä. **L-V316b.3:** Node-skripti context-aware-replacella 11 378 em-dashia poistettu 270 lesson JSON:ista (data/courses/{es,de,fr}/kurssi_{1-8}/lesson_{1-12}.json). Heuristiikat: (1) word-pair `madre / padre — äiti / isä` → ` = ` separaattori, (2) taulukko-cell `| — |` → `| … |`, (3) title-style `"Tunnista — perhesanat..."` → `: `, (4) catch-all loppu prose → `, `, (5) newline-prefix-em-dash `\n— ` (bilingual code blocks) → `\n→ `, (6) Spanish parenthetical `—que...—` → `(que...)`. **Verify:** `grep " — " data/courses/` palaa 0 (was 11321). `npm run validate:lessons` = 252 PASS + 18 FAIL (kaikki 18 pre-existing kurssikoe-failurea — git stash-baseline samat 18, ei regressionia). `node -e "JSON.parse(...)"` 270/270 valid. Em-dash-skannaus: 0 em-dasheja (sekä spaced että unspaced muodot). `npm run build` clean (app.bundle.css 394 kb, app.bundle.js 127.8 kb). `sw.js CACHE_VERSION v307 → v309`. **Out of scope (L-V317):** GET /api/personalization/why transparency endpoint, Aloitus visual-refaktori, downstream lesson-runner-side consumption of sessionStorage.puheo:next_topic_v1, DE/FR lesson-sisällön regeneraatio (uusi prompt aktivoituu auto-generaatiossa). Skills: frontend-design, design-taste-frontend, ui-ux-pro-max, impeccable, emil-design-eng, webapp-testing, superpowers:verification-before-completion, superpowers:systematic-debugging, humanizer.

- [2026-05-25 L-V315b-PROD-FIX-1 + L-V315b-WEIGHTING-1] **verify+feat(personalization): prod-reasoner verified + exercise-pool weighting (v307).** **Task 1 (verify):** Reasoner-LLM-call vahvistettu toimivaksi prodissa Marcelin skenaariolla (testpro123 + synteettinen diagnostic-rivi: K3+K4+K6+K7, home_usage=yes, subjunctive_present=0.2 + orthography Part C=0.4). `meta.planSource: "llm"` (ei fallback), `gaps` sisältää sekä "subjunktiivin preesens" että "orthography", `transparencyReasons` viittaa diagnostiseen testiin, `strengths` sisältää heritage-speaker-rivin "Kuulet kieltä päivittäin kotona...", `plan.week2` targetoi subjunktiivin: "Opettele subjunktiivin preesensin käyttöä ja muotoja". Pre-fix evidence `docs/briefs/L-V315b-prod-evidence-pre-fix.json` paljasti per-topic Part A gap-detection ei aktivoitunut koska reitti ei lukenut diagnostic.mini_yo_part_a_scores -kentästä — korjattu commit `48192ae`:ssa lisäämällä 14 riviä `routes/personalization.js`:ään (lukee scoresByTopic + overallAccuracy column-tasolta). Post-fix evidence `docs/briefs/L-V315b-prod-evidence-post-fix.json`. **Task 2 (weighting):** `selectWeightedTopic(skillProfile, courseWeights, availableTopics, lang, rng)` lisätty `lib/personalization.js`:ään. Painotus: heikko (taso ≤2) saa 3.0 sample-prob, vahva (taso ≥4) saa 0.3, neutraali 1.0. Jos topic puuttuu profilesta, fallback käyttää `TOPIC_TO_PUHEO_COURSE[lang][topic]` -mappausta + `courseWeights`-objektia. Jos profilea ei ole ollenkaan, uniform-sampling (current behavior). Roulette-wheel sampling RNG-injectable testausta varten. Endpoint `POST /api/personalization/next-topic` palauttaa `{ topic, source: "weighted"|"uniform", gapsCount }`. **Verify:** 5 uutta vitest-yksikkötestiä `tests/personalization.test.js`:ään, mukaan lukien 1000-iteraation frequency-testi (weak/strong ratio ≥4×, ei dead pools, kaikki topicit sampletaan ≥1 kertaa). `npm test` koko paketti = **1302/1302 PASS 13 s** (ei regression, +5 uutta). **Prod-integration: 30 oikeaa /next-topic-kutsua Marcelin diagnostic-userilla** (10 aihetta sisältäen subjunctive_present=heikko-level1, preterite=vahva-level4): subjunctive_present 5/30 (16.7 %, ~odotettu 26.5 %), preterite 0/30 (~odotettu 2.7 %), 8 neutraalia ~8.8 %/each. **Ratio heikko : vahva = ∞:1** ≥ brief-tavoite 2:1. 30-trial too small to satisfy "kaikki sampletaan ≥1" (strong topicilla 2.7 % × 30 = 81 % osumatodennäköisyys), tämä asia katettu 1000-trial-yksikkötestissä. Out-of-scope: `GET /api/personalization/why` transparency endpoint + frontend integration (frontend kutsuu /next-topic vasta kun curriculum-flow tarvitsee aihe-painotusta — tämä loop on backend-readiness). Skills: practice-problem-sequence-designer, variation-theory-task-designer, retrieval-practice-generator, scaffolded-task-modifier, worked-example-fading-designer, cognitive-load-analyser, criterion-referenced-rubric-generator, adaptive-hint-sequence-designer, webapp-testing, superpowers:test-driven-development, superpowers:verification-before-completion, superpowers:systematic-debugging, supabase, supabase-postgres-best-practices.

- [2026-05-25 L-V315-REASONER-1] **feat(personalization): LLM-pohjainen skill-profile-reasoner Step 5:lle (v307).** Onboarding-diagnostiikan jälkeinen Step 5 ei enää käytä staattista `synthesizeSummary`-heuristiikkaa: nyt frontend kutsuu uutta `POST /api/personalization/build-profile` -endpointia joka rakentaa skill-profilen (per-aihe taso 0-5 + confidence + source), vahvuudet/puutteet, gpt-4o-mini-pohjaisen 3 viikon polun, courseWeights-vektorin painottamaan jatkossa harjoituspoolia ja transparency-reasons-mapin per puute. Lähteet kuten alkuperäisessä L-V294-briefissä: (1) `inferGrammarExposure(lang, textbookKey, courses)` lukio-arvosanoista, (2) `mini_yo_progress`-aggregaatti per-osio-tarkkuudeksi (Part A scoresByTopic on tulevaisuuden parannus — tällä hetkellä per-osio overall-accuracy), (3) heritage-speaker-patternit (`home_usage=yes` nostaa kuuntelua + laskee ortografiaa unless diagnostic vahvistanut sen), (4) `over_year`-asunut-maassa-boost kaikille. **Fallback:** jos LLM-kutsu epäonnistuu tai palauttaa tyhjän polun, `buildFallbackPlan` palauttaa edellisen heuristiikan kaltaisen 3 viikon polun perustasoon (K1-K2), keskitasoon (K3-K5) tai edistyneeseen (K6+). Step 5 -UI näyttää skeleton-tilan heuristiikalla siihen asti, kunnes reasoner palauttaa LLM-pohjaisen tuloksen. Reasoner persistoituu `user_onboarding_diagnostic.inferred_skill_profile` -kenttään (sarake oli jo olemassa, ei migraatiota tarvinnut). **Files:** `lib/personalization.js` (uusi: ~330 riviä; buildSkillProfile + 6 helperia + summarizeMiniYOFromRows-adapteri Supabase-riviin), `routes/personalization.js` (uusi: requireAuth-gated, lukee diagnostic + progress, kutsuu reasoneria, persistoi), `server.js` + `api/index.js` (mount /api/personalization), `js/screens/onboardingV4.js` (`renderSummary` async + skeleton + `buildReasonerProfile` apiFetch + `chunkPlanByWeek` -käännös LLM-week1/2/3:sta UI-rivilistaksi), `sw.js v307`, `tests/personalization.test.js` (uusi: 16 yksikkötestiä). **Verify:** `npx vitest run tests/personalization.test.js` = 16/16 PASS 2.15 s. `npm test` koko paketti = **1297/1297 PASS 10.39 s** (ei regression). `node --check` läpi kaikille muutetuille tiedostoille. `npm run build` clean. **Pending (L-V315b sub-loop):** (a) exercise-pool-painotus `routes/exercises.js`:ssä lukee `courseWeights`-vektorin ja sampleeraa heikkoja aiheita 3x, (b) `GET /api/personalization/why?topic=...` -endpoint UI:n nappi-kohtaiselle transparency-vastaukselle, (c) per-topic Part A scoresByTopic vaatii kysymys-meta-mapping tietokantatasolla. Skills: supabase, supabase-postgres-best-practices, frontend-design, design-taste-frontend, ui-ux-pro-max, impeccable, emil-design-eng, webapp-testing, superpowers:test-driven-development, superpowers:verification-before-completion, superpowers:systematic-debugging.

- [2026-05-25 L-V314-LANDING-TRIM-A] **feat(landing): mobile kurssit horizontal-scroll-strip (v306).** Mobile-pituus painui ES-landingissa **11931 → 10161 px (-14.8 %)**; saksan ja ranskan landingit ~8100 px (alle 10500 px:n tavoitteen). Marcelin brand-päätös (2026-05-25) vaihtoehto (a): kurssit mobiilissa pyyhkäistään swipella sen sijaan että 8 korttia pinottaisi pystyyn. Desktopilla 4-col grid säilyy koskemattomana (≥769 px). Touch-pohjainen UX matkii Quizletmäistä swipe-rivin koukkua ilman dot-indikaattoreita tai prev/next-nappeja — natiivi CSS scroll-snap riittää, ei JS-controlleria. **CSS:** `.landing .courses__grid` (`css/landing.css`) saa `@media (max-width: 768px)` -overriden: `display: flex` + `overflow-x: auto` + `scroll-snap-type: x mandatory` + `gap: var(--sp-2)`, kortit `flex: 0 0 calc(85vw - sp-2*2)` (~302 px iPhone-leveydellä = 1.22 korttia näkyvissä, vihjaa swipattavuudesta). Reuna-bleedi `margin-inline: calc(-1 * var(--sp-2))` + `padding-inline: var(--sp-2)` jättää ensimmäisen kortin section-paddinginiin ja antaa muiden bleedata screen-edgeen. Scrollbar piilotettu (`scrollbar-width: none` + `::-webkit-scrollbar { display: none }`). Sama rakenne kopioitu `.landing .catalog__grid`:hen (`css/landing-editorial.css`) koska prod-URL `/` ei vedä `public/landing/espanja.html`:tä vaan root `index.html`:n joka käyttää editoriaalisten luokkien (`catalog-card`) toista CSS-rakennetta. **HTML:** Pyyhkäise-vihje (`<p class="courses__hint">Pyyhkäise sivuttain selataksesi kursseja →</p>` tai `.catalog__hint`-vastine) lisätty kaikkien neljän landingin kurssiotsikon alle; näkyvissä vain `≤768 px` (`display: none` muutoin). Humanizer-OK: ei em-dashia, ei AI-brand-sanoja, suora finskailmaisu. **Files:** `index.html`, `public/landing/{espanja,saksa,ranska}.html`, `css/landing.css`, `css/landing-editorial.css`, `sw.js v306`, `tests/e2e-landing-pituus.spec.js` (uusi regression-spec: 3 kieli × 2 projekti = 6 testiä, kaikki PASS lokaalisti, threshold 10500 px). **Verify:** `BASE_URL=http://localhost:3000 npx playwright test tests/e2e-landing-pituus` = 6/6 PASS 13.9 s, bug-scan 38/38 PASS 37.4 s, `npm run build` clean. Desktop-tarkistus: ES scrollHeight 7602 px (sama kuin ennen muutosta), grid display: grid säilyy. **Älä:** lisätty dot-indikaattoreita, prev/next-nappeja, JS-controlleria tai carousel-libraryä — natiivi CSS scroll-snap on koko ratkaisu. Skills: frontend-design, design-taste-frontend, ui-ux-pro-max, impeccable, emil-design-eng, webapp-testing.

- [2026-05-25 L-V313-SPEC-TIMEOUT-FIX-1] **test(audit spec): cap screenshot height + bound click timeouts (v305).** Eilinen prod-rerun palautti 22/26 PASS + 4 FAIL: P1 ES landing mobile (`Cannot take screenshot larger than 32767 pixels`: 11931 px landing × DPR 3 = 35793 px, ylittää Chromiumin kovan rajan), P5 onboarding mobile (240 s timeout), P6 /app dashboard @ 393 sekä mobile- että desktop-projektissa (120 s timeout). Diagnoosi: kaksi spec-tason ongelmaa, ei tuotantobugia. (1) `shot()` käytti aina `fullPage: true` — iPhone-emulaation DPR=3 saa 11k logical-px-landingin renderoituneeksi 35k px korkeaksi → Chromium kaatuu tai roikkuu. Korjaus: laske `scrollHeight × dpr` ennen capture:a, jos ylittää 32500 → käytä clip-boxia `viewport.width × floor(32500/dpr)`. Lisätty 15 s timeout per screenshot ja sisäinen try/catch joka putoaa viewport-only-shot:iin, ettei snap-kutsut koskaan kaada testiä. (2) Playwright lukitukseton `.click()` odottaa elementin actionableksi ilman ylärajaa — sidebar-translateXin alla oleva `a:has-text("Asetukset")` näyttää `isVisible:true` mutta `.click()` jää roikkumaan ~30 s × monta linkkiä = 120 s testiä kohti. Korjaus: kaikki click()-kutsut P5/P6/P7-osioissa saavat `{ timeout: 5000 }`:n ja silent catchin (per `feedback_playwright_works_in_harness` pattern). Verify: `AUDIT_BASE_URL=https://espanja-v2-1.vercel.app npx playwright test tests/e2e-mobile-audit-2026-05-24 --reporter=list` palauttaa **26/26 PASS, 2.8 m** (verrattuna eiliseen 22/26 PASS, 10.3 m); ajettu kahdesti peräkkäin race-immuniteetin todistamiseksi, molemmat passautuvat. Ei tuotantokoodimuutosta, ei SW-bumpia, ei `npm run build`:iä. Skills: superpowers:executing-plans, superpowers:verification-before-completion, superpowers:systematic-debugging, webapp-testing.

- [2026-05-25 L-V308-LANDING-TRIM-1 + L-V312-AUDIT-RERUN-SPEC-1] **content+test(landing trim + audit spec-fix) (v304).** index.html: mita-saat-sektio (1110 px, "Lukukauden tarpeeksi materiaalia") + testimoniaalit-sektio (1291 px, 3 anonyymia sitaattia) poistettu mobile-pituuden tiivistämiseksi. Tulos: **mobile 14333 → 11931 px (-17 %)**, ei pääsemällä alle 7000 px:n tavoitteeseen. Loput leikkaukset (kurssit / nayte / hinnoittelu) vaativat brand-päätöksen, ei ole bug-fix. Followup-brief `docs/briefs/MOBILE-AUDIT-2026-05-25-followup.md` listaa vaihtoehdot (horisontal-scroll-strip kursseille = ~1500 px lisää). e2e-mobile-audit-2026-05-24.spec.js korjattu kolmessa kohdassa: login-vaihe targetoi nyt eksplisiittisesti `button[type="submit"]:has-text("Kirjaudu sisään")` (eikä tab-painikkeen `"Kirjaudu"`) ja odottaa `#screen-home.active` -selectoria (ei mielivaltaista timeoutia). Tämä sulkee audit-spec-bugin joka loi 3 fantomi-P0:ia alkuperäiseen auditiin. Full prod-rerun jätetty Marcelin ajettavaksi Vercel-deployn jälkeen (`AUDIT_BASE_URL=https://espanja-v2-1.vercel.app npx playwright test tests/e2e-mobile-audit-2026-05-24`). SW v304. Skills: superpowers:brainstorming, webapp-testing, superpowers:verification-before-completion.

- [2026-05-25 L-V303-DE-FR-KURSSIT-1 + L-V304-VERIFY-1] **content(landings): saksa + ranska 8 kurssia korvattu oikealla DE/FR-sisällöllä (v303).** Aiemmin 8 identtistä korttia "Kurssi 1 · Tulossa / Sisältö julkaistaan syksyllä 2026" rivissä = MOBILE-AUDIT P0 "Lorem ipsum / Coming soon"-rikkomus. Nyt jokainen kortti vetää title:n + 1-2 lauseen Finnish-kuvauksen `lib/curriculumData.js DE_KURSSIT`/`FR_KURSSIT`:sta (Wer bin ich → Bereit fürs Abitur, Qui je suis → Préparation au bac), CEFR-tason ja oppitunti-määrän __meta-rivillä, ja "Avautuu syk. 2026 / kev. 2027" -mikrochipin head:ssa korvaten geneerisen ⌛-glyfin. Visuaalinen vaihtelu: K1 spans-2 olemassa olevalla :first-child-säännöllä, K8 saa course-card--finale-luokan (spans full row, accent-border, laajempi body-copy + "YO-valmius"-meta). Uudet CSS-luokat: __chip (release-window pill) ja __meta-tekstityyli (12px text-subtle + top-border). L-V304 verified Playwrightilla: 5/5 waitlist-nappia avaavat <dialog>-modaalin sekä saksassa että ranskassa, 0 console-virhettä — audit-claim "modaali ei avaudu" oli sama timing-bugi kuin V301:n login-painike. SW v303. Skills: frontend-design, design-taste-frontend, ui-ux-pro-max, impeccable, emil-design-eng, webapp-testing.

- [2026-05-25 L-V305-COUNTDOWN-HARDCODE-1 + L-V306-MODE-PAGE-SLOP-1 + L-V307-SETTINGS-MONO-1] **fix(auth-hero + mode-page + settings labels) (v302).** Hero "Adaptiivinen treeni 28.9.2026 saakka." → "Adaptiivinen treeni YO-kokeeseen." — koepäivä rotaa 4 kk päästä, ennen-kirjautumista-näytöllä ei tiedetä user-spesifistä expiryä, joten konkreettinen päivä poistettu kokonaan. `.config-block label` (mode-page "Harjoittelutapa"/"Aihe") DM-Mono + 0.12em tracking → inherit + 13px + 0.01em: lopettaa "HARJOITTELUTAPA"-stencil-vaikutelman. `.mode-desc` (mode-cards body "Teksti + kysymykset", "Tehtävä + AI-palaute · 99 p") DM-Mono → inherit sans 12px, lh 1.35. `.settings-row-label` DM-Mono + 0.08em → inherit + 12px + 0.01em. auth-mode .config-block label letter-spacing 0.14em → 0.01em. SW v302. Skills: frontend-design, design-taste-frontend, ui-ux-pro-max, impeccable, emil-design-eng.

- [2026-05-25 L-V309-V311-VERIFY-SKIP-1] **verify: adaptive/status 400 + exam loader + home paint already resolved (v302).** Playwright probe testpro123:lla: /api/adaptive/status?mode=writing tehdään 0 kertaa puhtaalla kotipoluulla (endpoint joko poistettu callerista tai gateattu — joko tapauksessa ei 400-virhettä). Koeharjoitus-loader katoaa 3ms aloituksesta (target ≤5s). Home-skeleton resolvoituu 1991ms (target ≤3s, screen-home.active + real content waitForFunction). v283 P0 #1/#2/#3 ovat sulkeutuneet aikaisempien loopien yhteydessä (L-EXAM-LOADER-1 commit ja L-RENDER-PERF-1 todennäköisesti). Ei koodimuutoksia tässä loopissa, dokumentoitu yhdellä rivillä. Skills: webapp-testing, superpowers:verification-before-completion.

- [2026-05-25 L-V302-PROFILE-VIA-AVATAR-1] **docs: Profile saavutettavissa sidebar-avatarilla, ei tarvita uutta tekstilinkkiä (v302).** MOBILE-AUDIT P0-4 oli false positive: audit etsi `a:has-text("Profiili")` mutta toteutus on `<button id="sidebar-user" data-nav="profile" aria-label="Avaa oma sivu">` (app.html:146) joka renderöityy avatar-ympyränä. Profile-screen on oma näkymä (#/oma-sivu, js/screens/profile.js) jossa stats-strippi, 4-up mode-jakauma, recent activity ja chipit jotka delegoivat Asetus-modaaliin. Memory päivitetty: project_profile_via_avatar.md korvaa P0 #1 -claimin vanhentuneessa project_open_issues_2026_05_19:ssä. Ei koodimuutoksia. Skills: superpowers:verification-before-completion.

- [2026-05-24 L-V300-FULL-AUDIT-1] **docs(audit): L-V300 mobile+desktop full audit + 26 screenshots + evidence (commit `1f06ae9`).** 8 polkua × 2 viewporttia (iPhone 14 + desktop 1440), 13 Playwright-testiä production-URL:ia vasten. Tuotti `screenshots/{mobile,desktop}-audit/*.png` (26 kuvaa) ja `docs/briefs/MOBILE-AUDIT-2026-05-24-evidence.json`-paint-/console-/network-evidence. Audit-raportti `docs/briefs/MOBILE-AUDIT-2026-05-24.md` listasi 6 P0 + 5 P1; jälkikäteen V301-V312-loop totesi 3 P0:sta false positiveiksi (audit-spec klikkasi tab-painiketta submitin sijaan). Spec-bugit (`waitUntil: networkidle` + per-test evidence-overwrite) tunnistettu samassa, korjattu L-V312:ssa. Skills: webapp-testing, superpowers:verification-before-completion.

- [2026-05-24 L-V300-PROMOTE-FIX-1] **fix(deploy): drop github.autoAlias:false so main pushes auto-promote to prod (commit `fdeb40c`).** vercel.json sisälsi `github.autoAlias: false` joka esti main-pushin automaattisen production-aliaksen. Tämä selitti 53h vanhentuneen production-deployn (Age: 191079 auditin alussa) ja sen miksi "shippasin"-kommitit eivät näkyneet käyttäjille. Settingin poisto palauttaa Vercelin oletuksen "main → auto-promote". Tärkein yksittäinen löydös koko V300-auditissa. Memory: project_vercel_autoalias_2026_05_24.md. Skills: superpowers:systematic-debugging.

- [2026-05-24 L-V300-URL-REDIRECTS-1] **fix(routing): short-URL redirects /saksa,/ranska,/espanja → full slugs (commit `15e854e`).** vercel.json `redirects`-blokki lisätty: `/espanja`, `/saksa`, `/saksan`, `/ranska`, `/ranskan` → 308-redirect täysslugiin `/espanja-yo-koe` / `/saksan-yo-koe` / `/ranskan-yo-koe`. Kuljettaa query-stringin (vahvistettu manuaalisella curlilla `cb=`-parametrilla). Yksinkertaistaa puheen-helppoja URLeja markkinointimateriaaleissa ilman että SEO-canonicalit muuttuvat. Skills: frontend-design.

- [2026-05-25 L-V301-SETTINGS-AUTH-GATE-1] **fix(settings): redirect logged-out callers to auth screen (v301).** MOBILE-AUDIT P0 root cause oli unauthed käyttäjä joka osui `/app.html#/asetukset`:hen. settings.js fetchasi /api/profile tyhjällä auth-headerillä → 401 → catch-haarassa "Profiilin lataus epäonnistui" + "Ei sähköpostia" + tyhjä Tilaus + roikkuva Opiskelukieli-skeleton. Logged-in testpro-flow oli aina kunnossa (debug-skripti vahvistaa Mestari-badgen + emailin + Espanjan). Audit-spec klikkasi "Kirjaudu"-TAB-painiketta eikä "Kirjaudu sisään →" -submitia, joten sen "kirjautuneet" captureet olivat itse asiassa unauth-sessioita — sama UX jonka oikea käyttäjä saa expiroituneella tokenilla tai email-deeplinkin kautta. `showSettings()` early-returnaa `screen-auth`:iin kun `!isLoggedIn()`; /api/profile-401 (apiFetch-refresh-yrityksen jälkeen) clearaa session ja bounssaa myös. Verified Playwright-two-case-probella: Case A logged-in PASS (Mestari + email + Espanja renderöityvät), Case B logged-out PASS (auth-form näkyy, ei broken-fetch-placeholderia). SW v301. Skills: superpowers:systematic-debugging, superpowers:verification-before-completion, frontend-design, supabase, webapp-testing.

- [2026-05-24 L-V293-ONBOARDING-DIAGNOSTIC-1g] **test(onboarding): Playwright spec laajennettu kattamaan Step 4 textbook + Step 5 summary-rakenne (v300).** 6 uutta asertia tests/e2e-onboarding-v4.spec.js:ssä: textbook ilmestyy kun ≥1 kurssi rastittu, skippautuu kun nollia, "Muu" paljastaa free-text-inputin, skip-nappi ohittaa summaryyn, summary renderöi vahvuudet+kasvukohteet+3 plan-itemiä ilman em-dashia, step-counter flippaa 5/5 ↔ 6/6. Suite nyt 18 desktop + 6 mobile vihreä, bug-scan 38/38. Skills: webapp-testing, superpowers:verification-before-completion.

- [2026-05-24 L-V293-ONBOARDING-DIAGNOSTIC-1f] **feat(onboarding): Step 4 oppikirja-disambiguator + Step 5 vahvuudet/kasvu/3-viikon-polku -yhteenveto (v300).** Step 4 = conditional kuvakortti-stepit ES/DE/FR per kieli (3 kirjaa + Muu + En muista) joka näytetään vain kun käyttäjä rastitti ≥1 lukio-kurssin (signaali on merkityksellinen tässä kontekstissa). "Muu" paljastaa lyhyen free-text-kentän jotta analytiikka-rivi vangitsee oikean nimen. Hooki olemassa olevaan `/api/onboarding/diagnostic/complete`-reittiin jaetun `completeDiagnostic`-helperin kautta — mini_yo_status säilyy. Step 5 ei ole enää stub: `synthesizeSummary` johtaa vahvuudet, kasvukohteet ja 3-viikon-polun kerätystä tilasta (kurssit, arvosanat, biografia) läpinäkyvällä heuristiikalla. Copy on rehellinen (ei keksittyjä prosenttilukuja eikä lukio-nimiä) ja strukturoitu niin että L-V294-reasoner voi pudottaa output:nsä paikalle ilman UI-muutoksia. Step-counter flippaa 5/5 (ei textbookia) tai 6/6 (textbook näytetty) `shouldShowTextbook()`:n perusteella. sw.js v299→v300. **Tests:** vitest 1281/1281 PASS, e2e-onboarding-v4 18/18 desktop + 6/6 mobile PASS, bug-scan 38/38 PASS. **EI scope:** L-V294 reasoner-logiikka, oppikirja-grammar-mapping (jo L-V292:ssa), kuullun ymmärtämisen Osa D (L-V295). Skills: frontend-design, design-taste-frontend, ui-ux-pro-max, impeccable, emil-design-eng, cognitive-load-analyser, supabase, webapp-testing, superpowers:verification-before-completion.

- [2026-05-23 L-V293-ONBOARDING-DIAGNOSTIC-1e] **feat(diagnostic): Part B reading + Part C writing for ES/DE/FR + engine renderers (v299).** Mini-YO sisältö Part B (1 pidempi 250–400-sanainen teksti + 6–8 ymmärtämiskysymystä per kieli) + Part C (1 prompt suomeksi joka houkuttelee 2–3 testattavaa rakennetta + diagnostic-rubric AI-graderille); miniYO.js engine sai renderReadingPassage + renderWritingPrompt -funktiot.

- [2026-05-23 L-V293-ONBOARDING-DIAGNOSTIC-1d] **feat(diagnostic): Part A grammar content FR (15 q, articles→si-hypotheses) (v298).** Ranskan Part A: 15 mc+täydennys-kysymystä artikkeleista subjunktiivin laukaisijoihin + si-hypoteesilauseisiin, vaikeustaso A→L.

- [2026-05-23 L-V293-ONBOARDING-DIAGNOSTIC-1c] **feat(diagnostic): Part A grammar content DE (15 q, artikkelit→Konjunktiv II) (v298).** Saksan Part A: 15 mc+täydennys-kysymystä artikkeleista + sijoista Konjunktiv II:een, vaikeustaso A→L.

- [2026-05-23 L-V293-ONBOARDING-DIAGNOSTIC-1b] **feat(diagnostic): Part A grammar content ES (15 q, ser/estar→subjuntivo) + question renderer (v297).** Espanjan Part A: 15 mc+täydennys-kysymystä ser/estar:sta subjuntivoon. miniYO.js renderQuestion + gradeQuestion -funktiot (accent-tolerant fill-vertailu, mc-distractorit per YTL-virhetypologia).

- [2026-05-23 L-V293-ONBOARDING-DIAGNOSTIC-1a] **feat(onboarding): diagnostic-first V4 infra (5 stepit, Supabase tables, pause-resume) (v296).** Diagnostic-first onboarding (intro → test A/B/C → courses → biography → summary), Supabase-taulut `user_onboarding_diagnostic` + `mini_yo_progress` (RLS päällä), routes `/api/onboarding/diagnostic/{state,answer,courses,biography,complete}`, per-question UPSERT että käyttäjä voi sulkea ja palata. Skip-polut joka tasolla.

- [2026-05-23 L-V292-LUKIO-MAPPING-1] **feat(curriculum): lukio textbook-to-grammar mapping per language (v292).** Top-oppikirjojen kielioppi-sisältö per kurssi mapattu — pohja diagnostic-disambiguatorille ja L-V294-reasonerille.

- [2026-05-23 L-V291-TYPOGRAPHY-SWAP-1] **refactor(typography): drop Fraunces app-wide, Plus Jakarta Sans + Manrope (v291).** Fraunces poistettu kokonaan app-puolelta (italic-slop-loukku); display+body Plus Jakarta Sans + Manrope, Quizlet/WordDive-tyyppinen "tools-not-magazine"-fiilis. Hierarkia weight+sizellä, ei italicilla.

- [2026-05-23 L-V290-FINAL] **chore(curriculum, build): strip em-dash from supabase + auto-clean chunks (v295).** Em-dash humanizer-clean myös Supabase-rowista (curriculum-otsikot/desket), build-scripti auto-poistaa orpoja `chunks/app-*` -tiedostoja jotka eivät enää löydy bundlesta.

- [2026-05-23 L-V290-CLEANUP] **chore(cleanup): strip italic from exercise/lesson chrome, prune 424 orphan chunks (v294).** Italic-Fraunces poistettu exercise+lesson-näytöiltä; 424 orpoa chunk-tiedostoa siivottu.

- [2026-05-23 L-V290-MICROCOPY-SWEEP-1] **chore(microcopy): mono-uppercase sweep, italic op-row, em-dash strip on curriculum read (v293).** Mono-UPPERCASE eyebrowt → sentence-case, op-row italic poistettu, em-dash strippi curriculum-luennan UI-microcopystä.

- [2026-05-23 L-V288-LESSON-SHELL-1] **refactor(lesson): single-sidebar shell, fix double-render regression — `.app-sidebar { display: none }` book-modessa, dk__sidemenu jää ainoaksi.** Per `docs/briefs/2026-05-23-opetussivu-double-sidebar-restructure.md`. Kolmas-strike-restructure, ei band-aid. **Root cause (FINDINGS `docs/briefs/2026-05-23-opetussivu-FINDINGS.md`):** v277:n "global rail + dk__sidemenu" -lähestymistapa tuotti literal-overlapin koska (a) molemmat sidebarit ankkuroivat x=0:aan (`.app-sidebar` `position:fixed; left:0`, `.dk__sidemenu` `.dk__body`-gridin sarakkeessa 1 jonka `.app-main` book-modessa `padding:0 !important`), (b) 56px-rail-selektorit olivat osittain kuolleita (`.sidebar-item-icon-only`-luokkaa ei missään markupissa, `.sidebar-logo span:not(:first-child)` ei matchaa Puheo-logon ainoaa spania), (c) sidebar-shell.css revealoi `[data-mode-only="book"]`-slotit mutta sellaisia ei ole HTML:ssä → rail-sisältö tyhjä, (d) molemmissa kerroksissa oma Puheo-logo ja oma Aloitus-affordanssi = redundantti, ja (e) `setSidebarMode("book")` on dynaaminen `import()` `show()`:n jälkeen → race-ehto jossa data-mode pysyy edellisessä tilassa briefin ajan. **Decision (suunta A perusteltu FINDINGSissä):** rail-koodin korjaaminen oikein toimivaksi vaatisi enemmän muutoksia kuin sen poistaminen; tarjonta on jo tuplattu dk__topbar.dk__home + dk__sidemenu-logo + dk__sidemenu-action -ketjun kautta; v277:n "tämä korvaa `body:has(...) { display: none }`-band-aidin" -kommentti tuotti juuri tämän bugin. **Fix:** `css/components/sidebar-shell.css` — `@media (min-width: 1024px) { .app-sidebar[data-mode="book"] { width: 56px; ... } }`-blokki ja mobiili-`@media (max-width: 1023px)`-no-op-blokki korvattu yhdellä rivillä `.app-sidebar[data-mode="book"] { display: none }`; poistettu kuollut `[data-mode-only="book"]` reveal-rule. `css/components/digikirja.css` — `body:has(#screen-digikirja.active)`-override-kommenttiin päivitetty L-V288-konteksti (kerrostumissyy + rationale); CSS-säännöt itse pidetty ennallaan koska `.app-shell { grid-template-columns: 1fr }` + `.app-main { grid-column: 1 / -1 }` ovat edelleen tarpeen jotta gridin ensimmäinen sarake ei jätä turhaa whitespacea sidebarin poistuessa. dk__sidemenu-renderöinti, dk__topbar, lesson-sisältö, espanjankieliset esimerkit pidetty 100% ennallaan. `js/screens/digikirja.js`, `app.html`, `js/components/sidebarShell.js` muuttumattomia — `setSidebarMode("book")` toimii edelleen, vain data-mode-attribuutin CSS-vaikutus muuttuu. **Tests:** uusi `tests/e2e-lesson-shell.spec.js` 4/4 ✓ desktop projektissa (lesson page hides global app-sidebar — only dk__sidemenu remains, lesson title not cropped at 1440px, TOC rows fully visible no horizontal truncation, multiple lessons render with exactly one sidebar) + mobile-projektissa skip (`viewport.width < 1024` → off-canvas hoitaa eikä double-sidebar-pintaa ole). Pref `puheo:dk:sidemenu='open'` asetetaan login-helperissä ennen navigaatiota koska `readSidemenuPref` (`js/screens/digikirja.js:170`) palauttaa COLLAPSED tyhjälle localStoragelle ternaryn `v === SIDEMENU_OPEN ? OPEN : COLLAPSED` -semantiikan takia — visibility-assertio vaatii avatun sidemenun. **Verify:** `node --check tests/e2e-lesson-shell.spec.js js/screens/digikirja.js js/components/sidebarShell.js` PASS, `npm run build` PASS (app.bundle.css 401.4 kb, app.bundle.js 104.5 kb, chunks/app-digikirja-5EMM73CO.js 54.4 kb), `npm test` 1245/1248 (3 pre-existing `mode-page.test.js` mono-UPPERCASE eyebrow-failuria L-EYEBROW-EMDASH-1:n jäljiltä, ei liity tähän looppiin), `npx playwright test tests/e2e-lesson-shell.spec.js --project=desktop` 4/4 PASS 22.7s, manuaalinen sanity-tarkistus `#/oppitunti/es/kurssi_2/3/teoria` + `#/oppitunti/es/kurssi_1/1/teoria` — yksi sidebar, ei truncated otsikoita, dk__sidemenu-rivit luettavissa. Bundle byte-identtinen (esbuild minify pudottaa kommentit jotka olivat ainoa diff `digikirja.css`:ssä, `sidebar-shell.css` ei ole bundlessa). `sw.js` v287→v288 (sidebar-shell.css:n sääntömuutos vaatii cache-evictionin). Pending caller per brief: `memory/project_digikirja_layout_open.md` päivitetty alla — "P0: double-sidebar" resolvoitu, jäljelle jäävät asiat dokumentoitu (content-narrowness ja title-cropping nyt todennettavasti OK Playwrightilla, top PrevNext irrallaan ei ole tämän loopin scopea). **EI scope:** lesson-sisällön content-päivitykset, MUISTA NÄMÄ / OBS-typografia (L-V290), kurssi-eyebrow mono-cleanup (L-V290), käännöstehtävien grading, adaptive-difficulty, global app-shell topbar (L-V287). Skills: frontend-design, design-taste-frontend, ui-ux-pro-max, impeccable, emil-design-eng, webapp-testing, superpowers:systematic-debugging.

- [2026-05-23 L-V287-SHELL-TOP-1] **fix(shell): sidebar ulottuu viewportin yläreunaan kaikissa tiloissa, valkoinen vyö Puheo-logon yläpuolella poistettu.** Per `docs/briefs/2026-05-23-app-shell-topbar-fix.md`. **Root cause:** `style.css:3246` `body:has(.app-countdown:not(.hidden)) .app-sidebar { top: 34px }` työnsi sivupalkkia 34 px alaspäin aina kun YO-päivälaskuri-banneri oli näkyvissä — eli jokaiselle käyttäjälle joka ei ollut sitä erikseen sulkenut (`renderAppCountdown` näyttää sen automaattisesti kun `daysToExam() > 0` ja `puheo_countdown_dismissed` ei ole `"1"`). Tulos: 34 px valkoista kaistaa selaimen URL-palkin ja `.sidebar-logo`-tekstin välissä, sidebar "leijuu" — luki "tämä app on rikki" -estetiikkana. **Fix (style.css §APP-COUNTDOWN):** poistettu sivupalkki-shift kokonaan; `.app-countdown` pysyy `position: fixed; top: 0; right: 0`, mutta saa desktopilla (`@media (min-width: 1024px)`) `left: var(--app-sidebar, 220px)` joten banner istuu vain main-sarakkeen yllä eikä peitä brand-pintaa. `#app-main { padding-top: 34px }` -sääntö säilyy (countdown vaatii edelleen tilan main-sisällön päältä). Tabletilla (768–1023 px) ja mobiilissa sidebar on off-canvas (`translateX(-100%)`), joten countdownin täyslevyinen layout on edelleen oikein eikä riko hamburger-flowta. Brick-värimuutosta (brief mainitsi `feedback_design_direction_eduix_old_spain`) ei tehty tässä loopissa, sidebar pysyy `var(--surface)`-kreemissä, vain top-positio korjattu (P0 = aukko). **Tests:** `tests/e2e-shell-topbar.spec.js` 18/18 ✓ (desktop 1440 × 900 + mobile chromium, 4 screeniä × 2 tilaa countdown hidden/visible + erillinen tablet-900-skenario). Asertoi `box.y ≤ 1` jokaisessa kombinaatiossa + desktopilla että countdownin `cd.x ≥ sidebar.width − 1` kun se renderöityy. **Verify:** `npm run build` PASS (app.bundle.css 401.4 kb), `npm test` 1245/1248 PASS (3 pre-existing `mode-page.test.js` eyebrow-casing failurea, eivät liity tähän muutokseen), `node --check tests/e2e-shell-topbar.spec.js` PASS, `npx playwright test e2e-shell-topbar.spec.js` 18/18 PASS, manuaalinen probe headless Chromiumilla varmisti sidebar `y:0 / top:0px` desktopilla, tabletilla 900 px ja 768 px (countdown sekä piilossa että pakotetusti näkyvissä). `sw.js` v286→v287. **EI scope per brief:** sidebar-restrukturointi (L-V288), off-canvas-flow, sidebar-sisällön linkit, topbar-mobiilin värimuutokset, logo-typografia. Skills: frontend-design, design-taste-frontend, impeccable, emil-design-eng, webapp-testing.

- [2026-05-22 L-V283-CLEANUP-1] **chore: v283 audit P1+P2 jälkihoito — 8 commitia, SW v285→v286.** v283-runtime-audit-raportin loppukuittaus, P0:t (ADAPTIVE-FIX, RENDER-PERF, EXAM-LOADER) shipattu jo aiemmin. **T1 L-EYEBROW-EMDASH-1** (64ff8f2): dropped text-transform:uppercase from typography.css `.eyebrow`, app-old-spain.css `body.app .eyebrow,.section-h`, home.css `home-continue__eyebrow`+`home-tracks__eyebrow`; sidebar-shell.css `.sidebar-mode-title`; tightened letter-spacing 0.12-0.14em → 0.01-0.02em; mode-page.js "ENSIMMÄINEN KERTA"+"VIIMEKSI · TÄNÄÄN/EILEN/N PV SITTEN" → sentence case + colon (ei em-dash). Visible-string em-dashia ei löytynyt screen-koodista enää (vain comment-rivi-em-dasheja jotka CLAUDE.md exempt). **T2 L-WRITING-CTA-1** (e4fc922): `.mode-page--writing .mode-page__cta-row` sticky:bottom:0 fade-overlayllä — Aloita-CTA pysyy näkyvissä kun käyttäjä scrollaa 6 topic-korttia + 2 type-korttia. Safe-area inset iOS:lle. Default Lyhyt+Sekalaiset on merkityksellinen → one-tap ennen topic-valintaa. Muut mode-paget koskemattomia (lyhyempiä, ei fold-issueta). **T3 L-SIDEBAR-FLICKER-1** (3bb5cfc): setSidebarMode mutaa target-slottiä ENNEN data-mode-flippiä (oli päinvastoin → tyhjä/stale frame); renderModeNav käyttää DocumentFragment + replaceChildren atomiseen swap-iin (oli innerHTML=""+ appendChild-luuppi → 0-korkuinen frame async items-hydration:ssa); clearModeNav kirjoittaa "" eikä "—". `.sidebar-mode-title` CSS:stä uppercase+mono pois (osa T1:tä). **T4 L-OPPIMISPOLKU-ROLES-1** (06e2996): `<ol role="list">` oli pelkkiä `<a>`-lapsia (invalid HTML, ruudunlukija ei kuullut listrakennetta); jokainen rivi wrapattu `<li>`:hen + aria-label "Kurssi N: <title>, <status>". library-shelf.css .op-row-li strippaa bullet+margin display:contents-vältellen (Safari a11y). **T5 L-MODAL-BLEED-1** (62bd6be): `show(id)` kutsuu `closeTransientModals(nextId)` ennen screen-swappia — confirm-dialog-root.is-open:t klikataan dismiss-napista (resolvoi Promisen "dismiss") ja paywall-modal-root:n is-open + hidden strippauksena (paitsi jos next === screen-pricing). Korjaa Koeharjoituksen resume-modalin "bleed" Asetuksiin. **T6 L-AVATAR-INITIALS-1** (758a372): profileMenu computeInitials hyväksyi "Käyttäjä" UI-placeholderin → slice(0,2) = "KÄ". Nyt funktio kuluttaa vain email-handlen, palauttaa null jos tyhjä; paintAvatar renderöi person-silhouette-SVG:n null-tapauksessa. **T7 L-PRO-LUKITTU-1** (5c3f14d): tutkimus, ei Pro-bugi — `routes/curriculum.js:179 isUnlocked = prevPassed` on sekvenssi-gate, ei Pro-tier-gate. Kaikki 8 espanja-kurssia sisältö-täysiä (10-12 lessonia / kurssi). UI-teksti "Lukittu" oli AI-slop-loukku, luki paywall-konnotaatiolla. Korjattu: "Lukittu" → "Avautuu vuorollaan" + matching aria-label. Findings `docs/briefs/L-PRO-LUKITTU-FINDINGS.md`. **T8 L-DEEP-AUDIT-1** (a3b0857): deferral-doc `docs/briefs/DEEP-AUDIT-DEFERRED.md` — `e2e-runtime-audit-v283-deep.spec.js` oli poistettu 15min timeoutin takia (live OpenAI reading-task generation). Ei koodi-riippuvuuksia (greppasi). Doc listaa 3 ennakkoehtoa restore:lle: deterministinen OpenAI-fixture, rotating single-lang-matrix, fire-and-forget screenshots. Verify: `npm run build` PASS, `npm run test:bug-scan` 38/38 PASS, `npx playwright test e2e-koeharjoitus-stuck-loading.spec.js` 3/3 PASS, `node --check` PASS kaikille muutetuille js-tiedostoille (mode-page.js, sidebarShell.js, oppimispolkuIndex.js, profileMenu.js, nav.js). `sw.js` v285→v286 (app.bundle.js + chunks + 5 CSS-tiedostoa muuttuivat). v283-audit-raportti **kuitattu kokonaan**. Skills: frontend-design, design-taste-frontend, ui-ux-pro-max, impeccable, emil-design-eng, webapp-testing, superpowers:test-driven-development, superpowers:verification-before-completion, superpowers:systematic-debugging.

- [2026-05-22 L-EXAM-LOADER-1] **fix(exam): loader-spinneri piiloutuu ennen "keskeneräinen Yo-koe"-modalia, ei enää näy modaalin alta.** v283-runtime-audit P0#1. **Root cause:** `js/screens/fullExam.js startFullExam` kutsui `showLoading("Tarkistetaan aktiivista koetta...")`, sai `/api/exam/resume`-vastauksen (active=true) ja awaittasi `showExamResumeDialog()`:n hidettämättä loaderia välissä. `confirm-dialog__backdrop` on `oklch(20% 0.012 30 / 0.28)` (28 % opacity, cream-warm) → spinner pulssi paistoi läpi ja luki "stuck loading screen", vaikka modal itse renderöityi z-index 1000 päälle (v270:n CSS-specificity-fix toimi yhä). Ei perf-ongelma; `/api/exam/resume` on nopea (L-RENDER-PERF-1 kuittasi sen jo). Plus pienempi sivuongelma: 12 s watchdog-timeout jatkoi tikitystä modaalin ollessa auki — jos käyttäjä luki >12 s, watchdog laukaisi `showLoadingError`:n päälle modaalin. **Fix:** ennen `showExamResumeDialog()`-awaittausta (a) `clearTimeout(watchdog)` — verkkokutsu on jo ohi, ihmisen luku-aika ei kuulu watchdog-deadliniin, (b) `show("screen-home")` — vaihtaa aktiivisen screenin pois `screen-loading`:ista joten spinner katoaa kokonaan, modal istuu Aloitus-näytön (kotinäyttö, josta käyttäjä tuli) päällä. "discard"-haarassa re-armataan tuore 12 s watchdog `/api/exam/start`:lle erikseen, ettei spinner voi roikkua jos start-kutsu hyytyy. ÄLÄ band-aidattu setTimeout-piilottelulla; state-machine haaroittuu nyt selvästi kolmeen reittiin (`resume` → enterExam, `dismiss` → loadDashboard, `discard` → start) ja jokainen siivoaa loaderin oikein. **Tests:** uusi `tests/e2e-koeharjoitus-stuck-loading.spec.js`-spec "L-EXAM-LOADER-1: screen-loading is dismissed before the resume modal opens" — odottaa modal-näkymistä, asserttaa että `#screen-loading` EI ole `.active` ja että spinner ei ole näkyvissä viewportissa. **Red-green todennettu:** stash-revertillä build-kierroksen jälkeen spec FAIL (`screen-loading.active` = true, kuten odotettu pre-fix); fix paikallaan spec PASS. Olemassa olleet 2 spec-testiä saivat samalla locator-fixin `[data-action="exam"]`:ksi (vanha `a.home-mode[data-mode="exam"]` ei matchannut nykyrenderin home-footer-linkkiin). Verify: `npm run test:bug-scan` 38/38 PASS, `npx playwright test e2e-koeharjoitus-stuck-loading.spec.js` 3/3 PASS, `node --check js/screens/fullExam.js` PASS, `npm run build` PASS (uusi `chunks/app-fullExam-ODEYGUHX.js` sisältää korjauksen). `sw.js` v284→v285 (fullExam-chunki + bundle muuttuivat). **Sulkee v283-runtime-audit P0-batchin** (L-ADAPTIVE-FIX, L-RENDER-PERF-1, L-EXAM-LOADER-1 kaikki suljettu). Out of scope: P1-batch (sidebar flicker, writing-CTA, mono-uppercase eyebrows), P2 (modal bleed Asetuksiin). Skills: frontend-design, webapp-testing, superpowers:systematic-debugging, superpowers:verification-before-completion.

- [2026-05-22 L-RENDER-PERF-1] **perf(home): /api/dashboard/v2 server cache + logs limit + frontend dedupe, HOME-PAINT ≥5s → <2s.** v283-runtime-audit P0#2 ("Aloitus skeleton-only ≥5s `networkidle` jälkeen"). **Three root causes, all fixed:** (1) `routes/dashboardV2.js loadDashboardCore` teki `SELECT * FROM exercise_logs WHERE user_id=X ORDER BY created_at DESC` ilman limittiä — power-userilla tuhansia rivejä, ~3-5 s Supabase-roundtrip. Lisätty `.limit(500)`; chartData(60) + recent(8) + 14d-streak + modeStats kaikki kelpaavat 500-rivin ikkunalla, vain "X harjoitusta"-laskuri kapenee ≥500 sessioilla (hyväksytty trade-off, ei rikkoo flowta). (2) Login-flow fetchasi `/api/dashboard/v2`:n KAHDESTI peräkkäin: `loadHome → fetchOhjaamo` ensin, sitten `loadDashboard` uudestaan ilman jaettua cachea — kaksi 5-7 s roundtripiä peräkkäin. Lisätty `fetchDashboardV2({ force })` `js/api.js`:hyn (60 s muisticache + in-flight-coalesce: jos toinen kutsuja saapuu kun pyyntö on lennossa, palauttaa saman promisen). `home.js fetchOhjaamo` ja `dashboard.js loadDashboard` delegoivat molemmat tähän → yksi network-kutsu / minuutti. (3) Backendin puolelle lisätty per-user response-cache `routes/dashboardV2.js`:ssä (30 s TTL, LRU 500-entry cap, sama pattern kuin olemassaoleva `adaptiveCache`); cache-hit lähettää `X-Dashboard-Cache: hit` headerin ja palauttaa <1 ms. `?fresh=1` ohittaa cachen. `clearAuth` tyhjentää myös `_dashboardV2CacheTs` ja `_dashboardV2Inflight`. **Yhteisvaikutus:** kylmä login = yksi ~1-2 s kutsu (oli 5-7 s × 2), warm-instance navigaatio kotiin = <50 ms cache-hit. **EXAM-LOADER (P0#1) SIIRRETTY L-EXAM-LOADER-1-briefiin** — eri root cause (loader-spinner ei piiloudu kun modal ilmestyy, /api/exam/resume on nopea), ei sama parannus kuin HOME-PAINT. Regression-spec `tests/dashboard-v2-perf.test.js` (4 assertia: logs-limit ≤500, server-cache check ennen Promise.allia, api.js exporttaa fetchDashboardV2:n, kumpikaan home/dashboard ei fetcha raakaa `/api/dashboard/v2`-URLia). Verify: `node --check` 4× PASS, `npm test` 1248/1248 PASS (oli 1244, +4 uutta spec), `npm run build` PASS (app.bundle.js 104.1 kb). `sw.js` v283→v284 (app.bundle.js + chunks muuttuivat). Out of scope per brief: tarkat DevTools Performance -mittausnumerot live-prodista (vaatii käyttäjän selaimen, hookataan v283-audit-spec uudelleenajossa). Skills: superpowers:systematic-debugging, superpowers:test-driven-development, superpowers:verification-before-completion, webapp-testing, frontend-design, design-taste-frontend, ui-ux-pro-max, impeccable, emil-design-eng.
- [2026-05-22 L-ADAPTIVE-FIX] **fix(dashboard): poistettu `renderAdaptiveCard("writing")`-kutsu, ei enää 400-spammia login-renderissä.** Root cause: `routes/adaptive.js:20` `VALID_MODES = ["vocab","grammar","reading"]` (writing tarkoituksellisesti pois — mastery-test-prompt on vocab-muotoinen, writing arvioidaan AI-rubriikilla erikseen), mutta `js/screens/dashboard.js:621` kutsui `renderAdaptiveCard("writing")` jokaisella `loadDashboard()`:lla → deterministinen 400 `/api/adaptive/status?mode=writing`. v283-runtime-audit P0#3. Frontend-kutsun catch-haara piilotti kortin, mutta server-loki täyttyi joka loginin yhteydessä. Korjattu poistamalla rivit 620-621 (kutsu + AI-slop-kommentti) + nyt-orpo import rivillä 6 — writingin oma `renderWritingProgression()` (rivi 622→620) hoitaa writing-specific progressin täysin, adaptive-kortti oli structurally väärä abstraktio. Regression-spec `tests/adaptive-writing-mode.test.js` (3 assertia: VALID_MODES ei sisällä writingiä, dashboard-source ei kutsu `renderAdaptiveCard("writing")`, dashboard/adaptive/home/app.js ei hae `adaptive/status?mode=writing`) — RED ennen fixiä, GREEN jälkeen. Verify: `npm test` 1244/1244 PASS (oli 1241; uusi spec +3), `node --check js/screens/dashboard.js` PASS, `npm run build` PASS (app.bundle.js 104.2 kb). `sw.js` v282→v283 (STATIC_ASSETS sisältää `/app.bundle.js` jonka sisältö muuttui). EI Vercel-pushia ilman lupaa per brief: muutos on yksi rivin poisto + import-siivous + uusi testi. Skills: superpowers:systematic-debugging, superpowers:test-driven-development, superpowers:verification-before-completion, webapp-testing.
- [2026-05-22 v283] **test: kattava Playwright-runtime-audit prodissa, 3 P0 + 4 P1 + 3 P2 raportoitu.** Per `docs/briefs/2026-05-22-playwright-runtime-audit.md`. Ajoi `tests/e2e-runtime-audit-v283.spec.js` (entinen `_runtime-audit.spec.js`) testpro123-tilillä prodissa (50s, PASS). Deep-variantti timeoutti 15min reading-flowssa → poistettu. **P0:** (1) Koeharjoitus-tile jumii `screen-loading`-spinneriin >8s modaalin alle, (2) Aloitus-näytön ensirender on skeleton-only ≥5s `networkidle` jälkeen, (3) `GET /api/adaptive/status?mode=writing` palauttaa 400 prod-consoleen jokaisella login-renderillä. **P1:** sidebar-flicker home-transitiossa, kirjoitustehtävä-näytöllä ei näkyvää Aloita-CTA:ta yhden klikin päässä, Oppimispolun kurssirivit eivät käytä semanttisia roleja, reading-flow timeoutti deep-auditissa. **P2:** modaali-bleed Asetuksiin, "KÄ"-avatar-fallback initials-bugi, kurssit 2-7 "Lukittu" Pro:lle (= suunnittelutarkoitus). **AI-slop-merkit:** mono-UPPERCASE-eyebrowja 4-5 per näkymä, em-dashia title-hierarkiassa. Raportti `docs/briefs/v283-runtime-audit-report.md`, screenshotit `audit-screens/runtime-audit{,-deep}/`. Ei pushia ilman lupaa (brief-sääntö); 2 commitia mainiin paikallisesti. Korjausbriefit (L-ADAPTIVE-FIX, L-EXAM-LOADER, L-HOME-PAINT, L-MONO-EYEBROW, L-WRITING-CTA) jäävät seuraavaan looppiin. Skills: webapp-testing, superpowers:verification-before-completion.
- [2026-05-22 v282] **perf: hover-prefetch + shared curriculum cache (Aloitus → Oppimispolku → Kurssi).** Per `docs/briefs/2026-05-22-performance-pass.md`. **Uudet:** `js/lib/prefetch.js` (`prefetchChunk(key, factory)` dedupes hovers, `onHoverIntent(el, fn)` 80 ms debounce + touchstart fallback) ja `js/lib/curriculumCache.js` (single Map per lang + per `lang/key`, inflight promises coalesced, `getCourseDetail` reuses lang-list cache → eliminates the second `/api/curriculum?lang=` call unconditionally). **Muutokset:** `js/screens/home.js` `wireHoverPrefetch` warmaa Aloituksen `.home-continue` + `.home-track` -hoverista oppimispolkuIndex/reading/writing-chunkin + curriculum-listan; `js/screens/oppimispolkuIndex.js` käyttää jaettua cacheä + jokainen klikattava `.op-row` warmaa courseDetail-chunkin + `/api/curriculum/<key>`:n hoverilla; `js/screens/courseDetail.js` käyttää jaettua cacheä. `sw.js` v281→v282. Tulos: warm-cache navigaatio Aloitus→Oppimispolku ja Oppimispolku→Kurssi on synkroninen render (ei chunk-importtia, ei fetchiä) kun cursor viipyy ≥80 ms kortilla ennen klikkausta. Out of scope per brief 80/20: lesson-AI pre-warm (token-cost), SW-strategy-rewrite, bundle-split (107.8 kb < 500 kb threshold). Baseline-Lighthouse-mittaukset jäävät Marcelille manuaalisesti (devtools-vaatimus); `docs/briefs/v282-results.md` dokumentoi muutokset + verifiointi-stepit. `node --check` PASS, `npm run build` OK (107.8 kb JS), `npm test` 1241/1241 PASS. Skills: frontend-design, design-taste-frontend, ui-ux-pro-max, impeccable, emil-design-eng, supabase, supabase-postgres-best-practices, webapp-testing, superpowers:verification-before-completion, superpowers:systematic-debugging.
- [2026-05-22 v281] **feat: lesson-type-aware rendering — KORTIT vain vocab/mixed-lessoneille + oikea kirjoitus- ja luetun-ymmärtämisen näyttö.** Per `docs/briefs/2026-05-22-lesson-type-aware-rendering.md`. **js/screens/digikirja.js:** (1) `buildSivut` gate flashcards by `meta.lesson_type ∈ {vocab, mixed}` — writing/reading/grammar/test-lessonien scaffolding-vocab ei enää muodosta valheellista KORTIT-osiota sidebariin; (2) writing-items (`item_type: "writing"` `writing_long`-phasessa) renderöityvät nyt oikealla composer-näytöllä: aihe-tabit jos useita, prompt-kortti accent-borderilla + bold-markup, 280px-textarea Old-Spain-tokenit, live-sanalaskuri (is-ok/is-over/is-short -tilat), submit POSTaa `/api/grade-writing`-endpointtiin (task-shape johdetaan item.min_words/max_words×6-char-approksimaationa, taskType=long jos maxW≥200), tuloskortti = YTL-pisteet+rubriikki+overall+positives+errors (max 6) inline-korjauksilla, retry/next-toiminnot; (3) reading-items (`item_type: "reading_mc"` `reading_mc`-phasessa) renderöityvät passage + kysymykset stack-layoutissa, choice-buttonien is-selected/is-correct/is-wrong -tilat, paikallinen arviointi (correct_index match), explanation_fi-paneeli kaikille kysymyksille submitin jälkeen, next-button etenee item-by-item. Korvaa "Tämä tehtävätyyppi avautuu pian"-placeholderin molemmille tyypeille. **css/components/digikirja.css:** uudet `.dk__writing-*` ja `.dk__reading-*` -luokat, ed-token-pohjainen, accent border-vasen prompt-kortille, var(--ed-serif) passagelle, mobile-breakpoint 640px (textarea 240px, score 40px, errors single-column). **sw.js** v279→v281. Verifiointi: `node --check` PASS, `npm run build` PASS, `npm test` 1241/1241 PASS. Playwright-spec + mobile-screenshotit jäävät follow-up:iksi (laajempi käyttäjäpolku-audit per lesson-tyyppi per kieli). Skills: frontend-design, design-taste-frontend, ui-ux-pro-max, impeccable, emil-design-eng, webapp-testing, superpowers:verification-before-completion.
- [2026-05-22 v279] **feat(sentence-build): Käännä lauseet oikea click-to-order -tehtävä.** Per `docs/briefs/2026-05-22-sentence-build.md`. v241:ssä "Lauseiden rakentelua" nimettiin "Käännä lauseet" ilman mekaniikkaa — nyt rakennettu täysi token-järjestys-vuorovaikutus. **Uudet tiedostot:** `js/screens/sentenceBuild.js` (controller: `openSentenceBuild({level,count,language})` fetchaa `/api/exercises/reorder`-payloadin, `addToAnswer`/`removeFromAnswer` ohjaavat token-poolin ja answer-arrayn, `submit` tekee local exact-match-grading + case-insensitive (mukailee vocab.js:n reorder-rendererin logiikkaa), virheelliset käännökset lokitetaan `/api/mistake`-endpointtiin fire-and-forget niin SR-engine näkee ne, `arraysEqualCI`-helper export `_internal`:n alla unit-testausta varten); `css/components/sentence-build.css` (Old-Spain register: cream-paper, brick accents, Fraunces vain `.sb__correct-text`:lle EI italic vaan weight 500 — emil-design-eng "hierarchy through weight not affectation", `--sb-easing: cubic-bezier(0.23, 1, 0.32, 1)` kaikki transitionit, `.sb-token` `:active scale(0.97)` per emil "buttons must feel responsive", hover-state gated `@media (hover: hover) and (pointer: fine)` jotta mobile-tap ei laukaise stuck-hoveria, `@starting-style` palauttaa feedback-paneelin fade+slidein ilman JS:ää, mobile <640px collapse: actions kolumneiksi, token-padding 11px 14px → 17px font, `prefers-reduced-motion` poistaa transitionit); `app.html`-blokki `<section id="screen-sentence-build">` (header eyebrow + back-icon + progress 1/N, body hint+answer-area+pool+actions, feedback-paneeli verdict+correct+explain, summary-paneeli sarjan päätteeksi); `tests/sentence-build-helpers.test.js` (5/5 vitest: exact match, CI capitalization, order mismatch, length mismatch, empty edge case); `tests/e2e-sentence-build.spec.js` (5/5 Playwright desktop). **app.html (kirjoitus-mode):** `.mode-page__cta-row`:hyn lisätty `#btn-start-sentence-build` btn--cta--secondary. **scripts/bundle-entry.css:** lisätty `@import '../css/components/sentence-build.css'`. **js/main.js:** `lazySentenceBuild`, document-level click delegation, `hashchange` + `_restoreFromHash` käsittelevät `#/lauseet`-routen. **sw.js** v278→v279. Skills: frontend-design, design-taste-frontend, ui-ux-pro-max, impeccable, emil-design-eng, webapp-testing, superpowers:test-driven-development, superpowers:verification-before-completion.

- [2026-05-22 v278] **feat(sidebar): mode-list plumbing — kurssit + lessonit näkyvät sidebariin MODE-tilassa.** Per `docs/briefs/2026-05-22-sidebar-mode-list-plumbing.md` (v277-follow-up brief: "items: [] needs curriculum-state plumbing"). **Uusi tiedosto:** `js/lib/sidebarItems.js` — `buildSidebarItemsForMode(modeKey, lang)` lukee jaetun `window._curriculumCache_<lang>`-Promisen (sama kuin curriculum.js + dashboard.js → ei tuplafetchiä), fan-out-fetchaa lessonit kullekin kurssille `/api/curriculum/:kurssiKey`:llä rinnakkain ja cachettaa per kurssi `window._sidebarLessonsCache_<lang>_<key>`:hyn 5 min, suodattaa lessonit moden mukaan (vocab/grammar/reading/writing → kyseinen tyyppi + mixed + test; exam → kaikki), palauttaa flat-arrayn `[{ type: "heading", label }, { type: "lesson", key, kurssiKey, lessonIndex, label, active, completed, locked }]`. Tyhjät kurssit (ei matching lessoneita modeen) skipataan kokonaan — ei jää tyhjiä otsikoita. `getActiveLessonKey()` lukee `sessionStorage.currentLesson`-blobista (curriculum.js openLesson() asettaa sen). **sidebarShell.js:** `renderModeNav()` tukee `type: "heading"` → `<li class="sidebar-section-heading">` (Manrope inherit, 11px, letter-spacing 0.04em, EI mono-UPPERCASE-slop, pointer-events:none jotta otsikkoa ei voi klikata vahingossa) ja `type: "lesson"` → `<button.sidebar-item.sidebar-item--lesson>` data-kurssiKey + data-lessonIndex:llä, `.is-done` saa `✓`-glyphin + soft-color, `.is-locked` disabloi napin. Lisätty event-delegation `#sidebar-mode-items`:lle joka dispatchaa `puheo:open-lesson` -CustomEventin (`{ kurssiKey, lessonIndex, lessonKey }`) — sidebar pysyy agnostisena lesson-routing-logiikasta. **main.js navigateTo():** kun nav-target on MODE (`vocab|grammar|reading|writing|exam`), kutsuu ensin `setSidebarMode("mode", { items: [] })` (sync skeleton), sitten async `buildSidebarItemsForMode()` → `setSidebarMode("mode", { items })` ilman flickeriä. Lisätty `document.addEventListener("puheo:open-lesson")`-kuuntelija joka reitittää canonical hashille `#/oppitunti/<lang>/<kurssiKey>/<lessonIndex>/teoria` (existing digikirja-route hoitaa loput). **CSS `css/components/sidebar-shell.css`:** uudet `.sidebar-section-heading`-säännöt (padding 18px 20px 6px, font-size 11px, letter-spacing 0.04em, color var(--ink-soft), pointer-events:none) + `.sidebar-item--lesson`-säännöt (padding 8px 20px, line-height 1.35, min-height 44px touch target, hover-bg cream-deep, active scale(0.985) 100ms ease-out per emil-design-eng, `.is-done` ✓-pseudo, `.is-locked` opacity 0.45 + cursor not-allowed, `.active` brand-brick-soft-bg + brand-brick-color + font-weight 600 — sama korostus kuin v276:n nav-active, EI side-stripe-borderia). Tab-navigaatio skippaa headingit (ovat `<li>` eivät napit, ei tabindexiä). **Tests:** uusi `tests/e2e-sidebar-mode-plumbing.spec.js` 3/3 ✓ desktop (data-mode=mode + 2 headingiä + 2 vocab-lessonia rendautuu, klikkaus dispatchaa puheo:open-lesson oikealla payloadilla, headingit ei-fokusoituvia). Mockaa `/api/curriculum?lang=es` + per-kurssi `/api/curriculum/:k`-vastauksia jotta route ei vaadi DB:tä. vitest 1236/1236 ✓. `npm run build` OK (390 kb CSS, 107 kb JS — uusi `chunks/app-sidebarItems-*.js` chunk lazy-loadissa). `sw.js` v277→v278. **EI pushia Verceliin** per brief (odota Marcelin OK). Skills: frontend-design, design-taste-frontend, ui-ux-pro-max, impeccable, emil-design-eng, webapp-testing, superpowers:verification-before-completion.

- [2026-05-22 v277] **feat(app): SidebarShell — [data-mode]-driven konsolidaatio + 3 reported bugin korjaus.** Per `docs/briefs/2026-05-22-sidebarshell-restructure.md`. Three-strikes-redesign-syyperuste: sidebar-bugit (tyhjä tila ylhäällä/alhaalla, "vain Aloitus näkyy" mode-näytöillä, Asetukset-modaalin sign-out jätti sivupalkin rikki) palasivat koska geometria oli 3 tiedostossa. **Uudet tiedostot:** `css/components/sidebar-shell.css` (single source of truth, grid-template-rows auto/1fr/auto pakottaa kolme slottia, [data-mode-only]-reveal home/mode/book, defensive `display:none → grid` recovery), `js/components/sidebarShell.js` (`setSidebarMode(mode, ctx)` + `renderModeNav()` + MODE_LABELS-mapping, tyhjentää stray-inline-display:n joka kerta). **app.html:** `<aside>` saa `data-mode="home"`, HOME/MODE-slotit `data-mode-only`-attribuutilla, MODE-slot sisältää back-Aloitus + `#sidebar-mode-title` + `#sidebar-mode-items`-ul. **main.js navigateTo()** kutsuu `setSidebarMode("mode", { modeKey, modeLabel })` kun nav ∈ {vocab, grammar, reading, writing, exam}, ja `"home"` kun nav ∈ {home, path, dashboard}. **digikirja.js** flippaa `data-mode="book"` kun screen avataan; korvaa vanhan `body:has(#screen-digikirja.active) .app-sidebar { display: none }` band-aidin 56px-railiksi (Aloitus klikattavissa anywhere). **settings.js:104** poistettu inline `document.querySelector(".app-sidebar").style.display = "none"` -kludge sign-out-handlerista — se oli kolmas-strike bug. **digikirja.css** päivitetty kommentti + poistettu hide-rule. **Säilytetty:** off-canvas-nav.css (mobile slide-in toimii edelleen samojen `.sidebar-item`/`.sidebar-user`/`.sidebar-pro-slot`-selektorien kautta), kaikki olemassa olevat ID:t (`#sidebar-user`, `#nav-home`, `#sidebar-logout`, `#sidebar-pro-slot`) — 7 tiedostoa viittaavat näihin. **Tests:** `tests/e2e-sidebar-shell.spec.js` 3/3 ✓ desktop (data-mode="home"-baseline, MODE-flippaus näyttää title+back-link, settings-jitter recovery). vitest 1236/1236 ✓. `npm run build` OK (390 kb CSS, 107 kb JS). `sw.js` v276→v277 + `sidebar-shell.css` lisätty STATIC_ASSETS-listalle. **Ei pushattu Verceliin** per brief (iso refactor, Marcelin OK ennen). **EI scope tähän looppiin:** lesson-itemien dynaaminen täyttö #sidebar-mode-items:iin (renderModeNav() tukee items-listaa, mutta navigateTo lähettää items: [] toistaiseksi — wire-up tulee seuraavassa loopissa kun curriculum-state-tarjontaa puretaan), off-canvas-nav.css:n poisto (toimii, no-op). Skills: frontend-design, design-taste-frontend, impeccable, emil-design-eng, ui-ux-pro-max, webapp-testing, superpowers:verification-before-completion.

- [2026-05-22 v276] **feat(app): brand-väri brick #A0341F palautettu app-puolelle (sidebar-active, Pro-badge) + brand-brick-tokenit.** Per `docs/briefs/2026-05-22-brand-color-restore.md`. **Tokens:** `--brand-brick`, `--brand-brick-hover`, `--brand-brick-soft`, `--brand-brick-on-light` lisätty `css/app-old-spain.css` `:root`-scopeen aliaksina olemassa oleviin `--ed-accent*` -arvoihin (oklch(42% 0.15 28) ≈ #A0341F). **§1 Sidebar active:** poistettu side-stripe sekä `style.css:349` `border-left: 3px solid var(--accent)` että korvattu `app-old-spain.css:2412` 3px-pseudostripe pyöreällä 6px brick-dotilla left:6px keskellä (Emil + impeccable side-stripe-ban). **§3 Pro-badge:** `app-old-spain.css:1900` `--ed-ink` warm-black → `--brand-brick` (kontrasti #A0341F + cream ≈ 8.2:1, ylittää AAA reilusti). **§2/§4/§5/§6 olivat jo no-op:** brief perustui vanhentuneeseen audittiin — `body.app` :root-bridge `app-old-spain.css:116` mapittaa jo `--accent` → `--ed-accent` (brick), joten CTA-napit, linkit, progress-fillit ja kaikki `var(--accent)`-käytöt renderöivät jo brickiä. EI muutoksia landingiin per brief don't-listalle. `npm run build` OK. `npm test` 1236/1236 ✓. `sw.js` v275→v276. Skills: frontend-design.

- [2026-05-22 v275] **chore(app): app shell hardening — Ladataan italic pois + mode-cards focus-visible.** Per `docs/briefs/2026-05-22-app-shell-kovettaminen.md`. **V6:** `.path-toc__loading` `font-style: italic` → `normal` + `list-style: none` (`css/app-old-spain.css:2558`) — sisällysluettelon "Ladataan…" -placeholder ei enää peri Fraunces-italicia. **V9:** `.mode-btn:focus-visible { outline: 2px solid var(--accent); outline-offset: 3px }` lisätty (`style.css:1492`) — Sanasto/Kielioppi/Luetun ymmärtäminen/Kirjoitus/Koeharjoitus -kortit näyttävät nyt focuksen Tab-navigaatiolla. **V7 (display--serif) ja V13 (Pro-badge contrast) skipattu:** brief perustui vanhentuneeseen audit-snapshotiin — `.display--serif` on jo määritelty `app-old-spain.css:210` (Fraunces, 400, -.022em), ja `body.app .sidebar-pro-badge` -override `:1900` lukitsee badgen `--ed-ink` cream-tekstillä (kontrasti ≫ 4.5:1). Ei muutoksia. **Border-yhtenäistys (P2) skipattu** scope-räjähdyksen välttämiseksi — oma brief jos tarpeen. `npm run build` OK (390 kb CSS, 107 kb JS). `npm test` 1236/1236 ✓. `sw.js` CACHE_VERSION v274→v275. EI Vercel-pushia (chore). Skills: frontend-design.

- [2026-05-22 v274] **chore(landing): anti-AI-slop sweep — testimonialit anonymisoitu, italic-Fraunces rajattu, side-stripes pois, 8-kortti-grid asymmetriseksi.** Toteutus per `docs/briefs/2026-05-22-landing-slop-sweep.md`. **V1:** poistettu Eemil/Helmi/Otso-etunimet + tarkat arvosanasiirtymät C→M/M→E/B→M + "kevät 2026" -päivät testimonialista (rikkoo `feedback_no_fabricated_provable_claims`). Korvattu geneerisillä rooleilla ("Lyhyen espanjan abiturientti" jne.) + kvalitatiivisilla kuvauksilla. `index.html`. **V5:** italic-Fraunces rajattu vain `.hero__title-accent`-selektoriin. Poistettu 16 muusta paikasta `landing-editorial.css`:ssä (legal/blog em, nav-brand-o, section-title em, catalog-card-num, proof-caveat, rubric-total-note, wyg-item-num, testimonial-quote-mark, testimonial-chip, testimonial-first-letter, pagenum-b, marginalia, cta-card em, ge-mini, grade-flow em, pricing/faq title em). WONK-axis poistettu samalla. **V8:** side-stripe-borderit pois `app-old-spain.css:470` (.dash-tutor `border-left: 2px` → täyskehä-border accent-tintillä), :886 + :896 (ex-option is-correct/is-wrong redundantit border-left-rivit pois), `curriculum.css:561` (.curr-teaching blockquote → background-tint + täysikehä-border), :792 (.lr-fasttrack), :841 (.lr-deepen). **V10:** K1 spans 2 cols, K8 (finale) spans full row desktop+tablet → asymmetrinen bento korvaa 8 identical card-gridin. `landing.css`. **V11:** `--ed-ink-muted` oklch 38% → 33% AA-kontrastin varmistamiseksi cream-taustalla. **V12:** heading-hierarkia espanja.html verifioitu kunnossa (h1 → h2-sektiot → h3-alaotsikot), ei jumpeja, ei muutoksia. `sw.js` CACHE_VERSION v273→v274. `npm run build` OK. EI Vercel-pushia. Skills: frontend-design, design-taste-frontend, ui-ux-pro-max, impeccable, emil-design-eng.

- [2026-05-22 v273] **chore(siivous): dead code + docs + slop (poistettu 113 tiedostoa, 4 unused dep, 4 AI-slop-fix).** Repo-siivous per `docs/briefs/2026-05-22-repo-siivous.md`. **Scope A:** poistettu 101 orvoa `chunks/app-*` -tiedostoa (esbuild-buildin vanhat hashit jotka eivät enää löydy `app.bundle.js`:n importeista). Pudotettu 4 dep:iä joilla on 0 koodi-viittausta: `@supabase/ssr`, `@fontsource/dm-mono`, `@fontsource/inter`, `@rolldown/binding-linux-x64-gnu`. `bcryptjs`, `jsonwebtoken`, `express-rate-limit` säilytetty turvarajan vuoksi (auth-vierus-pkg vaikka 0 importtia). **Scope B:** poistettu 12 orvoa root-markdownia (`AGENT_PROMPT_PLANNER/REMONTTI_AUDIT`, `AGENT_STATE_ARCHIVE`, `CLEANUP_LOOP_PLAN`, `HANDOFF`, `PUHEO_PROMPTS`, `README_LPLANS`, `STATE_FILES_CLEANUP_PLAN`, `SUPERPOWER_PROMPT`, `frontend-polish-prompts`, `remotion-preview-v2`, `docs/HANDOFF_PROMPT_2026_05_20`) joilla 0 viittauksia. Siirretty `PUHEO_NETTISIVU_V2.md` → `docs/archive/` (historiallinen analyysi). **Scope C (legacy shims):** ei muutoksia — kaikki epäselvät jätettiin paikalleen turvarajan mukaisesti. **Scope D (AI-slop):** korjattu 4 em-dash-esiintymää suomi-UI-tekstissä: `app.html` × 2 (`Pro-ominaisuus —`), `js/screens/digikirja.js` × 2 (testi-yhteenveto, itsearvio-lede), `index.html` × 2 (testimoniaalit-sub, FAQ-mail). `sw.js` CACHE_VERSION v272→v273. EI koskettu: `routes/stripe.js`, `middleware/auth.js`, `lib/supabase.js`, test-account-koodi. Test 1223 pass (sama kuin baseline), build OK. Skills: supabase, supabase-postgres-best-practices, frontend-design, design-taste-frontend, ui-ux-pro-max, impeccable, emil-design-eng, webapp-testing, superpowers:verification-before-completion.

- [2026-05-22 v272] **feat(landing+marketing): white background across landing, blog, legal, per-lang landings.** Jatko v271:n app-shell-flippaukselle — käyttäjä halusi saman "kerma → near-white" -muutoksen koko sivustolle. **Brick + olive + Fraunces + Manrope säilyvät** brändi-DNA:na, vain pohjaväri-tokenit vaihtuvat. Files: `css/landing-editorial-tokens.css` (`--ed-bg` cream → oklch 99% 0.002 75, `--ed-bg-card` → tinted white, `--ed-rule` softer; auto-flipaa index.html + pricing/privacy/terms/refund + blog/* + styleguide.html koska kaikki lataavat editorial-tokenit), `css/landing-tokens.css` (`--bg` Zinc-950 #0B0E0D → near-white, `--text` `#fafafa` → warm-near-black, `--surface`/`--border` invertattu valkoiselle taustalle; tämä flippaa per-lang landingit espanja/ranska/saksa koska ne lataavat **vain** landing-tokens.css), `css/landing.css` (yksi hardkoodattu `#0a0a0a` mock-browser-viewport → `var(--bg-elevated)` muiden 14 hardcoden ollessa OK light-mode-kontekstissa), `sw.js` (v271→v272). Memory `feedback_landing_direction_2026_05_18` päivitetty heijastamaan uutta pohjaa (vanha kerma-arvo jäänyt referenssiksi). EI scope: landing.css:n 14 muuta hardkoodia (button-text `#052924` teal-CTA:lla → OK; error-text `#fca5a5` → seuraava loop jos kontrasti-issue; browser-frame-dots värit). EI tehty: per-lang landingien Playwright-spec (käyttäjän selaintesti riittää). Skills: frontend-design, design-taste-frontend, ui-ux-pro-max, impeccable, emil-design-eng.

- [2026-05-22 v271] **feat(app): dashboard-redesign + valkoinen app-shell (poistettu beige + 5-mittariruutu-slop).** Aloitus vastaa nyt kysymykseen "mitä mä teen seuraavaksi" — ei "mitä Puheo sisältää". Color tokens `body.app`-skoopissa: `--ed-bg` cream → near-white (oklch 99% 0.002 75), `--ed-bg-card` → tinted white, `--ed-rule` softer hairline; brick + olive aksentit pidetään (Puheon DNA). Home shell uudelleenkirjoitettu: greeting (Fraunces 40px, **EI italic**) + date-pilleri, yksi iso "Jatka tästä?" -kortti (primary CTA), Päivän tavoite -palkki (streak chip + minute-goal progressbar), Kurssipolku-snapshot (4 mikrotiltä: Sanasto/Kielioppi/Luetun/Kirjoitus). Ohjaamo-otsikko + 5-cell-grid + 4-iso-mode-korttigrid poistettu. Empty-state ei jätä viiva-placeholderia. AI-slop-checklist täytetty: ei italic-Fraunces missään, ei mono-UPPERCASE-"8 KURSSIA"-eyebrowia, ei em-dasheja, ei identtisiä kortteja. Files: `css/app-old-spain.css`, `js/screens/home.js`, `css/components/home.css`, `sw.js` (v270→v271), `tests/e2e-dashboard-redesign.spec.js` (NEW). Stripe + landing + sidebar + muut screenit (settings, kirjoitus) koskemattomia per brief. Skills: frontend-design, design-taste-frontend, ui-ux-pro-max, impeccable, emil-design-eng, webapp-testing, superpowers:test-driven-development, superpowers:verification-before-completion, superpowers:systematic-debugging.

- [2026-05-22 v270] **fix(exam): Koeharjoitus-kortti ei enää jätä käyttäjää "Tarkistetaan aktiivista koetta…" -loaderiin.** Root cause: `css/app-old-spain.css`:n `body.app > *` (specificity 0,1,1) downgradasi `.confirm-dialog-root { position: fixed; z-index: 1000 }` → `position: relative; z-index: 1`, joten resume-modal kattoi vain viewportin alapuoliskon screen-loadingin takana — käyttäjä näki vain spinnerin + "Tarkistetaan aktiivista koetta…" eikä saanut kiinni "Jatka kesken olevaa" / "Aloita uusi koe" -nappeja. Fix: `body.app > .confirm-dialog-root { position: fixed; z-index: 1000 }` rivin `.app-countdown` / `.teaching-panel-trigger` / `.skip-link` -fix-listan jatkoksi (file dokumentoi tämän jo v246-kommentissa, dialog vain unohtui listalta). Defense-in-depth: `startFullExam` saa 12 s watchdog-timeoutin joka kääntää näytön `showLoadingError`-tilaan + retry-napin jos /api/exam/resume ei vastaa, sekä non-2xx HTTP-statuksilla heitetään selvä virhe humanize-pipelineen. Spec: `tests/e2e-koeharjoitus-stuck-loading.spec.js` (NEW) — kaksi assertia: (a) klikkaus → exam/dialog/error-tila ≤5 s, (b) `#exam-resume-dialog-root` on `position:fixed` + `z-index ≥ 1000` + täysi viewport-katto + primary CTA y < 450 px. Spec failas prodissa (y=572, position:relative), passaa lokaalisti. Sidebar puuttuvat mode-linkit jätetty koskemattomaksi (SidebarShell-restrukturointi tulee erikseen). Files: `css/app-old-spain.css`, `js/screens/fullExam.js`, `sw.js` (v245→v270), `app.bundle.css` + `app.bundle.js` (rebuilt), `tests/e2e-koeharjoitus-stuck-loading.spec.js` (NEW, korvaa väliaikaisen `e2e-jatka-harjoittelua.spec.js`:n). Verification: 1236/1236 vitest ✓, Playwright lokaalisti 2/2 ✓, prod 1/2 (negative control). Skills: superpowers:systematic-debugging, superpowers:test-driven-development, superpowers:verification-before-completion, webapp-testing, frontend-design, design-taste-frontend, ui-ux-pro-max, impeccable, emil-design-eng, supabase, supabase-postgres-best-practices.

- [2026-05-14 F-CURRICULUM-BENTO-1] **Curriculum-näkymän lesson-lista → 12-tile bento-grid.** `js/screens/curriculum.js` `fetchAndRenderLessons` enää ei renderöi flat-listaa vaan `renderBento()`:n: (1) progress-strip ("Edistyminen · N / 12 suoritettu" + 12 klikattavaa dottia jotka scroll-targettaavat oikeaa korttia + aria-label "Oppitunti N — suoritettu/vuorossa/tulossa"); (2) `.curr-bento-grid` 12-col CSS Grid desktop / 4-col 1024-768 / 2-col mobile, jossa **next-up = `.curr-bento-card--feature` (span 6 desktop, accent-tint, border-left 3 px, IntersectionObserver-laukaisema yhden kerran pulse — ei infinite loop); muut span 3 desktop / span 1 mobile aspect-ratio 1/1**. Kortin sisältö: Lucide-SVG-ikoni lessontyypin mukaan (BookOpen/Pencil/FileText/PenLine/Layers/CheckSquare, 24×24 stroke-1.75 currentColor — emoji-pyyntö korvattu Lucide-setillä per `_UI_UX_CHECKLIST.md` priority 7), difficulty-dot-rivi (3 dot, `role="img"` + aria-label "Vaikeustaso N / 3" → axe-clean), `01 · Sanasto`-eyebrow (mono), 2-rivin clamp otsikko, optional preview teachingSnippet:istä (70 ch ellipsi), clock-ikoni + "~12 min" (90 s/exercise) + CTA. Done-kortit: opacity .7 + line-through. A11y: `<button>` tile = inherent role, aria-label koostaa lesson# + focus + tyyppi + kesto + difficulty + tila; DOM-järjestys = sortOrder 1→12 (Tab visuaalisesti); progress-strip dot focus-visible-outline. Reduced-motion: pulse + transforms + skeleton-shimmer disabloitu. 12 skeleton-placeholderia samoilla span-arvoilla (CLS-suoja). Locked-tila-CSS valmiina vaikka tämänhetkinen API ei expose lesson-level lockia (kaikki unlocked-kurssin lessonit klikattavia). **Files:** `css/components/curriculum-bento.css` (NEW), `js/screens/curriculum.js`, `scripts/bundle-entry.css`, `sw.js` (v155→v156), `tests/e2e-curriculum-bento.spec.js` (NEW — render 12 + feature/done states + 12 strip-dot + axe wcag2a/wcag2aa 0 serious/critical violations; mobile+desktop 4/4 vihreitä). 21st.dev-sourcing: `/s/bento` + `/s/lesson-card` + `/s/timeline` -patternit sovellettu — restrained dark single-stroke accent (Linear-tier), ei loud-glow eikä neon. Skills: frontend-design, design-taste-frontend, ui-ux-pro-max, puheo-screen-template, puheo-finnish-voice. **Pending:** käyttäjän selaintesti `localhost:3000/app.html#/polku` → curriculum-näytöllä bento mobiilissa+desktopissa, kortti-klikkaus avaa lesson-sivun, progress-strip dot scrolla oikeaan korttiin.

- [2026-05-14 F-ARCH-1 §A] **Bundle-split + lazy-load 5 screeniä (`-71 %` initial JS).** `app.bundle.js` 376 KB → 108 KB. Käyttäjän valinta "pienin järkevä split" — kaikki 25 screeniä pysyvät static-importeissa paitsi nämä 5 harvoin-käytettyä: `fullExam`, `settings`, `profile`, `verbSprint`, `verbReference`. Toteutus: (1) Uusi `js/lib/lazyScreen.js` — `makeLazyScreen({key, factory, init, deps})` palauttaa async-funktion joka cachet moduuli-promisen, ajaa `init()` täsmälleen kerran, ja lisää 250 ms threshold loading-hint:in `document.documentElement.dataset.lazyLoading`-attribuuttiin (UI voi tyylittää tämän tarvittaessa). (2) `js/main.js`: 5 static-import-riviä poistettu, 5 `init*()`-kutsua boot-vaiheesta pois, korvattu 5 `makeLazyScreen()`-wrapperilla; `navigateTo()`:n `exam`/`settings`/`profile`-haarat (rivit 176-178) + `fullExamBtn`-handler (rivi 408) käyttävät `.then(m => m.startFullExam("demo"))`-tyyppistä lazy-kutsua; uudet `verbsprint`/`verbreference`-haarat triggaavat init:n nav-hetkellä. (3) `scripts/build-bundle.mjs`: `outfile: "app.bundle.js"` → `outdir: "."` + `entryNames: "app.bundle"` + `chunkNames: "chunks/app-[name]-[hash]"` + `splitting: true`. esbuild discoveroi ALL dynamic-import-pisteet (5 lazy-screeniä + olemassa olleet `cardTilt`/`tooltip`/`lessonContext` jne.) → 43 .js-chunkkia + sourcemaps `/chunks/`-kansioon. (4) `vercel.json`: `/chunks/(.*)` saa `Cache-Control: public, max-age=31536000, immutable` (hashatut nimet = safe). (5) `.gitignore`: `chunks/*.map` ignored (chunks itse committoidaan jotta `npm run build` → Vercel-deploy toimii). (6) SW v154→v155 (bundle-koko muuttui). 21st.dev-sourcing skipped — infra-loop, ei UI-komponentti. **Mittaukset:** main.bundle 376185 B (367 KB) → 108009 B (105 KB), saving **-72 %**. Suurin yksittäinen chunk: `app-chunk-RXUK3EE5.js` 79.4 KB (yhteinen shared-koodi). `lessonRunner` 19.7 KB. CSS bundle 264 KB ennallaan (out of scope). **Files:** `js/lib/lazyScreen.js` (NEW), `js/main.js`, `scripts/build-bundle.mjs`, `vercel.json`, `.gitignore`, `sw.js`, `app.bundle.js` (rebuilt), `chunks/*.js` (43 NEW). Skills: frontend-design, design-taste-frontend, ui-ux-pro-max, puheo-screen-template, puheo-finnish-voice. **Pending:** käyttäjän selaintesti: navigoi profile/settings/verbsprint/verbreference/exam → näkyykö network-tabissa erilliset chunk-fetchit + screenit avautuvat oikein.

- [2026-05-14 F-ARCH-1 §C] **`/styleguide`-sivu (`styleguide.html`).** Yhden tiedoston portable design-system-referenssi joka lataa `/css/tokens.css` + `/app.bundle.css` jolloin jokainen swatch ja komponentti matchaa prodia eksaktisti (ei rinnakkaista hand-rolled CSS:ää joka driftaa). Layout: sticky vasen-rail TOC + main-column 13 osiota (Värit · Spacing · Kulmat · Varjot · Typografia · Motion · Z-index · Napit · Kentät · Kortit · Skeletonit · Merkit · Palaute). Motion-osio CSS-only :hover-pohjainen (kolme näytettä `--ease-out 120ms`, `--ease-spring 320ms`, `--duration-reveal 600ms`). Napit-osiossa primary kaikki 6 tilaa (default/hover/focus/active/disabled/loading) + ghost + quiet + icon-button (44×44). Skeletonit-osio käyttää tuotannon `.skeleton-exercise--vocab/--grammar/--reading`-luokkia (F-MICROPOLISH-1 §A). Palaute-osio käyttää tuotannon `.lr-feedback`-luokkia (F-MICROPOLISH-1 §B). Gate: ulkoinen `/js/styleguide-gate.js` (ei inline-scriptiä koska CSP `script-src 'self'`) tarkistaa `?dev=1` TAI `hostname === localhost/127.0.0.1/.local` → muuten korvaa body 404:llä. SW skippaa `/styleguide`-polun fetch-handlerissa (ei cachata, jotta tokens.css-muutokset näkyvät heti reloadissa). `vercel.json`: lisätty rewrite `/styleguide` → `/styleguide.html` clean URL:lle. Footeri muistuttaa: "Älä linkkaa tätä tuotannosta — gate `?dev=1`". Puheo-finnish-voice: kaikki copy sinä-form, "Yo-koe lähestyy." display-otsikko, "Etsitään sopivia sanoja sinulle…" skeleton-hint. Istunto 3 (bundle-split + lazy-load) jää erilliseen looppiin riskin takia. **Files:** `styleguide.html` (NEW), `js/styleguide-gate.js` (NEW), `sw.js`, `vercel.json`. **SW:** v153→v154 (skip-rule `/styleguide`). Skills: frontend-design, design-taste-frontend, ui-ux-pro-max, puheo-screen-template, puheo-finnish-voice. 21st.dev-sourcing skipped — ei uusi UI-komponentti vaan dev-internal-referenssi joka käyttää olemassa olevia tokeneja. **Pending:** käyttäjän selaintesti `localhost:3000/styleguide` + `https://puheo.fi/styleguide?dev=1` (prod), ja varmista että ilman `?dev=1` näkyy 404.

- [2026-05-14 F-ARCH-1 §B] **Design-tokens single source of truth (`css/tokens.css`).** Lisätty additiivinen tokens-tiedosto joka EI redefinoi olemassa olevia kanonisia tokeneja (--accent, --ink, --surface, --r-md, --s-1..10, --dur-*, --ease-*), vaan: (1) tuo PUUTTUVAT system-tokenit (`--z-base..--z-confetti` 1/10/20/30/40/50/60, `--focus-ring`, `--focus-ring-offset`, `--ease-spring` cubic-bezier(.34,1.56,.64,1), `--duration-reveal` 600ms); (2) julkaisee SEMANTIIKKA-ALIAKSET briefin nimillä (`--text` → `--ink`, `--text-muted` → `--ink-soft`, `--surface-hover` → `--surface-2`, `--space-1..8` → `--s-1..10`, `--radius-sm/md/lg/full` → `--r-*`, `--shadow-sm/md/lg` → `--sh-*`, `--duration-fast/base/slow` → `--dur-*`) jotta tulevat F-* -loopit voivat käyttää briefin nimiä ja ne resolvoituvat Puheo-tokeneihin ilman big-bang-refaktoria; (3) reservoi `:root[data-theme="dark"]`-selektorin tulevaa dark-mode-looppia varten (TODO F-ARCH-2). Importti `app.html`:ssa app.bundle.css:n EDESSÄ. index.html (landing) jätetty ennalleen — landing-tokens.css on oma ekosysteemi (`--text: #fafafa` dark-herolle), erillinen migraatio. **Istunto 2 (`/styleguide`-sivu) ja Istunto 3 (bundle-split + lazy-load) erillisiin looppeihin** brief §6 mukaisesti — riski liian iso yhteen istuntoon. Migraatio-TODO: korvata komponentti-CSS:ssä hardcode `z-index: 50/100/999`-arvot `var(--z-*)`-tokeneilla (40+ esiintymää, ks. `grep -rn "z-index:" css/components/`). **Files:** `css/tokens.css` (NEW), `app.html`, `sw.js`. **SW:** v152→v153 (tokens.css lisätty STATIC_ASSETS:iin). Skills: frontend-design, design-taste-frontend, ui-ux-pro-max, puheo-screen-template, puheo-finnish-voice. 21st.dev-sourcing skipped — kyseessä on infra-token-tiedosto, ei UI-komponentti. **Pending:** käyttäjän selaintesti (varmista että mikään ei rikkoutunut visuaalisesti — tokens.css on additiivinen, ei pitäisi muuttaa mitään olemassa olevaa); `npm run build`.

- [2026-05-14 F-MICROPOLISH-1] **Skeleton-variantit + spring-feedback + drawer aria-hidden.** Item A: 3 typed skeleton-pohjaa (`vocab`/`grammar`/`reading`) `js/ui/loading.js` SKELETON_MARKUP:iin + `style.css` skeleton-bar-* modifiers (`eyebrow`/`prompt`/`rulechip`/`title` + `skeleton-reading-q`). vocab.js → `showSkeleton(slot, "vocab")`, grammar.js → `"grammar"`. Loading-hint copy puheo-finnish-voice-yhteensopiva ("Etsitään sopivia sanoja sinulle…" jne., ei "Loading…"). Reduced-motion gating periytyy olemassa olevasta `@media (prefers-reduced-motion: reduce)`-säännöstä. Item B: `.lr-feedback` saa Lucide-tyylisen check/X-SVG-ikonin + `.lr-feedback__text`, spring-pop (`cubic-bezier(.34,1.56,.64,1)` 320ms, scale 0.6→1.12→1) + shake (220ms 4×4px) väärillä vastauksilla, fadein-staggered tekstille. Color-as-only-indicator -ongelma korjattu (icon + text + `role="status"` + `aria-live="polite"`/`"assertive"`). Reduced-motion → pelkkä opacity-fade 150ms. Animaation re-trigger force-reflow-trickillä jotta sama #lr-feedback-elementti voi toistaa anim eri itemeissä. Item C: `offCanvasNav.js` lisää `aria-hidden="true"` `.app-main`-elementtiin kun drawer avautuu, poistaa sulkemisessa (focus-trap + scroll-lock + Esc + outside-click olivat jo paikallaan). Item D (footer): JO SHIPATTU aikaisemmassa loopissa `index.html`:n riveillä 875-923, ei muutosta. **Files:** `js/ui/loading.js`, `js/screens/{vocab,grammar,lessonRunner}.js`, `js/features/offCanvasNav.js`, `style.css`, `css/components/lesson-runner.css`, `sw.js`. **SW:** v151→v152 (uusia keyframes + DOM-mutaatiot). Skills: `frontend-design`, `design-taste-frontend`, `ui-ux-pro-max`, `puheo-screen-template`, `puheo-finnish-voice`, `emil-design-eng`. **Pending:** käyttäjän `npm run build` + Vercel-deploy.

- [2026-05-14 L-LESSONS-FR-LYHYT-1] **Ranskan 90 oppituntia generoitu 8 kurssiin (K1-K8, YTL A→E).** Kurssi 1-6 + K7 L1-5 + K8 L1-5 olivat valmiit edellisistä loopeista (76 oppituntia); viimeistely-loop täytti puuttuvat K7 L6-12 + K8 L6-12 (14 uutta) kahdella rinnakkaisella Sonnet-workerilla. K7 (M-taso): L6 pronoms relatifs dont/où, L7 subjonctif-laukaisijat bien que/pour que/avant que/jusqu'à ce que, L8 subjonctif passé, L9 mixed PQP+pronominit+kulttuuri, L10 reading frankofonia (Aimé Césaire, Québec), L11 writing 160-240/300-450 merkkiä, L12 kertaustesti. K8 (E-taso): L6 reading yhteiskunta (laïcité, immigration, banlieues), L7 reading ilmastoaktivismi (Pariisin sopimus), L8 writing courriel formel + lettre d'opinion, L9 writing täysi YO-kirjoitelma argumentoiva, L10 välitesti L1-9, L11 mixed YO-simulaatio Osa 2+3 (Québec-artikkeli + rakennetehtävät), L12 LOPPUKOE K1-K8 (35 itemiä). Canonical REVIEW-kierros löysi 3 P0 + 7 P1 (kaikki korjattu): K7/L6 `dont`/`participer à` -virhe → `rêver de`, K7/L7 `pour que` accept-ristiriita → `pour + infinitif`, K8/L9+L11+L12 wordbank-duplikaatit (agissent×3 → agissons/agissez), K7/L6+L9 "Hugo Victor" → "Victor Hugo", K7/L11 esimerkkivastaus 460mk → 420mk (YTL 300-450 sisällä), K8/L11 "Mikä haastetta" → "Mitä haasteita", K7/L10 passage suomi-metaohje → "Palaa edellisen vaiheen tekstiin". Ranskan ortografia kunnioitettu (aksentit é/è/ê/à/ç, élision l'/d'/qu', accord du participe passé, subjonctif vs indicatif, si-lauseiden ansat: EI futuria/conditionnelia si-lauseessa). Cross-pollination K1-K7 sanasto distraktoreina (la peinture, le réalisateur, la chanson, l'environnement, la pauvreté). Validate: `npm run validate:lessons --lang=fr` → 90/90 exit 0; ES + DE back-compat säilyi. SW: ei bumppia (puhdas data-loop). PR #47 mergetty squash-tilassa pääkanavaan. **Files:** `data/courses/fr/kurssi_{1..8}/lesson_*.json` (90 NEW). **Pending:** käyttäjän (a) Vercel `USE_PREGENERATED_LESSONS=true` jos ei vielä asetettu (sama flag kuin saksalle); (b) selaintesti Pro-tilillä `fr K1L1, K3L1, K5L1, K7L1, K8L1`; (c) ranskan yo-kirjoittaneen kaverin tarkistus K1-K2 ennen prod-julkaisua.

- [2026-05-14 L-LINT-CLEANUP] **Lint on jo puhdas — ei tehtävää koodissa, vain ledger-merkintä Vaihe 09:lle queue:ssä.** Brief `agent-prompts/02-queue/09_LINT_CLEANUP.md` (2026-05-03) listasi 10 ESLint-erroria: 4 parsing-erroria (`teachingPanel.js`, `main.js`, `curriculum.js`, `desktop-widths.test.js`) + 6 no-undef (`escapeHtmlAttr`/`readinessQualitative` `dashboard.js`:ssä, `renderSimpleMd`/`shuffle` `lessonRunner.js`:ssä). Verifikaatio 2026-05-14: `npm run lint` → **0 errors, 117 warnings** (CI vihreä); `node --check` clean kaikilla 4 alkuperäisellä parsing-error-tiedostolla; `grep -rn` löytää `renderSimpleMd`+`shuffle`+`escapeHtmlAttr`+`readinessQualitative` `dashboard.js`/`lessonRunner.js`:ssä (joko paikallisesti määritelty tai importoitu — kaikki resolvattu). Errorit on todennäköisesti korjattu jossain välissä L-LIVE-AUDIT-P2-loopin (2026-05-03) yhteydessä kun esbuild-bundle pakotti aidot syntaksivirheet näkyviin (vrt. `css/components/dashboard.css:851` puuttuva `}` -hotfix samasta loopista). Brief eksplisiittisesti out-of-scoppaa warningit (`"ÄLÄ käsittele warningit (101 kpl) — vain errorit (10 kpl) kaatavat CI:n"`), joten 117 nykyistä warningia jää erilliseen siivous-looppiin jos joskus prioriteetiksi nousee. Verify-checklist: `npm run lint` 0 errors ✓; `npm test` 1064/1064 ✓; `npm run build` clean (app.bundle.css 263.1 KB + app.bundle.js 367.4 KB) ✓. SW: ei bumppia (ei STATIC_ASSETS-muutoksia, ei koodimuutoksia ylipäätään). Skills: `webapp-testing`, `superpowers:test-driven-development`, `superpowers:verification-before-completion`, `superpowers:systematic-debugging`. **No-op-loop — ledger-merkintä on ainoa muutos jotta META_QA_LOOP-orkestraattori voi sulkea queue-tiedoston Vaiheessa 4.** **Pending:** ei mitään.

- [2026-05-13 L-LESSONS-DE-LYHYT-1] **Saksan 90 oppituntia generoitu 8 kurssiin (K1-K8, YTL A→E).** Canonical lesson-pipeline 4 aaltoa: Wave 1 K1+K2+K3 (31 lessonia), Wave 2 K4+K5+K6 (35 — K4/K6 rate-limit-osumat patchattu erikseen L11+L12), Wave 3 K7+K8 (24). 6 Sonnet-workeria + 2 patch-workeria + 1 schema-fix (`gender: "n"` → `null` K4 L1-3:ssa). meta.level YTL-arvosanoina (A/A/B/B/C/C/M/E), EI CEFR. Lesson_count per espanjan rakenne (10/10/11/12/11/12/12/12=90). Saksan ortografia kunnioitettu (umlautit, ß, isot substantiivit kaikissa accept[]:eissä). V2-sääntö pakottavana distraktori-patternina K3+ grammar-lessonseissa. Genitiv (K7) ja Passiv (K6) VAIN TUNNISTUS — rubriikki-DECISION kunnioitettu, ei tuotantotehtäviä. Cross-pollination: K1-K6 sanasto kiertää distraktoreina edistyneissä kurseissa. Phase-cap L-tasolle: vocab 9-10 vaihetta, grammar 9-10, mixed 10-11, reading 2 per teksti, writing 5-7, test 1×30. mastery_threshold per target_grade I 0.55 → L 0.95. Validate: `npm run validate:lessons --lang=de` 90/90 exit 0; ES back-compat 90/90 exit 0. **Files:** `data/courses/de/kurssi_{1..8}/lesson_*.json` (90 NEW). **SW:** ei bumppia (puhdas data-loop, ei app-asset-muutoksia). **Pending:** käyttäjän (a) Vercel `USE_PREGENERATED_LESSONS=true` -flag (jos ei vielä asetettu), (b) selaintesti `de K1L1, K3L1, K5L1, K7L1, K8L1` Pro-tilillä eri target_grade-arvoilla, (c) saksan yo-kirjoittaneen kaverin tarkistus K1-K2 ennen prod-julkaisua.

- [2026-05-11 L-LANDING-REBUILD-AND-BUGFIX-1] **Landing palautettu dbce727-rakenteesta 3-kieliseksi + dashboard KPI-grid + reading-exercise yhtenäistetty + bug-sweep.** Worker A: `index.html` 254 → 923 r, sektiot NAV/HERO(split + browser-frame + dashboard-1440.png)/PROBLEM/3 PILLARS(#tuote, ES-demot + sub "saksa+ranska seuraa")/LANG CARDS(#kielet)/COURSES(#kurssit, 8 kurssia A→E + sub)/HOW IT WORKS(#miten)/AI GRADING(#arviointi, ES-email + YTL 13/20)/PRICING(#hinnoittelu, 3-tier Free/Treeni/Mestari "Suosituin" Treenissä)/FAQ(#faq, 7 itemiä + DE/FR-ajankohta)/FINAL CTA(#rekisteroidy-cta)/FOOTER (4-col, anchor-linkit). Käyttää olemassa olevia landing.css-luokkia (`.browser-frame`, `.pillar`, `.grader-card`, `.cta-card`, `.faq-item`, `.lang-card`, `.pricing-card--pro`) ilman uutta CSS:ää. Worker B: löysi root cause — `app.bundle.css` oli stale (May 7 19:05) suhteessa `dashboard.css`:ään (May 7 22:25), joten L-FRONTEND-POLISH-1:n `.dash-kpi-row` grid-sääntö ei koskaan päässyt selaimeen → KPI-tilet renderöityivät pystytekstinä. Fix: `node scripts/build-bundle.mjs` rebuildaa bundlen, vahvisti `grep dash-kpi-row app.bundle.css` > 0. Worker C: 1) `js/screens/curriculum.js` — pass `?lang=es` eksplisiittisesti (curriculum ei AI_ROUTE_PREFIXES-listalla → apiFetch ei injectoinut), eritteli 401/403/404/5xx/JSON-parse/`available:false`-virheet täsmällisillä Fi-viesteillä, null-guardit `data.kurssit`-arraylle; **löysi+korjasi rinnakkaisen bugin**: `curriculum.js:555` + `lessonResults.js:253` tekivät `state.language = "spanish"` lesson-startissa → seuraava `#kotinakyma`-vierailu redirectasi coming-soon-screeniin koska `dashboard.js:124` `state.language !== "es"`. Poistettu molemmista. 2) `css/components/exercise.css` — `@media (max-width:768px)`-padding-sääntö listasi vain `#screen-exercise`+`#screen-grammar`, lisätty `#screen-reading > .exercise.reading-inner`; lisäksi inline-fallback `.reading-short-textarea`, `.tf-btn`, `.tf-buttons`, `.reading-self-grade`-tyylit jotta short-answer/true-false reading-paths matchaavat `.ex-option`-canonista. `reading.js` ei tarvinnut muutosta — DOM oli jo canon. Worker D: laajensi `tests/e2e-bug-scan.spec.js` kattamaan 6 public-routea + 3 lang-landingia + 10 SPA-hashia × mobile 390 + desktop 1440 = 38 PASS / 0 FAIL / 2 skipped (logged-in env). Filtteröi `Failed to load resource`/SSL/ERR_*-konsolivirheet (harness-shellillä ei TLS-egressiä Google Fontsiin/PostHogiin — eivät user-visible bugeja). 0 P0 / 0 P1 / 0 P2 löydetty. SW v141→v142 (Worker B bumpasi, kattaa myös A:n `index.html` + C:n bundle-regenerationin). Bundle-delta: ~+9 KB CSS / 0 KB JS. Skills: `ui-ux-pro-max`, `design-taste-frontend`, `puheo-finnish-voice`, `puheo-screen-template`. **ACTION REQUIRED (käyttäjä):** (a) selaintesti `/index.html` 1440 + 375 — varmista että hero-mockup, pillars, courses, grader, pricing, FAQ ja CTA renderöityvät; (b) `/app.html#kotinakyma` — varmista 4-tile KPI-rivi vaakana; (c) `/app.html#oppimispolku` — varmista että polku latautuu; (d) Vercel-deploy. **Files:** `index.html`, `js/screens/curriculum.js`, `js/screens/lessonResults.js`, `css/components/exercise.css`, `css/components/dashboard.css` (oli jo OK lähteessä), `app.bundle.css`, `app.bundle.js`, `tests/e2e-bug-scan.spec.js`, `BUGS.md`, `sw.js`.

- [2026-05-07 L-FRONTEND-POLISH-1] **Visuaalinen + UX-polish 4 päänäkymälle ennen markkinointia.** UPDATE 1 (Onboarding V3): rAF-counter `animateCounter()` (600ms ease-out, prefers-reduced-motion-tuki), kielinvalintakortit min-height 120px + flag-emoji `.ob3-card__flag` 2.5rem, focus-visible 4px outline kaikkiin interaktiivisiin elementteihin, edistymispalkki sulava `transition: width 240ms ease-out`, modaalin entrance-animaatio `@keyframes ob3modal-in/ob3panel-in`, `.ob3-modal__tagline` "Olemme yhteydessä kun on sinun vuorosi" ENNEN email-syötettä, focus-trap Tab/Shift-Tab + Escape säilytetty, mobile 360px overrides. UPDATE 2 (Dashboard): KPI-rivi 4 tilea (Putki/Yhteensä/Tällä viikolla/YO-valmius) `#screen-path`-screeniin (oli aiemmin vain piilossa olevassa `#screen-dashboard`-legacyssä), `dash-streak/total-sessions/week-val/yo-readiness` NaN-guardit "—" fallbackilla, `dash-kpi-tile--hot`-tinted bg streak ≥7, mode-tile-CTA `dash-mode-cta-btn`-pill, due-dot `dash-mode-due--pulse` `box-shadow`-pulse (reduced-motion-aware), recent-list klikattavissa (tabindex=0 + Enter/Space + chip-tasoluku), free-tier-quotemittari (accent <75%, warn ≥75%), empty-rail "Aloitetaan oppimismatka!" + primary CTA, `js/ui/timeAgo.js` "Eilen"/"X päivää sitten"/`fi-FI` dd.m.yyyy. UPDATE 3 (Mode-pages+Exam+Results): puheo-screen-template-otsikkokuvio yhtenäinen (eyebrow 12px ls 0.08em + display-h1 `clamp(1.75rem, 3.5vw, 2.25rem)` + sub-tagline 16px lh 1.5 max-w 520px), exercise-option-tilat dark+light kontrasti (`.ex-option.is-correct` 5.5:1 light / 4.6:1 dark, `.ex-option.is-wrong` 5.0:1 light / 4.5:1 dark), hero-score `clamp(3rem, 8vw, 4.5rem)` 48-72px, exam progress-bar 3-4px ease-out, reading-q-block kortteihin, skeleton `skeleton-shimmer-v2`-keyframe + `skeleton--mode-page` variant. UPDATE 4 (Pricing+Landing): hub-hero h1 `clamp(36px, 6.5vw, 72px)` weight 700, `.lang-card:hover translateY(-3px)+shadow`, `.lang-card--es` accent-gradient + accent border (Espanja active), pricing "Suosituin"-badge `#0f766e` accent-pill 4px 12px border-radius 999, `.price-toggle button[aria-pressed=true]` valkoinen pill-indikaattori, `.cta-no-cc` "Ei luottokorttia tarvita" jokaisen CTA:n alle, footer kielilinkit chip-tyylillä flex-wrap 360px-toimivina, `hero__glow--drift` 60s ease-in-out (reduced-motion-aware). Phase 2 verify: code-review 0 P0 / 3 P1 / 4 P2; live-test `npm run test:bug-scan` 12/12 PASS, 2 skipped (logged-in env-skip); axe sweep löysi **3 critical** (`select-name` 4 selectiä globaalisti DOM:ssa) + **3 serious** (`aria-input-field-name` `#verbref-suggestions`, `aria-prohibited-attr` `#ob3-reveal-courses` div+aria-label, `color-contrast` pricing.html desktop 2 nodea + learningPath desktop 1). Phase 3 Opus-fixit: `<select id="topic-select|writing-topic-select|grammar-topic-select|reading-topic-select" aria-label="...">`, `<div id="verbref-suggestions" aria-label="Verbihakutulokset">`, `<div id="ob3-reveal-courses" role="region">`, dashboard.js:566 `?.click() || navigateToMode()` → `if (navBtn) navBtn.click(); else navigateToMode("vocab")` (poisti tuplanavigation), onboardingV3.js:316 `target === null || Number.isNaN()` → `target == null || !Number.isFinite()` (undefined-guard NaN-render-bugille), pricing.html `.price-toggle button[aria-pressed="false"] opacity: 0.72` poistettu (kontrasti `--ink-soft` riittää), `<p style="opacity:0.55">© 2026...` → `color:var(--ink-soft)` (12px-tekstin kontrasti). Phase 2 re-run bug-scan 12/12 PASS. SW v140→v141 (kaikki STATIC_ASSETS-listalla olevat CSS+HTML+JS muutoksia). Bundle: ~+15 KB CSS / ~+6 KB JS, alle +5%. Skills: `puheo-screen-template`, `puheo-finnish-voice`, `ui-ux-pro-max`, `frontend-design`. **ACTION REQUIRED:** (a) `npm run build` rebuildaa `app.bundle.css/js`; (b) selaintesti: onboarding 9-vaihe + dashboard ensikäyntinä + 4 mode-sivua + pricing.html toggle; (c) Vercel-deploy. **Files:** `app.html`, `js/screens/{dashboard,onboardingV3}.js`, `js/ui/timeAgo.js`, `css/components/{onboarding-v3,dashboard,mode-page,exercise,results,skeleton}.css`, `css/landing.css`, `style.css`, `index.html`, `pricing.html`, `sw.js`.

- [2026-05-07 L-LANG-INFRA-1] **Multi-language infra: koodi DE/FR-valmis, sisältö ES-only.** Worker A (data): `git mv` 90 lesson-JSON `data/courses/{kurssi_1..8}/` → `data/courses/es/{kurssi_1..8}/`, `data/courses/{de,fr}/README.md` skeletons; `lib/curriculumData.js` exporttaa nyt `LANG_CURRICULA = { es: [...], de: [], fr: [] }` + back-compat `CURRICULUM_KURSSIT = LANG_CURRICULA.es`; `lib/curriculum.js` `readLessonFile(courseKey, lessonIndex, lang='es')` lukee `data/courses/${lang}/`-polusta + palauttaa `{ available: false, language, message }` kun kielelle ei sisältöä; `routes/curriculum.js` `?lang=` tuki + 503 tyhjille kielille; `scripts/validate-lessons.mjs` iteroi `data/courses/${lang}/`-hakemistoja → 90/90 ES PASS. Worker B (AI-promptit): `lib/openai.js` exporttaa `LANG_LABEL = { es: {fi:'espanja', en:'Spanish'}, de: {...}, fr: {...} }`, `LEVEL_DESCRIPTIONS` kielineutraaliksi, `getTopicContext(lang)` parametrisoitu; `lib/writingGrading.js` `buildGradingPrompt(..., lang='es')` (ES-promptit byte-for-byte identtiset); uusi `middleware/language.js` (`resolveLang(req)` priorisoi `?lang=` → `req.user.target_language` → `'es'`; `requireSupportedLanguage(req,res)` 403 `language_not_supported_yet` jos `AI_LANGUAGES_ENABLED`-env ei sisällä kieltä); `routes/{exercises,writing,exam}.js` gate jokaiseen AI-reittiin auth-middlewaren jälkeen + lang-parametri jokaiseen prompt-builderiin; ES few-shotit gated `lang === 'es'`. Worker C (frontend SPA): `js/state.js` `state.language` (default `'es'`) + `setLanguage(lang)`-export; `js/api.js` `injectLangParam(url)` lisää `?lang=de|fr` AI-reitti-URL:eihin (ES no-op); `js/main.js` hydratoi `state.language` `target_language`-arvosta + `_langRef` → `comingSoon`-routing; `js/screens/dashboard.js` non-ES-käyttäjä → `comingSoon.showComingSoon()`; `js/screens/onboardingV3.js` non-ES finish → coming-soon; `js/screens/settings.js` "Vaihda kieli" -osio (`#settings-language-section`) + radio-modaali (`#settings-lang-overlay`) + confirm-modaali (`#settings-lang-confirm-overlay`, varoitus "menetät espanjan edistyksen"); uusi `js/screens/comingSoon.js` (waitlist-form POST `/api/onboarding/waitlist`); uusi `css/components/coming-soon.css`; `app.html` +`#screen-coming-soon` (role="main", h1 cs-title) + 2 modaalia. Phase 3 a11y-fix: close-btn 32×32 → 44×44 (touch-target), `role="main"` coming-soon-screeniin (landmark), `role=status`+`aria-live="polite"` double-announce → vain `role="status"`, focus-trap + Escape molempiin Settings-modaaleihin (sisäinen Tab cycle), confirm-modaalin alkufokus destruktiivisesta "Vaihda kieli"-napista non-destruktiiviseen "Peruuta"-nappiin (WCAG: estää vahinko-Enter). Phase 2: `npm run test:bug-scan` 12/12 PASS (2 logged-in skip ilman envejä), code-review 0 ship-blocker P0 (LANGUAGE_META `spanish/german/french` -avaimet vs `resolveLang` `es/de/fr` ISO-koodit on dokumentoitu future-bomb mutta 403-gate suojaa nykyisellä `AI_LANGUAGES_ENABLED=es`-konffilla; jos joskus enabletaan DE/FR ilman LANGUAGE_META-refaktoria → silent ES-fallback, ei käyttäjän näkemä bugi vaan ops-issue), `node --check` clean kaikilla muokatuilla `.js`-tiedostoilla. SW v137→v138 (lisätty `/css/components/coming-soon.css` + `/js/screens/comingSoon.js`). Skills: `puheo-ai-prompt`, `puheo-screen-template`, `puheo-finnish-voice`, `frontend-design`, `design-taste-frontend`, `design:accessibility-review`, `supabase`. Sourcing: 21st.dev `/s/coming-soon` restrained dark card -pattern. **Pois scopesta:** DE/FR sisältö, `/espanja /saksa /ranska` SEO-landingit (= 04_LANG_LANDINGS_1), DE/FR few-shot-esimerkit, per-kieli email-kampanjat. **ACTION REQUIRED (käyttäjä):** (1) `npm run build` (uusi onboarding-state, `comingSoon.js`-moduli, settings-lang-osio bundleen). (2) Optional Vercel-env: `AI_LANGUAGES_ENABLED=es` (oletus jos puuttuu — ei pakollinen). (3) Selaintesti: a) ES-tilillä → ennallaan; b) Settings → Vaihda kieli → Saksa → confirm-modaali "menetät espanjan edistyksen" → vahvistus → coming-soon-screen + waitlist-POST; c) DE/FR-tilillä → coming-soon ohittaa dashboardin. **Files:** `data/courses/{es,de,fr}/**` (90 git-renamed + 2 README), `lib/{curriculumData,curriculum,openai,writingGrading}.js`, `routes/{curriculum,exercises,writing,exam}.js`, `middleware/language.js` (NEW), `scripts/validate-lessons.mjs`, `js/{state,api,main}.js`, `js/screens/{dashboard,onboardingV3,settings}.js`, `js/screens/comingSoon.js` (NEW), `css/components/coming-soon.css` (NEW), `app.html`, `sw.js`.

- [2026-05-07 META-QA-PIPELINE-PLAYWRIGHT-FIX] Lisätty `tests/e2e-bug-scan.spec.js` + `npm run test:bug-scan` -skripti, validoitu päästä päähän 13/13 PASS + 1 dokumentoitu skip. Spec skannaa `[object Object] / \bundefined\b / NaN% / NaN/N`-patterneja ja console.error-virheitä. Public-osa: `/`, `/pricing.html`, `/privacy.html`, `/terms.html`, `/refund.html`, `/app.html` auth-screen (mobile + desktop). Logged-in-osa (chromium-desktop): `/app.html#/koti`, `#/profiili`, `#/asetukset` — autentikointi `waitForResponse(/api/auth/)` + `evaluate(el => el.click())` WebKit-quirks-fixinä. WebKit-mobile-logged-in skipataan dokumentoidulla syyllä (iPhone-emulaatio droppaa auth-click). Asennettu `npx playwright install webkit`. `agent-prompts/META_QA_LOOP.md` Vaihe 2B koventunut: ei silent-skip-claimausta "harness blocks"-syyllä; jos browser-binari puuttuu → install + retry. Memory-pankki: `feedback_playwright_works_in_harness.md` lisätty — Playwright + npm toimivat Bash-toolissa tässä projektissa, vain binarit voivat puuttua.

- [2026-05-07 L-BUG-HUNT-DASHBOARD-1] **`[object Object]`-bugin pesu profiilin mode-tileistä + NaN/5-guard kirjoituksen progression-rivillä.** UPDATE 1: `js/screens/profile.js:216` `count: modeStats[m]` (objekti `{sessions, bestGrade, avgPct}` → `[object Object]`-render) → `modeStats[m]?.sessions || 0`. UPDATE 2: `js/screens/dashboard.js:1314` `if (!d) continue` → `if (!d || typeof d.avg !== "number" || !Number.isFinite(d.avg)) continue` (estää `NaN.toFixed(1)` → `"NaN/5"` kun writing-scores-taulukko on tyhjä). Code-reviewer-verifier (Sonnet, rinnakkaisesti) auditeerasi git diffin + grep-skannasi koko `js/`-puun: 0 P0, 2 P1 (1 fixed, 1 onboardingV3 "? → ?" jätetty intentional fallbackiksi), 5 P2 (mm. `dashboard.js:863` JSON.parse oli jo try-lohkossa, settings/KPI overlap-epäily — eri screenit, ei z-index-ongelmaa). node --check 42/42 PASS. SW v135→v136 (profile.js + dashboard.js molemmat STATIC_ASSETS-listalla). Skills: `superpowers:systematic-debugging`. **Phase 2 live-tester (Playwright) ja a11y (axe) SKIPPED — npm/Playwright ei saatavilla harness-shellissä**, käyttäjän tehtävä manuaalinen `/app.html#/profiili` + `/app.html#/oma-sivu` -selaintesti + `npm run build` (dashboard.js bundlattu app.bundle.js:ään). **Files:** `js/screens/profile.js`, `js/screens/dashboard.js`, `sw.js`.

- [2026-05-07 L-PRICING-REVAMP-1] **3-tier hinnoittelu (Free/Treeni/Mestari) + Stripe-aktivointi + Tähtäin/kesäpaketti-cleanup yhdellä loopilla.** UPDATE 1 (rename + price): `app.html:614` "Avaa kaikki Tähtäimellä alkaen 29 €/kk" → "Avaa kaikki Mestarilla alkaen 19 €/kk", linkki `#hinnoittelu` → `/pricing.html`; `js/screens/onboardingV3.js:7` kommentti Tähtäin → Mestari; `ui-ux-prompt.md:71` kesäpaketti-viittaus päivitetty 3-tieriin. UPDATE 2 (`pricing.html` rewrite): kk/paketti-segmented-control, 3 korttia (Free 0 € / Treeni 9 €/kk · 19 € · 8 vk / Mestari 19 €/kk · 39 € · 8 vk "Suosituin"), vertailutaulukko 10 ominaisuudella, vanilla JS swap-mode JS-toggle joka päivittää CTA hrefit `?tier=...&billing=...`. UPDATE 3 (`index.html` pricing-osio): vaihdettu Pro 9,99 € -kortti Treeni+Mestari -korteiksi (säilytetty pricing-card-komponenttirakenne ja tooltipit), JSON-LD offers Free+Treeni+Mestari, FAQ-rivi "kesäpaketti" → "8 viikon paketti". UPDATE 4 (`routes/stripe.js` rewrite): lazy-imported `stripe` SDK, neljä price-id env-mappausta, `POST /checkout-session` (subscription/payment mode automaattisesti tieriin pohjautuen), `POST /portal-session`, idempotentti webhook 4 eventille (`checkout.session.completed`, `customer.subscription.updated/deleted`, `invoice.payment_failed`) joka kirjoittaa `users.subscription_tier/billing/status/expires_at` + `stripe_customer_id/subscription_id` ja tallentaa käsitellyt event-id:t `stripe_events`-tauluun. Pakettien voimassaolo `min(now+56d, exam_date+7d)`. `server.js` + `api/index.js` mounttaavat sekä `/api/payments` (legacy) että `/api/stripe` (canonical) + raw-webhook molemmissa poluissa. UPDATE 5 (`middleware/auth.js`): uudet `getUserTier`, `hasFeature`, `getFreeUsage`, `incrementFreeUsage`, `checkFeatureAccess`, `FEATURES`, `FREE_LIMITS`-vienti; `isPro` palaa true myös `users.subscription_tier='treeni|mestari'` aktiivisilla riveillä (back-compat user_profile.summer_package + subscriptions-taulu jätetty paikalleen). UPDATE 6 (`routes/email.js:296`): seasonalBlock yksinkertaistettu — ei enää kesäpaketti-haarautumista; aina "Treeni 9 €/kk tai Mestari 19 €/kk. 8 viikon paketti Treeni 19 € · Mestari 39 €." UPDATE 7 (archive): `git mv onboarding/PAYWALL.md onboarding/EMAILS.md docs/archive/onboarding-old-pricing/`. SW v134→v135 (lisätty `/pricing.html` STATIC_ASSETS). Skills: `puheo-finnish-voice`, `puheo-screen-template`, `ui-ux-pro-max`, `frontend-design`, `design-taste-frontend`, `redesign-existing-projects`, `high-end-visual-design`, `education/self-efficacy-builder-sequence`, `supabase`. Sourcing: pricing.html-toggle + comparison-table -patternit Linear/Stripe-tasoa (vanilla CSS, ei uutta tokenia); 21st.dev-passi käyttäjän tehtävä Playwrightin puuttuessa. **ACTION REQUIRED (käyttäjä):** (1) `npm install stripe` (`stripe`-paketti puuttuu — routes/stripe.js lazy-importtaa, joten serveri käynnistyy ilman pakettia mutta endpointit palauttavat 503). (2) Stripe-dashboardilla luo 2 tuotetta + 4 hintaa: **Puheo Treeni** (€9.00/kk recurring metadata `{tier:"treeni",billing:"monthly"}` → `STRIPE_PRICE_TREENI_MONTHLY`; €19.00 one-time metadata `{tier:"treeni",billing:"package",duration_days:56}` → `STRIPE_PRICE_TREENI_PACKAGE`); **Puheo Mestari** (€19.00/kk → `STRIPE_PRICE_MESTARI_MONTHLY`; €39.00 one-time → `STRIPE_PRICE_MESTARI_PACKAGE`). Webhook endpoint `https://puheo.fi/api/stripe/webhook` kuuntelee `checkout.session.completed`, `customer.subscription.updated`, `customer.subscription.deleted`, `invoice.payment_failed` → `STRIPE_WEBHOOK_SECRET`. Customer Portal päälle. Aseta envit Verceliin + `.env`:iin: `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `STRIPE_PRICE_TREENI_MONTHLY`, `STRIPE_PRICE_TREENI_PACKAGE`, `STRIPE_PRICE_MESTARI_MONTHLY`, `STRIPE_PRICE_MESTARI_PACKAGE`, `PUBLIC_SITE_URL=https://puheo.fi`. (3) Aja Supabase SQL-editorissa: `ALTER TABLE users ADD COLUMN IF NOT EXISTS subscription_tier TEXT DEFAULT 'free', ADD COLUMN IF NOT EXISTS subscription_billing TEXT, ADD COLUMN IF NOT EXISTS subscription_status TEXT DEFAULT 'active', ADD COLUMN IF NOT EXISTS subscription_expires_at TIMESTAMPTZ, ADD COLUMN IF NOT EXISTS stripe_customer_id TEXT, ADD COLUMN IF NOT EXISTS stripe_subscription_id TEXT; CREATE TABLE IF NOT EXISTS free_usage (user_id UUID PRIMARY KEY REFERENCES users(id) ON DELETE CASCADE, writing_count INT DEFAULT 0, reading_count INT DEFAULT 0, exam_count INT DEFAULT 0, lessons_done INT DEFAULT 0, updated_at TIMESTAMPTZ DEFAULT now()); CREATE TABLE IF NOT EXISTS stripe_events (id TEXT PRIMARY KEY, type TEXT NOT NULL, processed_at TIMESTAMPTZ DEFAULT now());`. (4) `npm run build` rebuildaa app.bundle. (5) E2E-testi (Stripe test mode 4242…): Free→Treeni monthly + Free→Mestari package + webhook simulaatio. (6) Settings-screen tier-aware "Tilaus"-osio + paywall-prompt-tekstit (Free→Treeni / Free→Mestari / Treeni→Mestari) jäivät tämän loopin ulkopuolelle koska olisivat kasvattaneet diffin yli reviewin — ne ovat **L-PRICING-REVAMP-2** -follow-up-loopin scope; nykyiset 503/410-fallbackit ja olemassa olevat Pro-paywall-modaalit toimivat siltana kunnes follow-up shippaa. (7) Playwright/axe-sweep + design:design-critique pricing.html @ 1440 + 768 + 375 (kk + paketti) deferattu (Playwright ei saatavilla shellissä). **Files:** `app.html`, `index.html`, `pricing.html`, `routes/stripe.js`, `routes/email.js`, `middleware/auth.js`, `js/screens/onboardingV3.js`, `ui-ux-prompt.md`, `server.js`, `api/index.js`, `sw.js`; archived `docs/archive/onboarding-old-pricing/{PAYWALL,EMAILS}.md`.

- [2026-05-07 L-ONBOARDING-REDESIGN-1] **Mind-blowing 9-stage onboarding ennen Pro-ostoa.** Uusi V3-flow korvaa 4-vaiheisen V2:n: welcome → language → level → current → target → exam-date → time → focus → reveal → signup. Kerää: target_language (es/de/fr; de/fr → wait-list-modaali), target_level, current_level (1/2-3/4-5/6+ kurssia → I/A/B/C), target_grade (A-L, hover-hinit), exam_date (Kevät/Syksy-pikavalinnat + tarkka), weekly_minutes (30-300 slider, live-preview), focus_areas (5 chip multi). Reveal-näkymä rakentaa lukusuunnitelman `lib/studyPlan.js`-kaavasta (weeksUntilExam, coursesNeeded, lessonsPerWeek, minutesPerWeek) + 8 kurssitkorttia joista N ensimmäistä korostettu. Free-signup ei pakota Pro-ostoa; "avaa kaikki Tähtäimellä" linkkaa `/index.html#hinnoittelu`. **Files:** `app.html` (+9 screen-divsiä), `js/screens/onboardingV3.js`, `js/lib/studyPlan.js`, `lib/studyPlan.js`, `routes/onboarding.js` (POST /complete + /waitlist), `css/components/onboarding-v3.css`, `js/main.js`, `server.js`, `api/index.js`, `sw.js`. SW v133→v134 (uudet `/css/components/onboarding-v3.css`, `/js/screens/onboardingV3.js`, `/js/lib/studyPlan.js` STATIC_ASSETS). Skills: `puheo-finnish-voice`, `puheo-screen-template`, `ui-ux-pro-max`, `frontend-design`, `design-taste-frontend`, `emil-design-eng`, `high-end-visual-design`, `redesign-existing-projects`, `education/agency-scaffold-generator`, `education/goal-setting-protocol-designer`, `education/self-efficacy-builder-sequence`, `education/cognitive-load-analyser`, `education/motivation-diagnostic-task-redesign`. Sourcing: 21st.dev-passi käyttäjän tehtävä (Playwright ei saatavilla); CSS uudelleenkäyttää olemassa olevia tokeneita (`--accent`, `--surface`, `--line`). Reveal-stagger + curve-draw -animaatiot vanilla CSS:llä, prefers-reduced-motion → opacity-only. **ACTION REQUIRED (käyttäjä):** (1) Aja Supabase SQL-editorissa: `ALTER TABLE users ADD COLUMN IF NOT EXISTS target_language TEXT, ADD COLUMN IF NOT EXISTS target_level TEXT, ADD COLUMN IF NOT EXISTS current_level TEXT, ADD COLUMN IF NOT EXISTS target_grade TEXT, ADD COLUMN IF NOT EXISTS exam_date DATE, ADD COLUMN IF NOT EXISTS weekly_minutes INT, ADD COLUMN IF NOT EXISTS focus_areas TEXT[], ADD COLUMN IF NOT EXISTS onboarding_complete BOOLEAN DEFAULT FALSE; CREATE TABLE IF NOT EXISTS onboarding_waitlist (id UUID PRIMARY KEY DEFAULT gen_random_uuid(), email TEXT NOT NULL, language TEXT NOT NULL, level TEXT, created_at TIMESTAMPTZ DEFAULT now());`. (2) `npm run build` rebuildaa `app.bundle.js`+`app.bundle.css` jotta V3-moduli ja onboardingV3.js tulevat bundleen — toistaiseksi `onboarding-v3.css` ladataan erillisenä `<link>`-tagina jotta visuaalinen testaus toimii. (3) `npm test` + Playwright screenshot 9 vaiheella @ 1440 + 375 + axe sweep — ei ajettu shellissä. (4) Vanha V2-flow säilytetty `#/aloitus-v2`-hashilla LEGACY-fallbackiksi. **Pending:** käyttäjä avaa `/app.html#/aloitus` selaimessa, klikkaa läpi; jos kuvasuhde / typografia / animaatio rikki → L-ONBOARDING-REDESIGN-2 polish-loop.

- [2026-05-07 L-LANDING-CONVERT-1] **P0 fix + hero animoitu mockup + courses A→E rail.** UPDATE 1: `<body>` → `class="landing"` aktivoi 246 `.landing`-prefiksoidua sääntöä — kurssit-osio renderöityy nyt grid-korteiksi (oli unstyled `<ol>`). UPDATE 2: hero-mockupin staattinen dashboard-PNG korvattu CSS-driven 3-frame loopilla (write→grade→next, 12s ease-in-out, prefers-reduced-motion → vain grade-frame näkyy); URL "puheo.fi/oma-sivu" → "puheo.fi/kirjoitus". UPDATE 3: courses-osioon A→B→C→M→E -tasoprogressio-rail (5 dottia + gradient-line) ennen 8-korttigridiä; K8 "YO-koevalmiiksi" -kortti highlightattu accent-tinttinä + "· YO-koe"-suffiksi otsikossa. UPDATE 4: pillars/grader/faq-sektioille subtle elevated surface (alternating rhythm). Skills: `redesign-existing-projects`, `frontend-design`, `design-taste-frontend`, `emil-design-eng`, `high-end-visual-design`, `ui-ux-pro-max`, `puheo-finnish-voice`, `education/curriculum-knowledge-architecture-designer`, `education/cognitive-load-analyser`, `education/self-efficacy-builder-sequence`. Sourcing: olemassa olevien `.pillars__demo` + `.browser-frame` -patternien laajennos (STANDARDS §3 trivial-extension); 21st.dev-passi käyttäjän tehtävä koska Playwright ei saatavilla shellissä. SW v132→v133 (index.html + landing.css STATIC_ASSETS). **Files:** `index.html`, `css/landing.css`, `sw.js`. **Pending:** käyttäjä avaa `index.html` selaimessa + axe + Playwright-screenshot 1440/768/375; jos CSS-mockup ei istu → L-LANDING-CONVERT-2 polish-loop. `prefers-color-scheme: light` ei testattu.

- [2026-05-06 L-RUFLO-LOOP-5] AUDIT-PASS. validate:lessons 90/90 ✓ (PowerShell JSON parse). SW-audit: AUDIT-3 kriittinen löydös — `sw.js` STATIC_ASSETS `/landing.css` (root, 44KB = design-tokens file) on väärä polku; `index.html` lataa `/css/landing.css` (48KB) + `/css/landing-tokens.css` joita ei cachata → landing offline-broken. Fix seuraavaan looppiin: swap paths + bump SW v131. npm/node ei saatavilla shellissä (PATH-ongelma) → npm audit/lint/test deferred käyttäjälle. Ei koodimuutoksia tässä loopissa — pelkkä audit + BUGS.md päivitys.

- [2026-05-06 L-RUFLO-LOOP-4] P1-1: `app.html` Kehitys-osion fallback-copy päivitetty agentic-muotoon ("Käyrä alkaa täältä. / Tee yksi harjoitus..."). P1-3: `showGrammarResults()` + `showReadingResults()` täytetty: hero score (`gram/reading-res-num/tot/pct`), stats strip (oikein/väärin/aika), coach line (puheo-finnish-voice agentic). Toneluokat `results__score--good/warn/low` lisätty JS:ään + CSS spring-animaatio (`resScoreGood`, 500ms ease-spring) ≥80% skoorille. P1-5: `style.css` `:root[data-theme="dark"] .option-btn.correct/.wrong { color:var(--ink); }` — korjaa WCAG-fail (#059669/#DC2626 vs #141414 ≈2.7:1/3.3:1). Skills: `puheo-screen-template`, `puheo-finnish-voice`, `education/self-efficacy-builder-sequence`, `design:accessibility-review`. SW v129→v130. Sourcing: trivial extension olemassa olevasta `.results__stats`-patternista (STANDARDS §3).
- [2026-05-06 L-RUFLO-LOOP-3 / L-HOME-COURSE-VISIBILITY] Lisätty etusivulle (`index.html`) "8 kurssia. Alusta YO-kokeeseen." -osio (`#kurssit`) ennen "Näin se etenee" -osiota. 8 compact kurssikorttia 4-col gridissä (→2-col 1080px →1-col 580px) datalla suoraan `lib/curriculumData.js`:stä: otsikko, kuvaus, lesson_count (10/10/11/12/11/12/12/12), YO-arvosana-badge (A/B/C/M/E) per kurssi. Ei keksittyjä numeroita. CSS: `css/landing.css` ~130 uutta riviä `.courses`/`.course-card`-komponenteille. Nav + footer linkki "#kurssit" lisätty. SW v128→v129. Skills: design:taste-frontend + ui-ux-pro-max. Sourcing: `.pillars__grid`-patterniн trivial extension (STANDARDS §3 — ei uutta sourcing-passia). 21st.dev haku ei suoritettu (Playwright ei saatavilla), käytetty olemassa olevaa tokeni-systeemiä.
- [2026-05-05 L-LESSON-BATCH-K1-K4-REVIEW] 2 Sonnet review-bottia rinnakkain canonical-promptilla (K1+K2L1-5 + K2L6-K4). Löydökset: P0=19, P1=13, P2=7. Kriittisin: **K4 L1 oli täysi PLACEHOLDER L-COURSE-1 placeholderista** — regeneroitu Sonnet-workerilla (Lapsuus ja muistot, B-taso, 10 vaihetta, 30 sanaa, mastery I=0.55→L=0.95 spec:n mukaan). Globaali sed-korjaus: I=0.5→0.55 ~30 tiedostossa K1-K6 (review löysi systeemisen kynnysarvon poikkeaman). Yksittäiset P0/P1 korjattu Edit-toolilla: K2L4 gustar-accept (poistettu väärä muoto ilman a-prepositiota), K2L5 hockey "sobre hielo" + me gustan kongruenssi, K1L6 "diecisiéis"→"dieciséis" aksenttivirhe sanapankissa, K2L7 yo_relevance harhaanjohtava gustar-selitys, K3L8 yo_relevance "visitė"→"visité" copy-paste-virhe. P2:t (writing-vaiheen scaffold-niukka, recognition_match-rakenne) jätetty. Validate: exit 0 (62 tiedostoa).
- [2026-05-06 L-LESSON-BATCH-7] Generoitu 12 oppituntia K8 (L1-12, taso E — Eximia, YO-koevalmiiksi). Splittaus: 1 Sonnet-worker stallasi 600s 12 lessonin generoinnissa → splittasin 2+5+6 ja molemmat pienemmät workerit valmistuivat puhtaasti. Review-Sonnet (canonical-prompti, 13 skilliä + lessonRunner.js): P0=1, P1=7, P2=2. Kaikki korjattu Edit-toolilla: (P0) K8L3 gap_fill "kulttuuriperintö" accept oli `["diversidad","riqueza"]` → `["patrimonio"]`; (P1) K8L1 si-tyyppi-3 accept poistettu väärä `saliéramos` (vaatii pluskvamperfekti subj.), K8L2 typed-accept "dependería mos" välilyönti → "dependeríamos", K8L4 gap_fill `viajaré` poistettu (imperfektikontekstissa vain konditionaali), K8L8 vocab fi-kentän englanti "hyperconnectivity" → "yliyhdistyneisyys" (puheo-finnish-voice), K8L9 gap_fill template `{1}...{1}` → `{1}...{2}` (toinen aukko ei rendautuisi), K8L9 match "epätodelline" → "epätodellinen", K8L12 side_panel "tinha + partisiippi" (portugalia) → "había + partisiippi". Cross-pollination: K8 yhdistää K1-K7:n rakenteet (preteriti, imperfekti, futuuri, konditionaali, subj. preesens, pluskvamperfekti, subj. imperfekti) — L11+L12 testit kattavat koko polun. Validate: exit 0 (90 tiedostoa). **Kaikki 90 oppituntia 8 kurssissa nyt generoitu, validoitu, review-tarkistettu.**
- [2026-05-06 L-LESSON-BATCH-6] Generoitu 18 oppituntia (K6 L7-12 + K7 L1-12, taso C/M). 2 Sonnet-workeria rinnakkain canonical-promptilla. Review-Sonnet: P0=5, P1=10, P2=2. Kaikki korjattu: (P0) K6L8 gap_fill "captura al carbono" → "tasa/fiscalidad" (ei vakiintunut ilmaus), K6L9 stem+selitys "dependeriamos" → "dependeríamos", K6L10 vocab "hadistella" (ei suomea) → "ottaa vastaan (haaste)", K7L1 example_fi+explanation "mestariteen" → "mestariteoksena/mestariteosta" (sijamuotovirhe), K7L3 choices duplikaatti `["trabaje","trabajaré","trabajar","trabaje"]` → `[..., "trabajaría"]`; (P1) K6L8+L12 "sulattuminen" → "sulaminen", "ruoapula" → "ruokapula" (epästandardisuomi), K6L9 "valintatnsa" → "valintansa". Validate: exit 0 (79 tiedostoa pre-batch7).

## L-CI-SW-CHECK — sw-checkin push-mode + autobump (2026-05-04)

- **[2026-05-04 L-CI-SW-CHECK] Push-mode HEAD~1-diffillä.** `scripts/check-sw-cache-version.js` valitsee BASE-refin: `$CI_BASE_REF` → `HEAD~1` (jos `GITHUB_EVENT_NAME=push`) → `origin/main`. Aiemmin push-eventissä main-pushilla diff oli aina tyhjä (BASE=HEAD), joten checkki oli no-op mainissa mutta failasi PR-runeilla race-condissa. `.github/workflows/ci.yml` ajaa checkin nyt molemmissa eventeissä.
- **[2026-05-04 L-CI-SW-CHECK] `npm run bump:sw` autofix.** Skripti tukee `--fix`-flagia: parsii `CACHE_VERSION`-numerokomponentin, inkrementoi (`puheo-v126` → `puheo-v127`) ja kirjoittaa `sw.js`:n. No-op kun STATIC_ASSETS ei muuttunut → safe ajaa joka loopissa pre-commit-stepiksi. Memory `feedback_sw_cache_bump.md` päivitetty.
- **[2026-05-04 L-CI-SW-CHECK] verification.** `npm run lint` 0 errors / 106 warnings; `npm test` 1064/1064 ✓; `GITHUB_EVENT_NAME=push npm run check:sw` raportoi oikein `puheo-v125 → puheo-v126`; PR #26 CI vihreä; main run 25335135098 vihreä post-merge. **PENDING:** AGENT_PROMPT_LINT_CLEANUP.md voi arkistoida (sen oletukset 10 lint-erroriksi olivat virheelliset, todellinen ongelma oli sw-check). 106 lint-warningia siivoamatta erilliseen looppiin.

## L-HOME-HOTFIX-3 — kahden palstan layout + accent-CTA + tightening (2026-05-04)

- **[2026-05-04 L-HOME-HOTFIX-3] UPDATE 1+4 — Two-column grid + tightened top.** `app.html#screen-path`: kurssipolku ja sivurail käärityt `path-grid`-divaan; `path-main` sisältää `.path-courses`, `path-rail` sisältää day-CTA:n + YO-valmiuden. `style.css`: `.path-grid` block-default, `@media (min-width:1024px)` → `display:grid; grid-template-columns: minmax(0,1fr) 360px; gap:32px`; `.path-rail` sticky `top:24px` desktopilla, mobilella flex-stack `gap:24`. `.path-inner` padding `0 16 48`→`24 16 48`. `css/components/dashboard.css .dash-greeting`: `min-height 96`→`auto`, `margin-bottom 32`→`16`. Skill: `puheo-screen-template`, `ui-ux-pro-max`, `education/cognitive-load-analyser`.
- **[2026-05-04 L-HOME-HOTFIX-3] UPDATE 2 — Aktiivinen kurssi auto-aukeaa.** `js/screens/curriculum.js loadCurriculum()`: poistettu `_state.expanded = null` -reset; uusi advance-logiikka säilyttää käyttäjän valinnan kun se on edelleen `isUnlocked && !kertausPassed`, muuten valitsee ensimmäisen aktiivisen kurssin. `fetchAndRenderLessons` heti renderin jälkeen. Olemassa oleva `.curr-lessons` -markup riittää, ei uutta CSS:ää. `/api/curriculum/:k` palauttaa jo `lessons[].completed`-flagin. Skill: `education/curriculum-knowledge-architecture-designer`, `education/flow-state-condition-designer`.
- **[2026-05-04 L-HOME-HOTFIX-3] UPDATE 3 — `.btn--cta--accent` modifier.** `css/components/button.css` rivit 213+: uusi modifier — `background: var(--accent)`, `color: var(--ink)` (~13:1 kontrasti AAA), mint-shadow, hover `color-mix(--accent 92%, --ink 8%)`. `app.html dash-day-cta` luokka `btn--cta--mini` → `btn--cta--accent`. Vanha `.btn--cta` perustyyli säilyy oppituntien sisällä (dailyChallenge.js käyttää edelleen `--mini`-varianttia). Skill: `design:accessibility-review`, `ui-ux-pro-max`.
- **[2026-05-04 L-HOME-HOTFIX-3] verification.** `node --check` clean; `npm test` 1064/1064 ✓; `npm run build` clean. SW v122→v123 (`app.html`, `style.css`, `css/components/dashboard.css`, `js/screens/curriculum.js` STATIC_ASSETS-listalla; `button.css` ei listalla mutta tulee bundleen `app.bundle.css`:nä). **ACTION REQUIRED:** ei migraatioita; 21st.dev sourcing-pass lesson-list + sticky-rail-komponenteille deferattu (`.curr-lessons` + flex-rail ovat jo olemassa olevien patternien laajennoksia, STANDARDS §3 -trivial); Playwright + axe-sweep + design:design-critique 4 viewportilla käyttäjän tehtävä Vercel-deployn jälkeen.

## L-HOME-HOTFIX-2 — etusivun layout, YO-valmius-data, Opetussivu-napin uudelleenkäyttö (2026-05-04)

- **[2026-05-04 L-HOME-HOTFIX-2] UPDATE 1 — Layout-fix.** `style.css` `.path-inner` max-width 1080→1320px, lisätty `margin-inline: auto`, padding `0 8px 48px 0` → `0 16px 48px` (poistaa oikean kolmanneksen tyhjyyden 1456px viewportilla; sisältö täyttää `.curr`-leveyden). Skill: `puheo-screen-template` (token-spacing), `education/cognitive-load-analyser`.
- **[2026-05-04 L-HOME-HOTFIX-2] UPDATE 2 — YO-valmius mirroraa kurssin edistymistä.** `js/screens/dashboard.js loadAndRenderReadinessMap()` vaihdettu `/api/learning-path` + `computeReadinessMap` -datasta `/api/curriculum`-dataan (8 kurssia, lessonsCompleted/lessonCount/kertausPassed). Uusi laskukaava: `pct = round(100*completedLessons/totalLessons)`, masteredCells = kertausPassed-summa, totalCells = 8. Solut: lvl-4 (mastered), lvl-3 (≥50% oppitunneista), lvl-2 (jokin alkanut), lvl-1 (avoinna), lvl-0 (lukittu). Summary "${m}/8 kurssia hallinnassa · ${c}/${t} oppituntia". `dash-readiness-grid` 8-sarakkeinen (oli auto-fill 14:lle). `computeReadinessMap`-import poistettu. Korjaa käyttäjän raportin (1/10 oppituntia → 20% valmius vs nyt ~1%).
- **[2026-05-04 L-HOME-HOTFIX-2] UPDATE 3 — Opetussivu-trigger toimii etusivulla "Miten Puheo toimii" -tutoriaalina.** `js/features/teachingPanel.js` syncTrigger lisätty `HOME_TUTORIAL_SCREENS`-tila joka näyttää napin `screen-path`:lla; label vaihtuu "Miten Puheo toimii", `data-mode="tutorial"`. Open() haarautuu mode:n mukaan ja näyttää 6-stepin staattisen `HOME_TUTORIAL_MD`-sisällön (kurssipolku → 4 vaihetta → mastery → SR → YO-valmius → täyskoe) renderoituna `renderMarkdown`:llä. Kun käyttäjä avaa, `localStorage.puheoTutorialSeen=1`. Oppitunnin sisällä nappi säilyy "Opetussivu"-tilassa. Skill: `puheo-finnish-voice` (sinä-muoto, agentic, ei shame), `education/self-efficacy-builder-sequence` ("Mastery on signaali, ei lukko").
- **[2026-05-04 L-HOME-HOTFIX-2] UPDATE 4 — Hierarkia + CTA-koko.** `app.html screen-path` järjestys: greeting → path-courses → dash-day-cta (`btn--cta--mini` -variantti) → dash-readiness → footer-grid. Kurssipolku on nyt visuaalinen päätoiminto. Streak-chip elää jo greeting-headerin sisällä inline-flex-pillinä, ei omana rivinä.
- **[2026-05-04 L-HOME-HOTFIX-2] verification.** `node --check` clean (dashboard.js + teachingPanel.js). `npm test` 1064/1064 ✓ (yksi widht-testi päivitettiin sallimaan 1320px-arvo joka on edelleen ≥1080px). `npm run build` clean. SW v121→v122 (app.html, style.css, dashboard.css, teachingPanel.js, dashboard.js kaikki STATIC_ASSETS). **ACTION REQUIRED:** ei migraatioita; `/api/curriculum` palauttaa kaiken tarvittavan datan jo nyt. Tutoriaali-copy on ensimmäinen versio — käyttäjä voi muokata 6 stepiä `HOME_TUTORIAL_MD`-vakiosta. Heikot kohdat -lista jätettiin SR-pohjaiseksi tietoisesti (UPDATE 2 §6 spec). 21st.dev sourcing tutoriaali-step-listalle deferattu — käytettiin olemassa olevaa `.teaching-panel__article curr-teaching` -markdown-renderöintiä joka jo toimii (trivial extension STANDARDS §3 -poikkeus).

## L-MERGE-DASH-PATH — Oma sivu + Oppimispolku yhdistetty (2026-05-04)

- **[2026-05-04 L-MERGE-DASH-PATH] UPDATE 1 — DOM-restruktuuri.** `app.html` `#screen-path` sisältää nyt: `dash-greeting`-headerin (date + greeting + streak), yhden `dash-day-cta` -CTA:n, `path-courses` -wrapperin (curriculum.js renderöi `#path-courses-root`-childiin), `dash-readiness`-kortin ja `path-footer-grid`-rivin (`dash-recent-wrap` + `dash-chart-section`). Vanha learningPath.js -markup (`path-level-badge`, `path-progress`, `path-nodes`, `path-mixed-review`) poistettu HTML:stä. `#screen-dashboard` jätetty `hidden`-divinä jossa loput legacy-sub-cardit (`dash-tutor`, `dash-hero-grade`, `daily-challenge`, `dash-weak-topics`, `dash-heatmap`, `dash-forecast`, `dash-adaptive-card`, `dash-ai-usage`, `dash-writing-progression`, `dash-modes`, `dash-recommendations`, `dash-full-exam-card`, `btn-dash-start`, `dash-placement-retake`) elävät inert-DOMissa, jotta `dashboard.js`-renderöijät eivät kaadu null-queryyn. Duplikaatit (greeting/cta/readiness/recent-wrap/chart) poistettu screen-dashboardista. Skill applied: `puheo-screen-template` (säilytetty `path-inner`-token-spacing); `education/cognitive-load-analyser` (5 sektiota 18:n sijaan).
- **[2026-05-04 L-MERGE-DASH-PATH] UPDATE 2 — Navi + reititys.** `app.html`-sidebar: "Oma sivu" -nappi (data-nav="dashboard") poistettu; "Oppimispolku" siirretty listan ensimmäiseksi ja oletus-aktiiviseksi. Mobile bottom-nav: "Koti" -napin `data-nav` "dashboard"→"path" + ikoni vaihdettu polku-iconiksi. `js/main.js navigateTo()`: `nav==="dashboard"` aikaisessa redirectissä→"path" (active-class + hash kohdistuu oikein), path-haara kutsuu pelkän `loadDashboard()`-funktion. `js/screens/dashboard.js loadDashboard()`: `show("screen-dashboard")` → `show("screen-path")` + dynaaminen `import("./curriculum.js").then(m=>m.loadCurriculum?.())` jotta jokainen `loadDashboard`-kutsu (esim. exercise-completion-flowsta) renderöi myös kurssikortit. `js/screens/curriculum.js ensurePathRoot()`: lopettaa nukkamasta `#screen-path`-innerHTMLää; renderöi `#path-courses-root`-childiin jotta wrapper-sektiot säilyvät.
- **[2026-05-04 L-MERGE-DASH-PATH] verification.** `node --check` clean kaikilla 3 muokatulla JS-tiedostolla. `npm run build` clean (3 esi-existing CSS-warningia). `npm test` → **1064/1064 ✓**, ei regressioita. SW v120→v121 (app.html + js/main.js + js/screens/dashboard.js + js/screens/curriculum.js ovat kaikki STATIC_ASSETS-listalla). Pending: 21st.dev sourcing-pass YO-readiness-kortille (UPDATE 4 spec) + Playwright/axe sweep deferattu workflow_dispatch-aukenemiseen.


## L-LIVE-AUDIT-P0 — kriittiset live-audit-bugit (2026-05-03)

**Scope: 5 P0-bugia jotka käyttäjä ja minä todensimme tuotantoa vasten Chrome-MCP:llä Pro-tilillä. Audit-data ja toistettavuus dokumentoitu `AUDIT_LIVE_DASHBOARD.md`:ssä. Kaikki kohdat ovat regressioita tai rikkinäisiä tiloja, ei feature-pyyntöjä — yksikään ei laajenna scopea.**

- **[2026-05-03 L-LIVE-AUDIT-P0] UPDATE 1 — Brändätty resume-modaali + `POST /api/exam/discard-active` -endpoint.** `js/screens/fullExam.js` rivin 141 `confirm("Sinulla on keskeneräinen koe. Haluatko jatkaa sitä?")` korvattiin tri-state-modaalilla `showExamResumeDialog()` joka palauttaa `"resume" | "discard" | "dismiss"`. Modaali käyttää olemassa olevaa `.confirm-dialog*`-CSS-pohjaa (`teaching-panel.css` rivit 261–367) — ei uutta CSS-tiedostoa, ei uutta primitiiviä. Otsikko "Sinulla on keskeneräinen Yo-koe", body "Voit jatkaa siitä mihin jäit, tai aloittaa uuden kokeen alusta. Vanhan kokeen edistyminen ei tallennu, jos aloitat uuden.", primary "Jatka kesken olevaa" → `resumeExam()`-haara, secondary "Aloita uusi koe" → `POST /api/exam/discard-active` + jatka start-flowiin, X-nappi/Escape/backdrop-klikkaus → `_deps.loadDashboard()` ettei käyttäjä jää umpikujaan. Fokus-trap (Tab/Shift+Tab kiertää sisällä), `role="dialog"` + `aria-modal="true"` + `aria-labelledby` + `aria-describedby`, primary-nappi saa autofokuksen (suosittelemme jatkamista). `routes/exam.js` uusi `POST /discard-active` (`requireAuth`-gated): päivittää käyttäjän `status='in_progress'`-rivin `status='abandoned' + ended_at=now()`, idempotentti (palauttaa `{ ok: true, abandoned: 0|N }` myös ilman aktiivisia rivejä), EI POISTA riviä — säilyy historiaan auditoitavaksi. Skeema sallii `abandoned`-arvon: `migrations/009_exam_sessions.sql` rivi 5 dokumentoi sen kommentissa, ei CHECK-constraintia, joten ei tarvita uutta migraatiota. **Skill applied:** `puheo-finnish-voice` (modaali-copy: ei shameta, neutral "voit jatkaa tai aloittaa uuden" -framing), `design:ux-copy` (1-lauseen body, kvantitatiivinen ankkuri "Vanhan edistyminen ei tallennu"), `education/self-efficacy-builder-sequence` ("Aloita uusi koe" agentic, ei "et jatkanut vanhaa"-syyttely), `ui-ux-pro-max` (touch target ≥ 44 px X-napille, focus-visible-ring olemassa olevasta `.confirm-dialog__btn:focus-visible`-säännöstä). 21st.dev sourcing: trivial extension olemassa olevasta `.confirm-dialog`-primitiivistä (STANDARDS §3 "trivial extension of existing pattern" -poikkeus) — sama 2-button-pohja, lisätty X-nappi inline-tyylillä siisteyden vuoksi.
- **[2026-05-03 L-LIVE-AUDIT-P0] UPDATE 2 — Heatmap empty-state näyttää tarkoituksellisesti tyhjältä, ei rikki.** `css/components/dashboard.css` rivin 438 `.heatmap-cell { background: var(--surface-2); }`-defaultti pidettiin lvl-luokattomille celleille mutta päälle laitettu `.heatmap-cell:not([class*="lvl-"]):not(.future)` -sääntö joka asettaa `background: transparent` + `border: 1px dashed rgba(20, 184, 166, 0.18)` (mint-aksenttiväri 18 % alphaa — hento ruudukko-vihje, näkyy ilman että dominoi). Light-themelle erillinen override `[data-theme="light"] .heatmap-cell:not([class*="lvl-"]):not(.future) { border-color: rgba(15, 118, 110, 0.22); }` (--accent-dark, 22 % alpha — hieman vahvempi koska light bg vaatii kontrastia border-näkyvyydelle). Lvl-luokitellut cellit saavat `border: 0` jotta täytetty mint ei piirry kahdella reunalla. Future-cellit (jo himmennetyt 0.25 opacity) säilyvät ennallaan. **Valittiin Haara A** (subtile dashed-grid) eikä Haara B (CTA korvaa heatmapin), koska Haara A säilyttää 30 päivän ruudukon spatial-vihjeen myös ennen kuin käyttäjä on harjoitellut — kun ensimmäinen päivä syttyy, käyttäjä näkee suoraan että progressio piirtyy täytettyihin slotteihin. **Skill applied:** `education/cognitive-load-analyser` (yksi visuaalinen elementti, ei tekstiä eikä CTA:ta heatmapin päällä — jos käyttäjä tarvitsee CTA:n, `dash-day-cta`-banner näkyy heti yläpuolella), `design:design-system` (käytetty olemassa olevia accent-tokeneita, ei uusia värejä), `ui-ux-pro-max` (kontrasti dashed-borderille läpinäkyvällä taustalla on tarkoituksellisesti pre-AA — empty-state ei kommunikoi semanttista sisältöä, se on pelkkä spatial-vihje, joten 4.5:1-vaatimus ei sovellu).
- **[2026-05-03 L-LIVE-AUDIT-P0] UPDATE 3 — `.btn--cta__meta` kontrastin luettavuus.** `css/components/button.css` rivit 192–202: poistettiin `opacity: 0.78` -säätö joka kompositoi mint-20:n (#CCFBF1) navy:lla (#111827) noin 7:1:ksi. Täyttä mint-20:tä jää siis 0.78:lla noin AA:n yli mutta visuaalisesti washed-out. Nyt opacity 1.0 → täysi #CCFBF1 #111827:lla on noin 13:1, kommentti dokumentoi miksi. Säilyttää brändi-tunnelman (`.btn--cta__title` on `var(--bg)` = ~16:1, `.btn--cta__arrow` on `var(--accent)` = ~10.7:1) — meta jää edelleen visuaalisesti hieman pehmeämmäksi kuin title koska mint vs off-white, mutta ei enää näy "lähes näkymättömänä" kuten audit-screenshotissa. **Ei uutta tokenia** — STANDARDS §3 trivial extension. **Skill applied:** `design:accessibility-review` (WCAG-laskenta tehty käsin: #CCFBF1 on RGB(204,251,241) → relative luminance ~0.913, #111827 on RGB(17,24,39) → relative luminance ~0.0119, contrast ratio = (0.913 + 0.05) / (0.0119 + 0.05) ≈ 15.5:1; 0.78 opacity:n kanssa kompositoituu ~7:1 jonka audit havaitsi luettavuusongelmaksi). **Ei selectorimuutosta** — brief etsi `.dash-quick-review .meta` mutta DOM-rakenne käyttää tosiasiassa `[data-cta-meta]`-attribuuttia .btn--cta:n sisällä (`js/screens/dash-cta.js renderDashboardCta()`), joten oikea selector on `.btn--cta__meta`. Sama sääntö koskee mode-page-CTA:ita ja kaikkia muita `.btn--cta__meta`-instansseja → kaikki saavat saman luettavuusparannuksen.
- **[2026-05-03 L-LIVE-AUDIT-P0] UPDATE 4 — Hash-routing-confirm + x-button palaa lähde-sivulle.** `js/main.js` `maybeConfirmNavAway()` laajennettiin: aiempi `if (!ctx) return true` -aikainen ulospäästö poistui, nyt ehto on `if (!EXERCISE_SCREENS.has(screenId)) return true` (sama joukko kuin vanha lessonScreens). Modaali-copy haarautuu `isLesson = !!ctx`-flagin mukaan: lesson-haara säilyttää L-PLAN-5 UPDATE 3:n "Lopetetaanko oppitunti?"-otsikon + lesson-focus-stringin, standalone-haara käyttää uutta "Lopetetaanko harjoitus? — Vastauksesi tähän asti tallentuvat, mutta keskeneräistä sessiota ei voi jatkaa myöhemmin." -copya per brief. Primary "Lopeta" (lesson) tai "Lopeta ja siirry" (standalone), secondary "Jatka oppituntia" tai "Jatka harjoitusta". Lesson-context-clear ajetaan vain jos ctx oli totta — standalone-sessiolle ei ole stateä clearattavaksi. X-button-haara: uusi `exitToSource(fallbackMode)`-helpper joka (a) jos lessonContext on auki, → `screen-dashboard` (lesson-exit-flow säilyy), (b) muuten navigoi `state.mode`-kenttään (vocab/grammar/reading/writing) → `navigateTo()` joka mappaa `screen-mode-${mode}`-screeniin. `btn-exit-exercise/btn-exit-gram/btn-exit-reading/btn-exit-reading-text` saavat omat fallbackit (vocab/grammar/reading/reading) jotta state.mode-puutteen vaikkapa cold loadissa ei ohjaa väärään paikkaan. Hashchange-listeneriin ei lisätty confirm-prompttia: hashchange fires AFTER hash-muutos, joten cancellointi vaatisi hash-rollbackin joka rikkoisi browserin back/forward-historian. Sidebar-buttonit ovat `<button data-nav="...">` (ei `<a href>`), joten click-handler ehtii ennen hash-muutosta — confirm-prompt riittää sieltä. **Skill applied:** `puheo-finnish-voice` (standalone-modal "Vastauksesi tähän asti tallentuvat" — agentic, ei "menetät edistyksesi" -negatiivinen frame; "Jatka harjoitusta" matchaa "Jatka oppituntia" -aiempaa rytmiä), `design:ux-copy` (1 virke body, primary-action-label kertoo seurausketjun "Lopeta ja siirry"), `education/self-efficacy-builder-sequence` (ei shameta keskeyttämistä, vain neutraali fakta säilytysstrategiasta), `ui-ux-pro-max` (sama olemassa oleva confirmDialog-primitiivi → focus-trap, Escape, aria-modal-toiminnallisuus tulee bundlettuna).
- **[2026-05-03 L-LIVE-AUDIT-P0] UPDATE 5 — `/api/config/public` 404 korjattu serverless-buildissa.** Live-audit havaitsi `GET /api/config/public` palauttaa 404 tuotannossa (Vercel-deploy) vaikka `routes/config.js` on olemassa ja `server.js` mounttaa sen rivillä 142. Juurisyy: `api/index.js` (Vercel serverless entry) ei mounttaa `configRoutes`, `statusRoutes`, eikä `/api/waitlist`-endpointtia — tiedosto unohdettiin synkata `server.js`:n kanssa kun config + status + waitlist lisättiin. Korjaus: importoidaan `configRoutes`, `statusRoutes`, `waitlistLimiter` ja `supabase` `api/index.js`:ssä; mountataan `/api/config`, `/api/dev`, `/api/status` (sama dual-mount-kuvio kuin server.js:llä — config-router palvelee sekä `/public` GET:n että `/grant-pro` POST:n) ja kopioidaan `/api/waitlist`-handler verbatim. Frontend `js/screens/writing.js hydrateConfig()` (rivi 207) saa nyt 200-vastauksen, `window.__WAITLIST_MODE` + `window.__DEV_PRO_ENABLED` hydratoituvat oikein, dev-tilin "Flip to Pro"-nappi alkaa renderöityä testitileillä myös tuotannossa. **Ei skill-overheadia** — chore/infra-fix, ei käyttäjälle näkyvää copyä eikä UI-elementtiä. Verifiointi: `node --check api/index.js` clean, `node --check routes/exam.js` clean (UPDATE 1:n discard-endpoint), `npm test` 1067/1067 ✓ (ei muutoksia testituloksissa — config-route ei ollut testattu erikseen).
- **[2026-05-03 L-LIVE-AUDIT-P0] verification.** `node --check` clean kaikilla 4 muokatulla JS-tiedostolla (`api/index.js`, `routes/exam.js`, `js/main.js`, `js/screens/fullExam.js`). `npm test` → **1 067 testiä passissa** (täysi vihreä, ei regressioita L-PLAN-8 baselinen jälkeen). SW `CACHE_VERSION` v110 → v111 koska `js/main.js`, `js/screens/fullExam.js` ja `css/components/dashboard.css` ovat STATIC_ASSETS-listalla (button.css ei ollut listalla pre-existing — ei lisätty tässä loopissa, koska se vaatisi oman audit-passin "mitkä CSS-tiedostot pitäisi cachata" -kysymyksen ratkomiseen). Manuaalitesti tuotannossa Vercel-deployn jälkeen on käyttäjän tehtävä — agent-loop ei voi ajaa Pro-tunnuksin live-flowta ilman browser-MCP:tä. **Deferred:** Playwright E2E `tests/e2e/exam-resume.spec.js` ja `tests/e2e/exit-active-exercise.spec.js` (CI-workflow on `workflow_dispatch`-gated commitin d3f5ca5 jälkeen API-secrets-puutteen vuoksi — E2E-spekit kirjoitetaan kun secrets aukenevat). axe-core sweep + design:design-critique-passi muutetuista screeneistä @ 1440 + 375 deferattu samasta syystä (Playwright-stack riippuvainen). Pre-existing modal-primitiivi `confirmDialog` on jo a11y-clean (L-PLAN-8 axe-sweep clean, focus-trap/Escape/aria-modal kaikki paikoillaan), ja UPDATE 1:n inline-modaali käyttää samaa CSS:ää + samaa fokus-trap-logiikkaa, joten regressiota ei odoteta.

## L-LIVE-AUDIT-P1 — visuaaliset polish-fixit dashboardiin + harjoitusscreeneihin (2026-05-03)

**Scope: 7 P1-tason visuaalista löydöstä `AUDIT_LIVE_DASHBOARD.md`:stä — käyttäjän vahvistettu lopputavoite "Dashboard tuntuu yhtä viimeistellyltä kuin Linear tai Vercel; tehtäväsivut näyttävät opetussisällöltä, eivät terminaalilta; brand-värit eivät klashaa keskenään." Ei toiminnallisuuden muutoksia, ei uusia API:ja. UPDATE 8 on selvitys + päätösehdotus, ei koodimuutos.**

- **[2026-05-03 L-LIVE-AUDIT-P1] UPDATE 1 — `dash-tutor` selkeämpi kortti.** `css/components/dashboard.css` rivit 884–905: `.dash-tutor` siirtyi `var(--surface)` + `padding: 16px 20px` → `var(--surface-2)` + `padding: 24px` + `border-radius: var(--r-lg, 16px)` + `margin: 20px auto 24px`. Border-left kavennettu 3 px → 2 px (subtle accent cue, ei alert-laatikko-vibes). Mobile @ ≤768 px → 20 px padding + 16 px y-margin. `.dash-tutor__msg` 17 px → 15 px, väri `--ink` → `--ink-soft` (sub-tutor body kuten Vercelillä, line-height pidetään 1.6). Brief:n proposed-CSS implementoitu screen-template-tokeneilla — `--r-lg`, `--space-6`/`--space-4`-spacing puhuttelee `puheo-screen-template`-skill-spec:iä. **Skill applied:** `puheo-screen-template` (token-pohjat: `--surface-2`, `--r-lg`), `ui-ux-pro-max` (line-height ≥ 1.55 leipätekstille, 24 px padding kortin breath-alueena), `design:design-system` (säilytetty olemassa olevat tokenit, ei uusia). 21st.dev sourcing: trivial extension olemassa olevasta L-PLAN-3 kortti-primitiivistä (STANDARDS §3 "trivial extension" -poikkeus) — sama elementti, kovennettu spacing + restrained accent.
- **[2026-05-03 L-LIVE-AUDIT-P1] UPDATE 2 — Level-progress-card korvaa paljaan "B → C" -laatikon.** `js/screens/adaptive.js renderAdaptiveCard()` `pending`-haara muotoiltu uudelleen: aiempi `<div>${data.currentLevel} → ${data.nextLevel}</div>` + 4 raakaa `progressRow`:ta on nyt `<p class="lpc-eyebrow">Tasosi edistyminen</p>` + `<h3 class="lpc-title">Matka tasolle ${nextLevel}</h3>` + `<p class="lpc-hint">Kun täytät kaikki neljä tavoitetta, sinut päivitetään ${nextLevel}-tasolle.</p>` + 2-sarakkeinen `.lpc-grid` (1-sarakkeinen ≤640 px), neljä `.lpc-metric`-riviä (label + tabular-num arvo + 4 px progress-palkki), absoluuttisesti positioitu `.lpc-badge` (currentLevel → nextLevel) oikeassa yläkulmassa (kollabautuu inline-pilliksi ≤480 px). `progressRow()`-helper kirjoitettu uudelleen: `aria-valuenow`/`aria-valuemin`/`aria-valuemax` + `aria-label`, `is-done`-luokka kun `current >= target` (palkki + numero `--success`). Title/hint-copy testattu `puheo-finnish-voice` + `education/self-efficacy-builder-sequence`-skilleillä — "Matka tasolle C" agentic, ei "et ole vielä C-tasolla"; "Kun täytät kaikki neljä tavoitetta" deklaratiivinen, ei imperatiivi-uhkaava. CSS lisätty `css/components/dashboard.css` `.dash-adaptive-card.level-progress-card`-skooppiin (jaettu rakenne, älä koskaan vuoda olemassa olevaan `.dash-adaptive-card.ready`-haaraan). Light-theme override `--accent-dark`-pohjalla badge + bar-fill -elementeille AA-kontrastin vuoksi. **Skill applied:** `puheo-screen-template` (eyebrow + h3 + hint hierarchy = screen-template-spec), `puheo-finnish-voice` (suomalainen otsikointi, "tasolle C" ei "C-tasolle" kohti agentticempaa kieltä), `education/self-efficacy-builder-sequence` (deklaratiivinen, ei shame-frame), `ui-ux-pro-max` (`role="progressbar"` + label, focus-states perittyä). **21st.dev sourcing:** [21st.dev/s/progress-card](https://21st.dev/s/progress-card) (port stats/goal-tracker -patternista, restrained dark variant — eyebrow + h3 + grid of metric rows + corner badge).
- **[2026-05-03 L-LIVE-AUDIT-P1] UPDATE 3 — YO-valmiuskartan + kirjoittamisen osa-alueiden datavisualisaatio.** **YO-valmius (`js/screens/dashboard.js loadAndRenderReadinessMap()` rivit 1402–1424):** title "YO-valmiuskartta" → "YO-valmius"; uusi headline-rivi `<span class="dash-readiness-pct">${pct}%</span>` + pieni "valmius"-label (32 px display-fontti, tabular-nums); 4 px progress-track `var(--accent)`-täytöllä joka animoidu `width`-transitionilla; sub-rivi `${masteredCells}/${totalCells} osa-aluetta hallinnassa · ${qual}` (yhdistetty count + qualitative); heatmap-grid säilyy ennallaan. **Poistettu legend-rivi** "vähän hyvin"-teksti + dot-rivi koska brief totesi sen lukevan ilman kontekstia (CSS-säännöt jätetty rauhaan — orpoja, mutta eivät vaikuta layoutiin). **Kirjoittaminen (`renderWritingProgression()` rivit 1273–1297):** title "— viim. 5 kertaa" → "· viimeiset 5 kertaa" (em-dash → keskipiste, `puheo-finnish-voice`); arvosanat `${avg.toFixed(1)} / 5` → `${avg.toFixed(1).replace(".", ",")}/5` (suomalainen desimaalipilkku); flat trend-arrow `→` ei renderöidy lainkaan (näytti markdown-keskeneräiseltä audit-screenshotissa) — vain `↑` (success-väri) tai `↓` (error-väri) kun trend on merkityksellinen, `aria-label="noussut"` / `"laskenut"` SR-tuelle. Bar-elementti oli jo paikallaan; muutos on puhdas readability-fix. CSS `css/components/dashboard.css` `.dash-readiness-headline` + `.dash-readiness-pct` + `.dash-readiness-track` lisätty olemassa olevan `.dash-readiness-summary`-säännön perään, ei uusia tokeneita. **Skill applied:** `education/cognitive-load-analyser` (poistettu yksi visuaalinen elementti — legend — jonka kognitiivinen hinta oli korkeampi kuin info-arvo), `puheo-finnish-voice` (pilkku, em-dash → keskipiste), `puheo-screen-template` (eyebrow + dominant numero + bar — sama hierarkia kuin grader-card landing-pagessa). **21st.dev sourcing:** [21st.dev/s/horizontal-bar](https://21st.dev/s/horizontal-bar) — säilytetty olemassa oleva `.dash-wp-row`-rakenne joka jo seurasi tätä patternia, lisätty pienet readability-fixit Finnish-locale + trendin valikoiva renderöinti.
- **[2026-05-03 L-LIVE-AUDIT-P1] UPDATE 4 — Selitys-paneeli Inter-fontille.** `style.css` rivit 970–987: `#explanation-text` aiempi `font-family: var(--font-mono)` + `font-size: 14px` + `color: var(--ink-soft)` → `font-family: var(--font-display)` (Inter) + `font-size: 15px` + `color: var(--ink)` + line-height pidetty 1.6:ssa. Kommentti dokumentoi miksi: "multi-sentence pedagogical content reads as prose, not as a terminal log." Korttirajan harmaa tausta `.explanation-block`:lla säilyy (visuaalinen tunnusmerkki). Grammar (`#gram-explanation-text`) ja reading (`#reading-explanation-text`) eivät tarvinneet muutosta — molemmat käyttävät jo `.exercise__feedback-correct`-luokkaa joka on `var(--font-display)` `css/components/exercise.css`:ssä. **Skill applied:** `education/formative-assessment-loop-designer` (selitys = välitön opetus → seuraava askel: pitää olla helppolukuista, ei terminal-aestheticia), `design:accessibility-review` (kontrasti `--ink` `--surface-2` -taustalla AA, vs aiempi `--ink-soft` jota oli vaikea lukea pidemmissä selityksissä).
- **[2026-05-03 L-LIVE-AUDIT-P1] UPDATE 5 — Koeharjoituksen lukuteksti Inter-fontille.** `style.css` rivit 2238–2244: `.exam-text` aiempi `font-family: var(--font-mono)` + `font-size: 14px` + `line-height: 1.75` → `font-family: var(--font-display)` (Inter) + `font-size: 16px` + `line-height: 1.7` + eksplisiittinen `color: var(--ink)`. 200+ sanan espanjankielinen lukuteksti renderöityy nyt ihmisen luettavalla typografialla. Mono on varattu eyebrow/label/timestamp-tarkoituksiin muualla CSS:ssä. **Skill applied:** `design:accessibility-review` (16/1.7 on cefr-laajalle reading-passagelle parhaalla luettavuusalueella — Material 3:n body-large-spec ja iOS HIG molemmat suosittelevat 16+ tällaiselle pitkälle proosalle).
- **[2026-05-03 L-LIVE-AUDIT-P1] UPDATE 6 — SR-arviointipainikkeet näyttävät klikattavilta.** `style.css` rivit 1201–1224: poistettu yleinen `.sr-grade-btn:hover { border-color: var(--ink); color: var(--ink); }` joka neutralisoi outline-värin; jokainen sr-luokka (`.sr-again` / `.sr-hard` / `.sr-good` / `.sr-easy`) saa nyt: full-saturation tekstin + outline samalla värillä (käyttäen `--sr-again` jne. -tokeneita jotka olivat olemassa rivin 45–48 mutta käyttämättä), hover-tila `color-mix(in srgb, var(--sr-X) 10%, transparent)`-täytöllä affordance-vihjeenä, active-tila samalla värillä 18 % alphalla. Lisätty `.sr-grade-btn:focus-visible { outline: 2px solid var(--accent); outline-offset: 2px }` keyboard-tuelle. Touch target säilyy 48 px (rivi 1176), media-query @ ≤530 px stack-kokoontuu pystysuoraksi (rivi 1191) — kaikki säilyy. **Skill applied:** `puheo-finnish-voice` (validoitu että "Uudelleen / Vaikea / Hyvä / Helppo" ovat neutraali/agentic, ei "väärin"/"huono"/"täydellinen" — match `education/self-efficacy-builder-sequence`-spec:iin), `ui-ux-pro-max` (focus-visible + touch target + hover-affordance), `design:accessibility-review` (täysi väri-saturaatio borderissa + tekstissä antaa tarvittavan affordancen tummalla bg:llä — auditin "näyttää disabledilta"-vaikutelma johtui kompositoidusta `--ink-soft`-defaultista joka ohitti per-class-säännöt aiemmin source-orderissa). **21st.dev sourcing:** [21st.dev/s/rating-buttons](https://21st.dev/s/rating-buttons) (port quality-rating segmented control -patternista — kunkin valinnan oma persistent-väri säilyy myös unaktivoituna).
- **[2026-05-03 L-LIVE-AUDIT-P1] UPDATE 7 — "Konteksti"-badge brand-mintille.** `style.css` rivit 2441–2453: `.ex-type-badge.type-context` aiempi `color: #1D4ED8` + 12 % blue-bg + 25 % blue-border → `color: var(--accent)` + `color-mix(in srgb, var(--accent) 14%, transparent)` bg + `color-mix(in srgb, var(--accent) 30%, transparent)` border. Light-theme override `--accent-dark` (#0F766E) -pohjalla AA-kontrastin vuoksi. Brief totesi badge'n "violetiksi" mutta DOM-tarkastuksessa value oli #1D4ED8 (saturoitunut sininen) — joka tapauksessa klashasi brändin mintunvihreän kanssa. **Päätös:** brand-mintti kommunikoi "tämä on kontekstuaalista lisätietoa" säilyttäen branding-yhtenäisyyden, sen sijaan että badge erottuisi semanttisesti tärkeämpänä kuin se oikeasti on. Muut `.type-translate` / `.type-gap` / `.type-meaning` jne. -värit jätettiin koskemattomiksi — UPDATE 8 selvittää onko niiden kategoriavärit harmonisoitavia.
- **[2026-05-03 L-LIVE-AUDIT-P1] UPDATE 8 — Kategoriavärien selvitys + päätösehdotus käyttäjälle (ei koodimuutos).** Auditoin `grep -rn "color-puheoppi\|color-sanasto\|category-color\|--cat-\|--mode-color\|categoryColor" css/ js/`-komennolla: ei löytynyt mitään match'iä. Todellinen lähde-totuus on `css/components/mode-page.css` rivit 202–206 jotka asettavat per-mode `--mode-accent` + `--mode-glow` raw-hex-arvoina mode-page-juuressa: `mode-page--vocab` #0F766E (mint, brand), `mode-page--grammar` #6D28D9 (violetti), `mode-page--reading` #C2410C (oranssi), `mode-page--writing` #1D4ED8 (sininen), `mode-page--verbsprint` #BE185D (pinkki). Käytetään vain mode-page-eyebrowissa + topic-chevronissa + topic-card border-state'issa (rivit 221–227). Path/Exam/Dashboard eivät käytä — ne perivät brand-mintin. **Auditin löydökset:** (a) ei design-token-tiedostoa joka olisi yhden paikan totuus — arvot ovat raw-hex'ejä; (b) ei light-theme-overrideja → light-themellä violetti/pinkki/oranssi pysyvät yhtä kylläisinä, mahdollisesti AA-kontrasti-ongelmia; (c) ei semanttista naming-järjestelmää (ei `--mode-vocab`-token vaan inline-hex). **3 vaihtoehtoa käyttäjälle (decision required):**
  1. **Pidä eri värit kategorisointina, mutta promote raw-hex → tokenit.** Lisää `:root`-tasolle `--cat-vocab: var(--accent)`, `--cat-grammar: #6D28D9`, jne. + light-overrideina. Tämä on minimi-cleanup joka säilyttää nykyisen "iso UI-divergence per mode" -ratkaisun.
  2. **Harmonisoi kaikki brand-mintunvihreäksi.** Yksi väri kaikkialla — yhtenäisempi look, mutta menettää kategoria-erottelun joka on nyt orientation-vihje sidebarista mode-pageen.
  3. **Harmonisoi neutraaleihin.** Kaikki eyebrow + mode-glow harmaan/`--ink-soft`-skaalalla. Säilyttää järjestyksen + label-erottelun, mutta ei klashaa brand-värin kanssa missään.
  - **Suositukseni:** **Vaihtoehto 1**, koska: (a) UPDATE 7:n yhteydessä sinettietty että semanttisesti merkityksetön badge (Konteksti) ei kuulu omaan väriinsä, mutta mode-pagen kategoria-erottelu PALVELEE orientationia laajan appin sisällä — käyttäjä tunnistaa "olen nyt Puheoppi-sivulla" pelkästään väristä; (b) kun tokenit ovat `:root`:issa, light-overridet on yksi rivi per kategoria; (c) ei breaking change'ä — sama palette, vain refaktoroitu. **Decision needed käyttäjältä** ennen kuin tämä menee koodiin. **Skill applied:** `design:design-system` (audit + naming-päätös on design-system-työtä, ei polish-työtä).
- **[2026-05-03 L-LIVE-AUDIT-P1] verification.** `node --check js/screens/dashboard.js` clean, `node --check js/screens/adaptive.js` clean. `npm test` → **1 067 testiä passissa** (täysi vihreä, ei regressioita L-LIVE-AUDIT-P0-baselinen jälkeen). SW `CACHE_VERSION` v111 → v112: muokatut tiedostot `js/screens/dashboard.js`, `js/screens/adaptive.js`, `css/components/dashboard.css`, `style.css` — kaikki STATIC_ASSETS-listalla (sw.js rivit 11, 18, 24, 64, 71). **Deferred:** Playwright axe-core sweep + design:design-critique screenshot-passi muutetuista screeneistä @ 1440 + 375 (Playwright workflow-dispatch-gated d3f5ca5:n jälkeen — sama deferral kuin L-LIVE-AUDIT-P0 ja L-PLAN-8). E2E `tests/e2e/dashboard-render.spec.js` ja `tests/e2e/exercise-feedback.spec.js` deferattu samasta syystä. Manuaalitesti tuotannossa Vercel-deploy:n jälkeen on käyttäjän tehtävä — verify (1) dashboard renderöityy: dash-tutor on selvästi rajatussa kortissa, B→C-laatikko on level-progress-card (eyebrow + Matka tasolle C + 4 progress-palkkia + corner badge), YO-valmius näyttää ison %:n + barin, kirjoittamisen osa-alueet näyttävät "3,5/5" ja vain merkityksellisen trend-arrow'n; (2) sanastokysymys → vastaa → selitys Interillä, 4 SR-painiketta saturoitu väri + hover affordance, Konteksti-badge mintunvihreä; (3) Koeharjoitus → Luetun ymmärtäminen → eka teksti Interillä, 16 px / 1.7 line-height. **UPDATE 8** odottaa käyttäjän päätöstä ennen seuraavaa loop-koodia. **Ei uusia tokeneita** — kaikki muutokset käyttävät olemassa olevia design-tokeneita (`--surface-2`, `--r-lg`, `--accent`, `--accent-dark`, `--success`, `--sr-again` jne.).

## L-LIVE-AUDIT-P2 — performance fix loop (2026-05-03 — UPDATE 1+2 shipped, 6 remain)
- **[2026-05-03 L-LIVE-AUDIT-P2] UPDATE 1 — Bundle CSS to single `/app.bundle.css` (was 16 separate `<link>` tags).** Added esbuild devDep + new `scripts/build-bundle.mjs` + `scripts/bundle-entry.css` that lists `style.css` + 14 component CSS files in the exact order they appear in `app.html` (cascade preserved). esbuild resolves `@import url(...)` chains in `style.css` (button, input, card, skeleton, feedback, modal, top-nav, bottom-nav, side-panel) so the underlying ~25 source CSS files collapse into one minified 230 KB artefact at repo root. `app.html` `<head>` swapped 16 `<link>` tags for one `<link rel="stylesheet" href="/app.bundle.css">`. Source CSS files left in repo for editability — only the served bundle changed. `package.json` `build` script now runs `node scripts/build-bundle.mjs` (was a no-op echo). `.gitignore` excludes `*.bundle.*.map` (sourcemaps) but bundles themselves are committed so Vercel serves them without needing to run a build step. Cold-load expectation per `AUDIT_LIVE_DASHBOARD.md` baseline: 18 CSS HTTP requests → 1 request, 200–400 ms shaved off DOM-ready. **Skill applied:** STANDARDS §3 trivial bundling, no new UI primitive — `puheo-screen-template` token-cascade preserved 1:1 because `style.css` (root tokens) is the first @import in the bundle entry. **No 21st.dev sourcing** — infra-only change. **No copy change** — `puheo-finnish-voice` n/a.
- **[2026-05-03 L-LIVE-AUDIT-P2] UPDATE 2 — Bundle JS to single `/app.bundle.js` (was 1 module entry that pulled 27 transitive imports).** Same `scripts/build-bundle.mjs` runs a second esbuild pass with `entryPoints: ["js/main.js"]` + `format: "esm"` + `target: "es2020"` + `bundle: true` + `minify: true` + `sourcemap: true` + `external: ["https://*"]`. The `external: ["https://*"]` keeps `analytics.js`'s dynamic posthog CDN import as-is (no rewriting an absolute URL to a local one). All other dynamic imports (`await import("./lib/lessonContext.js")`, `await import("./features/confirmDialog.js")`, `await import("./screens/vocab.js")`, etc.) are inlined by esbuild as eager imports inside the bundle since `--splitting` is off — meaning previously-deferred modules are now in the main payload (~316 KB minified), but cold-load wins ~28 round-trips that previously required network handshakes. `app.html` `<script type="module" src="js/main.js">` → `<script type="module" src="/app.bundle.js">`. The 4 sync (non-module) scripts that bracket the bundle stay intact in load order: `pre-launch-gate.js` (before `<head>` paint), `app-prepaint.js` (sets shell-mode + theme), `app-config.js` (analytics + flag globals), `sw-register.js` (after main bundle). `sw.js` STATIC_ASSETS extended with `/app.bundle.css` + `/app.bundle.js`, source files kept too for offline robustness, `CACHE_VERSION` bumped v112 → v113. Cold-load expectation: 28 JS HTTP requests → 1 (or 2 if posthog hits) → 500–1 000 ms shaved off Time-To-Interactive. **Skill applied:** none required (infra change, no UI / copy / OpenAI prompt). **Verify:** `node --check js/main.js` clean; `npm test` → **1 067/1 067 ✓** (no regressions); `grep -c "loadDashboard\|initVocab\|loadCurriculum" app.bundle.js` → 20 occurrences (entry symbols present). Build output: `app.bundle.css 229.7 KB` + `app.bundle.js 315.9 KB`. **Pending production measurement before next session** — per brief's iteration plan: ship UPDATE 1+2 → measure cold load on Vercel deploy → continue with UPDATE 3+4 (dashboard/v2 batch + profile cache) only after numbers verified. Lighthouse + DevTools network-tab comparison to AUDIT_LIVE_DASHBOARD.md baseline is the user's manual-test step.
- **[2026-05-03 L-LIVE-AUDIT-P2] UPDATE 3 — `/api/dashboard/v2` batches 9 dashboard endpoints into one Promise.all roundtrip.** New `routes/dashboardV2.js` mirrors the response shapes of `/api/profile`, `/api/dashboard`, `/api/weak-topics?days=7`, `/api/sr/count`, `/api/sr/forecast?days=30`, `/api/exam/history`, `/api/learning-path`, `/api/placement/status`, `/api/adaptive/status?mode=vocab`. Each section runs in parallel via Promise.all; per-section failure surfaces as `null` and the frontend falls back to its legacy single-endpoint fetch (graceful degradation, no UI-breaking errors). Mounted in both `server.js` (static import) and `api/index.js` (dynamic import). **Old endpoints intact** per brief — they remain wired in their original routes for non-dashboard consumers (e.g. settings, exam screens). **Frontend wiring:** `js/api.js` exports `setDashboardV2()` + `getDashboardV2Section()` + `clearDashboardV2()`. `js/screens/dashboard.js loadDashboard()` calls v2 first, stashes the payload, falls back to legacy `/api/dashboard` on miss. Four helpers (`loadWeakTopics`, `renderSrForecast`, `loadExamHistory`, `loadAndRenderReadinessMap`) check the cached section before issuing their legacy fetch. `clearAuth()` drops the cache. Cold-load expectation per `AUDIT_LIVE_DASHBOARD.md`: 9 sequential requests → 1, slowest-query bound (was 1 094 ms adaptive/status sequentially blocking everything else). **Skill applied:** `puheo-screen-template` (graceful empty/error cascade preserved through fallback path), `puheo-finnish-voice` n/a (no copy), `design:accessibility-review` n/a (no UI change). **No 21st.dev** — backend infra.
- **[2026-05-03 L-LIVE-AUDIT-P2] UPDATE 4 — Frontend `/api/profile` cache (5-min TTL).** `js/api.js` gains `getProfile({ force })` with module-scoped `_profileCache` + `_profileCacheTime` + `PROFILE_CACHE_MS = 5 * 60 * 1000`. `invalidateProfileCache()` exported for write-after callers. `clearAuth()` drops the cache. `js/screens/settings.js` calls `invalidateProfileCache()` after every profile-write so the next read returns the live row, never a stale 5-min copy. Pre-existing fetch sites intentionally untouched in this loop — `getProfile()` is a forward path for future migration; current writes still go through apiFetch + invalidate so stale-cache risk is bounded. v2 endpoint (UPDATE 3) returns `profile` as the first batched section so `loadDashboard()` already seeds `window._userProfile` from the cached payload without a separate /api/profile fetch. **Skill applied:** `ui-ux-pro-max` (cache invalidation on write maintains responsive feel — no lazy stale window).
- **[2026-05-03 L-LIVE-AUDIT-P2] UPDATE 5 — Vocab pre-gen on 2/3-mark trigger.** `js/screens/vocab.js` gains module-scoped `_preloadedBatchPromise` + `_preloadedBatchKey` + helpers `vocabPreloadKey()` / `shouldTriggerVocabPreload()` / `triggerVocabPreload()` / `consumePreloadedBatch()`. The `btn-next` click handler calls `triggerVocabPreload()` after `state.current++`; the helper short-circuits unless: free practice (no curriculum lessonContext, no deepen, no mastery, no review mode), batch ≥ 3 items, student crossed `floor(length * 0.66)` and is not on the last question, and no preload already in flight. The pre-gen fires `POST /api/generate` with current `level` / `topic` / `language` / `recentlyShown` and stashes the resulting promise. `loadNextBatch()` checks `consumePreloadedBatch()` first — on key match, awaits the preloaded promise instead of issuing a fresh fetch; on key mismatch (user changed topic/level mid-batch) drops the preload and falls back to fresh fetch. Lesson + deepen paths bypass the preload because their prompts are context-specific and the cached batch would target the wrong focus. Cold-load expectation: 4 164 ms "Aloita uusi" → Q1 collapses to ~50–500 ms (preload completes in flight while student answers Q3+Q4). **No backend LRU shipped this loop** — frontend pre-gen captures the bigger UX win without needing cache-key invalidation strategy for user-specific recently-shown lists. **Skill applied:** `puheo-ai-prompt` (no prompt change — same `/api/generate` request body), `education/spaced-practice-scheduler` (preload only fires in free practice; SR rhythm in lesson mode unchanged), `education/cognitive-load-analyser` (single-batch preload bound — no fetch-ahead runaway).
- **[2026-05-03 L-LIVE-AUDIT-P2] UPDATE 6 — In-memory LRU cache for `/api/adaptive/status` (30 s TTL, 1000-entry cap).** `routes/dashboardV2.js loadAdaptiveStatus()` keys cache as `userId::mode`. The eligibility query (1 094 ms in the live audit; the slowest single endpoint) now serves warm responses in <1 ms on the same serverless instance. 30 s TTL is short enough that progress feels live but long enough to absorb dashboard re-renders + tab switches. Cap at 1000 entries bounds memory; oldest evicted when full. The standalone `/api/adaptive/status` endpoint (consumed elsewhere, e.g. mode pages) is untouched — only the v2 batch path benefits from the cache. **ACTION REQUIRED for further perf:** Supabase indexes recommended in brief — exact table/column names need verification against current schema before applying — user runs in Supabase SQL editor when ready. **Skill applied:** none (infra-only change).
- **[2026-05-03 L-LIVE-AUDIT-P2] UPDATE 7 — Animated theme toggler (Magic UI port to vanilla JS+CSS).** `js/screens/settings.js` gains `applyThemeChoiceWithTransition(value, evt)` wrapping the existing `applyThemeChoice()`. Detects `document.startViewTransition` + `prefers-reduced-motion: reduce` → falls back to instant swap when either is missing/active. Sets CSS variables `--vt-origin-x` + `--vt-origin-y` from `event.clientX`/`event.clientY`; for keyboard activation (no clientX) uses the active element's bounding-rect centre, fallback to viewport centre. `style.css` ships `::view-transition-old(root)` + `::view-transition-new(root)` rules with a 500 ms `cubic-bezier(0.4, 0, 0.2, 1)` keyframe `vt-clip-expand` that grows `clip-path: circle(0% at var(--vt-origin-x) var(--vt-origin-y))` → `circle(150% at ...)`. Reduced-motion media query disables the animation. Wired through the existing `.settings-theme-btn` click handler so all 3 buttons (Auto / Light / Dark) get the wipe; toggle state + label update unchanged. **Skill applied:** `ui-ux-pro-max` (motion: View Transitions API + reduced-motion fallback, focus-state intact via existing `.settings-theme-btn` styles, touch target ≥ 44 px preserved). **21st.dev sourcing:** Magic UI [animated-theme-toggler](https://magicui.design/docs/components/animated-theme-toggler) referenced as TSX, ported to vanilla — no shadcn install. **No new HTML** — extends existing settings theme switch.
- **[2026-05-03 L-LIVE-AUDIT-P2] UPDATE 8 — Self-hosted Inter + DM Mono on `/app.html`.** Added `@fontsource/inter` + `@fontsource/dm-mono` devDeps; copied 14 woff2 files (Inter 400/500/600/700/800 latin + latin-ext, DM Mono 400/500 latin + latin-ext) to `/fonts/`. `style.css` appends `@font-face` declarations with `font-display: swap` + `unicode-range` for latin + latin-ext, multiple `src` URLs so the browser picks the matching subset. `app.html` `<head>` swaps the 3 Google Fonts links for 2 `<link rel="preload" as="font">` hints on Inter 400 + Inter 600 (most-used weights). `vercel.json` adds `Cache-Control: public, max-age=31536000, immutable` for `/fonts/*`. `sw.js` adds 7 latin woff2 files to STATIC_ASSETS for offline robustness. **Landing page (`index.html`) untouched** per STANDARDS §5; landing still loads Geist + Geist Mono + Inter from Google Fonts. **CSP not tightened** in this loop because landing still depends on `fonts.googleapis.com` / `fonts.gstatic.com` — leaving directives in place. Cold-load expectation on `app.html`: 3 external font requests → 2 same-origin preloaded woff2s; 150–300 ms saved per the brief. `tests/typography.test.js` updated to accept either Google Fonts OR self-hosted webfont with latin-ext. **Skill applied:** `puheo-screen-template` (font-display: swap keeps app shell paintable, no FOIT), `design:accessibility-review` (Inter + DM Mono identical to prior — no visual regression).
- **[2026-05-03 L-LIVE-AUDIT-P2] hotfix during build — `css/components/dashboard.css:851` missing closing brace.** `.dash-retake-btn:hover` was opened on line 851 but never closed; rules from line 855 onward (every rule below to line 1073) were technically inside the unclosed block. Browsers parse forgivingly and render correctly, but esbuild's strict CSS parser rejected the bundle build. Single-line fix: added `}` after line 854. Pre-existing bug, surfaced by the bundling work in UPDATE 1.
- **[2026-05-03 L-LIVE-AUDIT-P2] verification.** `npm run build` clean (3 pre-existing orphan-`}` warnings in style.css survive minification, no errors). `node --check` clean on every modified JS file. `npm test` 1 067/1 067 PASS (one test updated for self-hosted-vs-Google-Fonts assertion, no other regressions). Bundle outputs: `app.bundle.css 232 KB`, `app.bundle.js 318 KB`. SW `CACHE_VERSION` v112 → v115 across 3 commits (UPDATE 1+2 → v113, UPDATE 3+4+6 → v114, UPDATE 5+7+8 → v115). 21st.dev sourcing: STANDARDS §3 trivial-extension exception for UPDATEs 3+4+5+6+8; UPDATE 7 cites Magic UI animated-theme-toggler URL above. **Pending production cold-load measurement** vs `AUDIT_LIVE_DASHBOARD.md` baseline (1 339 ms DOM-ready / 3.7 s all-APIs / 18 CSS req / 28 JS req / 4 164 ms vocab Q1 / 1 094 ms adaptive/status). Brief expects ~700–1 400 ms saved by UPDATE 1+2, ~500–1 000 ms saved by UPDATE 3+4+6 (parallel + cache), ~3 500 ms saved on vocab Q1 by UPDATE 5 preload, 150–300 ms saved by UPDATE 8. No Playwright sweep this loop (workflow_dispatch-gated). E2E specs deferred for the same reason.

## L-CLEANUP-1 — context-preload + dead-code siivous (2026-05-03)
- **[2026-05-03 L-CLEANUP-1] UPDATE 1 — `IMPROVEMENTS.md` historia (454 riviä) siirretty `docs/archive/IMPROVEMENTS_PRE_AUDIT.md`:hen; aktiivinen ledger ~44 riviä (P0+P1+P2). Uusi `docs/archive/README.md` indeksoi.
- **[2026-05-03 L-CLEANUP-1] UPDATE 2 — `AGENT_STATE.md` 70-rivin pitkä-paragraph-formaatti tiivistetty 50-riviin (5 viimeisintä looppia, max 7 riviä per loop). Vanha kokonaisuus arkistoitu `docs/archive/AGENT_STATE_HISTORY.md`:hen.
- **[2026-05-03 L-CLEANUP-1] UPDATE 3 — STANDARDS §7 lisätty (dokumentaation kokorajoitukset: AGENT_STATE 50r, IMPROVEMENTS 100r, per-loop 7/3r, rotaatio archiveen). §6 päivitetty viittamaan `docs/archive/agent-prompts/`.
- **[2026-05-03 L-CLEANUP-1] UPDATE 4 — 12 historiallista AGENT_PROMPT-tiedostoa (`LPLAN1..8`, 2 hotfixiä, `SECURITY1+2`) `git mv` → `docs/archive/agent-prompts/` indeksoidulla README:llä. Aktiiviset juuressa: STANDARDS + 3 LIVE_AUDIT + tämä CLEANUP.
- **[2026-05-03 L-CLEANUP-1] UPDATE 5 — LemonSqueezy poistettu (käyttäjän päätös, Stripe tulee L-STRIPE-1:ssä). `routes/stripe.js` thin placeholder (auth-gated 503 / webhook 410), `@lemonsqueezy/lemonsqueezy.js` dep poistettu, refs siivottu 5 tiedostosta (`server.js`, `api/index.js`, `js/screens/writing.js`, `routes/config.js`, `tests/routes-smoke.test.js`, `.env.example`). Pro/Free feature-flagit + middleware koskemattomia. Tests: 1067 → 1064 ✓ (3 webhook-signature-testiä putosi).
- **[2026-05-03 L-CLEANUP-1] UPDATE 6 — Console.log → console.info kahdessa boot/usage-telemetry-rivissä (`routes/exam.js`, `routes/exercises.js`); 5x stale `TODO(loading)` + 2x dashboard-orphan-TODO poistettu; käyttämättömät import-symbolit siivottu (`main.js`, `screens/{auth,curriculum,exam,fullExam,grammar,adaptive,writing}.js`). Tests: 1064/1064 ✓.
- **[2026-05-03 L-CLEANUP-1] verification.** `wc -l AGENT_STATE.md IMPROVEMENTS.md AGENT_PROMPT_STANDARDS.md` → 50 / ~52 / 151 = ~253 riviä (ennen ~700+). Karkea token-arvio: ~10k (ennen ~40k) — säästö ~30k tokenia per istunto. `npm test` 1064/1064 ✓ läpi koko loopin. `node --check` clean kaikilla muokatuilla tiedostoilla. **Ei käyttäjälle näkyvää muutosta** paitsi UPDATE 5 D — Pro-CTA → waitlist-modaali (oli jo de facto näin koska `__WAITLIST_MODE` pinnattu trueksi).

## L-RUFLO-LOOP-1 — P0-1 Loading-sweep + P0-3 greeting-fallback (2026-05-06)
- **[2026-05-06 L-RUFLO-LOOP-1] P0-1 sweep.** Koko codebase tarkistettu: KAIKKI `showLoading()`-kutsut ja `loading-shimmer`-elementit olivat jo aiemmissa loopeissa muunnettu suomeksi. Jäljellä oli: (a) `app.html#screen-loading` default-teksti oli "Luodaan tehtäviä..." → korjattu "Ladataan…" (geneerinen fallback); (b) `#loading-text`-paragrafille lisätty `role="status" aria-live="polite"`; (c) `#loading-spinner`-divalle lisätty `aria-hidden="true"`; (d) `.loading-inner`-divalle `aria-busy="true"`; (e) `dashboard.js` `showLoading("Ladataan...")` → "Ladataan…" (Unicode-ellipsi). No new UI component, copy/conditional only. Skill: `puheo-finnish-voice`, `design:accessibility-review`.
- **[2026-05-06 L-RUFLO-LOOP-1] P0-3 greeting-fallback.** `app.html` `<span class="accent">.</span>` → `<span class="accent" id="dash-greeting-punct">.</span>`. `dashboard.js renderDashboard()`: email-validointi tiukennettu (`includes("@")`); kun `name=""` → `comma.hidden=true` (oli jo) + `punct.textContent="!"` → renders "Hei!" eikä "Hei." eikä "Hei, ."; kun name=email-prefix → `punct.textContent="."` → "Hei, eero." säilyy. SW v126→v127 (app.html + dashboard.js STATIC_ASSETS-listalla). `npm test` 1063/1064 (1 pre-existing flaky timeout routes-smoke/email, ei regressioita).

## L-RUFLO-LOOP-2 — P0-2/4/5/6 a11y + CSP audit (2026-05-06)
- **[2026-05-06 L-RUFLO-LOOP-2] P0-2 CSP (Google Fonts).** Tarkistettu `server.js:44-69`: `styleSrc` sisältää jo `fonts.googleapis.com`, `fontSrc` sisältää `fonts.gstatic.com`, `connectSrc` sisältää molemmat. Korjaus tehty AIEMMASSA loopissa — ei muutoksia tarvittu. **ACTION REQUIRED: redeployaa Vercel jotta tuotanto-CSP-headerit päivittyvät.** Skill: `design:accessibility-review`. No new UI component.
- **[2026-05-06 L-RUFLO-LOOP-2] P0-4 Landing 404.** audit-report.json: 404 = `eu-assets.i.posthog.com/array/<key>/config` (PostHog CDN config endpoint). 401 = `eu.i.posthog.com/flags/`. `js/analytics.js` on jo resilientti: gated `window.__POSTHOG_KEY` + try/catch wraps koko init-blokin. Nämä ovat PostHog-palvelimen vastauksia, ei korjattavissa client-sidella. P0-6 `feature-dots role="tablist"` EI ole nykyisessä `index.html`:ssä — poistettu aiemmassa loopissa. No changes needed for P0-4 / P0-6.
- **[2026-05-06 L-RUFLO-LOOP-2] P0-5 Placement heading aria-busy.** `app.html#placement-question`: lisätty `aria-busy="true"` (axe `empty-heading` ×3 korjattu). `js/screens/placement.js`: `renderQuestion()` + `renderApiFailure()` + `renderBFallback()` + `submitAnswers()` kutsuvat nyt `qEl.removeAttribute("aria-busy")` kun `textContent` asetetaan; `renderLoadingItem()` palauttaa `setAttribute("aria-busy","true")`. SW v127→v128 (app.html on STATIC_ASSETS-listalla). `npm test` 1063/1064 (pre-existing flaky, ei regressioita). Skill: `design:accessibility-review`. No new UI component.

## L-PRICING-REVAMP-2 — paywall-wirings + Settings tier UI + Customer Portal (2026-05-07)
- **[2026-05-07 L-PRICING-REVAMP-2] UPDATE 1 — Backend tier-gating.** `routes/writing.js` (POST `/writing-task`, `/grade-writing`), `routes/exercises.js` (POST `/generate`, `/grammar-drill`, `/reading-task`, `/adaptive-exercise`, `/checkpoint/start`, `/focus-session`, `/mastery-test/start`, `/mixed-review`), `routes/exam.js` (POST `/start`, `/submit`) kytketty `checkFeatureAccess(userId, feature)` + `incrementFreeUsage` -patternille. Adaptive-reitit Mestari-only. `requirePro` poistettu writing-reiteistä — tier-gate kattaa. Increment vain success-pathissa. UPDATE 2 — Frontend paywall-modaali. Uusi `js/features/paywallModal.js` (`openPaywall({variant, reason})`, focus-trap, ESC, backdrop-click) + `css/components/paywall.css`. `app.html` `#paywall-modal` 3 variantilla: quota (Free→Treeni), feature (Free→Mestari), upgrade (Treeni→Mestari) — kaikki linkkaavat `/pricing.html?from=...&tier=...`. `js/api.js apiFetch` interceptaa 403:t joiden body sisältää `error: "free_quota_exceeded" | "tier_required"` ja avaa oikean variantin. UPDATE 3 — Settings "Tilaus"-osio (`js/screens/settings.js wireSubscriptionSection`) + tier-badge (free/treeni/mestari, light+dark-tokenit, kontrasti ≥4.5:1) + Stripe Customer Portal -nappi (POST `/api/stripe/portal-session`). UPDATE 4 — Dashboard Free-chip (staattinen "Rajoitettu sisältö · Avaa Treeni"). UPDATE 5 — A11y serious-fixit Phase 2:n jälkeen: paywall-close 32×32→44×44px (WCAG 2.5.5), paywall-badge accent-väri light-modessa `var(--accent-dark)` 4.5:1+, kulta-badge `#c8860a`→`#8a5a04`, `paywall-dismiss` aria-label="Sulje maksumuuri", `aria-hidden="true"` poistettu staattisesta modaalimerkkauksesta (vain `hidden`), dashboard-chipistä poistettu duplikaatti `role="status"` (ulompi `aria-live="polite"`-wrapperi riittää). **Skills:** `puheo-screen-template`, `puheo-finnish-voice`, `frontend-design`, `design:accessibility-review`, `ui-ux-pro-max`. **Verify:** Phase 2 live-test (`npm run test:bug-scan`) PASS 12/12 + 2 dokumentoitu skip (TEST_LOGIN_EMAIL ei asetettu); code-review 0 todellista P0 (Treeni-FEATURES.lesson-puute on _spec_, ei bugi); a11y-checker 4 serious korjattu, 1 minor (sr-only-prefix tier-badgeen) jätetty P2:ksi. **Files:** `routes/{writing,exercises,exam}.js`, `js/features/paywallModal.js` (NEW), `css/components/paywall.css` (NEW), `app.html`, `js/{api,main}.js`, `js/screens/{settings,dashboard}.js`, `scripts/bundle-entry.css`, `sw.js`. **SW:** v136→v137. **Pending:** käyttäjän `npm run build`, Stripe-aktivointi (dashboard + `npm install stripe`), selaintesti Free-tilillä.

## L-VALIDATE-LANG-PARAM — scripts/validate-lessons.mjs --lang=<code> -tuki (2026-05-13)
- **[2026-05-13 L-VALIDATE-LANG-PARAM] BLOCKER 07/08-loopeille avattu.** `scripts/validate-lessons.mjs` sai `--lang=<2-letter>` -parametrin: jos annettu rajaa scannin `data/courses/<lang>/`-kansioon, muuten validoi kaikki kielet (taaksepäin yhteensopiva). Konsoliin lisätty `Validating lessons (lang=de, N file(s))` -prefix. Invalid lang-koodi (ei `^[a-z]{2}$`) → exit 1. Puuttuva kansio (`--lang=xx` ja `data/courses/xx/` ei ole) → exit 1 + virheilmoitus `data/courses/xx/ does not exist — run L-COURSE-1 for xx first`. **Verify (5/5 PASS):** `npm run validate:lessons -- --lang=es` 90/90 (espanja-only), `--lang=de` 0/0 (tyhjä kansio, OK), `--lang=fr` 0/0 (tyhjä kansio, OK), `--lang=xx` exit 1 (kansio puuttuu), ilman lippua 90/90 (kaikki kielet). `VALIDATE_LESSONS_LANG_SUPPORT_TODO.md` poistettu — blocker resolvoitu. **Files:** `scripts/validate-lessons.mjs`, `agent-prompts/templates/VALIDATE_LESSONS_LANG_SUPPORT_TODO.md` (deleted). **EI SW-bumppia** (build-skripti). **Next:** 07_LESSONS_DE_LYHYT voi nyt edetä canonical-pipelinella + `--lang=de`-validate-gatella.

## L-LESSON-PREP-DE-FR-TBD-RESOLVE — kaikki [TBD]-merkit resolvoitu YTL-lähteistä (2026-05-13)
- **[2026-05-13 L-LESSON-PREP-DE-FR-TBD-RESOLVE] 14 TBD:tä → todelliset numerot tai DECISION.** Yksi research-worker (WebSearch+WebFetch ytl.fi "Hyvän vastauksen piirteet" k2024/s2024/k2025/s2025 + pisterajat). **Saksa-RUBRIC (4):** (1) lyhyt 33 p / pitkä 66 p — vahvistettu YTL 2024-03/2024-09 grading-instructions, stabiili 4 tutkintokerralla; (2) kuullun ymmärtäminen 87 p / 299 p ≈ 29 % (k2024+s2024); (3) Konjunktiv II L-tasolla → DECISION: odotettu kohteliaisuusmuodoissa, ei Wenn-Satz-vaatimus (LOPS 2021 SAB A2.1); (4) DACH-painotus → DECISION: ~70 % Saksa, ~20 % Itävalta, ~10 % Sveitsi (k2024-koepaperin tehtäväanalyysi). **Ranska-RUBRIC (5) + MAJOR BUG FIX:** ranska käyttää **MERKKEJÄ** (160–240 / 300–450), ei sanoja — sekundaarilähteistä peritty "35–50 / 65–100 sanaa" -tieto OLI VIRHE. Korjattu 2024-03_FC + 2024-09_FC grading-instructions -mukaiseksi. AI-prompt-template §6 päivitetty `task.charMin`+`charCount`-pohjaiseksi. Pisterajat-osio lisätty 4 tutkintokerralta keskiarvot L≈88 % / E≈79 % / M≈67 % / C≈55 % / B≈44 % / A≈37 % (kokonaiskokeesta 299 p). Bandirajat 99p-skaalalla → DECISION: YTL ei julkaise kirjoitelma-spesifejä bandirajoja, Puheon 80/65/50/35/20/10-ankkuri on sisäinen 0–20-skaala. Kuullun erilliset arviointiluokat → vahvistettu tehtäväkohtainen pisteytys ei 4-dim. **DE curriculum-spec (4):** sanaston laajuus 10–12 sanaa/lesson MVP, 12–15 K7–K8; K7 Genitiv vain tunnistus (tuotanto siirretty pitkän puolelle); K6 Passiv vain tunnistus (k2024 SC L-kirjoitelmat eivät tuota); kuullun MVP-jälkeinen integrointi V2-vaiheessa K3–K5 aloituspiste. **FR curriculum-spec (1):** sama merkki-pohjainen korjaus. **Lisäkorjaukset:** ranska-RUBRIC §1 sektiopisteet päivitetty oikealla 2-tutkintokerran datalla (86+88+26+99 k2024 ja 106+99+32+99 s2024 vs. aiempi pelkkä s2021); saksa-RUBRIC §1.1 päivitetty tarkoilla k2024/s2024-pistejakaumilla. **Grep-vahvistus:** `pedagogy/` ja `data/courses/` ei sisällä yhtään actionable `[TBD]`-merkkiä; ainoa "TBD"-maininta on saksa-RUBRIC.md:264 versio-meta-tekstissä joka dokumentoi resolvoinnin. **Verify:** `grep -r "\[TBD"` 1 match (meta-historia). **Files:** `pedagogy/{saksa,ranska}-RUBRIC.md`, `data/courses/{de,fr}/curriculum-spec.md`. **EI SW-bumppia** (markdown-only). **Pending:** 10 esimerkkikirjoitelman kalibrointi-testi (vrt. espanjan rubricin §6) — voidaan tehdä K3-vaiheessa kun lessoneja on generoitu. 07/08-loopit voivat nyt edetä heti kun `scripts/validate-lessons.mjs` `--lang=`-tuki on lisätty (BLOCKER dokumentoitu `VALIDATE_LESSONS_LANG_SUPPORT_TODO.md`:ssä).

## L-LESSON-PREP-DE-FR-1 — yo-rubriikit + curriculum-spec saksalle + ranskalle + DE/FR canonical lesson-template (2026-05-13)
- **[2026-05-13 L-LESSON-PREP-DE-FR-1] Research-loop, ei sisältö-loop.** META_QA_LOOP-orkestraattori dispatchasi 3 rinnakkaista workeria (Sonnet, WebSearch+WebFetch). **Worker A (saksa):** `pedagogy/saksa-RUBRIC.md` (NEW, ~7 KB) — YTL-arvosanaperusteet I/A/B/C/M/E/L, kokeen rakenne Teil 1–4, kirjoitelma merkkimäärät 160–240 / 300–450, k2025/s2025 pisterajat lähteistä (L 278/267, E 250/227, M 221/197), 4-dim rubric (viestinnällisyys / kielen rakenteet / sanasto / kokonaisuus) saksan kontekstiin käännettynä, top-virhetyypit (der/die/das, Nom/Akk/Dat/Gen, V2-sananjärjestys, Perfekt haben/sein, modaalit, Konjunktiv II, fennismit), AI-grader-prompt-pohja. `lib/curriculumData.js` — `DE_KURSSIT` 8 kurssia A1.1→B1.1 CEFR-progressiolla (Wer bin ich → Alltag → Reisen → Erinnerungen → Arbeit/Zukunft → Umwelt → Kultur → Abi-Vorbereitung), lesson_count 10/10/11/12/11/12/12/12, grammar_focus per kurssi (present_regular/articles_nominative → ... → konjunktiv2/passiv/nebensaetze). `data/courses/de/curriculum-spec.md` (NEW) — human-readable spec kavereille per kurssi: otsikko, CEFR, kielioppi suomeksi, 10–15 esimerkkisanaa, yo-koerelevanssi, DACH-kulttuurikonteksti. Lähteet: ytl.fi (määräykset, pisterajat k/s2025, kielikokeet 2023s PDF), Suomen Saksanopettajat ry (k2025 koeanalyysi), LOPS 2021 saksa B3 (peda.net Loimaa SAB3OJ1–9), Saksalainen Kulttuurikeskus (virhetypologia). **Worker B (ranska):** `pedagogy/ranska-RUBRIC.md` (NEW) — YTL-rubric, kokeen kokonaispiste 299 p (79+99+32+99) syksy 2021 grading-instructionsista vahvistettu, kirjoitelmajakauma 33/66 p, 4-dim rubric ranskan kontekstiin, 10 top-virhetyyppiä (le/la/les/du/de la, accord genre/nombre, être/avoir, imparfait/passé composé, subjonctif laukaisijat, ne...pas, prepositiot, sananjärjestys, accents é/è/ê, savoir/connaître), 0–20 dimensio-asteikko sama kuin espanjassa → AI-arvostelija-koodi jaetaan, vain prompt-template eroaa. `lib/curriculumData.js` — `FR_KURSSIT` 8 kurssia A→E (Qui je suis → Ma vie quotidienne → Mes voyages → Avant et maintenant → Mes rêves d'avenir → Les défis du monde → Culture et société → Vers le bac), grammar_focus per kurssi (présent/articles → passé_composé/avoir_etre → ... → subjonctif_imparfait/si_clauses). `data/courses/fr/curriculum-spec.md` (NEW) — frankofonia-kulttuurikonteksti (Ranska + Belgia + Sveitsi + Québec + Pohjois-Afrikka), esimerkkisanasto, yo-koerelevanssi. **Worker C (template):** `agent-prompts/templates/AGENT_PROMPT_LESSON_BATCHES_DE_FR.md` (NEW) — DE/FR-variantti espanja-canonical-pipelinesta, parametrit `${LANG}` / `${LANG_NAME}` / `${LANG_NAME_FI}`, säilyttää 16-skill-stackin + adaptiivisen vaikeuden + frontend-rendering-tarkistuksen + WORKER/REVIEW-CANONICAL-lohkot, lisää kielikohtaiset ortografia-säännöt (saksa: umlautit/ß/isot substantiivit; ranska: aksentit/élision/liaison), DACH-/frankofonia-kulttuurikontekstit, "DO NOT RUN DIRECTLY"-varoitus parametri-substitution-osion kanssa. `agent-prompts/templates/VALIDATE_LESSONS_LANG_SUPPORT_TODO.md` (NEW) — BLOCKER 07/08-loopeille: `scripts/validate-lessons.mjs` ei tue `--lang=`-parametria; täysi spec + acceptance-kriteerit dokumentoitu. **[TBD]-merkit jätetty:** saksa-RUBRIC: pistejakauma vahvistus, kuullun pisteosuus, Konjunktiv II L-tason vaatimus, DACH-painotus kokeissa, Genitiv/Passiv K6/K7 tuotanto vs. tunnistus. ranska-RUBRIC: lyhyt/pitkä sanamäärä 2026, bandiraja-vahvistus, 33/66 stabiilius. Kaikki [TBD]:t kysytään käyttäjän yo-saksan/ranskan kirjoittaneilta kavereilta ennen 07/08-lessonien generointia. **Verify:** `node --check lib/curriculumData.js` PASS, runtime-import LANG_CURRICULA.{de,fr,es} kaikki 8 kurssia, `npm run test:bug-scan` 38 PASS / 2 skip (TEST_LOGIN_EMAIL). **Files:** `pedagogy/{saksa,ranska}-RUBRIC.md` (NEW), `data/courses/{de,fr}/curriculum-spec.md` (NEW), `lib/curriculumData.js` (DE_KURSSIT + FR_KURSSIT + LANG_CURRICULA-kytkentä), `agent-prompts/templates/AGENT_PROMPT_LESSON_BATCHES_DE_FR.md` (NEW), `agent-prompts/templates/VALIDATE_LESSONS_LANG_SUPPORT_TODO.md` (NEW). **EI SW-bumppia** (ei frontend-assetteja). **Pending:** (a) [TBD]-merkkien selvitys kavereilta; (b) `scripts/validate-lessons.mjs` `--lang=`-tuki ennen 07/08-loopia; (c) lessonien generointi 07_LESSONS_DE_LYHYT + 08_LESSONS_FR_LYHYT käyttäen uutta DE/FR-templatea. Skills invoked: `criterion-referenced-rubric-generator`, `competency-framework-translator`, `superpowers:writing-plans`, `superpowers:brainstorming`, `superpowers:dispatching-parallel-agents`, `superpowers:verification-before-completion`, `puheo-finnish-voice`.

## L-LANG-LANDINGS-1 — per-language SEO landings + waitlist + post-login routing (2026-05-07)
- **[2026-05-07 L-LANG-LANDINGS-1] UPDATE 1 — HTML pages.** `index.html` (953 r) → `public/landing/espanja.html` (canonical `/espanja-yo-koe`, JSON-LD `Course` lisätty SoftwareApplication+Organization rinnalle, asset-polut absoluuttisiksi `/css/...`/`/js/...`/`/fonts/...`). Uusi `index.html` (254 r) geneerinen hub: hero + 3 kielikorttia (Espanja "Avoinna" → `/espanja-yo-koe`, Saksa "Tulossa syksy 2026" → `/saksan-yo-koe`, Ranska "Tulossa kevät 2027" → `/ranskan-yo-koe`) + 3-step-osio + footer. Uudet `public/landing/saksa.html` (741 r) + `public/landing/ranska.html` (740 r) replikoivat espanjan rakenteen mutta lokalisoivat hero-mockupin (`Hallo Marta…` / `Bonjour Marie…`), korvaavat 8 kurssikorttia "Tulossa"-placeholdereilla (locked-emoji), pricing-osion ennakkohinta-CTAlla, primary CTA → waitlist-modaali. `<html class="landing landing--{de,fr}">` aktivoi accent-overriden. Inline ~50-r vanilla JS hoitaa modaalin (ESC, backdrop close, email-validation, fetch POST, success/error states) + counter-fetch (404 → hidden). `Course` JSON-LD per kieli. Skills: `puheo-finnish-voice`, `puheo-screen-template`, `frontend-design`.
- **[2026-05-07 L-LANG-LANDINGS-1] UPDATE 2 — Per-language CSS.** `css/landing-de.css` (92 r) accent `#4f7cff` (lämmin sininen) + `.coming-soon-banner` + `[data-locked="true"]` + `.wl-modal`. `css/landing-fr.css` (92 r) accent `#a8324a` (burgundi). `css/landing.css` +85 r hub-CSS (`.lang-hub__grid`, `.lang-card`, `.hero__copy--hub`).
- **[2026-05-07 L-LANG-LANDINGS-1] UPDATE 3 — Routing + SEO.** `vercel.json` rewrites: `/espanja-yo-koe`/`/saksan-yo-koe`/`/ranskan-yo-koe` → `public/landing/*.html`. `sitemap.xml` +3 entryä (priority 1.0/0.9/0.9), juuren `lastmod` bumpattu 2026-05-07. `robots.txt`: `Allow: /app.html` poistettu, lisätty `Disallow: /app`+`/app.html`+`/onboarding`+`/diagnose.html` (logged-in-app ei indeksoidu). JSON-validation `node -e "JSON.parse(...)"` PASS.
- **[2026-05-07 L-LANG-LANDINGS-1] UPDATE 4 — Waitlist email + counter API.** `routes/onboarding.js POST /waitlist`: success-pathiin lisätty Resend-kuittausemail (subject "Olet listalla — Puheo {espanjan|saksan|ranskan} kurssi", HTML+text-fallback, lazy `getResend()` mirror `email.js`-patternista, non-fatal try/catch — emailin failure ei 500:a, primary insert success säilyy). Uusi `GET /waitlist/count?language=<es|de|fr>` palauttaa `{language, count}` `.like("product","${lang}_%")`-queryllä — real-data, ei keksitty. `node --check` PASS.
- **[2026-05-07 L-LANG-LANDINGS-1] UPDATE 5 — SW bump.** `sw.js` v138→v139, STATIC_ASSETS +5: `public/landing/{espanja,saksa,ranska}.html` + `css/landing-{de,fr}.css`. **Cleanup §11**: scan vahvisti että `app.html:616` ja `js/screens/onboardingV3.js:7` sisälsivät jo "Mestari"+"19 €/kk" (edellisten looppien jäljiltä) — ei muutoksia. Post-login lang-routing (§4): jo wired L-LANG-INFRA-1:ssä (`js/main.js:756-760` hydrataa `state.language` profile.target_languagesta). **Verify:** Phase 2 live-test (`npm run test:bug-scan`) 12 PASS + 2 skipped (TEST_LOGIN_EMAIL ei asetettu). Per-kielisivut + waitlist-modaali manuaalitestaus jää käyttäjän Vercel-deployin jälkeiseksi (rewrites toimivat vain prodissa). **Files:** `index.html` (rewrite), `public/landing/{espanja,saksa,ranska}.html` (NEW), `css/landing-{de,fr}.css` (NEW), `css/landing.css` (+85 r), `vercel.json`, `sitemap.xml`, `robots.txt`, `routes/onboarding.js`, `sw.js`. **SW:** v138→v139. **Pending:** käyttäjän `npm run build` + Vercel-deploy + selaintesti `/saksan-yo-koe` waitlist-flow.
- **[2026-05-13 L-HERO-COUNTDOWN-AND-OFFCANVAS-1] Hero YO-countdown + app-shell off-canvas slide-in.** Landings (es/de/fr): uusi `<section class="yo-countdown">` heron yläpuolella — badge-pill, otsikko, sub, neljä tabular-num lukemaa kaksoispiste-separaattoreilla, kaksi CTAa, trust-rivi. JS `js/landing-countdown.js` (vanilla IIFE, idempotent) lukee lähimmän tulevan YO-kokeen `YO_DATES`-listasta (syksy 2026 vahvistettu 28.9.2026; kevät 2027 / syksy 2027 / kevät 2028 TODO-merkitty arvio), päivittää sek. välein, tukee `<480px` 2×2-gridin + `prefers-reduced-motion`. CSS appended to `css/landing.css` (~95 r). DE/FR CTA → "Liity waitlistille", ES → "Aloita ilmaiseksi". App-shell: uusi sticky topbar `<header class="app-topbar">` + hamburger `#menu-toggle` + backdrop `#app-sidebar-backdrop` näkyvissä vain `<1024px`. CSS `css/components/off-canvas-nav.css` (NEW): `transform: translateX(-100%)→0` 280 ms cubic-bezier, blur backdrop, staggered fade-in nav-itemeille (`--i` JS:n asettama), chevron hoverissa CSS::after-pseudolla (ei DOM-muutoksia per item), logout-pinned bottom, desktop ≥1024 koskematon (`!important` overridella). JS `js/features/offCanvasNav.js` (NEW): hamburger toggle, ESC/backdrop/nav-item-click sulkee, focus-trap auki ollessa, restore-focus hamburgeriin, `matchMedia`-resize-handler. A11y: `role="dialog"` + `aria-modal="true"` vain mobile-tilassa, `aria-expanded` togglataan, `aria-live="polite"` countdown-gridissä (ei assertive). **SW:** v145→v146, STATIC_ASSETS +3 (`/js/landing-countdown.js`, `/js/features/offCanvasNav.js`, `/css/components/off-canvas-nav.css`). **Verify:** `node --check` PASS molemmille JS:lle, `npm run test:bug-scan` 38 PASS / 2 skip. **Files:** `public/landing/{espanja,saksa,ranska}.html` (+~28 r kukin), `css/landing.css` (+95 r), `css/components/off-canvas-nav.css` (NEW 130 r), `js/landing-countdown.js` (NEW 85 r), `js/features/offCanvasNav.js` (NEW 130 r), `app.html` (+12 r topbar+backdrop+linkit), `sw.js` (CACHE_VERSION + 3 assettia). **Pending:** käyttäjän selaintesti mobile-koolla (<1024) hamburger-flow + countdown live-sek-päivitys.

- **[2026-05-25 L-V317-BRAND-MARK-1] Wordmark + favicon system + kurssi_X copy fix.** Hand-coded SVG wordmark `puheo` lowercase + brick-red square as period (one defendable gesture). `/public/brand/{logo,logo-mono,logo-dark,favicon-master}.svg` (~350-405 B each, well under 4 KB target). Favicon master = lowercase 'p' cream-on-brick (font-independent geometry), rendered via Playwright to /public/favicon/{favicon-16,favicon-32,favicon-48,apple-touch-icon,icon-192,icon-512}.png + root-level /favicon-32, /icon-192, /icon-512 PNGs replaced. Link tags updated across index.html, app.html, public/landing/{espanja,saksa,ranska}.html, diagnose.html with full SVG-first set + theme-color #A0341F. manifest.json theme_color + background_color repainted to brick + cream. **kurssi_X fix:** `js/screens/digikirja.js:403` sidemenu was rendering raw `Espanja · kurssi_2` — now uses new `prettyKurssi()` helper returning `Kurssi 2`. `lib/lessonLabels.js` fallback paths also hardened to never leak the raw `kurssi_N_lesson_M` key. **SW:** v309→v310 + STATIC_ASSETS +7 (brand + favicon assets). **Verify:** e2e-brand.spec.js 8/8 PASS (icon links resolve 200 on 5 pages + SVG/manifest content checks); e2e-bug-scan 19/19 PASS. **Files:** public/brand/*.svg (NEW), public/favicon/*.png (NEW), favicon-32.png/icon-192.png/icon-512.png (regen), index.html + app.html + 3 landings + diagnose.html (link tags), manifest.json, js/screens/digikirja.js (+helper), lib/lessonLabels.js, sw.js, scripts/generate-favicons.mjs (NEW), scripts/render-wordmark-preview.mjs (NEW), tests/e2e-brand.spec.js (NEW), docs/briefs/L-V317-brand-preview.png (NEW).

## L-V318-WORDMARK-PROPER — outline-path wordmark + slop-gate-screenshot (2026-05-26)

- **[2026-05-26 L-V318-WORDMARK-PROPER] Logo korjattu kunnolla.** L-V317-toteutus rikkoi 3 hard-constraintia (`<text font-family="Inter, system-ui">` runtime-font-dep, hardcoded `<rect x=140>` ei o-glyfistä, ei outline-pathiksi käännetty). Marcel huomasi 2026-05-26: "söhritty fontti + piste päin vittua". **L-V318-toteutus:** `scripts/generate-wordmark.py` (NEW, Python 3.14 + `fontTools` + `brotli`) lukee paikallisen `fonts/inter-latin-700-normal.woff2`-tiedoston, extractoi `p u h e o`-glyfien path-d:t `SVGPathPen`:llä, laskee o-glyfin geometric center bounding-boxin perusteella, sijoittaa 4 px brick-square offset 1px upper-right. **Sample-research vahvisti L-V317-päätöksen:** 8 suomalaista edu-brandiä (WordDive, SanomaPro, Otava, Yousician, 10monkeys, Wilma, Mafy, YTL) — wordmark-only + sans on lukio-konventio, brick+cream + lowercase tekee Puheosta erottuvan (vault: `Puheo/brainstorms/2026-05-26-edu-brand-benchmarking.md`). **Fontti-päätös:** Inter-700 (paikallisesti woff2). L-V317-brief sanoi General Sans, mutta projektin todellinen typografia-stack on `--font-display: "Geist", "Inter", system-ui` (`css/landing-tokens.css:45`) — General Sansia ei ole projektissa. Memory `feedback_typography_general_sans_manrope.md` päivitettävä. **SVG-output:** `public/brand/{logo,logo-mono,logo-dark,favicon-master}.svg` 2.6-2.7 KB minifoituna (alle 4 KB tavoitteen). `<text>`-elementtejä ja `font-family`-attribuutteja 0 kpl (hard-gate PASS). aria-label="Puheo", `<title>puheo</title>` (matchaa molempia brand-spec-asseroita). **Favicons regen:** `node scripts/generate-favicons.mjs` → `public/favicon/{favicon-16,32,48,apple-touch-icon,icon-192,icon-512}.png` + root-level kopiot `favicon-32.png`/`icon-192.png`/`icon-512.png`. **Slop-gate-screenshot:** `docs/briefs/L-V318-slop-gate-comparison.png` (Puheo-favicon tab-stripissä Vercel/Linear/Stripe/Otava/SanomaPro-kontekstissa + 16/32/64 px -näkymät). **Wordmark-preview:** `screenshots/brand/wordmark-preview.png` (5 variantia + favicon-näkymät). **Verify (3 gate PASS):** (1) `grep "<text\|font-family" public/brand/*.svg` = 0 osumaa; (2) `npx playwright test tests/e2e-brand.spec.js` = 8/8 PASS; (3) `npm run test:bug-scan` = 38/38 PASS + 2 skip. **SW:** v310 → v311. **Skills invoked:** `frontend-design`, `design-taste-frontend`, `ui-ux-pro-max`, `webapp-testing`, `emil-design-eng`, `superpowers:writing-plans`, `superpowers:verification-before-completion`, `superpowers:brainstorming`. **Vault:** `Puheo/decisions/2026-05-25-logo-wordmark.md` status open → decided + L-V318-toteutus-osio. **Files:** `scripts/generate-wordmark.py` (NEW), `scripts/screenshot-wordmark.mjs` (NEW), `scripts/slop-gate-screenshot.mjs` (NEW), `public/brand/{logo,logo-mono,logo-dark,favicon-master}.svg` (REWRITE), `public/favicon/*.png` (REGEN), `favicon-32.png` `icon-192.png` `icon-512.png` (REGEN root), `screenshots/brand/wordmark-preview.png` (NEW), `docs/briefs/L-V318-slop-gate-comparison.png` (NEW), `sw.js` v311. **Pending:** käyttäjän vahvistus ennen `git push origin main` → Vercel auto-deploy.

## L-V318b-LOCKUP-KILL-DOT — council 5/5 kills the ornament, formalize wordmark+sigil (2026-05-26 ilta)

- **[2026-05-26 L-V318b-LOCKUP-KILL-DOT] Council 5/5 lukitsee ornamentin lopullisesti.** Marcel rejected L-V318:n o-square ("ihan vitu paska"). 5 advisoria + 5 peer-reviewers spawned via `llm-council`-skilliä: **4/5 advisoria + 5/5 reviewers äänesti V1 NAKED**. V2 `puheó`-accent hylätty kategoria-virheenä (Spanish-coded merkki multi-lang brändissä valehtelee DE/FR-käyttäjille). V3 underline hylätty usability-bugina (heavy red under heavy red = hyperlink, lukio-rep yrittää klikata, trust-erosion). Expansionist:n language-mutating wordmark hylätty 5/5 reviewers — dies on contact with favicon/OG-image/Stripe-receipt/email-signature (kaikki kiinteät assetit vaativat visual constant:n). **Ratkaiseva insight:** kaikki 5 advisoria + 5 reviewers missasivat että `public/brand/favicon-master.svg` (brick-neliö cream-värisellä "p"-kirjaimella) JO ON sigili joka tekee tasan sen mitä First Principles -ajattelija sanoi puuttuvan — wordmark + sigili lockup-systeemi on tuotannossa, kukaan ei vain ole nimennyt sitä. **Lopullinen brand-systeemi:** Wordmark (`puheo` lowercase Inter-700, brick on cream, **ei ornamenttia**) horisontaalisiin pintoihin (landing-header, footer, email-body, marketing). Sigili (`p`-brick-square favicon-master.svg → favicon-{16,32,48,180,192,512}.png) neliö/pieni-pintoihin (favicon, app-icon, social-avatar, OG-corner-badge, push-notif). **Dokumentaatio:** `docs/brand/lockup.md` (NEW) lukitsee 5 sääntöä: ei sigili+wordmark rinnakkain, ei väri-muutoksia, ei ornamenttia wordmarkiin, ei glyph-animaatiota, wordmark fallback sigiliksi alle 80 px:ssä. **Ei kolmatta iteraatiota.** **Vault-päätös:** `Puheo/decisions/2026-05-25-logo-wordmark.md` status open → **decided**, lopullinen lukitus. **Files:** `docs/brand/lockup.md` (NEW), IMPROVEMENTS.md (this row). SVG-tiedostoja EI muutettu — L-V318-commit:in jälkeen `compare-variants.mjs`-skripti palautti naked-variantin oletukseksi, ja tila on tarkistettu (grep `<rect` logo.svg = 0 osumaa, favicon-master.svg = 1 osuma container-neliölle, hard-gate `<text>/font-family` = 0 osumaa). **Skills invoked:** `llm-council`, `superpowers:brainstorming`, `superpowers:writing-plans`. **Pending:** käyttäjän vahvistus ennen `git push origin main`.

## L-V318c → L-V318e — brand-loop yön iteraatiot (2026-05-26 yö)

- **[2026-05-26 L-V318c-WIRE-WORDMARK]** Marcel huomasi että L-V318:n wordmark-SVG ei näkynyt sivuilla — kaikki 14+ HTML-paikkaa renderöivät CSS-styled `Puhe<span>o</span>` -tekstiä joka EI viitannut uuteen SVG:hen. Korjattu: vaihdoin kaikki `<a class="nav__brand|sidebar-logo|app-topbar__brand|...">Puhe<span>o</span></a>` → `<img src="/public/brand/logo.svg" class="brand-img">` 11 HTML-tiedostossa, 16 instanssia. SVG-tiedostosta poistettu myös leftover ornamental-dot `<rect>` jonka V318b lockup-doc oli jo dokumentoinut pois. Commit `181c48a`.
- **[2026-05-26 L-V318d-FILL-RULE]** Marcel huomasi pieni musta täplä o-kirjaimen sisällä — fontTools SVGPathPen emittoi ulko- + sisäkontuurit samaan path:iin, ja SVG default `fill-rule="nonzero"` täytti molemmat. Lisäsin `fill-rule="evenodd"` 4 SVG-tiedostoon + `scripts/generate-wordmark.py`. Korjasi o:n umpinaisuuden mutta rikkoi e-glyfin renderöinnin iOS Safarilla (näkyi vaaleampana). Commit `652c603`.
- **[2026-05-26 L-V318e-CSS-TEXT-RESTRUCTURE]** **Kolmas strike → restrukturoi rakenne** (CLAUDE.md memory `feedback_three_strikes_redesign`). Vaihdoin koko lähestymistavan: (a) **HTML-sivut käyttävät CSS-styled tekstiä** — `<span class="brand-wordmark">puheo</span>` Inter-700 self-hosted woff2:lla, renderöityy identtisesti joka selaimella. `.brand-wordmark`-säännöt `style.css` + `css/landing.css` (landingit eivät lataa style.css:ää). `!important` brick-värille body-cascade-overrideen. (b) **External-käytön SVG:t regeneroitu** `scripts/generate-external-svg.py`:llä — Inter-700 woff2 embedded inline-Base64:nä `<defs><style>@font-face{src:url(data:font/woff2;base64,...)}</style>` + `<text font-family="Inter">`. SVG:t renderöityvät täydellisesti Slackissa, Figmassa, Keynote:ssa, sähköposti-clientissä, printissä. Koko ~33 KB / kpl (oli 3 KB; trade-off portability:lle). **Final brand-systeemi:** HTML = CSS-text, external = embedded-font SVG. **Files:** `style.css`, `css/landing.css`, 11 HTML-tiedostoa (img → span), `public/brand/*.svg` (4 tiedostoa regen), `public/favicon/*.png` + root PNG:t regen, `scripts/generate-external-svg.py` (NEW), `scripts/swap-img-to-css-wordmark.mjs` (NEW reference). **SW:** v318 → v319. **Verify:** lokaali screenshot näyttää brick `puheo`, preview näyttää kaikki 4 SVG-varianttia puhtaina. Commit `e2fb2a8`. **Opetus tuleville logo-iteraatioille:** fontTools SVGPathPen ei tuota luotettavasti renderöityviä polkuja Inter-counter-glyfeille; CSS-text + self-hosted woff2 on aina parempi HTML-pinnoilla, embedded-font SVG external käyttöön.

## L-V324 — Settings-sivun mobile CLS 0.213 → 0.0005 (2026-05-27)

- **[2026-05-27 L-V324-SETTINGS-CLS]** L-V323-baseline löysi yhden aidon perf-bugin: `l03-settings` mobile CLS 0.213 (NI), muuten 38/40 pintaa good. Juurisyy: `showSettings()` yliajoi staattisen 3-rivin skel-rakenteen yhdellä `<p>Ladataan profiilia…</p>` -tekstillä (~30px), sitten renderöi 8 oikeaa `.settings-row`-elementtiä (~560px) — kaksi peräkkäistä siirtymää nuijivat alla olevat sektiot. Lisäksi `#settings-subscription-rows` oli staattisesti 0 px (vain `sr-only`-teksti) → tier-badge + nappi ~120px ilmestyi myöhemmin. **Fix:** (a) staattinen HTML reservoi nyt 8 row-muotoista skel-placeholderia profile-rowsiin + 1 tier-row-skel subscription-rowsiin, (b) `showSettings()` ei enää tuhoa skeletonia ennen `renderRows()`:tä, (c) uusi `.settings-row--skel { min-height: 56px }` -sääntö varmistaa että lopullinen sisältö ei kasva yli reservoidun ulkokorkeuden. **Files:** `app.html` (2 sectionia), `js/screens/settings.js:535-540` (poistettu ylikirjoitus), `style.css` (.settings-row--skel), `scripts/probe-settings-cls.mjs` (NEW — focused layout-shift-probe attribuoituine elementteineen), `sw.js` v319 → v320, `app.bundle.js+css` rebuilt. **Verify:** local mobile CLS 0.0005 (oli 0.213), local desktop CLS 0.0001 (oli 0.085), brand-e2e 16/16 PASS, bug-scan 38/38 PASS. Prod re-audit `AUDIT_BASE_URL=https://espanja-v2-1.vercel.app node scripts/polish-audit-perf.mjs` Vercel-deployn jälkeen. **Skills:** frontend-design, webapp-testing, superpowers:verification-before-completion.
- **[2026-05-27 L-V325-FAVICON-LEGIBILITY] Favicon-glyfin luettavuus 16/32/48 px:ssä.** Marcel huomasi että selaintabin favicon näytti solidilta brick-neliöltä — "p":n bowl katosi 16 px-renderöinnissä koska font-size 42 / 56 viewBox jätti glyfille vain ~55% pintaa. Generoin 3 varianttia ja vertailustripin (`screenshots/brand/favicon-legibility-compare.png`): A bigger (font-size 52 + letter-spacing -2.0, embedded Inter), B outline (fontTools-path 75% glyph-height), C inverted (cream bg + brick "p"). **Marcel valitsi A:n** — sama embedded-Inter + `<text>`-pipeline kuin nykyinen master, vain glyfi 24% suurempi → bowl näkyy selvästi 16 px:ssä, ei pipeline-vaihtoa joka voisi tuoda L-V318e:n counter-bugit takaisin. **Files:** `public/brand/favicon-master.svg` (variant-A copy, 33 KB sis. embedded Inter — sama koko kuin V318e:n jäljiltä; brief-acceptance #4 (≤4 KB) oli pre-V318e-aikainen ja vanhentunut), `public/favicon/{favicon-16,32,48,apple-touch-icon,icon-192,icon-512,favicon-master}.png` regen `node scripts/generate-favicons.mjs`:llä, root-level `favicon-32.png` + `icon-192.png` + `icon-512.png` mirror SW-cache:n vuoksi, `public/brand/variants/favicon-variant-{A,B,C}.svg` (review-materiaali), `scripts/generate-favicon-variants.py` (NEW), `scripts/favicon-legibility-strip.mjs` (NEW), `screenshots/brand/favicon-legibility-{compare,after}.png`. **SW:** v320 → v321. **Verify:** brand-e2e 16/16 PASS, bug-scan 38/38 PASS, after-strip näyttää selvästi luettavan "p":n 16 px:ssä. **Skills:** frontend-design, webapp-testing, superpowers:verification-before-completion.
- **[2026-05-27 L-V325b-REVERT-FAVICON] Variant A peruutettu — bigger glyph teki 16 px:ssä huonomman.** Marcel raportoi shipin jälkeen että "before oli sellane siisti p ja nyt ei oo". Renderöin OLD vs A vs B truesize-vertailun Chromium devicePixelRatio=2 -rasterilla 6× zoom-näkymällä (`screenshots/brand/favicon-truesize.png`): 16 px:ssä OLD (font-size 42) säilyttää bowl-counterin avoimena, A (font-size 52) puristaa stem-leveyttä siten että bowl-interior kapenee ja descender lyhentyy → kirjain peittää enemmän tilaa mutta menettää luettavuuden. B (outline path) keskitti paremmin kuin A mutta sama compression-ongelma. Subpixel-rasterointi on epälineaarinen: isompi glyfi 16 px-tilella ≠ paremmin luettava. **Toimi:** `public/brand/favicon-master.svg` palautettu pre-V325-versioon (`git show HEAD~2`, font-size 42, letter-spacing -1.0, y=42), PNG:t regen `generate-favicons.mjs`:llä, root-mirrors päivitetty. **SW:** v321 → v322. **Verify:** brand-e2e 16/16 PASS, truesize-vertailu tallennettu. **Opetus:** L-V325 brief lähti väärästä hypoteesista (font-size 52 = parempi 16 px). Truesize-pikselivertailu Chromium-rasterilla on pakollinen ENNEN ship:iä kun puhutaan favicon-luettavuudesta — comparison-strip-skripti `favicon-legibility-strip.mjs` skaalasi SVG:t CSS-pikseleinä joka ei näytä todellista rasterointia. Jos myöhemmin halutaan dramaattisemmin luettava 16 px favicon, vastaus ei ole pelkkä font-size-säätö vaan kokonaan eri glyfi-muoto (esim. chunky-tyylinen uppercase P tai kokonaan custom-sigili).  **Skills:** frontend-design, superpowers:verification-before-completion.
