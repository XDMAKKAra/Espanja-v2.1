# Pass 1.5 — Viewport priority correction (single prompt, layout fix)

You are working in `C:\Users\marce\OneDrive\Documents\espanja paska\`. Pass 1 (visual design system) shipped tokens, components, typography, and re-skinned every screen. However, Step 1's spec was updated mid-build with a critical clarification about viewport priority that didn't make it into Step 2's output. This pass closes that gap.

**Prerequisites:** Pass 1 Step 2 fully merged. Branch this pass off a fresh `main`.

## Skills

- `.claude/skills/ui-ux-pro-max/SKILL.md` — for layout reference, especially desktop dashboard / two-column patterns.

## What's already correct (don't touch)

- Color tokens, typography scale, spacing scale, radius scale, shadow scale.
- Component primitives (button, input, card, chip, skeleton, banner, toast, modal).
- Navigation (top-nav, bottom-nav, sidebar).
- Marketing site (`index.html`, `pricing.html`, `diagnose.html`, `blog/*.html`) — these are correctly mobile-first and should stay that way.

## What needs fixing

**The two-track viewport priority** added to `plans/01-design-system-step1.md` after Step 2 had already started running:

- **App (`app.html` + screens under `js/screens/`) is desktop-first**, not mobile-first. Real users do exam prep at a laptop. Phone use is incidental.
- **Marketing site is mobile-first**, as it already is. No change needed there.

Pass 1 Step 2 likely re-skinned app screens with mobile-first proportions (compact widths, single-column layouts, mobile-first padding scales) that work on phone but waste space on a 1440px laptop. This pass corrects that for the app only.

## Audit step (do first, before touching anything)

Open the live site at 1440×900 and screenshot every app screen:
- Dashboard
- Learning path
- Vocab mode page + vocab exercise
- Grammar mode page + grammar exercise
- Reading mode page + reading exercise
- Writing mode page + writing exercise
- Exam mode page + exam screen
- Settings

For each, write to `viewport-fix/AUDIT.md`:
- Current content width
- Whether the layout is single-column or multi-column
- Whether the screen "uses the laptop" or feels like a phone screen with margins
- File + line citation for the container CSS rule

Also screenshot at 390×844 (phone) for comparison — phone layouts must remain usable. The goal is "good on laptop AND good on phone," not "good on laptop, broken on phone."

## The fix

Per the breakpoint spec in `plans/01-design-system-step1.md`:

- **Mobile (<768px):** unchanged. Whatever Pass 1 Step 2 shipped for phone stays.
- **Tablet (768–1023px):** transitional, sidebar collapses, content ~720px.
- **Desktop (≥1024px):** content uses **≥960px** on dashboard, learning path, every exercise screen. Use two-column layouts where they make sense:
  - **Exercise screens:** exercise on left, feedback/hints panel on right.
  - **Dashboard:** stats + chart on left, recent activity on right.
  - **Learning path:** topic tree on left, current topic detail on right.
  - **Writing editor:** editor on left, rubric/word count panel on right.
- **Wide (≥1440px):** cap at 1280–1440px, centered.

For each app screen audited, modify the container CSS to honour these rules. Don't introduce new design tokens — use existing spacing/breakpoint tokens from Pass 1.

If Pass 1 Step 2 introduced container utility classes (e.g. `.app-container`, `.content-narrow`, `.content-wide`), use them. If it didn't, don't invent them this pass — just fix the per-screen widths inline against existing tokens.

## Side-panel pattern (from Pass 1 Step 1 TODO)

Pass 1 Step 1 specced a "Kokeile vaikeampaa →" side affordance. Verify Step 2 shipped it. If it did, reuse the side-panel container for the right-side panels (feedback, rubric, etc.). If it didn't, add a minimal `.exercise-side-panel` class — single column on phone, fixed-width right rail on desktop. Spec it briefly in `viewport-fix/SIDEPANEL.md`.

## Commit sequence

Target: 4–6 commits, single branch `fixes/viewport-priority`.

1. **`chore(audit): viewport-fix audit screenshots + AUDIT.md`** — no code, just the artifact.
2. **`fix(layout): exercise screens use ≥960px on desktop, two-column where applicable`** — vocab, grammar, reading, writing, exam. May split into 2–3 commits if the diff is large.
3. **`fix(layout): dashboard + learning path use desktop-first layouts`**
4. **`fix(layout): settings + auxiliary screens`**
5. **`chore(test): visual regression snapshots updated for new desktop layouts`** — re-baseline Playwright screenshots in `exercises/baselines/` for the affected screens. Phone snapshots should NOT change; desktop snapshots will.

## Quality bar

- Every modified screen looks intentional on a 1440×900 laptop. No huge empty margins.
- Every modified screen still works on 390×844 phone. Mobile experience is not regressed.
- Visual regression: phone baselines unchanged; desktop baselines re-snapped intentionally.
- No changes to color tokens, typography, components, or marketing pages.
- `viewport-fix/POSTSHIP.md` — one paragraph: which screens were fixed, before/after width, what went into side-panel pattern.

## Done

- All commits merged straight to main.
- Deployed to production.
- marcel verifies on a real laptop that the app no longer feels "phone-shaped" and on a phone that it still works.
