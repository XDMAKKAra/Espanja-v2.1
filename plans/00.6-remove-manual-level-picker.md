# Pass 0.6 — Remove manual taso-picker, use per-topic mastery (single prompt, bugfix)

You are working in `C:\Users\marce\OneDrive\Documents\espanja paska\`. Pass 0.5 shipped 3 critical fixes (onboarding persistence, login link, desktop-width criterion). This is a follow-up pass for one more bug that didn't make it into 0.5 because it was discovered after 0.5 had already run.

**Prerequisites:** Pass 0.5 merged. Branch this pass off a fresh `main`.

## Skills

No specialised skill. Straight bugfix discipline: reproduce → root-cause → minimal fix → regression test.

## The bug

**User report:** "minun nettisivuni pitäisi tietää minun tasoni eri kohdissa esim sanasto kirjoittaminen yms, joten tuo että voin valita tasoni tuolla alhaalla on aika huono."

**Root cause:** Vocab, grammar, and reading mode pages each render a manual B/C/M/E/L (and I/A on vocab) "TASO" picker that the user must click before starting. The picker overrides whatever the system already knows about the user from `user_mastery` and `diagnostic_results`. This defeats the entire adaptive system: a user who placed at C in ser_estar but B in subjunctive sees the same default-active button on every screen and either guesses, picks their target grade (too hard), or sticks with the default (B for vocab, C for grammar/reading) regardless of their real level.

**Real-world failure mode:** B1 student who tested at M in vocabulary clicks the default-active "B" button without thinking, gets too-easy items, gets bored, leaves. Or: same student clicks "M" because that's their target grade, gets crushed, leaves.

## Code locations to remove (verify line numbers first — Pass 0.5 may have shifted them)

All in `app.html` unless noted. Search for `class="level-picker"` to re-confirm locations before editing:

1. `#vocab-page-level-picker` (vocab mode page) — around `app.html:657`
2. `#grammar-page-level-picker` (grammar mode page) — around `app.html:700`
3. `#reading-page-level-picker` (reading mode page) — around `app.html:856`
4. Legacy `#level-picker` (vocab inline) — around `app.html:966`
5. Legacy `#grammar-level-picker` (grammar inline) — around `app.html:1034`
6. Legacy `#reading-level-picker` (reading inline) — around `app.html:1059`

**⚠️ DO NOT remove `#verbsprint-duration-picker`** (around `app.html:741`). It uses the same `.level-picker` CSS class but represents *duration* (10/20 seconds), not CEFR level. Verify by checking the `data-duration` attribute vs `data-level`.

## Listeners to remove or rewrite

- `js/main.js:132-178` — page-level-picker listeners + `state.level` / `state.grammarLevel` / `state.readingLevel` reads. Replace with mastery lookup (see fix below).
- `js/main.js:329-352` — legacy `#level-picker`, `#grammar-level-picker`, `#reading-level-picker` listeners. Delete entirely; the elements are gone.
- `js/screens/dashboard.js:629-654` — picker active-class toggling on dashboard navigation. Delete; pickers don't exist anymore.
- `app.js:844-880` and `app.js:1070-1180` — duplicate handlers in the legacy bundled `app.js`. Before editing: verify whether `app.js` is still loaded by any HTML page (grep `<script.*app\.js` in `app.html` + `index.html`). If dead code, mark for deletion in a follow-up commit but at minimum don't leave conflicting handlers. If still loaded, apply the same edits as `js/main.js`.

## The fix (new behaviour)

When user clicks "Aloita harjoittelu" on vocab/grammar/reading mode pages, the client must:

1. Read the selected topic from the topic-picker (topic choice stays — that's a real choice, not level).
2. Call `GET /api/mastery?topic={topic}`.
   - Check `routes/progress.js` first — an endpoint may already exist under a different name. `user_mastery` table is queried in `routes/exercises.js:895`, so data access is already wired; you may just need to expose a lookup.
   - If endpoint doesn't exist, add it to `routes/progress.js`: takes `topic` query param, returns `{ topic, current_level, updated_at }` or `{ topic, current_level: null }` if no row.
3. If a mastery row exists for this topic, use `mastery.current_level` for the exercise request.
4. If no mastery row exists (new topic for this user), fall back to the user's global placement level from `diagnostic_results.placement_level`.
5. If no placement either (placement test not taken), fall back to `"B"` for vocab and `"C"` for grammar/reading (current defaults). This case should be rare because Pass 0.5's onboarding fix forces placement completion.

State variables `state.level`, `state.grammarLevel`, `state.readingLevel` become derived-on-demand instead of picker-driven. They can stay as variables but their values come from the mastery lookup, not from a DOM read.

## Empty-space handling

Removing 6 picker rows leaves a visual gap above the "Aloita harjoittelu" button. Don't paper over it — let the topic-picker breathe. Pass 1 (design system) will rework these mode pages. If the gap looks really bad in the meantime, replace it with a small read-only status line: `<div class="user-level-display">Tasosi tässä aiheessa: <strong>M</strong></div>` that reads from mastery. No buttons, no interaction. Just a hint to the user about what the system knows.

## "Haasta itseäsi" affordance (NOT this pass)

Some users will legitimately want to try a harder level. That affordance is deferred to Pass 1 Step 2 — a small "Kokeile vaikeampaa →" link on the exercise page (not the mode page, not in the start flow). Pass 1's DESIGN.md already has it as a spec requirement (verify in `design-system/DESIGN.md` after Pass 1 Step 1 runs). This pass: just remove the picker.

## Commit sequence

Target: 2 commits, single branch `fixes/no-manual-level-picker`.

1. **`fix(api): add /api/mastery?topic= endpoint if missing`** — server-side only. Verify current state first; if endpoint exists under a different name, this commit becomes empty and you skip to commit 2.
2. **`fix(exercises): remove manual taso-picker, use per-topic mastery`** — delete 6 level-picker blocks from `app.html`, rewrite state reads in `js/main.js` + `js/screens/dashboard.js` + `app.js` (if loaded), add `tests/no-manual-level-picker.test.js`.

## Regression test

`tests/no-manual-level-picker.test.js`:

- Start a session for a user with `user_mastery` row `{ topic: "ser_estar", current_level: "C" }`. Trigger vocab generation request (POST `/api/exercises/vocab` with `topic: "ser_estar"`). Assert the request includes `level: "C"`, not `"B"`.
- Repeat for grammar with mastery `"M"` — assert `"M"`, not the default `"C"`.
- Third case: user with no mastery row and `diagnostic_results.placement_level = "E"`. Assert request uses `"E"`.
- Fourth case: user with neither mastery nor placement. Assert request uses `"B"` (vocab) or `"C"` (grammar/reading).

## Quality bar

- All 4 test cases pass.
- Manual QA on live site: log in as marcel, open vocab mode page. Picker is gone. Select a topic for which he has mastery, start exercise, confirm items feel right for his level.
- No console errors from orphan picker event listeners.
- `state.level` et al. are either deleted or computed from mastery — no dead variables.

## Done

- 2 commits merged (straight to main, no PR).
- Deployed to production.
- `fixes/POSTSHIP-0.6.md` — one paragraph: what was the root cause, what shipped, what regression test guards it.
- marcel confirms picker is gone and exercises feel right-levelled on live site.
