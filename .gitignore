.aider*
.env
node_modules/
*.db
*.db-shm
*.db-wal
.DS_Store
*.log
.vercel
.agents/

# Local Claude / Continue / scratch setup — do not commit
.claude/
.continue/
skills/
skills-lock.json
spanish/
graphify-out/
scripts/screenshot_recon.py

# Audit / pass artefacts (local only — see training/, ui-ux/, audit/ in work dir)
audit/
ui-ux/
training/
*-prompt.md

# Design-system: puheo/ is auto-generated boilerplate (see FINDINGS.md §1);
# only the Step 1 spec docs at design-system/*.md are tracked.
design-system/puheo/

# Playwright test artifacts
test-results/
playwright-report/
