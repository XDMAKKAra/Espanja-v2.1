.aider*
_audit/
.env
.env.local
.env.*.local
.env.production
.env.development
*.pem
*.key
node_modules/

# L-LIVE-AUDIT-P2 — sourcemaps for bundles (committed bundles serve from repo root)
app.bundle.js.map
app.bundle.css.map
# F-ARCH-1 §A — code-split chunk sourcemaps (chunks themselves are committed
# so `npm run build` → deploy works, but their maps stay local).
chunks/*.map
*.db
*.db-shm
*.db-wal
.DS_Store
*.log
.vercel
.agents/

# Local Claude / Continue / scratch setup — do not commit
.claude/
.continue/
.superpowers/
skills/
skills-lock.json
spanish/
graphify-out/
scripts/screenshot_recon.py

# Audit / pass artefacts (local only — see training/, ui-ux/, audit/ in work dir)
audit/
ui-ux/
training/
*-prompt.md

# Playwright test artifacts
test-results/
playwright-report/
coverage

# Agent test outputs — heavy binaries, regenerable per loop
scripts/agent-test/screenshots/
scripts/agent-test/inspiration/
scripts/agent-test/lighthouse-*.json
scripts/agent-test/audit-report.json

# Remotion preview reel — captures + rendered MP4s are regenerable
marketing/preview/public/captures/
marketing/preview/out/
marketing/preview/node_modules/
