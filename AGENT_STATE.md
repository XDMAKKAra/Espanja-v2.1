# Puheo Agent State

**Last updated:** 2026-04-29T16:30:00Z
**Last completed loop:** L-PLAN-3 (exercise loop wired to curriculum + dashboard tutor voice — UPDATE 1 `lib/lessonContext.js` + curriculum-aware payload `{ lesson: { kurssiKey, lessonIndex } }` accepted by `/api/generate`, `/api/grammar-drill`, `/api/reading-task`. Helper resolves the lesson from Supabase or `lib/curriculumData.js` mirror, overrides level + topic + count from kurssi/lesson, injects a Finnish "stay on focus" instruction into the OpenAI prompt (recognition→production order, repeat ydinsanoja in last item) or a kertaustesti instruction (every grammar_focus[] topic, even coverage, topic_key per question). Bank lookups + saves bypassed when context active so the generic free-practice bank stays clean. Frontend: new `js/lib/lessonContext.js` with `getLessonContext()` / `clearLessonContext()`; vocab.js + grammar.js + reading.js spread `{ lesson }` into their fetch bodies. UPDATE 3 `GET /api/curriculum/tutor-message` (registered BEFORE `/:kurssiKey` to avoid being shadowed) — daily-cached AI greeting via new `user_profile.tutor_greeting` + `tutor_greeting_at` columns; `optionalAuth` returns `{ message: null }` for anonymous so the dashboard hides the card cleanly; cache miss path pulls last user_curriculum_progress row + placement_kurssi + weak_areas[0] + exam_date (defaults 28.9.2026) and calls `callOpenAI(prompt, 80, { temp 0.7 })`. Sanity-checks the response (≥20 chars, ≤300, no leading bullet) — falls back to one of 4 hardcoded time-of-day Finnish strings on AI failure. New `<section class="dash-tutor">` + skeleton inserted directly under `header.dash-greeting` in app.html; dashboard.js `loadTutorMessage()` runs in parallel with renderDashboard, sessionStorage-cached per tab. UPDATE 2 post-session lesson results card — new module `js/screens/lessonResults.js` + new `#screen-lesson-results` shell + ~140 lines of token-only CSS appended to `curriculum.css`. Single-column dark card with mono score, accent accuracy bar (animated width 600 ms cubic-bezier, prefers-reduced-motion gated), italic metacognitive 1-liner, prose tutorMessage, fast-track callout when 3 perättäistä >85% in same kurssi. POST `/complete` extended to accept `wrongAnswers[]` (sanitised: ≤20 entries, fields ≤200 chars) and return BOTH `tutorMessage` AND `metacognitivePrompt` from a single `callOpenAI(prompt, 200, { temp 0.5 })` plus `fastTrack` boolean from the new `checkFastTrack()` helper. vocab.js/grammar.js/reading.js short-circuit their legacy results screens at the top of showXResults when getLessonContext() is set, build wrongAnswers from existing session state, dynamic-import lessonResults.js. Removed the temporary `Merkitse suoritetuksi (väliaikainen)` button + dead markComplete helper from curriculum.js. SW v97→v98, added lessonResults.js + lessonContext.js to STATIC_ASSETS. Migration file extended with the 2 new tutor_greeting columns. Verified live: anonymous /api/curriculum/tutor-message returns {message:null}; route ordering intact (kurssi_3 still 200). All 1 061 vitest tests still pass. Five deferred items in IMPROVEMENTS.md L-PLAN-3 block: Supabase migration application, axe sweep on lesson-results + dashboard-with-tutor-card, 21st.dev sourcing pass on /s/results + /s/score-card, real per-question reading data, defensive A→B reading mapping for kurssit 1–2.).
**Next loop:** L-PLAN-4 (streak system + fast leveling UI + Kurssi 1 seed content full test — read AGENT_PROMPT_LPLAN4.md if it exists, otherwise CURRICULUM_SPEC.md §4 + §7 again. Apply the SQL migration to Supabase via the dashboard SQL editor as the very first step (closes L-PLAN-1, L-PLAN-2, L-PLAN-3 deferred DB work in one shot — `supabase/migrations/20260429_curriculum.sql` includes `curriculum_kurssit` + `curriculum_lessons` + `teaching_pages` + `user_curriculum_progress` + 9 new `user_profile` columns including the L-PLAN-3 `tutor_greeting` pair). Then flip the L-PLAN-1 V2 onboarding from `#/aloitus` hash entry to the default boot path in `checkOnboarding()`. Then run an end-to-end Kurssi 1 walkthrough — sign up → onboarding → kurssi 1 lesson 1 → exercises → results card → complete → kurssi 1 lesson 10 (kertaustesti) → unlock kurssi 2 → verify dashboard tutor message refreshes after 24h or on session-cache invalidation. Add streak persistence (current `js/screens/dashboard.js` reads streak from /api/dashboard but doesn't persist on completion of curriculum lessons specifically — verify the bridge fires) and the fast-leveling shortcut UI (>90% on first lesson of kurssi → "Tämä vaikuttaa tutulta — tehdäänkö suoraan kertaustesti?" callout per CURRICULUM_SPEC §4 fast-track rule). Do NOT bump SW unless STATIC_ASSETS change. Do NOT touch landing page.).

**21st.dev is now the primary sourcing library** for L46–L49 (and the post-L49 popup). Per the user's mid-session brief 2026-04-28: visit `21st.dev/s/pricing` via Playwright, screenshot 2-3 candidates to `references/landing/21stdev/pricing/`, pick best fit for Puheo's restrained Linear-tier aesthetic (avoid loud / over-animated), port React+Tailwind → vanilla matching `css/landing.css` patterns, cite exact 21st.dev component URL in IMPROVEMENTS.md. Structure from 21st.dev, copy from Puheo (Finnish, never generic SaaS).

Build:
1. **Pricing** (`#hinnoittelu`, 2 cards side-by-side equal width, Free left + Pro right with mint border + "Suosituin" eyebrow chip). Prices: Free 0 €, Pro from `routes/stripe.js` config — grep first; if hardcoded, hardcode here with TODO comment. Each card: plan name, big price, value prop, ~6-item feature list with check / × marks (Lucide), single CTA. **Both CTAs → `/app.html#rekisteroidy`** per Y-tunnus / live-payment constraint (spec appendix #2). Pro upsell happens inside dashboard, never on landing.
2. Verify via `scripts/agent-test/loop46-pricing.mjs`. 0 axe violations both viewports. One IMPROVEMENTS line citing the exact 21st.dev component URL + Linear/Vercel pricing as cross-reference.

**FAQ moves to L47** (21st.dev /s/faq, smooth accordion under `<details>` for a11y, 6 honest answers from spec appendix #5).

**Active roadmap:** `PLAN.md` — user-directed P0 items, now expanded to **10 items** (4 bugs ✓ + 3 redesigns L40 ✓ L41 ✓ + 8-loop landing rebuild L42 ✓ L43 ✓ L44 ✓ L45 ✓ L46 ✓ → L49 + 2-loop right-rail rebuild + Pro popup L50–L51 + 4-loop dark-theme polish L52–L55).

**Mid-session updates received 2026-04-28** (replace earlier "regression token-leak fix" instruction):
- Token leak from L42/L43 was a **happy accident** — user likes the dark teal landing palette and wants the entire app on the same palette for visual continuity. Do it **properly** post-L49: shared tokens file (`css/tokens-shared.css`), keep existing app theme toggle (Auto/Vaalea/Tumma) working, "Tumma" gives the new dark, "Vaalea" stays mint/light, "Auto" follows system. Do NOT pause landing momentum to do this now.
- Right rail Pro card is too big — keep small top-right user chip (already correct from L41), remove large Pro panel from rail, replace with daily-progress + word-of-day shown to ALL users. Pro upsell becomes a dismissible bottom-right popup (~320 px, slide-up + fade-in 300 ms ease-out, only after 5 s on page, 7-day suppress on dismiss, free users only). Source pattern from Magic UI / Aceternity / 21st.dev / shadcn sonner. Do NOT pause landing momentum to do this now.

## What I just did
Loop L-PLAN-2 — **curriculum structure + Oppimispolku screen**, three updates in one loop. **UPDATE 1** (`supabase/migrations/20260429_curriculum.sql` + `lib/curriculumData.js`): authored the curriculum migration creating `curriculum_kurssit`, `curriculum_lessons`, `teaching_pages`, `user_curriculum_progress` with proper FKs + `ON DELETE CASCADE`; folded the L-PLAN-1 deferred `user_profile` columns into the same migration so applying it once closes both loops' DB work. Seed: 8 kurssit with `grammar_focus` keyed only to **existing `LEARNING_PATH`** entries (no new keys needed — verified each maps), `vocab_theme` to existing `VALID_VOCAB_TOPICS`. 90 lessons total (brief miscounted; spec §3 line counts add to 90, not 86). Worked-example fading applied: lesson 1 of each kurssi has a full worked example in `teaching_snippet`, last 2 oppituntia before kertaustesti carry "Vihje:" hint sentences, every kertaustesti uses spec verbatim "Nyt testataan mitä olet oppinut — [topic]. [X] kysymystä." Backwards-design: every `focus` field names a measurable outcome, never "lesson N of kurssi M". Interleaving verified against §3. JS mirror `lib/curriculumData.js` matches SQL row-for-row so the routes have a graceful pre-migration fallback. **Migration NOT auto-applied** — no Supabase CLI configured locally; user runs once via Supabase dashboard SQL editor (deferred-item ledger). **UPDATE 2** (`routes/curriculum.js` + mount in `server.js`): 4 endpoints. `GET /api/curriculum` returns kurssit with per-kurssi `lessonsCompleted / lastScore / kertausPassed / isUnlocked`; unlock chain = kurssi_1 always unlocked, kurssi_N unlocks when previous kurssi's last-lesson kertaustesti score ≥ 80%. `GET /api/curriculum/:kurssiKey` joins `user_curriculum_progress` for completion flags per lesson. `GET /api/curriculum/:kurssiKey/lesson/:lessonIndex` lazy-generates teaching pages for `grammar`/`mixed` types via `callOpenAI(prompt, 400, { temp 0.3 })` returning `{ "contentMd": "..." }` (forced-JSON, lib/openai always JSON-parses) — prompt enforces §5 structure verbatim (`# title` / 1-paragraph / `## Muodostus` / `## Esimerkki` / `## YO-vinkki 💡`), explicitly forbids abstract level-talk + bullets outside tables. Cached in `teaching_pages` keyed `${kurssiKey}_lesson_${sortOrder}`; hardcoded fallback when AI text < 50 chars. `POST /api/curriculum/:kurssiKey/lesson/:lessonIndex/complete` (requireAuth) upserts to `user_curriculum_progress`, computes `kurssiComplete = isKertaustesti && score/total ≥ 0.80`, generates `tutorMessage` via `callOpenAI(prompt, 140, { temp 0.5 })` — success path kehu vaikeinta osa-aluetta + lupaa seuraava kurssi, remediation path nimeä konkreettisesti yksi kielioppirakenne, ≤2 lauseetta, sinä-muoto, ei prosentteja. Hardcoded Finnish fallback per kurssi name. `tablesMissing(err)` helper detects `42P01`/`PGRST205`/"relation does not exist" so all routes degrade to JS data when migration hasn't been applied. **Verified end-to-end via the live dev server** — `curl /api/curriculum` returns clean JSON with correct anonymous unlock chain; `curl /api/curriculum/kurssi_3/lesson/2` returns the lesson + AI-generated Markdown teaching page including a properly-formatted Muodostus table. **UPDATE 3** (`#screen-path` rebuild + new `#screen-lesson` + `js/screens/curriculum.js` + `css/components/curriculum.css`): rebuilt the Oppimispolku screen as a vertical 8-card timeline with single-stroke connector through 48 px step circles (Lucide check-icon for done, lock-icon for locked, number for current). Title row carries level chip (A/B/C/M/E in `--accent`), Linear-tier `role="progressbar"` with `aria-valuenow`, status label ("Suoritettu ✓" / "Jatka →" / "Aloita →" / "Lukittu"). Locked cards: `opacity 0.45` + `cursor: not-allowed` + `aria-disabled`; current kurssi: `border-left: 3px solid var(--accent)`; done: muted accent. Tap unlocked card → `is-expanded` toggle (no new screen) + lazy `/api/curriculum/:kurssiKey` fetch → 48 px lesson rows with inline SVG type icons. Loading = 8 shimmer skeletons (1400 ms gradient sweep, animation:none on prefers-reduced-motion). Empty (anonymous) = kurssi_1 only + Rekisteröidy CTA. Error = retry button. **`#screen-lesson`** added to app.html shell; back button → `loadCurriculum()`; eyebrow + h1 + teaching-page card via in-house minimal Markdown renderer (escapes-first, then unwraps headings/blockquotes/4-col tables/inline `**bold**` + `code`). `Aloita harjoittelu →` navigates to existing exercise screen by lesson type and stashes `sessionStorage.currentLesson` for L-PLAN-3 to consume. `Merkitse suoritetuksi (väliaikainen)` POSTs to `/complete` with mock score = exerciseCount/exerciseCount and renders the `tutorMessage` in a banner — temporary scaffold so the loop is closeable manually. Wired `data-nav="path"` left-nav to `loadCurriculum()` in `js/main.js`; the legacy `loadPath()` still fires from `learningPath.js`'s `nav-path` click but bails out gracefully because its target DOM ids no longer exist after rebuild. Mastery-intro/result screens kept intact for now. SW v96→v97, added `curriculum.css` + `curriculum.js` to STATIC_ASSETS. All 1 061 vitest tests still pass. **Deferred** (in IMPROVEMENTS.md L-PLAN-2 block): apply migration to Supabase via dashboard SQL editor; 21st.dev Playwright sourcing pass for `/s/timeline` + `/s/steps` candidates; axe + design:design-critique sweep on Oppimispolku at 1440 + 375; L-PLAN-3 work (real exercise-result wiring + tutor voice dashboard).

## Previously
Loop L-PLAN-1 — **onboarding redesign + placement upgrade**, three updates in one loop. **UPDATE 1** (`lib/placementQuestions.js` + `lib/placement.js`): audited every B/C/M-tier distractor against the brief's plausibility rule — only B5 needed surgery (joke "sää" → "kauppa" + "loma"), A1 also got its only weak distractor swapped (`kissa` → `tie` so all 4 are physical-place nouns); marked every audited item with `plausible_wrong_options: true`. Added two `M_hard` anchors — `MH1 se habían ido` (pluskvamperfekti, classic ennen-toista-mennyttä), `MH2 si tuviera` (subjunktiivi imperfekti in si-rakenne) — these are only injected by the OB-2 client when the student answers every A/B/C item correctly, the brief's signal of "consider Kurssi 7". Kept `selectDiagnosticQuestions(...)` returning a flat array (legacy `/api/placement/submit` route still calls it, breaking the shape would 500-cascade); added a parallel `selectOnboardingQuestions(...)` returning `{ core, mHardCandidates }`. New `placementConfidence(answers)` (median `time_ms < 4000` → `low`) and `suggestStartingKurssi(level, confidence)` mapping per CURRICULUM_SPEC §6. **UPDATE 3** (`routes/placement.js` + new `lib/curriculum.js`): added `POST /api/placement/onboarding` (the brief said `/submit` but `/submit` is the legacy endpoint and renaming would break the existing diagnostic flow — flagged this deviation in IMPROVEMENTS.md). Optional-auth wrapper (custom inline since `softProGate` 403s anonymous users): when authenticated, upserts `self_reported_grade / target_grade / weak_areas / daily_goal_minutes / placement_confidence / placement_kurssi / tutor_assessment / current_grade` into `user_profile`; when anonymous, returns the same payload so OB-3/OB-4 still render. Companion `GET /api/placement/onboarding-questions` returns the core 8 + 2 M_hard candidates *with* answers + Finnish explanations (placement isn't graded — surfacing the keys client-side is fine and OB-2 needs the explanation strings to render inline feedback). New `lib/curriculum.js`: `KURSSI_META` (key → name + level for all 8 kursseja), the first ~5 lessons of every kurssi sourced verbatim from CURRICULUM_SPEC §3, `generateFirstWeekPlan(kurssiKey, dailyGoalMinutes)` deterministic (no AI) with an SR-review tail when goal ≥30 min, `fallbackTutorAssessment(level, kurssi, confidence)` template Finnish prose so the endpoint never fails. Tutor assessment via `callOpenAI(prompt, 160, { temperature: 0.5 })` returning `{ "tutorAssessment": "..." }` — forced JSON object because the lib/openai wrapper always JSON-parses; prompt prepends the `puheo-ai-prompt` system preamble verbatim and explicitly forbids abstract level-talk + bullet lists per CURRICULUM_SPEC §7. Sanity check on AI text (≥30 chars, no leading bullet) before accepting. **UPDATE 2** (4 new screens): `#screen-ob1-profile` → `#screen-ob2-test` → `#screen-ob3-assessment` → `#screen-ob4-plan` built side-by-side with the legacy `screen-ob-*` flow (kept untouched per the brief). New `js/screens/onboardingV2.js` (state machine, single export `showOnboardingV2()` + `initOnboardingV2({ loadDashboard })`) and new stylesheet `css/components/onboarding-v2.css` token-only, `min-h-100dvh`, max-width 560 px shell, `clamp()` typography, focus rings everywhere, 44 px touch targets, `prefers-reduced-motion` gates. OB-1 has 4 fields with school-grade auto-pre-selecting target one notch above; "En tiedä vielä" chip is mutually-exclusive with the real weak-area chips. OB-2 tracks `time_ms` per answer via `performance.now()`, shows green/red border + Finnish verdict + the question's `explanation` string, auto-advances at 2 s or on tap-anywhere. OB-3 renders skeleton during the AI call, then a tutor card (prose, not bullets), 4 score bars (Sanasto = (A+B)/2, Kielioppi = (B+C)/2, Vaativa taso = M, Kokonaisuus = mean), accent "Aloitamme [Kurssi N]" line, low-confidence note framed as a feature ("varmistuksen vuoksi") per `self-efficacy-builder-sequence`. OB-4 renders the deterministic `firstWeekPlan` as 5 day-rows with Lucide-style SVG icons + monospace minute estimates; finish CTA marks legacy `onboarding_completed:true`, then routes to dashboard if logged in or `/app.html#rekisteroidy` (with result stashed in `sessionStorage`) otherwise. Wired into `js/main.js` via `initOnboardingV2` and a `#/aloitus` hash entry — V2 isn't yet the default, so the live boot path is undisturbed; flip the entry once L-PLAN-2 lands and the curriculum tables are seeded. SW v95→v96, added `onboarding-v2.css` + `onboardingV2.js` to STATIC_ASSETS. All 1 061 vitest tests still pass. **Deferred** (in IMPROVEMENTS.md L-PLAN-1 block): formal 21st.dev Playwright sourcing pass, axe-core sweep at 1440+375, DB migration for the 7 new `user_profile` columns (Supabase silently no-ops unknown columns until run).

## Previously
Loop 49 — **LANDING_PLAN.md mobile + polish + a11y/perf audit — final landing-rebuild loop.** Wrote `scripts/agent-test/loop49-responsive.mjs` (NEW) — 4-viewport sweep at 375 / 768 / 1024 / 1920 capturing full-page screenshots, probing horizontal-overflow (`scrollWidth` vs `clientWidth` + per-element `getBoundingClientRect().right`), running axe-core 4.10.2 (`wcag2a/aa/21a/21aa`), scanning heading hierarchy for `H{n}→H{n+2+}` jumps, scanning `target="_blank"` for missing `rel=noopener`, inventorying every `<img>`. **First run, zero fixes needed:** all four viewports clean — `hScroll=false` everywhere, `axe=0/0/0/0`, `headingJumps=0`, `unsafeBlankTargets=0`, single `<img>` already had `alt="" w=1440 h=900 loading=eager decoding=async`. The 768-px mid-band concern (`≤880` is the smallest desktop-collapse breakpoint) was a non-issue — at 768 the page already renders single-column mobile because the ≤880 query hits, leaving only the 768-880 band as desktop with content clamped to 720 px inner via `--content-max: 1200px` minus 24 px `.landing-section` padding. Read all four screenshots: 1920 sits Linear-style at 1200 px clamped centre with breathing dark margins, 1024 hits desktop 60/40 hero + 3-col pillars + 50/50 grader + 2-col pricing, 768 collapses to single column, 375 stacks cleanly. **Lighthouse** (`npm run audit:lighthouse:landing`, desktop preset, headless): first run **97 / 100 / 96** — best-practices was dragged down by `errors-in-console` ×2 (404s on `/favicon.ico` + `/icon-192.png`; `manifest.json` referenced icon-192/icon-512 which never existed on disk). Generated three icons via new `scripts/agent-test/loop49-gen-icons.mjs` — Playwright renders a 22 %-radius dark-slate square with a Geist-700 "Po" mark (the o in `var(--accent) #2DD4BF`, matching the L48-hotfix nav wordmark) plus a 30/25 radial accent glow, headless-screenshots at the requested DPR-1 size. New files at repo root: `icon-192.png`, `icon-512.png`, `favicon-32.png`. Added `<link rel="icon" href="/favicon-32.png" type="image/png" sizes="32x32">` + `<link rel="apple-touch-icon" href="/icon-192.png">` to **both** `index.html` and `app.html` for parity. Re-audit: **97 / 100 / 100** — all above the ≥95 gate. FCP 0.7 s, LCP 1.1 s, TBT 0 ms, CLS 0.001, SI 0.7 s. **Old `landing.css` deletion deferred** — grep surfaced 14 active references beyond `index.html`: 6 blog pages (`blog/{index,espanja-yo-koe-2026-lyhyt-oppimaara,por-vs-para-selkea-ero,ojala-subjunktiivi-yleisimmat-virheet,ser-vs-estar-milloin-kumpaakin,preteriti-vs-imperfekti-opas}.html`), 4 legal pages (`pricing.html`, `privacy.html`, `refund.html`, `terms.html`), `sw.js` STATIC_ASSETS, 7 unit tests in `tests/`. Confirmed all are live on dev (`pricing.html`, `terms.html`, `blog/` all 200). Brief assumed orphan; reality was broader and immediate deletion would visually break the blog + legal pages. Punts cleanly to L52–L55 dark-theme migration. Appended L49 ledger line + a `## Landing rebuild — final` summary block to `IMPROVEMENTS.md` (table of all final scores, 21st.dev sourcing trail L46→L48, deferred items, TODO placeholders for real social-proof / Y-tunnus / blog imagery / socials). SW cache `puheo-v94 → puheo-v95` because both `index.html` and `app.html` are in STATIC_ASSETS and were edited; added the 3 icons to STATIC_ASSETS so they cache offline.

## Previously
Loop 48 — **LANDING_PLAN.md Section C 9–10 — Final CTA + Footer.** Two sections: a closer card after `#faq` and the page's first real `<footer>` outside `<main>`. **CTA sourced from `21st.dev/community/components/dhiluxui/spotlight-cta/default`** — picked over the louder gallery-style, email-capture, countdown, and feature-grid variants because the final CTA is a closer. Adapted the source's horizontal title-left/button-right layout to **vertical-centred** (title → body → button → fineprint) per LANDING_PLAN.md §C9. Cursor-spotlight effect deliberately skipped (restraint > novelty for the closing surface; CSS-only motion per spec). **Footer sourced from `21st.dev/community/components/shadcnblockscom/footer-7/default`** — matches §C10 exactly: brand + tagline at top, 3 columns of links, bottom row with copyright + legal. Skipped social-icon row (Puheo doesn't have public socials yet — invented social-proof rule). **Markup port** (+96 lines into `index.html`): `<section id="rekisteroidy-cta">` with `.cta-card` block (max 720 px, mint accent border + accent-glow halo, gradient lift via `linear-gradient(180deg, color-mix(--accent 6%, --bg-elevated) 0%, --bg-elevated 70%)`); H2 "Espanjan YO-koe on lähempänä kuin luulet." + 18 px muted body verbatim §C9 + big primary button → `/app.html#rekisteroidy` + 13 px fineprint "Tehty Suomessa • Toimii selaimessa • Sisältö päivittyy YO-vaatimusten mukana". `<footer class="landing-footer" role="contentinfo">` outside `<main>`: 2-col desktop grid (brand left, 3 link-cols right), bottom row spans both halves with `© 2026 Puheo · Tehty Suomessa` + machine-readable `<time datetime="2026-09-28">28.9.2026</time>` exam date. Each `<nav>` carries its own `aria-label` (Tuote / Tili / Tieto). Tuote column (4 same-page anchors) acts as a sitemap; Tili column (Kirjaudu + Aloita) hits `/app.html#kirjaudu` + `/app.html#rekisteroidy`; Tieto column links `/privacy.html`, `/terms.html`, `/refund.html`, `mailto:tuki@puheo.fi`. **CSS port** (+165 lines into `css/landing.css`) all token-only. **A11y catch:** initial pass had axe `color-contrast` × 5 (serious) — `.landing-footer__col-title` and `.landing-footer__bottom` used `--text-subtle: #71717a` against the explicit `var(--bg) #0a0a0a` footer background = 4.09:1. Note: `--text-subtle` passes elsewhere because most sections inherit the page's `--bg-gradient` which lifts the bg toward `#1a1a2e`, but the explicit `#0a0a0a` on the footer drops contrast below 4.5:1. Fix: bumped both to `var(--text-muted) #a1a1aa` (9.7:1, well above AAA), exam-date `<time>` bumped to `var(--text)`. After fix: axe **0 violations** at 1440 + 375 across the entire page (now hero + problem + pillars + steps + grader + pricing + FAQ + CTA + footer). **Verified** via `scripts/agent-test/loop48-cta-footer.mjs` (NEW): CTA title verbatim, button href `/app.html#rekisteroidy`, fineprint string verbatim, footer brandmark "Puheo", three column titles `["Tuote","Tili","Tieto"]`, link counts `[4, 2, 4]`, copy "© 2026 Puheo · Tehty Suomessa", `<time datetime>` reads `2026-09-28`. **6 register-CTAs** total on the page now all point at `/app.html#rekisteroidy` (nav + hero primary + free-pricing + Pro-pricing + final CTA + footer Tili-column-Aloita) — spec D5 structurally enforced. Read screenshots both viewports: desktop CTA card sits as a glow-bordered closer with the mint button centred and the mint-glow bloom subtle behind the title; desktop footer reads as a clean 3-col sitemap. Mobile CTA collapses gracefully (title two-line balanced, button widens); mobile footer stacks brand-then-cols-then-bottom without horizontal scroll. No SW bump per spec.

## Previously
Loop 47 — **LANDING_PLAN.md Section C 8 — FAQ.** Section sourced from **21st.dev/community/components/tailark/faq/faq-accordion-with-title** — 50/50 left-title + right-accordion-list pattern. Ported the source's Radix/shadcn `<Accordion>` → **native `<details>`** for built-in keyboard + screen-reader a11y (the user's hard requirement: no hand-rolled `aria-expanded` JS). Browse harness `scripts/agent-test/loop47-21stdev-browse.mjs` (NEW, clone of L46) hit `/s/faq` and captured 6 candidates; the listing pulled mixed cards (originui dialogs + a reuno hero ranked higher than actual FAQ candidates), so a follow-up `loop47-21stdev-faq-targeted.mjs` (NEW) visited the three real `tailark/faq/*` candidates directly. Picked `faq-accordion-with-title` over `default` (no accordion) and `faq-accordion-with-text` (redundant left-side body copy). Appended `<section id="faq">` to `index.html` (+98 lines) after `#hinnoittelu`, reusing the `.landing-section` scaffold; `.faq__inner` is a 1fr/1.25fr 2-col grid stacking @ ≤880 px. Left col: 12 px eyebrow "UKK" in `--accent`, display H2 "Usein kysyttyä Puheosta." (`text-wrap: balance`), 16 px muted sub with `mailto:tuki@puheo.fi` styled as a mint-underlined link. Right col: 6 `<details class="faq-item" name="puheo-faq">` items separated by 1 px `--border` lines (the `name="puheo-faq"` attribute is the new ExclusiveAccordion HTML primitive — opens one closes the others, gracefully no-op on older browsers). Each `<summary>` is a flex row: question text (display 18 px, weight 500, balance) + Lucide `chevron-right` SVG (18 px). Default disclosure markers killed across browsers (`list-style:none` + `::-webkit-details-marker` + `::marker`). Six **honest answers** verbatim from spec appendix #5: (1) **YTL endorsement** ("Ei. YTL ei myönnä yksittäisille palveluille hyväksyntöjä, eikä Puheo ole YTL:n tuottama tai vahvistama. Sen sijaan kirjoitusten arviointi on rakennettu suoraan YTL:n julkaiseman vieraan kielen lyhyen oppimäärän rubriikin päälle …" — leads the FAQ; honesty is the most trust-buying answer per RESEARCH.md), (2) format-changes, (3) GDPR/EU servers, (4) mobile/PWA, (5) AI-vs-teacher (3 criteria + "harjoittelukumppani niiden tuntien välissä, ei opettajan korvaaja"), (6) cancellation + 14-day refund. **Did NOT** reuse "10 vuoden YO-arkisto" — `landing/AUDIT.md` flagged it un-defensible. CSS port +115 lines into `css/landing.css`: chevron rotates 0→90° + paints `--accent` on `[open]` (pure transform, animates in every browser); smooth height-open uses `interpolate-size: allow-keywords` + `details::details-content` (`block-size: 0 → auto` + `opacity: 0 → 1` + `content-visibility allow-discrete`) wrapped in `@supports (interpolate-size: allow-keywords) { ... }` — modern Chromium animates, older browsers skip the height transition but `<details>` still toggles natively. Pure CSS, **zero JS** for toggle. Reduced-motion cancels both. **A11y catch:** initial pass had axe `aria-allowed-role` minor × 6 — I'd over-instrumented the list with `role="list"` + `role="listitem"` on each `<details>`; axe correctly flagged that `listitem` is not allowed on `<details>` (which is its own disclosure-widget primitive). Fixed by stripping both attributes — `<details>` carries the semantics. After fix: **0 axe violations** at 1440 + 375 across the entire page. Verified via `scripts/agent-test/loop47-faq.mjs` (NEW, clone of L46-pricing): 6 `<details>` rendered, all 6 expected questions present (exact-match set), `summary.click()` flips `el.open === true`, chevron transform parses as exactly **90.0°** (computed via `Math.atan2(b, a) * 180 / Math.PI`), answer panel renders (152 px desktop, 229 px mobile — first item is the 7-sentence YTL answer), second click flips back to false. Read screenshots both viewports — desktop matches the tailark reference (head-left + chevroned-list-right), mobile stacks cleanly with longer questions wrapping to 2 lines without horizontal overflow at 375. No SW bump per spec.

## Previously
Loop 46 — **LANDING_PLAN.md Section C 7 — Pricing.** Section sourced from 21st.dev/community/components/efferd/pricing/default — restrained dark "Plans that Scale with You" pattern (centred eyebrow + display H2 + sub above a card grid; recommended card has accent border + soft accent-glow + a small mint chip; each card: name + lede, big price line with bottom-bordered separator, feature list with check / × marks, full-width CTA at bottom). Browse harness `scripts/agent-test/loop46-21stdev-browse.mjs` (NEW) hit `21st.dev/s/pricing`, captured 6 candidates to `references/landing/21stdev/pricing/`. Picked efferd over louder gradient candidates (animated-glassy-pricing, ui-layouts/pricing-section-4) because efferd matches Puheo's Linear-tier register. Ported to vanilla as 95-line section in index.html + 165 lines in css/landing.css. **Both CTAs href `/app.html#rekisteroidy`** per Y-tunnus / live-payment constraint. Pro price `9,99 €/kk` (comma-separator per Finnish convention) sourced from `index.html` JSON-LD + `onboarding/PAYWALL.md`. Free card 6 features (3 yes, 3 no — explicitly marking what's gated communicates Pro's value without a comparison table). Pro card 6 features all yes. Pro accent border via `color-mix(in srgb, var(--accent) 35%, var(--border))` escalating to 55% on hover; Pro background subtle `linear-gradient` from `color-mix(--accent 4%, --surface)` to `--surface`; box-shadow `0 24px 48px -32px var(--accent-glow)` halo. "Suosituin" chip absolute-positioned at `top:-12px; right:24px`. Lucide-style inline check + × SVGs, `currentColor` so `--yes`/`--no` modifier classes cascade colour. Mobile (≤880 px) stacks 1-col, max-width clamps to 480 px. New utility `.btn--full { width:100%; display:inline-flex }`. Verified via `loop46-pricing.mjs` (NEW) at 1440 + 375: 2 cards, names ["Ilmainen","Pro"], prices ["0 €","9,99 €"], both CTAs `/app.html#rekisteroidy`, chip "Suosituin", free 3-yes/3-no, Pro 6-yes/0-no, axe **0 violations** at both viewports. Read screenshots both viewports — desktop matches efferd reference closely, mobile clean stack with no horizontal overflow. No SW bump per spec.

## Previously
Loop 45 — **LANDING_PLAN.md Section C 5–6 — How It Works + AI-grading showcase.** Two more `<section>` blocks appended. **How It Works** (`#miten` — smooth-scroll target from the hero ghost CTA): centred eyebrow + H2, then `<ol class="steps__grid">` of three steps with 48 px thin-weight (300) `--accent` numbers (01/02/03), display H3, 16 px muted body. Thin connector via `.steps__grid::before` 1 px linear-gradient at 0.3 opacity diving behind each number's `var(--bg)` background strip — Linear "How it works" pattern. List markers killed (`list-style:none`, caught + fixed in-loop). **AI-grading showcase** (`#arviointi` — spec D3 hybrid). 50/50 grid (1.0fr / 0.85fr) on a section-wide `--accent-glow` radial backdrop. Left = `.grader-card` rendering the L42 fixture (`references/puheo-screens/grading-response.json`): `.grader-prose` shows the studentText with 5 inline `<span class="ge" data-cat="grammar|spelling" tabindex="0">` errors using `text-decoration: underline wavy` + per-category colour (`#ef4444` grammar, `#facc15` spelling); on hover/focus a CSS-only `.ge-tip` popover fades in showing **Oikein: <correction>** + the Finnish explanation verbatim from the fixture. Below: `.rubric` with all 4 dimensions (Viestinnällisyys 4/5, Kielen rakenteet 2/5, Sanasto 4/5, Kokonaisuus 3/5) and a `.rubric-total` strip "YO-arvioivuus · 13 / 20 · Arvosana E" pill. Right = `.grader-copy` H2 "Tekoäly, joka korjaa kuten YTL-arvioija." + body + 3 Lucide-check-icon bullets (LANDING_PLAN.md §C6 verbatim). **A11y catch**: initial pass had axe-core flag `aria-hidden-focus` (serious × 1) — `aria-hidden="true"` on the card with focusable `.ge tabindex="0"` children. Fixed by switching to `role="figure" aria-label="Esimerkki Puheon arviointinäkymästä …"` since the showcase content is informative for screen readers. After fix: 0 violations at both viewports. Verified via `scripts/agent-test/loop45-howitworks-grading.mjs` (NEW): step nums ["01","02","03"], 5 error annotations, 4 rubric rows, total "13 / 20", tooltip pops on hover. No SW bump per spec.

## Previously
Loop 44 — **LANDING_PLAN.md Section C 3–4 — Problem + 3 product pillars.** Appended two `<section>` blocks to `index.html` under the existing `<main>`, reusing the `.landing-section` scaffold from L43. **Problem** (`#ongelma`): centred two-line statement, 32 px display H2 + 18 px muted sub (LANDING_PLAN.md §C3 verbatim), `text-wrap: balance` on the headline so the line break lands evenly. **Pillars** (`#tuote`): eyebrow "Mitä Puheo tekee" in `--accent` + H2 "Kolme aluetta. Sama YO-rubriikki." over a 3-col grid (1-col @ ≤880 px) of `<article class="pillar">` cards. Each card has a 40 px Lucide-icon tile in `--accent-soft`, display H3, 16 px muted body, mono `.pillar__metric` line, and a 16/9 thumbnail cropped per-card via `object-position` from `references/puheo-screens/{mode-vocab,profile,writing}-1440.png`. Hover lifts cards by `translateY(-2px)` and zooms the thumbnail to `scale(1.03)`. Copy uses **2 000+** (real bank total, spec appendix #3), names the highest-tested grammar topics specifically rather than vague superlatives, and anchors the Kirjoittaminen pillar in **YTL:n mukainen** (highest-trust signal per RESEARCH.md). Verified via `scripts/agent-test/loop44-problem-pillars.mjs` (NEW) at 1440 + 375: pillar count 3, titles ["Sanasto","Kielioppi","Kirjoittaminen"], thumbnails all loaded at 2880×1800, problem + pillars text strings match. axe-core across the now hero+problem+pillars page: **0 violations** at both viewports. Read screenshots: desktop pillars match Stripe's product-feature grid rhythm; mobile stacks cleanly with no overflow. No SW bump per spec.

## Previously
Loop 43 — **LANDING_PLAN.md Section B + C 1–2 — sticky nav + hero.** Rewrote `index.html` from the legacy 1938-line light-mint document into a 175-line dark-mode shell scoped under `<html class="landing">`. Kept SEO/JSON-LD scaffolding (SoftwareApplication + Organization), swapped fonts to Geist + Geist Mono via Google Fonts (Inter remains in the fallback stack from L42's `@font-face Geist Fallback / size-adjust:100%`). Built `css/landing.css` (NEW, 326 lines) under `.landing` scope: scaffold + buttons + sticky nav + hero 60/40 + browser-frame component + floating Puheo streak-chip + skip-link + radial accent glow. Sticky 64 px nav transparent → `rgba(10,10,10,0.78)` + `backdrop-filter: blur(12px)` + 1 px bottom border via `[data-scrolled="true"]` toggled by a 12-line passive scroll listener. Hero uses `clamp(40px, 6.5vw, 72px)` H1 with the first line `linear-gradient(180deg, #fafafa, #a1a1aa)` background-clip:text fade per Linear's hero treatment, full LANDING_PLAN.md §C2 verbatim copy, primary CTA wired to `/app.html#rekisteroidy`. Browser-frame at `rotate(-3deg)` embeds `references/puheo-screens/dashboard-1440.png`; floating Puheo streak-chip ("ronja / 12 pv" with mint flame SVG) sits behind/below-left at `rotate(-3deg)` with `box-shadow: 0 40px 80px -20px var(--accent-glow)`. Subtle radial accent glow blob behind the entire visual. Verified via `scripts/agent-test/loop43-nav-hero.mjs` (NEW, 130 lines) at 1440 + 375: every soft-assert passes; axe-core 4.10.2 ran against the live page and reported **0 violations** at both viewports (caught and fixed one `color-contrast` serious on `.browser-frame__url` mid-loop — `--text-subtle:#71717a` on the elevated frame surface read 3.21:1; bumped to `--text-muted:#a1a1aa` + 12 px). Read the captured screenshots: desktop matches Linear's gradient-into-muted display + isolated UI fragment lower-right; mobile stacks cleanly with the frame + chip falling beneath the trust strip, no horizontal overflow at 375. No SW bump per spec — landing stays out of `STATIC_ASSETS` for always-fresh marketing.

## Previously
Loop 42 — **LANDING_PLAN.md Section A (Setup) — six sub-deliverables.** (a) Skimmed `landing/` (DESIGN.md, AUDIT.md, RESEARCH.md, PLAN.md) and folded a 5-bullet survivors block into the spec doc as an appendix — exam date pinned at **28.9.2026**, Y-tunnus / live-payment constraint flagged (LANDING_PLAN.md's free-signup-only CTA rule sidesteps it), 2 080+ exercises as the round-number "2 000+ harjoitustehtävää" claim, tone-of-voice rules from RESEARCH.md (calm + direct; "YTL:n mukainen" = highest-trust signal), 6 FAQ answers reusable verbatim. Discards listed. (b) Captured Linear / Vercel / Stripe / Cron (cal.com) / Duolingo at 1440×900 hero + below-fold via `scripts/agent-test/loop42-refs-capture.mjs` → 10 PNGs in `references/landing/<site>/`. Verified Linear's restrained-dark aesthetic is on-target; Duolingo confirmed as the kid-friendly counter-example we're not. (c) Wrote `css/landing-tokens.css` (162 lines) — scoped to `.landing` so it doesn't leak into app.html — dark surface palette, Geist + Inter fallback (with size-adjust for FOUT), 8/16/24/32/48/72/96 type scale, --tracking-tight for displays, motion 150-300ms ease-out, prefers-reduced-motion collapse, exam-date custom property. Reuses the app's `--accent: #2DD4BF` for landing→app brand continuity. (d) Built `scripts/agent-test/loop42-puheo-screens.mjs` — Playwright with `serviceWorkers: "block"`, 17 explicit `/api/*` mocks plus a `**/api/**` catch-all, registered in REVERSE-priority order (Playwright matches most-recently-added first — discovered the hard way after my first run rendered the placement-test fallback). Captured 8 screenshots: dashboard / profile / mode-vocab / writing × {1440, 375}. Real Puheo content visible: "Iltaa, ronja", 12-day streak, 47 sessions, daily challenge, SR review card, charts, settings chips. (e) Built `scripts/agent-test/loop42-capture-grading.mjs` (logs in with Pro test creds, fetches a writing-task, posts a deliberately-flawed B1/B2 student answer, saves the JSON). No creds in `.env` yet, so I shipped a hand-crafted placeholder fixture at `references/puheo-screens/grading-response.json` with the `_placeholder: true` marker — matches the real response shape exactly so L45's showcase markup ships either way. User can replace with real output any time before L45. (f) Extended `js/screens/auth.js` to honour `#rekisteroidy` / `#kirjaudu` URL hashes alongside the existing `?mode=register` query-param so landing CTAs can deep-link. Parse-checked.

No SW bump (landing files are not in `STATIC_ASSETS` per the spec — landing stays out of the cache for always-fresh marketing).

## Previously
Loop 41 — **PLAN item 6: right sidebar rebuilt to 2-panel.** Top: compact user-identity card (36px gradient avatar + monospace name + 🔥 streak chip + hover arrow) → click navigates to profile (Linear top-right user menu pattern). Below: mutually exclusive panels — free user gets Pro upsell card adapted from Astra Buy+ (Puheo+ wordmark + "+" badge, "Pääse YO-kokeesta läpi paremmin arvosanoin." tagline, 3 +-bullet features, accent CTA "Kokeile Pro 7 pv ilmaiseksi +" wired to existing startCheckout/LemonSqueezy); Pro user gets daily peek (today's minutes goal + word of day). Removed duplicated stats (streak/readiness/total/countdown — they live on the dashboard greeting and the rebuilt profile now). Verified both states via `loop41-rail-rebuild.mjs`. SW v90 → v91. Sources: Linear (top-right user menu), Astra Buy+ card (`references/astra/Screenshot 2026-04-27 221631.png` — wordmark + "+" badge + single CTA pattern).

## Previously
Loop 40 — **PLAN item 5: profile rebuilt to ONE viewport (1440×900).** Replaced 8-section scrollable profile with: compact hero strip (avatar + name + handle + badges + YO-koe countdown chip Astra-style), 4-cell stats row (streak / total / week / YO-valmius% — readiness derived from L38's fixed formula), 4-up mode breakdown (uppercase mono label + 24px display number, was 2-up list rows), two-column footer (3 recent activities | 5 inline-editable settings chips with Muokkaa pill). Achievements section dropped from profile (still on dashboard rail). New `openSettingsEditor(fieldKey)` exported from settings.js + a `puheo:profile-updated` CustomEvent dispatched on save so chips re-render without re-fetching. Verified: 1440×900 fits with 106px breathing room; chip click opens modal correctly; all 8 settings fields still work; blur-fade hero from L36 still works. SW v89 → v90. Sources: Linear/Vercel/Cron/Raycast desktop profile aesthetic; Astra Buy+ chip pattern (references/astra/Screenshot 2026-04-27 221631.png + 221659.png).

## Previously
Loop 39 — **PLAN items 3 + 4 bundled: NaN pv sitten + right-rail overflow.** (3) New shared `js/ui/timeAgo.js` defensive helper — accepts ISO/epoch/Date, falls back to "äskettäin" on bad input, never emits NaN. Both `dashboard.js` and `profile.js` now import it. (4) Couldn't reproduce overflow at any of 1180/1280/1440/1920 (probe = scrollWidth > clientWidth on every rail descendant). Applied belt-and-braces defensive guard anyway: `.app-rail, .app-rail * { min-width: 0 }` + `overflow-wrap: anywhere; word-break: break-word` on rail content. SW v87 → v89 (also added `settings.js`, `learningPath.js` to STATIC_ASSETS — silently missing despite being part of the SPA). All 4 P0 bugs from the user's brief now closed.

## Previously
Loop 38 — **PLAN item 2: YO-readiness math fixed.** Two compounding bugs in `js/features/writingProgression.js computeReadinessMap`: (A) path-node `bestPct` is a 0..1 fraction but `pct = Math.round((bestPct||0) * 100) / 100` divided by 100 again, so the 60/30/10 thresholds never fired — every in-progress topic stayed at level 0; (B) `readinessPct = mastered/total` treated partial progress as zero. Fixed both: scale (drop the second `/100`) + weighted partial credit (`sum(level) / (totalCells * 4) * 100`). 8 sessions in a representative payload now read 36 % (was 14 %). SW v86 → v87.

## Previously
Loop 37 — **PLAN item 1: Settings Muokkaa buttons fixed.** Real-browser click at the button center hit-tested through to `<main id="app-main">` instead of the button — verified via `document.elementFromPoint()` and `page.mouse.click(x,y)`. Root cause: `.app-main` (z-index:1, position:relative) creates stacking context A; `.screen.active` (position:absolute, z-index:auto) is layer-6 inside A but doesn't establish its own context; descendants share resolution with `<main>` and hit-testing resolves to the parent. **Fix:** added `z-index: 1; isolation: isolate;` to `.screen.active` (style.css:354). All 8 settings fields verified (`loop37f-all-fields.mjs`); profile screen regression-checked via `loop36-profile-blur-fade.mjs`. SW v85 → v86.

## Previously
Loop 36 — **Profile hero blur-fade arrival** — extends L35's pattern to a second screen surface. Avatar / name / handle / badges with 0 / 80 / 140 / 200 ms stagger. Trigger via double-rAF + `profile-hero--in` class in `loadProfile`. Reduced-motion short-circuits. SW v84 → v85. Verified — 160 ms readback shows avatar 58% opacity, name 21%, handle/badges still pre-start.

## Previously
Loop 35 — **Dashboard hero blur-fade arrival** sourced from Magic UI blur-fade. Pure CSS — no `motion` dep. Children animate `opacity` + `transform: translateY(6px → 0)` + `filter: blur(6px → 0)` with per-child `--bf-delay` (0 / 80 / 160 ms) so the eyebrow → display → sub land in reading order over ~640 ms. Trigger: double-rAF + `dash-greeting--in` class in `renderDashboard`. Reduced-motion short-circuits. SW v83 → v84. Verified — 140 ms readback shows correct mid-transition states (eyebrow 58% opacity, display 21% with 4.84 px blur, sub still hidden).

## Previously
Loop 34 — **Profile activity timeline staggered reveal** sourced from Magic UI animated-list. Pure CSS + a single rAF in JS — no `motion` dep. Initial state opacity:0 + translateY(8px) + scale(0.985), per-row `--enter-delay: ${i*55}ms`, `.--enter` flips to opacity:1 + identity. 8-row cascade ~440ms. `prefers-reduced-motion` short-circuits to instant. SW v82 → v83.

## Previously
Loop 33 — **Global shadcn-style tooltip** replacing native `title=`. New module `js/features/tooltip.js` listens for `pointerenter`/`pointerleave`/`focusin`/`focusout`/`pointerdown` in capture phase, walks via `closest("[data-tooltip]")`, shows a single shared `<div role="tooltip">` with token colors + 140 ms fade + flippable arrow. Suppresses native `title=` during hover, restores on leave. Honors `prefers-reduced-motion`. Migrations: 4 share-button `title=` → `data-tooltip=`; readiness cells `title=` → `data-tooltip=`. SW v81 → v82. Bug surfaced + fixed: capture-phase events fire on Document/Window where target isn't an Element — added `asElement(node)` guard.

## Previously
Loop 32 — **Auth-screen grid-pattern backdrop**. Sourced from Magic UI `grid-pattern.tsx`; ported to vanilla SVG (single `<pattern>` tile with a corner-piece `M.5 40V.5H40` path). CSS: `position: absolute; z-index: -1; opacity: 0.10; color: var(--ink);` plus `mask-image: radial-gradient(ellipse at center, black 30%, transparent 85%)` so the grid fades at corners. Gentle 18s drift animation gated by `prefers-reduced-motion`. `#screen-auth` got `position: relative; isolation: isolate;`; `.auth-shell` got `z-index: 1` so it stacks above. SW v80 → v81. Verified at 1440 + 375 — grid lines visible across the screen with corners fading.

## Previously
Loop 31 — **3D tilt + cursor-tracked glare on the .mode-btn cards** (Sanasto / Puheoppi / Luetun / Kirjoittaminen / Koeharjoitus). New `js/features/cardTilt.js` writes `--tilt-rx`, `--tilt-ry`, `--tilt-scale`, `--tilt-mx`, `--tilt-my`, `--tilt-glare-opacity` as CSS vars on pointermove. CSS in `style.css` adds `perspective: 1200px` to `.mode-picker`, applies `transform: rotateX/Y scale` to `.mode-btn`, and a `::before` radial-gradient glare. Coarse-pointer + reduced-motion both short-circuit. Max ±8° rotation, scale 1.015. Wired via dynamic `import` in `js/main.js`. SW v79 → v80. Verified — hover at upper-right of Puheoppi gave rx=3.83°, ry=5.60°, glare=1, accent glow visible.

## Previously
Loop 30 — **"Kehitys ajan mittaan" chart polish** sourced from shadcn line-chart aesthetic. Replaced hardcoded `#e63946` / `#f59e0b` literals with `var(--accent)` + `currentColor` (dark-mode-ready). Ported a Catmull-Rom-to-cubic-Bezier interpolation by hand so the line is a smooth natural spline (`type="natural"` in shadcn). Removed per-point dots; kept a 3-layer bullseye endpoint (halo + solid + inner surface punch). Removed the dashed amber "tänään" vertical line — shadcn-style charts don't carry that decoration. Verified with a 14-point grade series. SW v78 → v79.

## Previously
Loop 29 — **YO-valmius readiness ring** on the right rail. Sourced Magic UI `animated-circular-progress-bar`; ported to vanilla SVG with a single accent-stroke fill ring (the original's two-arc gap aesthetic reads as noise at 80 px). `<circle r="34">` × 2 (track + fill) with `stroke-dasharray: 213.628; stroke-dashoffset: 213.628;` rotated -90deg so 0% starts at 12 o'clock. `transition: stroke-dashoffset 1.4s cubic-bezier(...)` + accent drop-shadow halo. Inner number is flex-baseline-aligned so "21" + "%" read as one composed unit. Wired via `loadAndRenderReadinessMap` — `countUp` on the digit + `requestAnimationFrame(() => ringEl.style.strokeDashoffset = ...)` for the arc. SW v77 → v78. Verified mid-anim and settled at clip 1080,0 360x400 — 19%→21% with matching arc length.

## Previously
Loop 28 — **Meteors decoration behind dashboard hero, mounts when streak ≥ 7**. Sourced from Magic UI `meteors.tsx`; ported to vanilla CSS + a tiny mounter that animates `--mx` / `--my` directly in screen-space (registered via `@property` for smooth interpolation) and applies a static `rotate(-25deg)` for tail orientation. Tail is a `::before` extending 90 px right of the head with a linear-gradient fade. New files: `css/components/meteors.css`, `js/features/meteors.js` (`mountMeteors` / `unmountMeteors`, idempotent via `dataset.meteorsMounted`). Wired in `js/screens/dashboard.js` after the streak class toggle: 16 meteors at streak 7-29, 24 meteors at streak ≥ 30, none below. `app.html`: added `<div class="meteors-fx" id="dash-meteors" hidden>` inside `.dash-greeting` + linked the stylesheet. `.dash-greeting` got `position: relative`, `isolation: isolate`, `min-height: 96px` so the absolute meteor field has somewhere to live; eyebrow / display / sub got `position:relative; z-index:1` to stay readable above the streaks. Honors `prefers-reduced-motion` and suppresses below 600 px viewport. Verified at 3 streak states via `scripts/agent-test/loop28-meteors.mjs` with deterministic-frame pause for screenshots. SW v76 → v77.

## Session arc (L26 → L36, all under the new curator/integrator model)
- **L26** — achievements grid + sourced Magic UI `shine-border` polish (mask-composite: exclude → unlocked badge foil) + `localStorage`-tracked "Uusi" pill pulse for newly-seen badges.
- **L27** — `countUp` (in-house, conceptually = Magic UI `number-ticker`) on dashboard streak + total counters; new `celebrateStreakMilestone(streak)` in `js/features/celebrate.js` reusing `canvas-confetti` for once-per-crossing 3/7/30 burst.
- **L28** — Magic UI `meteors` ported to vanilla, mounted behind dashboard hero on streak ≥ 7.
- **L29** — Magic UI `animated-circular-progress-bar` ported as the YO-valmius readiness ring on the right rail.
- **L30** — shadcn line-chart visual language ported to `renderProgressChart` (natural spline + token colors + bullseye endpoint).
- **L31** — Aceternity 3d-card-effect + Magic UI magic-card cursor-spotlight ported to the .mode-btn cards (CSS-var-driven, coarse-pointer / reduced-motion safe).
- **L32** — Magic UI grid-pattern ported to vanilla SVG as the auth-screen backdrop with a radial mask + 18s drift.
- **L33** — shadcn/ui Tooltip primitive ported to a single global vanilla module replacing native `title=` across share buttons + readiness cells.
- **L34** — Magic UI animated-list reveal pattern ported as a CSS-only top-down stagger on the profile activity timeline.
- **L35** — Magic UI blur-fade ported as the dashboard hero arrival — eyebrow / display / sub stagger with simultaneous opacity + translateY + filter:blur transitions.
- **L36** — Same blur-fade pattern extended to the profile hero (avatar / name / handle / badges, 4-child stagger).
- **Cumulative SW bump:** v73 (start of session) → v85 (now).

## What I'm doing next
**Loop 50 = right-rail rebuild + dismissible Pro popup** — first of the two-loop right-rail rebuild from PLAN.md. The user's mid-session brief 2026-04-28: "Right rail Pro card is too big — keep small top-right user chip (already correct from L41), remove large Pro panel from rail, replace with daily-progress + word-of-day shown to ALL users. Pro upsell becomes a dismissible bottom-right popup (~320 px, slide-up + fade-in 300 ms ease-out, only after 5 s on page, 7-day suppress on dismiss, free users only)." Source pattern from Magic UI / Aceternity / 21st.dev / shadcn sonner.

L51 = same surface, polish + telemetry.

**After L50–L51: L52–L55 dark-theme polish across the entire app** — shared tokens file `css/tokens-shared.css`, app theme toggle (Auto/Vaalea/Tumma) keeps working, blog + legal pages migrate off the old `landing.css` (which the L49 grep showed still has 14 active references). PLAN.md polish queue (cmd-K palette, dialog primitive, marquee, auth tabs redesign) is now L56+.

## Working context
- Dev server still on :3000.
- Test creds still empty in `.env`. All L26-L28 tests mock the dashboard payload via `page.route("**/api/dashboard", ...)` after `serviceWorkers: "block"` (CRITICAL — without it the SW intercepts /api/* and bypasses the route stub; lost 30 min in L27 to this).
- BUGS.md status unchanged from L24 (5 P2 left, low-impact / interaction-bound).
- npm new in this session: zero. Everything ported in CSS or used existing `canvas-confetti@1.9.4`.
- Files touched L28: `css/components/meteors.css` (NEW), `js/features/meteors.js` (NEW), `app.html` (CSS link + meteors div in dash-greeting), `css/components/dashboard.css` (.dash-greeting positioning), `js/screens/dashboard.js` (imports + mount/unmount call), `sw.js` (v76→v77 + 2 STATIC_ASSETS), `scripts/agent-test/loop28-meteors.mjs` (NEW — deterministic-frame pause technique).
- Risky / mid-flight: nothing. All additive; meteors silently no-op for streak < 7.

## Sources catalog (curator-model toolkit) — keep growing
- **Magic UI** (`github.com/magicuidesign/magicui` — `apps/www/registry/magicui/`).
  - **L26 sources:** `border-beam.tsx`, `shine-border.tsx` (mask-composite: exclude technique → ported to `.profile-badge-card--unlocked::before`), `magic-card.tsx` (radial-spotlight hover concept).
  - **L27 sources:** `number-ticker.tsx` (concept reference; in-house `countUp` already implements the spring-eased count-up).
  - **L28 sources:** `meteors.tsx` (full port — animation simplified to direct screen-space translate via `@property` `--mx` / `--my`; original's `rotate(angle) translateX(-720px)` produces unintuitive paths).
  - **Available, unsourced:** `bento-grid`, `animated-circular-progress-bar` (target for L29), `marquee`, `dock`, `aurora-text`, `sparkles-text`, `text-animate`, `typing-animation`, `word-rotate`, `interactive-grid-pattern`, `flickering-grid`, `shimmer-button`, `pulsating-button`, `ripple-button`, `globe`, `orbiting-circles`, `animated-list`, `animated-shiny-text`, `animated-subscribe-button`, `blur-fade`, `dot-pattern`, `grid-pattern`, `hero-video-dialog`, `hyper-text`, `lens`, `light-rays`, `line-shadow-text`, `morphing-text`, `neon-gradient-card`, `noise-texture`, `particles`, `progressive-blur`, `rainbow-button`, `retro-grid`, `ripple`, `scroll-progress`, `striped-pattern`, `text-reveal`, `warp-background`.
- **Aceternity UI** — 3D-card-effect pattern noted; hover-effect-cards 404'd, revisit via GitHub source.
- **shadcn/ui** (`ui.shadcn.com`). Not yet sourced from. Top targets: `chart`, `command`, `dialog`, `data-table`, `tabs`. **L29 candidate.**
- **HyperUI / Tailwind UI free / DaisyUI / Headless UI / Tabler / Preline** — none sourced yet.
- **21st.dev** — no achievement components surfaced; will widen.
- **Lucide icons** — already inline as path-d strings in `BADGES`.
- **canvas-confetti** (npm, installed L0) — used L20 (results) and L27 (streak milestone).
- **Playwright** (npm, installed L0) — `serviceWorkers: "block"` is mandatory when mocking `/api/*`.
- **Inspiration screenshots** — `scripts/agent-test/inspiration/` from earlier session.

## Skills and plugins in use
- `puheo-screen-template` — applied L4..L11, L25, L27 (countUp on dashboard)
- `puheo-finnish-voice` — applied across most loops
- `puheo-ai-prompt` — applied L16..L22
- `ui-ux-pro-max` — most loops; L26 P2+P5+P6, L27 P5, **L28 P2+P5+P6+P7**
- npm: `canvas-confetti@1.9.4`. Playwright (devDependency).

## Files I have memorized — do NOT re-read on resume
- `CLAUDE.md`, all 3 puheo-*/SKILL.md, ui-ux-pro-max/SKILL.md
- `app.html` — 39-screen structure incl. `#screen-profile` with `#profile-achievements`; `<header class="dash-greeting">` now has `<div class="meteors-fx" id="dash-meteors" hidden>` as first child; linked `meteors.css` after `profile.css`.
- `js/main.js` boot order; NAV_HASH `profile: "#/oma-sivu"`; sidebar selector incl. `.sidebar-user[data-nav]`.
- `js/screens/profile.js` — calls `renderAchievementsInto`.
- `js/features/achievements.js` — `BADGES` (10), `evaluateAchievements`, `renderAchievementsInto`. localStorage `puheo_seen_badges`.
- `js/features/celebrate.js` — `celebrateScore(pct)` (L20) + `celebrateStreakMilestone(streak)` (L27). localStorage `puheo_streak_milestone`. STREAK_BANDS `[3, 7, 30]`.
- `js/features/meteors.js` — `mountMeteors(el, {count, angleDeg, min/maxDelaySec, min/maxDurationSec})` + `unmountMeteors(el)`. Idempotent via `dataset.meteorsMounted`. Default angle `-25deg` (visual tilt, not motion direction).
- `js/screens/mode-page.js` — `countUp(el, target, duration=1200)`, ease-out-cubic + reduced-motion fallback.
- `js/screens/dashboard.js` — `loadDashboard`, `renderDashboard` (local), MODE_META, `saveProgress`, `navigateToMode`, `loadLastSettings`, `saveLastSettings`. **L27**: countUp on `#dash-streak` + `#dash-total-sessions`, `celebrateStreakMilestone` after streak class toggle. **L28**: `mountMeteors(#dash-meteors, {count: streak >= 30 ? 24 : 16})` when streak ≥ 7; `unmountMeteors` otherwise.
- `js/screens/{vocab,grammar,reading,writing}.js` — share-card handlers, anti-repetition, daily challenge wiring.
- `js/features/shareCard.js`, `js/features/dailyChallenge.js`, `js/features/wordOfDay.js` — per L18..L23 entries.
- `js/state.js` — recent* anti-repetition fields.
- `lib/openai.js`, `lib/writingGrading.js`, `routes/exercises.js`, `routes/writing.js` — shape unchanged in L26-L28.
- `routes/progress.js` — `/api/dashboard` returns `{ totalSessions, modeStats, recent, chartData (60 days), estLevel, gradeEstimate, streak, weekSessions, prevWeekSessions, suggestedLevel, modeDaysAgo, pro, aiUsage }`.
- `css/components/profile.css` — L26 shine-border + pulse + hover.
- `css/components/dashboard.css` — `.dash-greeting` updated L28 with position:relative, isolation, min-height 96px, z-indexed children.
- `css/components/meteors.css` — `.meteors-fx` + `.meteor` + `::before` tail; `@keyframes meteorFall` animates `--mx`/`--my` (registered via `@property`); `prefers-reduced-motion` and `<600px` short-circuits.
- `sw.js` is `puheo-v77`. STATIC_ASSETS now includes meteors.css + meteors.js. Other tracked files (celebrate.js, achievements.js, profile.css, profile.js, dailyChallenge.js, shareCard.js, wordOfDay.js, app.html) already listed since earlier loops.
- `server.js` CSP after L3.
- **Playwright gotcha (L27):** `serviceWorkers: "block"` when mocking `/api/*`.

## Files that ARE worth re-reading on resume
- `AGENT_STATE.md` (this file)
- `IMPROVEMENTS.md` (last 30 lines)
- `BUGS.md` (5 P2 left)
- The single file the next loop will edit (Loop 29 — likely `app.html` + dashboard.css for the readiness ring SVG, or `routes/progress.js` if readiness needs a real value).

## Loop measurement (unchanged from L25)
- axe violations: 28 → 0 (held since L6)
- App console errors: 69 → 0 (one CSP-script-src worker block from sentry blob — pre-existing)
- BUGS.md P0: 6 → 1 (P0-4 server-restart-bound)
- BUGS.md P1: 12 → 0
- BUGS.md P2: 12 → 5
- Lighthouse desktop landing: 99 / 100 / 92
- Lighthouse desktop app: 93 / 100 / 96 (CLS 0.000)

## Note on context budget for next /clear
L26 + L27 + L28 each cited at least one outside source. Curator model is sticking. Three screen surfaces touched (profile achievements, dashboard counters, dashboard hero). All additive — zero existing functionality lost. Safe to `/clear` here.
