# Puheo Agent State

**Last updated:** 2026-05-03T20:30:00Z
**Last completed loop:** L-LIVE-AUDIT-P2 (session 1 of ~5) — UPDATE 1 + UPDATE 2: CSS + JS bundling. Added `esbuild@0.28.0` devDep + new `scripts/build-bundle.mjs` + `scripts/bundle-entry.css`. CSS bundle: 16 separate `<link>` tags + chain of `@import url('css/components/*.css')` in `style.css` collapsed to single `/app.bundle.css` (229.7 KB minified, sourcemap gitignored). JS bundle: `js/main.js` ESM entry + 27 transitive static + dynamic imports collapsed to single `/app.bundle.js` (315.9 KB minified, ESM, ES2020 target, posthog CDN URL kept external via `external: ["https://*"]`). `app.html` swapped 16 CSS links → 1, `<script type="module" src="js/main.js">` → `<script type="module" src="/app.bundle.js">`; sync scripts (`pre-launch-gate`, `app-prepaint`, `app-config`, `sw-register`) untouched. `package.json` `build` script wired to `node scripts/build-bundle.mjs` (was no-op echo). `.gitignore` excludes `*.bundle.*.map`. SW `CACHE_VERSION` v112 → v113 + bundles added to STATIC_ASSETS (source files kept for offline robustness). Source CSS+JS files NOT removed from repo per brief (`ÄLÄ poista source-CSS/JS-tiedostoja repo:sta — vain bundlaa ne`). Tests: `npm test` 1 067/1 067 ✓; `node --check` clean on `js/main.js`, `sw.js`, `scripts/build-bundle.mjs`; bundle smoke-test `grep -c "loadDashboard\|initVocab\|loadCurriculum" app.bundle.js` → 20. **Pending user/agent action before session 2:** measure cold load `/app.html` on production Vercel deploy (Lighthouse + DevTools network panel) and compare against `AUDIT_LIVE_DASHBOARD.md` baseline (1339 ms DOM-ready / 3.7 s all-APIs / 18 CSS req / 28 JS req). Brief expects 200–400 ms saved by UPDATE 1 + 500–1 000 ms by UPDATE 2. Remaining UPDATEs in this loop: 3 (`/api/dashboard/v2`), 4 (profile cache), 5 (vocab pre-gen), 6 (adaptive/status optim), 7 (theme toggle View Transitions), 8 (self-host fonts).

**Previously completed:** L-LIVE-AUDIT-P1 — 7 P1-tason visuaalista polish-fixiä `AUDIT_LIVE_DASHBOARD.md`:n nojalla + UPDATE 8 selvitys (käyttäjän decision required). UPDATE 1 `css/components/dashboard.css` `.dash-tutor` siirtyi `--surface` → `--surface-2`, padding 16/20 → 24 px (mobile 20 px), border-radius `--r-lg`, border-left 3 px → 2 px, msg 17 px → 15 px `--ink-soft`. UPDATE 2 `js/screens/adaptive.js renderAdaptiveCard()` pending-haara: `<p class="lpc-eyebrow">Tasosi edistyminen</p><h3>Matka tasolle ${nextLevel}</h3><p class="lpc-hint">Kun täytät kaikki neljä tavoitetta, sinut päivitetään ${nextLevel}-tasolle.</p>` + 2-col `.lpc-grid` (1-col ≤640 px) jossa 4 metric-riviä (label + tabular-num + 4 px progress-palkki, `is-done`-state `--success`-värillä, `role="progressbar"` per metric), absoluuttinen `.lpc-badge` oikeassa yläkulmassa (inline ≤480 px). Light-theme override `--accent-dark`-pohjalla. UPDATE 3 dashboard.js readiness `<span class="dash-readiness-pct">${pct}%</span>` + 4 px `.dash-readiness-track`-bari + sub-rivi count + qualitative + "vähän hyvin"-legend poistettu DOMista; `renderWritingProgression()` arvosanat "3,5/5" suomalaisella pilkulla, flat trend-arrow ei renderöidy lainkaan. UPDATE 4 `style.css` `#explanation-text` `--font-mono` → `--font-display`, 14 → 15 px, `--ink-soft` → `--ink`. UPDATE 5 `.exam-text` `--font-mono` → `--font-display`, 14/1.75 → 16/1.7. UPDATE 6 `.sr-grade-btn`-perheelle full-saturation per-class color + outline (`--sr-again` jne. -tokenit), hover `color-mix(... 10%, transparent)`, `:focus-visible` 2 px `--accent`-rengas. UPDATE 7 `.ex-type-badge.type-context` #1D4ED8 (sininen) → `var(--accent)` (brand mintti), light-theme override `--accent-dark`. UPDATE 8 audit-raportti kategoriaväreistä (mode-page.css rivit 202–206, raw-hex per-mode, ei tokenia, ei light-overridea) + 3 vaihtoehtoa käyttäjän päätettäväksi: (1) tokenoida nykyiset eri värit, (2) harmonisoi brand-minttiin, (3) harmonisoi neutraaleihin — agent suosittelee 1 koska orientation-vihje on arvokas, mutta päätös käyttäjälle. SW v111 → v112. Tests: `npm test` 1 067/1 067 ✓, `node --check` clean kahdella muokatulla JS-tiedostolla. **Deferred:** Playwright axe-sweep + design:design-critique-screenshot-passi @ 1440 + 375 (workflow-dispatch-gated). 21st.dev sourcing: UPDATE 2 [21st.dev/s/progress-card](https://21st.dev/s/progress-card), UPDATE 3 [21st.dev/s/horizontal-bar](https://21st.dev/s/horizontal-bar) (säilytetty olemassa oleva pattern), UPDATE 6 [21st.dev/s/rating-buttons](https://21st.dev/s/rating-buttons), UPDATE 1+7 STANDARDS §3 trivial extension olemassa olevista primitiiveista.

**Previously completed:** L-LIVE-AUDIT-P0 — 5 P0-bugia jotka käyttäjä ja minä todensimme tuotantoa vasten Chrome-MCP:llä Pro-tilillä (audit-data `AUDIT_LIVE_DASHBOARD.md`). UPDATE 1 vaihtoi `js/screens/fullExam.js` rivin 141 natiivin `confirm()`:n tri-state-modaaliin `showExamResumeDialog()` (resume / discard / dismiss) joka käyttää olemassa olevaa `.confirm-dialog*`-CSS-pohjaa, X-nappia + Escape + backdrop-klikkausta dismiss-haaraan (palaa dashboardiin, ei umpikujaan); `routes/exam.js` sai `POST /api/exam/discard-active` (idempotent, status='abandoned' säilyy historiaan). UPDATE 2 `css/components/dashboard.css` `.heatmap-cell:not([class*="lvl-"]):not(.future) { background: transparent; border: 1px dashed rgba(20,184,166,0.18) }` + light-theme override `--accent-dark`-pohjalla — empty-state ei näytä enää 35 mustana laatikkona. UPDATE 3 `css/components/button.css .btn--cta__meta` poistettu `opacity: 0.78` (täysi mint-20 #CCFBF1 navy:lla on ~13:1, audit havaitsi washed-out-rendöinnin). UPDATE 4 `js/main.js maybeConfirmNavAway()` laajennettu kattamaan myös standalone exercise-screenit (ei vain lessonContext-pohjaisia); standalone-haara saa modaali-copyn "Lopetetaanko harjoitus? — Vastauksesi tähän asti tallentuvat...", uusi `exitToSource()` ohjaa x-buttonin lähde-mode-pageen (state.mode → screen-mode-{mode}) eikä aina dashboardiin. UPDATE 5 `api/index.js` (Vercel serverless entry) sai mounted `configRoutes`, `statusRoutes`, `/api/waitlist` — `/api/config/public` ei enää 404:ää tuotannossa (juurisyy: `server.js` ja `api/index.js` olivat erkaantuneet). SW v110 → v111. Tests: `npm test` 1067/1067 pass, `node --check` clean kaikilla 4 muokatulla tiedostolla. Brief:n explicit deferred-items: Playwright E2E-spekit (workflow_dispatch-gated d3f5ca5:n jälkeen, secrets-puute) ja axe-core/design:design-critique sweep muutetuista näkymistä (Playwright-riippuvuus). 21st.dev sourcing: STANDARDS §3 "trivial extension of existing pattern" -poikkeus, modaali on suoraan `.confirm-dialog`-primitiivin extension.

**Previously completed:** L-PLAN-8 completion-pass (deferred-subset shipped). 6 updates: UPDATE 4 pricing-card x-row tooltips wrapped in `<a href="#hinnoittelu" data-tooltip>` with inline "7 pv →" tail-CTA + `.tt-popover` ported into `css/landing.css` + `installTooltip()` imported from `index.html`; UPDATE 5B kertaa-banner kontrasti staattisesti verifioitu (≥10.7:1 kaikilla rakenteilla, ei muutosta); UPDATE 5C `#screen-dashboard` + `.dashboard-inner` overflow-x: clip + max-width: 100vw -guard X-overflowin estoon Y-scrollia rikkomatta; UPDATE 6B `.curr-card.is-locked` opacity 0.45 → 0.55 (kompositiokontrasti ~5.7:1); UPDATE 6C lukittujen kurssikorttien `data-tooltip="Suorita ensin Kurssi N (kertaustestissä ≥ 80 %)"` + `tabindex="0"` näppäimistö-saatavuuden takaamiseksi; UPDATE 8 uusi `scripts/agent-test/lplan8-axe.mjs` 6 sweep-konfiguraatiota (landing+dashboard+curriculum × 1440+375) + reduced-motion-passi — ensimmäinen passi paljasti 3 pre-existing curriculum-violationia (aria-progressbar-name, list ol[role=button] -lapset, color-contrast `--accent` light-themellä); kaikki kolme korjattiin (lisätty inner aria-label, poistettu role="button" `<li>`:stä, uusi `[data-theme="light"] .curr-card.is-current/.is-done .curr-status { color: var(--accent-dark); }` -override). Toinen passi 0 violations + RM-raportti clean. SW v105 → v106 (4 muokattua jo-cachattua tiedostoa). UPDATE 7 (H1 swap) jää default "pidetään" — odottaa käyttäjän päätöstä. 21st.dev sourcing-screenshotit deferattu STANDARDS §3 "trivial extension of existing pattern" -poikkeuksena (jokainen primitiivi tässä loopissa on port olemassa olevasta in-repo-patternista). Tests: `npm test` 1 067 pass, `node --check` clean, axe-sweep 0 violations.

**Previously completed:** L-PLAN-8 (landing-polish + dashboard empty-state + "elävä" hero ilman generoituja kuvia — code-only subset). 6 updates shipped: UPDATE 1A new `js/features/landingHero.js` rotates the sanasto pillar's demo card through 6 B1–B2 word pairs every 5 s (IntersectionObserver-gated, opacity-only crossfade, reduced-motion-safe); UPDATE 1B `.hero__glow--drift` keyframe drifts the radial accent glow on a 60-second translate3d loop (≤2 % range); UPDATE 2 four copy changes — second hero CTA "Näe esimerkkiarviointi →" to `#arviointi` anchor, trust-row "8 kurssia, 90 oppituntia" (numbers verified against `lib/curriculumData.js`), writing pillar metric "~30 s arviointi · 4 osa-aluetta", step 01 "kertausjono" → "harjoituslista" (technical term reserved for sisäänkirjautuneelle käyttäjälle); UPDATE 3 new `js/features/scrollReveal.js` + `[data-reveal]` CSS primitive applied to grader-card+copy and the 3 pillar articles with 0/100/200 ms staggered delays (cubic-bezier 0.16/1/0.3/1 ease-out, 600 ms); UPDATE 5A dashboard YO-arvio empty-state shortened from 80 chars to "Tee 10 harjoitusta 3 osa-alueesta — sitten arvio." (49 chars); UPDATE 6A `loadCurriculum()` now dynamically refreshes `teachingPanel` so the floating Opetussivu trigger hides immediately on path-screen entry. SW bumped v103 → v104, added `landingHero.js` + `scrollReveal.js` to STATIC_ASSETS. **DEFAULT for UPDATE 7 (hero H1 decision):** pidetään nykyinen H1 — user can override in one line edit. **Deferred** (separate brief, requires Playwright/external tooling): UPDATE 4 pricing-card tooltips (needs `.tt-popover` to be reachable from landing), UPDATE 5B/5C dashboard banner-contrast + viewport-overflow axe sweeps, UPDATE 6B/6C locked-card contrast + hover-tooltip, UPDATE 8 full a11y + critique + Lighthouse pass, 21st.dev sourcing screenshots. Tests: `npm test` 1 067 pass (no regressions vs. L-SECURITY-2 baseline); `node --check` clean on `landingHero.js`, `scrollReveal.js`, `dashboard.js`, `curriculum.js`; `npm run security:scan` → exit 0.

**Previously completed:** L-SECURITY-2 (code-side action items käyttäjän pyytämänä — LemonSqueezy jää paikalleen, Stripe-migraatio tulee myöhemmin omana loopinaan). 3 fixes: FIX 1 lisäsi `requireAuth` `/grade/advisory`-endpointtiin (anonyymi enumerointi seed-bankistä estetty); FIX 2 vaihtoi `routes/email.js` CRON_SECRET-vertailun string-`!==`:stä `crypto.timingSafeEqual`-pohjaiseen `cronSecretValid()`-helperiin (sama pattern kuin `middleware/basicAuth.js`:ssa); FIX 3 lisäsi uuden `waitlistLimiter`-IP-rajoittimen `middleware/rateLimit.js`:hen (20 pyyntöä/IP/h) ja mountattu `/api/waitlist`-endpointiin. Auditoin koko `routes/`-kansion: kaikki AI-endpointit OK (auth + aiLimiter + costLimit), seed-only endpointit OK (auth, ei AI), admin-endpointit OK (auth + ADMIN_EMAILS), cron-endpointit OK (timing-safe FIX 2:n jälkeen). Stripe webhook signature-vertailu (`routes/stripe.js handleWebhook()`) ei ole timing-safe mutta sen handler on suunniteltu uusittavaksi Stripe-migraation yhteydessä — `stripe.webhooks.constructEvent()` hoitaa sen oikein. **No SW bump.** Tests: `npm test` 1 067 pass (1 testi päivitetty auth-gated-shaping mukaiseksi); `node --check` clean; `npm run security:scan` → exit 0.

**Previously completed:** L-SECURITY-1 (hardcoded-secrets sweep + .env hygienia + `npm run security:scan` + SPF/DKIM/DMARC ACTION REQUIRED — käyttäjä deferattu DNS:n koska sivusto ei vielä live). 5 updates: UPDATE 1 sweep found ZERO real keys (only the brief itself, migration comments referencing the *concept* `service_role`, and one literal `"test-webhook-secret"` test fixture); git history clean — `.env` never committed, `.env.example` only ever held template placeholders. UPDATE 2 (refactor) is therefore a no-op — no rotations needed. UPDATE 3 expanded `.gitignore` to all `.env.*` variants + `*.pem`/`*.key`, and rewrote `.env.example` to cover every `process.env.X` the code reads (new docs: `SUPABASE_ANON_KEY`, the previously-undocumented pre-launch gate vars `SITE_USERNAME`/`SITE_PASSWORD`, `EXAM_WEEK`/`OPENAI_DISABLED` feature flags, `PRO_TEST_LIST`/`FREE_TEST_LIST` legacy aliases, the Playwright e2e creds, `BASE_URL`/`VISUAL_BASELINE`/`CI_BASE_REF`/`GIT_SHA`/`NODE_ENV`). All non-pakolliset vars carry `# optional`. UPDATE 4 wrote the SPF/DKIM/DMARC ACTION REQUIRED block (Resend uses Amazon SES so the include is `amazonses.com`, NOT Google) into IMPROVEMENTS.md including a callout that `*.vercel.app`-aliaksesta lähettäminen ei toimi — käyttäjän pitää ostaa custom-domain ja vaihtaa `EMAIL_FROM` siihen. UPDATE 5 added `scripts/security-scan.mjs` + `npm run security:scan` (~55-line script that reads `git ls-files` content from disk and exits 1 on any of: Stripe live + webhook, OpenAI ≥32-char, Resend, AWS, Supabase service-role JWT). Verified clean. **No SW bump** — every changed file is server-side or tooling, none in STATIC_ASSETS. **No new throws added** — UPDATE 2 had nothing to refactor. Tests: `npm test` 1 067 pass (no regressions vs. L-PLAN-7); `node --check` clean; `git status` no `.env` staged.

**Previously completed:** L-PLAN-7 (kumulatiivinen kertaus — sisäinen kertaus oppitunnin sisällä + cross-kurssi kertaus K(N≥2):n alussa + SR-pohjainen henkilökohtainen kertaus joka 3. oppitunti + Kertaus-badge frontendissä + post-results "Kertasit myös tätä" -osio). 8 updates (UPDATE 1–8 + ledger UPDATE 9), all shipped + verified. **UPDATE 6** new `lib/lessonLabels.js` + `js/lib/lessonLabels.js` mirror map source-keys (`kurssi_X_lesson_Y`, `kurssi_X_review`) to Finnish labels. **UPDATE 1** `lib/lessonContext.js` `resolveLessonContext()` now returns `reviewTopics: [{ focus, type, source, label, slot }]` via new `buildReviewTopics()` helper. For lesson M >= 3 picks the weakest prior lesson (score < 0.85) or seed-based deterministic fallback. **UPDATE 2** for kurssi N >= 2 lessons 1–2 adds `crossKurssi` slot from previous kurssi's `grammar_focus[]`. **UPDATE 4** every 3rd lesson queries all `user_curriculum_progress` rows, picks oldest weak (<70%) topic across all kurssit, adds `sr` slot with +1 count bump in `applyLessonContext()`. `curriculumFocusInstruction()` adds three optional prompt blocks: "KERTAUS — SISÄINEN" (last 2 items), "KERTAUS — EDELLINEN KURSSI" (middle item), "HENKILÖKOHTAINEN KERTAUS" (+1 tail item). All require AI to set `topic_key` field on each exercise. **routes/exercises.js** new `annotateReviewTags(exercises, ctx)` post-processes the AI response: matches each exercise's topic_key against `ctx.reviewTopics`, tags `is_review` + `review_source` + `review_source_label`, idempotent. Reading + writing lesson types are intentionally excluded from review (1-task formats). **UPDATE 3** new `js/features/reviewBadge.js` exports `setReviewBadge(exercise, hostSelector)`; vocab + grammar render functions call it with `.exercise__meta` host. Badge is a flex-child pill with `↻` icon + label, body text in `--ink` for theme-stable AA, soft accent fill + 30% accent border, 200 ms fade respecting `prefers-reduced-motion`. **UPDATE 5** post-results "Kertasit myös tätä" -osio: `js/screens/vocab.js recordItem()` and `js/screens/grammar.js showGrammarResults()` capture a `reviewItems[]` array; backend `routes/curriculum.js` new `sanitiseReviewItems()` + `buildReviewSummary()` bin items by review_source and emit headline-band rows ("Vahvistui" 100%, "Pieni muistutus" >0%, "Tämä kaipaa vielä huomiota" 0%); the summary is also injected into the AI tutorMessage prompt so the message names the kertaus by topic. `js/screens/lessonResults.js renderReviewSummary()` inserts a `.lr-review-summary` section after the tutor block (region/aria-label, mono eyebrow, 3-col grid collapsing to 2-row stack on ≤480 px). **UPDATE 7** new E2E `scripts/agent-test/lplan7-cumulative-review.mjs` validates: (a) `setReviewBadge()` hides on non-review and renders correct label on review, (b) `.lr-review-summary` renders with the expected headline / label / score. Lesson-launch path is already covered by `lplan5-lesson6-e2e.mjs`. **UPDATE 8** new axe sweep `scripts/agent-test/lplan7-axe.mjs` over 2 states × 2 viewports; first pass surfaced 1 color-contrast violation (badge text in `--accent` on soft fill, mobile-375), fixed by switching body text to `--ink` and only keeping icon in accent. Final pass: 0 violations.

**Previously completed:** L-PLAN-6 (tavoitepohjainen vaikeustaso — `target_grade` ohjaa exercise count + level + tone + Syvennä-callout). 9 updates (UPDATE 1–9), all shipped + verified. **UPDATE 1** `lib/lessonContext.js` exports `TARGET_GRADE_MULTIPLIERS` (I 0.7 / A 0.85 / B/C 1.0 / M 1.15 / E 1.3 / L 1.5), `TARGET_GRADE_PASS_THRESHOLDS` (I/A 0.7 / B/C 0.8 / M 0.85 / E/L 0.9), `applyTargetMultiplier`, `passThresholdFor`, `buildLevelDirective`. `resolveLessonContext(payload, userId)` fetches `user_profile.target_grade`, exposes `adjusted_exercise_count` + `levelDirective` + `isDeepen`. `routes/exercises.js` threads `req.user.userId` through every call site; both `curriculumFocusInstruction()` + `curriculumTestInstruction()` append the level directive and pivot recognition→production thirds off the adjusted count. **UPDATE 2** `routes/curriculum.js` `GET /:k/lesson/:i` adds top-level `lessonContext: { targetGrade, exerciseCount, baselineExerciseCount }` (existing contract preserved — `lesson.exerciseCount` stays baseline). `js/screens/curriculum.js` `ctaLabel(lesson, lessonContext)` prefers the adjusted count: B→"(8 tehtävää, ~12 min)" / L→"(12 tehtävää, ~18 min)" / A→"(7 tehtävää, ~11 min)". **UPDATE 5** `/complete` reads target_grade and uses `passThresholdFor()` instead of static 0.80; `passLabel` now flips at the per-tavoite threshold inside the AI tutor prompt. **UPDATE 6** New `TONE_DESCRIPTORS` table in `routes/curriculum.js` (I "erittäin kannustava" → L "erittäin vaativa, oleta täydellinen kontrolli"); `toneBlock(targetGrade)` injected into both `/complete` tutor-message + `/curriculum/tutor-message` greeting prompts, with explicit "ei Hyvää työtä! -geneerisyyksiä" rule. **UPDATE 7** New `.ob1-target-detail` block in `app.html` under the targetGrade pill row, populated by `js/screens/onboardingV2.js TARGET_DESCRIPTIONS` ({title, pace, fit} per grade) + `renderTargetDetail()` on every pill click + auto-pre-select. `aria-live="polite"` so SR users hear the framing without focus jump. Style: restrained `--accent` left-border card in `css/components/onboarding-v2.css`. Frame: "Sopii sinulle, jos…" — never moittelua. **UPDATE 3** `routes/profile.js` `VALID_TARGET_GRADES` extended to full I..L; `js/screens/settings.js` `TARGET_GRADE_LABELS` adds I/A + new `TARGET_GRADE_PACE_HINTS` map; target_grade field gains the brief's hint copy + a target_grade-only success toast on save via `js/ui/toast.js`. Backend never touches `user_curriculum_progress` so completed lessons survive every change. `tests/routes-profile.test.js` updated: invalid case now uses "Z"; new happy-path test asserts "A" is accepted post-L-PLAN-6. **UPDATE 4** `js/screens/lessonResults.js` renderResolved appends a `.lr-deepen` callout when `target_grade === "L" && score/total ≥ 0.85 && lessonType ∈ {vocab, grammar, mixed} && !kurssiComplete`. New `js/lib/lessonContext.js` exports `setDeepenRun()` + `isDeepenRun()`; vocab + grammar loaders include `mode: "deepen"` + `count: 4` when the flag is set; `resolveLessonContext` reads it and `curriculumFocusInstruction()` swaps to a "L-tason syvennys" prompt branch. After the 4-item run, `showLessonResults()` notices the flag, skips the `/complete` re-POST, renders a `.lr-card--deepen` mini-summary, clears the flag. `clearLessonContext()` also clears the deepen flag. Reading + writing intentionally excluded (1-task formats). **UPDATE 8** `scripts/agent-test/lplan6-target-grade-flow.mjs` walks the full happy path: B→assert CTA "(8 tehtävää, ~12 min)" → flip to L → assert "(12 tehtävää, ~18 min)" → render results 12/12 → assert Syvennä callout → click Tee → assert `mode: "deepen"` + `count: 4` lands → replay deepen result → assert `.lr-card--deepen` mini-summary. 5 screenshots captured. **UPDATE 9** `scripts/agent-test/lplan6-axe.mjs` runs axe over 5 states × 2 viewports (OB-1 target detail, lesson CTA, results+Syvennä, deepen summary, settings target_grade modal). First pass found 4 violations: `.lr-bar` missing aria-label (pre-existing, surfaced by new sweep) → added; `.lr-score--good .lr-score-num` accent on light bg failing AA → added `--accent-dark` light-theme override; `.lr-card--deepen .lr-eyebrow` same → same override; `.settings-opt.selected` `--accent-hover` on soft bg failing AA in light mode → switched to `--accent-dark`. Final: 0 violations. **Verification.** `node --check` clean on every modified file; SW bumped v101→v102 (no new STATIC_ASSETS — every modified file was already listed); all 1 062 vitest tests pass. Three deferred items: 21st.dev `references/app/target-grade/21stdev/` Playwright capture (built from existing `.lr-fasttrack` primitive + `21st.dev/s/callout` structural reference, no new capture pass), `design:design-critique` skill pass against the captured screenshots (screenshots on disk, skill not run this loop), Supabase migration apply (still user-side).
**Next loop:** L-LIVE-AUDIT-P2 (käyttäjän AGENT_PROMPT_LIVE_AUDIT_P2.md:n mukaan) — performance-fixit: CSS+JS-bundlaaminen (~1 s säästö cold loadissa), adaptive/status-cache + dashboard-API-yhdistäminen, sanasto-pre-gen / streaming. **Pending decision käyttäjältä:** L-LIVE-AUDIT-P1 UPDATE 8 — kategoriavärit (token-cleanup vs. harmonisoi minttiin vs. harmonisoi neutraaleihin). Suositus: token-cleanup. Ei estä L-LIVE-AUDIT-P2:n aloitusta.

**Recurring blockers (carried):** Playwright E2E-CI (workflow_dispatch-gate d3f5ca5:n jälkeen, secrets-puute). axe-core sweep muutetuista screeneistä L-LIVE-AUDIT-P0:n jälkeen on deferred kunnes Playwright-stack on käytössä. Manuaalitesti tuotannossa Vercel-deployn jälkeen on käyttäjän tehtävä.

**Edellinen suositusjärjestys (L-SECURITY-3 — käyttäjä päättää):**
1. **Supabase RLS-audit** — `db/AUDIT.md` on jo aloittanut työn, viimeistele se: jokainen `user_*`-taulu RLS:ssä, anon-key ei pääse mihinkään, service_role-only-policyt missä ne kuuluvat.
2. **Helmet/CSP-tightening** — selvitä mitkä `'unsafe-inline'`-tarpeet ovat oikeita (vanilla JS event-handlerit?) ja siirrä niitä eksternaaleihin tiedostoihin; self-host Inter + DM Mono jotta `googleapis.com`/`gstatic.com` voidaan poistaa connect/style/font-src:istä.
3. **Prompt-injection-suojaus AI-endpointtien sisällä** — varmista että käyttäjän input ei pääse väliin OpenAI system-promptiin (esim. `topic`, `recentlyShown`, lesson `focus`-stringit) — kategorinen escape + whitelist.
4. **Stripe-migraatio (kun olet valmis)** — vaihda `@lemonsqueezy/lemonsqueezy.js` `stripe`-pakettiin; uudelleenkirjoita `routes/stripe.js handleWebhook()` käyttämään `stripe.webhooks.constructEvent()`-funktiota (hoitaa timing-safe signature-verification SDK:ssa).
5. **Secret-rotation-policy** — dokumentoi mistä paneeleista jokainen avain rotatoidaan + miten env-muuttujat päivitetään Vercelillä; kvartaali-rotaatio-aikataulu jos haluat OWASP-tasoisen hygienian.

Recurring blockers / deferred items (carried from previous loops):
(a) **Supabase migration apply** — same item as every loop since L-PLAN-2; käyttäjä ajaa `supabase/migrations/20260429_curriculum.sql` kerran SQL-editorissa.
(b) **21st.dev Playwright capture passes** for: target-grade callouts (L-PLAN-6), review-badge + review-summary (L-PLAN-7).
(c) **`design:design-critique` Playwright pass** against the L-PLAN-6 + L-PLAN-7 screenshots on disk.
(d) **Reading + writing review extension** — intentionally excluded; switch is `REVIEW_ELIGIBLE` in `lib/lessonContext.js`.
(e) **DNS-recordit (SPF + DKIM + DMARC)** — käyttäjä lisää itse domain-rekisteröijällä, exact values in IMPROVEMENTS.md L-SECURITY-1 ACTION REQUIRED block.
(f) **Custom-domain osto** jos `EMAIL_FROM` edelleen `*.vercel.app`-aliaksella (Resend ei salli verifioimattomalta domainilta lähetystä).

Do NOT bump SW unless STATIC_ASSETS change. Do NOT touch landing page.

**Active roadmap:** `PLAN.md` — user-directed P0 items, now expanded to **10 items** (4 bugs ✓ + 3 redesigns L40 ✓ L41 ✓ + 8-loop landing rebuild L42 ✓ L43 ✓ L44 ✓ L45 ✓ L46 ✓ → L49 + 2-loop right-rail rebuild + Pro popup L50–L51 + 4-loop dark-theme polish L52–L55).

## What I just did
Loop **L-LIVE-AUDIT-P1** — visuaaliset polish-fixit dashboardiin + harjoitusscreeneihin (`AUDIT_LIVE_DASHBOARD.md` P1-kohdat 6, 7, 8, 9, 10, 11, 12). Brief: käyttäjä sanoi "Oma sivu on ruma" — tavoite "yhtä viimeisteltyä kuin Linear/Vercel". 7 koodimuutosta + 1 selvitys. **Dashboard:** dash-tutor card tightened (24 px padding, `--surface-2`, `--r-lg`); B→C "raw 4-rivin laatikko" → level-progress-card (eyebrow "Tasosi edistyminen" + h3 "Matka tasolle ${nextLevel}" + hint + 2-col grid 4 metric-rivillä joissa kullakin progress-bar + corner badge); YO-valmiuskartta sai dominantin %-headlinen + 4 px progress-trackin, count + qualitative yhdistettiin ja confusing "vähän hyvin"-legend poistettiin DOMista; kirjoittamisen osa-alueet → suomalainen pilkku ("3,5/5") + flat-trendit ei renderöidy. **Harjoitukset:** `#explanation-text` Inter (15/1.6 `--ink`); `.exam-text` Inter (16/1.7); SR-painikkeet (.sr-again/.sr-hard/.sr-good/.sr-easy) full-saturation persistent color + hover `color-mix 10%` + `:focus-visible` accent-rengas — käyttävät jo olemassa olevia `--sr-*`-tokeneita rivillä 45–48; Konteksti-badge (.ex-type-badge.type-context) #1D4ED8 → `var(--accent)` brand-mintille light-override `--accent-dark`-pohjalla. **UPDATE 8 selvitys (ei koodi):** mode-page.css rivit 202–206 sisältävät 5 raw-hex-kategoriaväriä per mode, ei tokeneita, ei light-overrideja. Annoin käyttäjälle 3 vaihtoehtoa + suosituksen "tokenoi nykyinen palette `--cat-*`-tokeneiksi" (säilyttää orientation-vihjeen sidebarista mode-pageen). Päätös käyttäjälle. **Verifiointi:** `node --check` clean (`adaptive.js`, `dashboard.js`); `npm test` 1 067/1 067 ✓; SW v111 → v112 (kaikki 4 muokattua tiedostoa STATIC_ASSETS-listalla). **Deferred:** Playwright axe-sweep + design:design-critique screenshot-passi (workflow-dispatch-gated, sama deferral kuin P0:ssa). 21st.dev sourcing dokumentoitu IMPROVEMENTS.md-rivissä per UPDATE.

## What I previously did (older)
Loop L-PLAN-8 — **landing-polish + dashboard empty-state + live hero ilman generoituja kuvia** (code-only subset). Two new tiny modules: `js/features/landingHero.js` (sanasto pillar live rotation through 6 word pairs every 5 s + hero-glow drift trigger; both IntersectionObserver-gated and reduced-motion-safe) and `js/features/scrollReveal.js` (`[data-reveal]` primitive applied to grader-card+copy + 3 pillar articles with 0/100/200 ms stagger). Four landing copy strings updated: hero second CTA → "Näe esimerkkiarviointi →" (`#arviointi` anchor), trust-row gains "8 kurssia, 90 oppituntia" (verified against `lib/curriculumData.js`), writing pillar metric → "~30 s arviointi · 4 osa-aluetta", step 01 "kertausjono" → "harjoituslista". Dashboard YO-arvio empty-state shortened from 80 chars to 49. `loadCurriculum()` now refreshes `teachingPanel` to hide the floating Opetussivu trigger immediately on path-screen entry. SW v103 → v104 with two new STATIC_ASSETS entries. `prefers-reduced-motion` is the unconditional opt-out for both new effects. `npm test` 1 067 pass, `node --check` clean, `npm run security:scan` exit 0. UPDATE 7 (hero H1 swap) defaulted to "pidetään" — user-decision branch per the brief. UPDATE 4 / 5B / 5C / 6B / 6C / 8 + 21st.dev sourcing screenshots are deferred (Playwright + tooltip-on-landing wiring, all out of this loop's "code-only" scope) — see IMPROVEMENTS.md L-PLAN-8 Deferred block.

## Even older loop
Loop L-PLAN-2 — **curriculum structure + Oppimispolku screen**, three updates in one loop. **UPDATE 1** (`supabase/migrations/20260429_curriculum.sql` + `lib/curriculumData.js`): authored the curriculum migration creating `curriculum_kurssit`, `curriculum_lessons`, `teaching_pages`, `user_curriculum_progress` with proper FKs + `ON DELETE CASCADE`; folded the L-PLAN-1 deferred `user_profile` columns into the same migration so applying it once closes both loops' DB work. Seed: 8 kurssit with `grammar_focus` keyed only to **existing `LEARNING_PATH`** entries (no new keys needed — verified each maps), `vocab_theme` to existing `VALID_VOCAB_TOPICS`. 90 lessons total (brief miscounted; spec §3 line counts add to 90, not 86). Worked-example fading applied: lesson 1 of each kurssi has a full worked example in `teaching_snippet`, last 2 oppituntia before kertaustesti carry "Vihje:" hint sentences, every kertaustesti uses spec verbatim "Nyt testataan mitä olet oppinut — [topic]. [X] kysymystä." Backwards-design: every `focus` field names a measurable outcome, never "lesson N of kurssi M". Interleaving verified against §3. JS mirror `lib/curriculumData.js` matches SQL row-for-row so the routes have a graceful pre-migration fallback. **Migration NOT auto-applied** — no Supabase CLI configured locally; user runs once via Supabase dashboard SQL editor (deferred-item ledger). **UPDATE 2** (`routes/curriculum.js` + mount in `server.js`): 4 endpoints. `GET /api/curriculum` returns kurssit with per-kurssi `lessonsCompleted / lastScore / kertausPassed / isUnlocked`; unlock chain = kurssi_1 always unlocked, kurssi_N unlocks when previous kurssi's last-lesson kertaustesti score ≥ 80%. `GET /api/curriculum/:kurssiKey` joins `user_curriculum_progress` for completion flags per lesson. `GET /api/curriculum/:kurssiKey/lesson/:lessonIndex` lazy-generates teaching pages for `grammar`/`mixed` types via `callOpenAI(prompt, 400, { temp 0.3 })` returning `{ "contentMd": "..." }` (forced-JSON, lib/openai always JSON-parses) — prompt enforces §5 structure verbatim (`# title` / 1-paragraph / `## Muodostus` / `## Esimerkki` / `## YO-vinkki 💡`), explicitly forbids abstract level-talk + bullets outside tables. Cached in `teaching_pages` keyed `${kurssiKey}_lesson_${sortOrder}`; hardcoded fallback when AI text < 50 chars. `POST /api/curriculum/:kurssiKey/lesson/:lessonIndex/complete` (requireAuth) upserts to `user_curriculum_progress`, computes `kurssiComplete = isKertaustesti && score/total ≥ 0.80`, generates `tutorMessage` via `callOpenAI(prompt, 140, { temp 0.5 })` — success path kehu vaikeinta osa-aluetta + lupaa seuraava kurssi, remediation path nimeä konkreettisesti yksi kielioppirakenne, ≤2 lauseetta, sinä-muoto, ei prosentteja. Hardcoded Finnish fallback per kurssi name. `tablesMissing(err)` helper detects `42P01`/`PGRST205`/"relation does not exist" so all routes degrade to JS data when migration hasn't been applied. **Verified end-to-end via the live dev server** — `curl /api/curriculum` returns clean JSON with correct anonymous unlock chain; `curl /api/curriculum/kurssi_3/lesson/2` returns the lesson + AI-generated Markdown teaching page including a properly-formatted Muodostus table. **UPDATE 3** (`#screen-path` rebuild + new `#screen-lesson` + `js/screens/curriculum.js` + `css/components/curriculum.css`): rebuilt the Oppimispolku screen as a vertical 8-card timeline with single-stroke connector through 48 px step circles (Lucide check-icon for done, lock-icon for locked, number for current). Title row carries level chip (A/B/C/M/E in `--accent`), Linear-tier `role="progressbar"` with `aria-valuenow`, status label ("Suoritettu ✓" / "Jatka →" / "Aloita →" / "Lukittu"). Locked cards: `opacity 0.45` + `cursor: not-allowed` + `aria-disabled`; current kurssi: `border-left: 3px solid var(--accent)`; done: muted accent. Tap unlocked card → `is-expanded` toggle (no new screen) + lazy `/api/curriculum/:kurssiKey` fetch → 48 px lesson rows with inline SVG type icons. Loading = 8 shimmer skeletons (1400 ms gradient sweep, animation:none on prefers-reduced-motion). Empty (anonymous) = kurssi_1 only + Rekisteröidy CTA. Error = retry button. **`#screen-lesson`** added to app.html shell; back button → `loadCurriculum()`; eyebrow + h1 + teaching-page card via in-house minimal Markdown renderer (escapes-first, then unwraps headings/blockquotes/4-col tables/inline `**bold**` + `code`). `Aloita harjoittelu →` navigates to existing exercise screen by lesson type and stashes `sessionStorage.currentLesson` for L-PLAN-3 to consume. `Merkitse suoritetuksi (väliaikainen)` POSTs to `/complete` with mock score = exerciseCount/exerciseCount and renders the `tutorMessage` in a banner — temporary scaffold so the loop is closeable manually. Wired `data-nav="path"` left-nav to `loadCurriculum()` in `js/main.js`; the legacy `loadPath()` still fires from `learningPath.js`'s `nav-path` click but bails out gracefully because its target DOM ids no longer exist after rebuild. Mastery-intro/result screens kept intact for now. SW v96→v97, added `curriculum.css` + `curriculum.js` to STATIC_ASSETS. All 1 061 vitest tests still pass. **Deferred** (in IMPROVEMENTS.md L-PLAN-2 block): apply migration to Supabase via dashboard SQL editor; 21st.dev Playwright sourcing pass for `/s/timeline` + `/s/steps` candidates; axe + design:design-critique sweep on Oppimispolku at 1440 + 375; L-PLAN-3 work (real exercise-result wiring + tutor voice dashboard).

## Working context
- Dev server still on :3000.
- Test creds still empty in `.env`. All L26-L28 tests mock the dashboard payload via `page.route("**/api/dashboard", ...)` after `serviceWorkers: "block"` (CRITICAL — without it the SW intercepts /api/* and bypasses the route stub; lost 30 min in L27 to this).
- BUGS.md status unchanged from L24 (5 P2 left, low-impact / interaction-bound).
- npm new in this session: zero. Everything ported in CSS or used existing `canvas-confetti@1.9.4`.
- Files touched L28: `css/components/meteors.css` (NEW), `js/features/meteors.js` (NEW), `app.html` (CSS link + meteors div in dash-greeting), `css/components/dashboard.css` (.dash-greeting positioning), `js/screens/dashboard.js` (imports + mount/unmount call), `sw.js` (v76→v77 + 2 STATIC_ASSETS), `scripts/agent-test/loop28-meteors.mjs` (NEW — deterministic-frame pause technique).
- Risky / mid-flight: nothing. All additive; meteors silently no-op for streak < 7.

## Skills and plugins in use
- `puheo-screen-template` — applied L4..L11, L25, L27 (countUp on dashboard)
- `puheo-finnish-voice` — applied across most loops
- `puheo-ai-prompt` — applied L16..L22
- `ui-ux-pro-max` — most loops; L26 P2+P5+P6, L27 P5, **L28 P2+P5+P6+P7**
- npm: `canvas-confetti@1.9.4`. Playwright (devDependency).

Älä lue arkistoa ellei seuraava looppi sitä eksplisiittisesti vaadi.
